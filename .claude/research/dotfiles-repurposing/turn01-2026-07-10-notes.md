# turn 1 — 2026-07-10 01:03:16 CDT

Task: hostility-audit of Dorc's baked design decisions against the "general dotfiles /
dev-environment / system-management" repurposing (properties: 1. local, 2. dynamic /
seat-of-your-pants, 3. heterogeneous). Secondary: cheap feature-adds that raise the
value-prop there. Conversational deliverable; NO corpus notes minted (human directive).
Four gathering subagents dispatched (dotfiles-managers landscape · community pains ·
whole-machine cohort · script-cohort+Windows); this file = in-corpus half + working memory.

## Findings (in-corpus half, pre-fan-out)

- repurp-fd1 +SURE: the use-case is *named* as target-market in the corpus (DESIGN
  target-market para; `076` "homelab/dotfiles target" sizing; `131` Windows-plurality note;
  `24A` rul24-acceptance-shape's tabled dotfiles acceptance-day) but has never received a
  design-fit audit. This task is fresh, not relitigation.
- repurp-fd2 +SURE: localhost-as-target (no SSH — the dominant dotfiles deployment) is
  mentioned exactly once, `plans/130` A1.2 (`@local`/`dorc apply localhost`), only as the
  case that would force a local sh on a Windows controller. Zero treatment in the transport
  plan (`142` — grep clean). Nothing *forbids* a local-exec transport; the kFIDELITY leaf
  seam + kCOMMS executorless-OOB should admit it trivially. Gap = never-designed, not baked.
- repurp-fd3 +SURE (r25 verify-errand, LIVING_STATUS): `case "$(hostname)"` poison-walls an
  entire book at HEAD; no read-value command (hostname/uname/whoami) is modeled anywhere in
  the 145-case corpus. Host/OS-branching is THE central dotfiles idiom (per-machine
  variance), so for this cohort the read-value/Query-lane gap is existential, not
  nice-to-have. Fix is already roadmapped (P5 "read-value commands need the Query/predict
  lane") — the audit's contribution is priority re-weighting.
- repurp-fd4 ~SUSPECT: heterogeneity (different PM per machine) is handled by CONTROL FLOW
  in idiomatic sh (`case $(uname)` picks one branch per host), so per-host analysis stays
  single-PM and MH2's cross-PM lattice is NOT on this cohort's critical path — but only if
  repurp-fd3 is fixed (the branch must fold per-host). `command -v x || install` sidesteps
  PM identity entirely; a `command -v` oracle lifts the cohort's main guard idiom.
- repurp-fd5 +SURE: `sudo cmd` is an unmodeled opaque wrapper today (generalizes the r25
  `su - postgres -c` permanent-wall finding). Ops books run as root; personal-machine books
  sudo per-line. Without sudo argv-transparency (deferred-surface item: 23J lane-privilege /
  17N §7 wrapper-context kinds — seam reserved, not baked away) the system-level half of
  every dotfiles book stays walled. Not cheap (privilege changes probe semantics); the
  priciest high-value item for this cohort.
- repurp-fd6 +SURE: the two-users split collapses here — one hobbyist, admin-hat only,
  writes zero oracles. Cohort value = f(stdlib coverage of dotfiles idioms). Current P5
  slice is ops-flavored (apt/systemctl/ufw). Dotfiles slice: ln -s, cp/install, mkdir -p,
  git clone/pull, curl -o, macOS `defaults`, brew (+bundle), chmod, command -v. Mostly
  small, existing machinery.
- repurp-fd7 +SURE: marker-gating (`24M`: unmarked book = plain sh; marker gates syntax
  only) means an EXISTING install.sh gets stage-1 value with zero edits — "point Dorc at
  your install.sh, get a plan" is the killer demo for the script cohort. Off-ramp doubles as
  fresh-machine bootstrap (book runs bare before Dorc exists). Half-B guard-subsumption
  rewards the cohort's existing `command -v … ||` defensive idioms directly (pipe-guard lift
  24J covers `curl|sh`-guarded-installer shapes).
- repurp-fd8 ~SUSPECT: DESIGN's perf posture (network dominates; analysis ~free) inverts
  locally — no tunnel to hide analysis latency behind in a hot loop. kPRECISION/kFACTS are
  low/med lock-in; watch-item, not a bake.
- repurp-fd9 +SURE: native-Windows config management is welded out (kLANG; `139` tier-B
  degrades, WSL is the answer). The one real capability cliff vs chezmoi for heterogeneous
  personal fleets. Eyes-open weld, priced not relitigated.
- repurp-fd10 ~SUSPECT: no host→book direction exists (chezmoi re-add / aconfmgr capture /
  mackup analogs). Product-scope gap, unbaked (probe/predict machinery could feed a degraded
  "here's the diff, paste it" form via the why-lens). Common feature in the space — agents
  to confirm how load-bearing.
- repurp-fd11 -GUESS: interactive prompts (`read` from stdin, chsh) exist in wild dotfiles
  scripts; under Dorc's captured execution they hang/fail. A "book reads stdin" lint is
  cheap and kWARN-rich-sanctioned.
- repurp-fd12 ~SUSPECT: secrets (op/pass/age pipes) — unmodeled walls today; oracle-able.
  New-ish design tension: plan's attention-honesty (show content diffs) vs secret hygiene in
  plan display. Flag for human, not build.
- repurp-fd13 ~SUSPECT: oracle-rot risk is higher in this domain (brew/mise/npm churn fast
  vs apt/systemctl stability); fail-toward-run + attribution degrade gracefully but
  check-tax accrues. Exclusion-check (other-reliability cell) surfaced this.
- repurp-fd14 +SURE: $HOME/~-relative entity coordinates (dominant in dotfiles) are
  host-dependent values — covered by existing probe-time derivation (host-run touches();
  stage-5 dynamic frames). No bake.
- repurp-fd15 +SURE: macOS `defaults write` batches (100s of lines in famous repos) are the
  attention-product stress-test; a `defaults` oracle (natural read-back probe) is cheap and
  a huge attention win.

## Provisional verdict (pre-fan-out; to be tested against gathered evidence)
No welds actively HOSTILE to the repurposing found; several actively favor it (fd7). The
exposures are priority-weightings (fd3 read-value lane, fd5 sudo, fd6 stdlib slice) and one
accepted cliff (fd9 Windows-native), plus scope gaps (fd10 adopt-direction, fd2 local
transport).

## Citations
(pending — sources register on read; fan-out digests land later this turn)

## Agent-D digest (script cohort + Windows) — landed, curated

- repurp-fd16 +SURE (corrects fd7): the famous plain-script repos target BASH or ZSH, not
  POSIX sh — holman `${BASH_SOURCE[0]}` (in a `#!/bin/sh` file!), paulirish bash arrays,
  mathiasbynens `read -n 1`/`[[`, lewagon `#!/bin/zsh` + `exec zsh`. Dorc's parse-as-is
  claim ("99% of scripts") calibrates against the OPS corpus, not this cohort. The
  existing-artifact on-ramp is weaker than fd7 claimed; kWHICHSH (lean
  barely-more-than-classic-POSIX, r23 h3) is where this repurposing is MOST sensitive.
  New-books-written-for-Dorc are fine; existing bash artifacts need a rewrite pass.
- repurp-fd17 +SURE (confirms fd11, upgrades severity): interactive conflict-prompts are in
  the CORE loop of the most-copied installer (holman link_file: `read -n 1 action`,
  [s]kip/[o]verwrite/[b]ackup per conflicting file) — not fringe. Under Dorc these hang.
  Positioning flip: the prompt IS manual divergence-resolution; Dorc's probe/plan does that
  job pre-run. But the lint (fd11) is needed so the failure is loud.
- repurp-fd18 +SURE: the sudo-keepalive daemon idiom (`sudo -v; while true; do sudo -n true;
  sleep 60; kill -0 "$$" || exit; done &`) is cargo-culted across the cohort (mathiasbynens
  .macos, paulirish). Background jobs + infinite loops + `$$` — an analysis stressor beyond
  fd5's plain `sudo cmd`. Likely permanent-wall material; worth a named lint.
- repurp-fd19 +SURE: cohort convergence idioms map cleanly onto Dorc mechanisms where they
  are non-interactive: readlink-compare (`[ "$(readlink $dst)" = "$src" ]`) is a hand-written
  convergence guard (Half-B lift material); lewagon's backup-real-files-then-`[ ! -e ] &&
  ln -s` is pure guard-idiom; holman `bin/dot` "run periodically" is the cohort's reconcile
  model (Dorc plan/apply is a strict upgrade). rsync-over-$HOME (mathiasbynens) is the
  no-symlink camp — a content-sync wall unless oracled.
- repurp-fd20 +SURE (raises fd12's weight): secrets are chezmoi's OWN #1 graduation
  trigger (why-use page: work-vs-personal secret split is structurally unsolvable by plain
  scripts); the script cohort's answer is the untracked `~/.extra`/`extra.ps1` sourced-file
  idiom. NB `. ~/.extra` (sourcing unknowable content) is a mid-book poison wall for us.
  Dorc currently has NO secrets story; for this cohort that's a top-3 graduation pressure we
  don't relieve.
- repurp-fd21 +SURE (softens fd9): nobody solves native Windows well — chezmoi is
  best-in-class and still second-class there (symlink-privilege/DevMode; junction
  workarounds; "Chezmoi is not a good tool for that scenario" accepted-answer; Dev Drive
  double-apply). The WSL-vs-native two-worlds problem is unsolved by ALL tools; users keep
  two config worlds. Dorc's WSL-only stance loses less than feared; the two-worlds pain is
  not addressable from our side of the weld anyway.
- repurp-fd22 ~SUSPECT: graduation triggers distilled = {second OS family, secrets/
  work-personal split, public-repo desire, per-host content divergence}; plain scripts
  survive while single-OS/few-machines/no-secrets. Dorc relieves per-host divergence (via
  fd3 fix) and multi-OS (branch folding), NOT secrets (fd20), NOT public-repo hygiene.
  Escalation overshoots (Ansible/Make) before landing on chezmoi or Nix (seds.nl ladder).
- repurp-fd23 +SURE (confirms fd15, adds ordering): defaults-write walls come with
  `osascript 'quit System Preferences'` pre-steps and `killall Dock/Finder` post-steps —
  order-sensitive side-effect companions, not pure key-writes. A defaults oracle must scope
  its claims accordingly.
- repurp-fd24 +SURE: brew-install walls are LINEAR, unguarded, error-swallowing (~60
  sequential installs, no set -e). A brew oracle elides installed formulae = big wallclock
  (brew is slow) + attention win; confirmed top-tier cheap-add alongside `defaults`.
- repurp-fd25 ~SUSPECT (supports fd10): capture-direction ("what's on the old machine":
  `brew leaves > txt`, `npm list -g`, copy ~/.ssh) is treated as manual/un-automatable by
  the script cohort (paulirish migration-manifest genre). The want exists; script-land has
  no answer; aconfmgr/mackup live there (agent-C to confirm).

Agent-D source list retained in its digest (graded-by: subagent, provisional); load-bearing
subset to be registered + spot-read in main context after all four digests land.

## Agent-A (manager landscape) + Agent-C (whole-machine cohort) digests — landed, curated

- repurp-fd26 +SURE (in-corpus, `23D` §3): per-iteration loop verdicts are DEFERRED by the
  atomic-command axiom — "the enrichment path to granularity is the AUTHOR rewriting to a
  loop … so it all folds under 'figure out loops eventually' (per-iteration guarding,
  deferred)." For ops books (straight-line) that deferral is benign; for dotfiles books the
  symlink-farm LOOP (`for f in …; do ln -sf …; done` — universal core per agent-D) is the
  STARTING shape. Under this repurposing "eventually" becomes "early": without per-iteration
  handling, the heart of every dotfiles book is one opaque unit. Priority-inversion finding,
  same class as fd3.
- repurp-fd27 +SURE: drift REPORTING is near-absent across the whole space (agent-C: only
  aconfmgr + winget-DSC-in-theory even claim it; home-manager/Brewfile/topgrade/ansible-pull
  are apply-and-hope, "authoritative only at the moment you run them"; ayats: HM guarantees
  erode the moment a live app rewrites a file). Dorc's probe-first plan IS a drift report
  from measured reality — a genuine differentiator, and it needs no new build (positioning,
  CLI verb at most).
- repurp-fd28 +SURE (confirms fd4 from the market side): NOBODY does unified cross-PM
  reconcile ("punted to run-the-right-tool-per-platform" — comtrya/Ansible pattern; Brewfile
  can *name* winget/cargo entries only). Dorc's per-host branch-folding is the same punt the
  market accepts ⇒ MH2 not needed for competitive parity.
- repurp-fd29 +SURE: package-install is NO dotfiles-manager's first-class feature — all
  route it through their script/hook escape hatch (chezmoi run_onchange_install-packages,
  dotbot shell:, yadm bootstrap). chezmoi's own docs: "Scripts break chezmoi's declarative
  approach and should be used sparingly." The script layer = every tool's weakest, most
  load-bearing seam = exactly Dorc's home turf. Sharpest positioning finding of the fan-out.
- repurp-fd30 +SURE: chezmoi's run_once_/run_onchange_ semantics are STATE-based convergence
  approximation (SHA256 of script content in a local DB — "did this content run before"),
  not reality-probing. Staleness/wrong-machine-memory failure modes are inherent. Dorc's
  probe-reality + kSTATE-recompute lean is the strictly more honest mechanism for the same
  user need. (Also home-manager: activation-time authority only, backups-forever, fd27.)
- repurp-fd31 +SURE: the refusal camp's values (reversibility, legibility, no lock-in —
  "You know exactly what's happening: symlinks"; "Git is the tool… since 2008") are
  EXACTLY Dorc's off-ramp/spelled-as-sh values. Dorc is the only tool-shape that serves the
  refusal camp without asking them to leave their script. Counter-consideration fd16 (their
  scripts are bash/zsh).
- repurp-fd32 +SURE: secrets confirm-and-sharpen fd12/fd20: chezmoi is the ONLY tool with
  live password-manager fetch (op/bitwarden/pass/vault template functions at apply-time);
  per-file age/gpg encryption; the `private_`≠encrypted footgun is a top community warning.
  In sh idiom the equivalent is `op read`/`age -d` pipes — oracle-able (pure-read vouches) —
  plus the plan-display hygiene question (fd12). Weight: top-3 graduation trigger.
- repurp-fd33 ~SUSPECT: the space is FILE-centric (unit = config file; repo mirrors $HOME;
  add/re-add/diff per file); Dorc is COMMAND-centric. The file-centric affordances users
  expect (adopt this file; diff this file; forget this file) come out of Dorc only via
  book-edits + probes. fd10's capture-direction gap is the biggest consequence (confirmed
  signature feature: chezmoi add/re-add, aconfmgr save, brew bundle dump, winget export,
  mackup — ALL have it; Dorc has nothing).
- repurp-fd34 +SURE: bootstrap-dependency is a real wedge (chezmoi's single-static-binary,
  "not even git" one-liner is its #1 cited advantage; dotbot's python+git is cited
  against it). Dorc: Rust static binary + the book-runs-bare-without-Dorc off-ramp ⇒
  actually STRONGER than chezmoi on the fresh-machine story (sh is the only dep). Marketing
  fact, zero build.
- repurp-fd35 ~SUSPECT: preview-before-apply is table stakes across the space (chezmoi
  diff/status, dotdrop compare, stow --simulate, dotbot --dry-run) — Dorc's plan is that,
  plus honesty; no gap, mild positioning advantage (plan quality/why-lens beats their
  diffs).
- repurp-fd36 (misc pains bank, for cheap-adds calibration): Stow folding/--adopt danger +
  conflicts-hard-fail; chezmoi source-state naming tax ("weird and verbose" per own FAQ) +
  single-source rigidity (refuses path remapping on principle); mackup symlinked-prefs BROKE
  on macOS Sonoma+ (symlink-based config is fragile against OS vendors); yadm alternates
  only on top-level tracked files; topgrade = no-state no-rollback aggregation (its
  fragility = inherited breakage of N wrapped tools; "removes your chance to eyeball each
  step" — Dorc's plan/consent is the antidote shape); Brewfile no-pinning + cleanup
  over-removal; asdf shim tax (mise PATH-activation won); ansible-pull = the local-apply
  laptop idiom (supports fd2's local-exec transport expectation).

## Interim synthesis pressure-points (pre-agent-B)
1. Hostile-bake candidates so far: NONE hard. Sensitivities: kWHICHSH dialect (fd16),
   loop-granularity deferral (fd26), read-value lane (fd3), sudo wrapper (fd5).
2. Cheap-adds bank: local-exec transport (fd2); read-value oracle family (fd3); dotfiles
   stdlib slice ln/cp/mkdir/git/defaults/brew/command-v (fd6, fd15, fd24); stdin-read +
   sudo-keepalive lints (fd17, fd18); drift-report positioning verb (fd27); bootstrap
   marketing fact (fd34).
3. Positioning: Dorc = the script layer done right (fd29), probe-reality vs state-memory
   (fd30), serves the refusal camp (fd31), plan/consent vs topgrade-style blind aggregation
   (fd36).

## Agent-B digest (community pains) — landed, curated; quote-discipline verified

Verification: five distinctive quotes grep-verified byte-exact against the raw
[C-hn-dotfiles-megathread-2026] Algolia JSON in main context; trust extended to the digest.
[B-chezmoi-why-use-2026] re-read in full in main context; agents' characterization holds.

- repurp-fd37 +SURE: PAIN-1 (most-recurring across all platforms) = home-manager's
  edit-loop tax + read-only store ownership ("every single edit … means rebuilding";
  Lazy can't write its own lockfile) [B-jade-use-nix-less-2025]
  [C-hn-dotfiles-megathread-2026]. The community's #1 wish — "declarative/per-machine power
  WITHOUT an evaluation/rebuild step in the edit loop" — is nearly a restatement of Dorc's
  thesis (inference over plain imperative sh; no compile step; files stay ordinary).
- repurp-fd38 +SURE (tempers fd16): the long-haul portability practitioners of the script
  cohort DO deliberately write POSIX sh ("The dotfiles cross-os scripts are all POSIX sh +
  git, both are available everywhere", 16-year jitl account; "80% of my dotfiles without
  root on arbitrary systems over ssh") [C-hn-dotfiles-megathread-2026]. So the
  bash-artifact problem (fd16) is real for the FAMOUS repos but the cohort's
  portability-minded core is already inside Dorc's dialect. ~SUSPECT the split is
  starred-showcase (bash) vs quiet-practitioner (POSIX).
- repurp-fd39 +SURE: the re-run tax is real here — nickjj's 2,500-line multi-distro script,
  "1 command … fresh system in about 10 minutes", re-run for any change
  [C-hn-dotfiles-megathread-2026]. A Dorc-shaped book if there ever was one.
- repurp-fd40 +SURE: PAIN-2 stow write-through drift ("Every edit on every machine writes
  straight through the link into that machine's clone … dirty working trees … changes I had
  no memory of making") [B-rednafi-chezmoi-stow-2026] — drift-VISIBILITY pain; a copy-based
  book + probe-diff plan answers it (fd27's differentiator restated from the demand side).
- repurp-fd41 +SURE: the community wish-list maps near-1:1 onto existing Dorc properties:
  rootless/single-user standalone; single static binary, no runtime deps; no eval step;
  one tool spanning files+packages+OS-settings+commands "without a second bootstrap
  dependency"; templating that doesn't break editor highlighting (Dorc books are plain sh;
  target configs stay real files, no .tmpl) [C-hn-dotfiles-megathread-2026]
  [B-chezmoi-why-use-2026].
- repurp-fd42 ~SUSPECT: tool-version heterogeneity (tmux v1.2 vs v9001 config
  incompatibility) is a recurring cross-machine pain; the sh spelling is branch-on
  `tmux -V` — ANOTHER read-value-probe case for fd3's lane (version-read, not just
  hostname/uname).
- repurp-fd43 -GUESS: a wave of "less-bloated-chezmoi" projects (Janus, doot, lnko, hjem)
  announced as feature-fatigue reactions = market signal favoring Dorc's
  no-tool-config/it's-just-your-script posture; thin evidence (thread mentions only).

## Registered sources (turn 1)
[B-chezmoi-why-use-2026] · [C-hn-dotfiles-megathread-2026] · [B-jade-use-nix-less-2025] ·
[B-rednafi-chezmoi-stow-2026] · [A-chezmoi-design-faq-2026] ·
[A-holman-dotfiles-bootstrap-2026] · [A-mathiasbynens-dotfiles-2026] ·
[A-gitforwindows-symlinks-2024] · [A-brew-bundle-docs-2026]
(Remaining agent-cited URLs live in the agents' digests, graded-by: subagent, unregistered
— register on demand if any becomes load-bearing to a durable claim.)
