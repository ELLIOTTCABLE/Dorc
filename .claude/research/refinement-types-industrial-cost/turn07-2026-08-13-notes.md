# turn 7 — 2026-08-13 20:48:18 CDT

CROSSCHECK ADJUDICATION — full-scope review of this research effort (turns 01–06 +
the pending-amendments ledger + the Lean sparing spike), run at the human's direction
by a Fable suborchestrator distinct from the conductor. Fleet, human-specified: four
read-only reviewers — fable-neutral / fable-adversarial (Anthropic lineage, fresh
contexts) and sol-neutral / sol-adversarial (GPT-5.6-Sol, foreign lineage, codex
shims) — paired per the adversarial-crosscheck method (neutralised vs
disowned-and-inverted rewrites; constraints bracketed; manufactured-fault guard in
the adversarial briefs). Raw reports + the dispatch bundle: `crosscheck/`
(`report-{fable,sol}-{neutral,adversarial}.md`, `dispatch-bundle.md`,
`input-turn06-pending-amendments.md` — a verbatim repo-side copy of the session
ledger). Reviewers were given context and latitude, no target list; my own
verification targets were pre-registered before dispatch and withheld from all
briefs, so lane-vs-me convergence below is genuine.

Adjudication discipline: every load-bearing lane claim re-verified against
corpus/code/spec before crediting; verdicts are {CONFIRMED / PLAUSIBLE-unverified /
REFUTED / SPEC-AMBIGUOUS}; lane attribution recorded because cross-lineage
convergence outweighs in-lineage agreement. Headline: NO finding invalidates any
human-typed decision (all four lanes independently concur, and my verification
agrees) — but two "awaiting human go" items must not be greenlit on their current
framing, and the synthesis-of-record carries a small number of confidence-inflation
defects that must be corrected in any digestion into `plans/`.

## Adjudicated findings, severity-ranked

### adj-quarantine-claims-stamped-verified — CONFIRMED (fable-adversarial; re-verified here)

Turn06 portfolio item 3 stamps "Honest costs (all verified):" over claims that exist
in the corpus ONLY in turn01's do-not-cite quarantine block: Flux build-from-source +
z3/liquid-fixpoint binaries (turn01 Q2, flagged "VERIFY THIS — it is
decision-relevant"; the Flux install guide stayed on the never-read To-read list;
turn02 verified only the nightly-coupling MECHANISM). Likewise item 2's "multi-year
ordinary-team sustainment precedent (Firecracker/s2n-quic CI)" appears nowhere but
turn01 quarantine Q4, and enters the ranked portfolio citation-free and hedge-free.
Both claims are ~SUSPECT-true in the world (which bounds the substantive damage);
the process damage is that the round's own verification currency — minted by its
opening summariser-defect finding — is spent on material the ritual never touched.
DISPOSITION: before the plans/-tier digestion, either verify the two claims raw or
re-hedge them explicitly; treat "(all verified)" stamps as an audit target generally.

### adj-certifier-preauthorization-stretched — CONFIRMED (fable-neutral + fable-adversarial + sol-adversarial + my pre-registered check; strongest cross-lineage convergence in the set)

Turn06 item 1's "executes plans/021 §1 + plans/055 dec-6's own pre-authorized narrow
exception — kVERIFY weld NOT reopened" over-claims its pedigree. Verified: `plans/055`
item 6 names ONE justified targeted property/Why3 site — the inert-classification
kernel, not the solve seam; `plans/021` §1's exception is GUESS-graded, "a v2
unit-confidence tool for one function, not a project methodology. Do not adopt up
front", naming the inert kernel + elision's "skip ⇒ proven-converged" (only the
latter plausibly covers a post-fixpoint certifier, in spirit). Turn04 recorded the
honest chain; the over-claim entered at turn06's compression. The weld-half survives
(a targeted certifier + calibration-tier lanes do not promise end-to-end soundness —
sol-neutral verified compatibility with kVERIFY-calibrate). DISPOSITION: the
certifier go is a FRESH decision wanting its own typed ack, not an inherited one;
one honest sentence fixes the record. Related bookkeeping (fable-neutral): the
combined plan-state (Lean-at-settle + Aeneas artifact D) functionally exceeds the
kVERIFY registry text — a status-line note is owed, mirroring the kSURVIVAL one the
ledger already contemplates.

### adj-lean-composition-soundness-not-machine-checked — CONFIRMED with nuance (both Sol lanes High; both Fable lanes filed it under honest-hedging — a live cross-lineage split, adjudicated in Sol's favor on substance, Fable's on severity)

`SoundCompare` HYPOTHESIZES that every determined verdict of the whole `compare`
implementation is true of the world; the consumer theorems project that hypothesis.
No theorem derives `SoundCompare` from generator-level truth conditions (resolver
honesty + dialect-row truthfulness + the kind-fence's world assumption), so the
REPORT's modeling-note paraphrase — "the machine-checked content is: misfires trace
to generator-incorrectness or contradiction, NEVER to the composition algebra" — is
NOT what was checked; the composition step is exactly what `SoundCompare` assumes
away. The REPORT does flag `SoundCompare` as statement-uncertain (honest), and
statement review by spec-owners is its own stated destination — which is why this is
severity-medium, not High: the artifact hedges, the LEDGER's compression ("ALL FIVE
theorem families PROVED") does not (REPORT's own ledger marks #4 API-shape,
not-a-theorem). DISPOSITION: (1) downstream summaries must say "four proved + one
structural, all conditional on compare-soundness"; (2) the natural next theorem —
derive compare-soundness from named per-generator hypotheses — is small, states the
actual "our composition algebra is correct" obligation the amendments ledger cares
about, and should be the spike's first follow-up if it graduates from throwaway.

### adj-lean-model-divergences-beyond-declared-gaps — CONFIRMED (fable-neutral + fable-adversarial + sol-neutral on the first; fable-adversarial + my pre-registered check on the second; sol-adversarial + sol-neutral + fable-neutral on the third)

Three unflagged divergences beyond the REPORT's seven declared SPEC-GAPS:
1. **Context gate ahead of the kind-fence** (`Compare.lean` layer 1): cross-kind
   pairs in different contexts yield `unknown`, forfeiting the cross-kind
   disjointness `272` §4 prices as "free by construction" ("Cross-entity/kind
   disjointness unchanged", `277` §3). Safe direction (under-spares), dead at v1
   (single context), but a spec-vs-model divergence asserted in a docstring as if
   spec-derived — and an under-execution-of-VALUE cost under the ledger's
   survival-is-the-core-product ruling the day contexts multiply.
2. **Equal-but-unminted tokens compare `same`** (`selectorTier`'s equality branch
   carries no dialect-row check): `277` §3 makes unminted tokens ⊤-selector and §6's
   fence says ⊤ never identifies — so two byte-equal unminted tokens should not
   yield `same` (which feeds transport, the dangerous direction). Unreachable under
   the engine invariant (backing selectors come from minting lines) — but the spike
   ITSELF declares the standard "checked anyway, so the formal algebra does not
   silently depend on that engine invariant" for the disjoint branch, then depends
   on exactly that invariant on the same branch.
3. **Coverage-claim overstatement**: the README/REPORT headline ("§2 ternary compare
   + consumer map … §5 set-lifting") oversells — §2's generator registry is
   subsetted (axis-invariance, lend/re-keying, contradiction rows absent; disclosed
   in code comments, not in the REPORT's scope/gap sections), and §5's TRANSPORT
   half of set-lifting ("every member must transport") is unmodeled and nowhere
   named as out-of-scope.
DISPOSITION: treat the REPORT's gap list as a floor, not a census, exactly as its
own statement-review recommendation implies; add these three to the ledger before
any spec-owner statement review. Also two reclassifications from the lanes, both
credited: gap-top-selector-self-sameness is spec-DETERMINED (§1's ⊤-at-consumers +
§6's fence compose to never-same), so it should be recorded as an acknowledged
COST, not underdetermination (sol-neutral); and `LoadsOnly` permits same-family
retraction, so the "growth operation" docstring claims less than the spec's
grow-by-load — the monotonicity theorem is thereby stronger than advertised, a
doc-precision nit (sol-neutral).

### adj-lean-tier-plan-state-unreconciled — CONFIRMED (fable-adversarial + my pre-registered check; sol lanes adjacent via aggregate-budget/disposition findings)

The combined plan-state holds three coexisting vehicles for Lean-tier assurance of
the sparing algebra with no governing ruling: (a) amendment-survival step 4 —
promote the Rust reference model to Lean/Dafny AT ALGEBRA-SPEC SETTLE ("Lean waits
for settle" as the churn-tax defense); (b) amendment-aeneas — near-IMMEDIATE Lean
proofs over the Aeneas translation with "the sparing-algebra laws" among the
crown-jewels; (c) the already-built hand-written Lean model of 277-as-written — the
"handwritten-Lean" shape the same ledger records the human as dispreferring,
justifiable as a statement-probe but never reconciled (does it discharge step 4?
does the at-settle trigger survive? is the hand model dead weight or a second
permanent sync obligation?). Turn06 item 5's Lean-vs-Dafny "language tension held
open" has meanwhile been closed by facts on the ground with no recorded decision.
Softener: the ledger honestly declares itself mid-flight (adjudication not run,
dispatches HALTED). Adjacent, both Sol lanes: no aggregate budget or
success-case disposition exists across the Kani + Flux + Aeneas/Lean lanes (two
separate nightly pins; overlapping small-core/facade surfaces; joint upgrade and
proof-repair load unpriced). DISPOSITION: one reconciliation ruling at the next
human sitting: name the governing trigger for Lean-tier sparing work, the hand
model's status, and the multi-lane disposition-if-both-spikes-succeed.

### adj-flip-three-run-to-run-overstated — CONFIRMED (both Fable lanes; mechanics re-verified here against solve.rs)

FLIP-3 / portfolio item 2's premise sentence — the Eq-exclusion makes "explanation
var[y] run-to-run while the license plane converges identically" — is false at HEAD:
the solver is deterministic by construction (FIFO worklist, ordered collections,
`solve_is_deterministic`; inv-determinism), so identical inputs give identical mint
multisets. The Eq-exclusion is real (hand-written `PartialEq` on `Reach` excluding
the Top cause; the narrative-carrier convention) and the defensible content
survives conditioned: the explanation multiset is an artifact of scheduling policy,
unpinned under worklist changes, renumbering refactors, and the parked reactive
engine (26C) — which is precisely when the multiset-permutation pin earns its keep.
Also unpriced: the pin requires a schedule-permutation capability the solver
deliberately lacks. DISPOSITION: recondition the finding's wording in digestion
("under scheduling variation the engine does not yet have"); the pin itself stands;
its "most important NEW finding" billing partially deflates.

### adj-certifier-spike-brief-obligations — CONFIRMED cluster (mixed lanes; each item re-verified)

Design notes that must reach the certifier spike brief before any go:
- **degrade-to-⊤ is unspellable generically** (fable-neutral + sol-adversarial,
  cross-lineage): `Lattice` has no `top()`; `Powerset`/`MapL` are deliberately not
  `BoundedLattice`, so the literal Blazy fallback cannot be written against the
  trait. The amended `Certified | Refused(EdgeWitness)` shape resolves it by moving
  the stage-0 floor to the consumer — record that mapping explicitly.
- **must-orientation boundary vacuity** (fable-neutral): entry nodes have no
  incoming edges, so the per-edge check is vacuous exactly where forward-must
  boundary seeding lives — and `lattice.rs` records that seeding as deliberately
  unbuilt (note 167 DP-8). Boundary conditions are the load-bearing half of the
  checker; spell them.
- **witness ≠ root cause** (sol-adversarial): "the first failing edge localizes the
  oscillation" overstates — at cap-trip the first failing edge is
  traversal-order-dependent and downstream states may merely be stale; the true
  violation may be non-monotone transfer / unbounded height / non-semantic Eq
  elsewhere. `Refused(EdgeWitness)` is evidence of non-certifiability, not a cause
  verdict — and the ledger's own mis-attribution sharpening (pope-sin tier) applies
  to our own witness narration.
- **price never revisited** (fable-adversarial): "~50 lines"/"~1 agent-day" survive
  from the pre-narrative analysis while the same paragraph adds closed-outcome
  shape, collapse-record minting, partial-certification, and disclosed-k-cap
  duties; "ROSE twice under re-analysis" books duty as value with no cost delta.
  Reprice in the brief.
- **domino-lemma statement hazard** (fable-adversarial; math re-verified):
  "certifier-accept ⇒ post-fixpoint ⇒ path-coverage" needs a named hypothesis —
  abstract path-coverage (MOP-inclusion) requires transfer MONOTONICITY (the
  obligation the certifier argument celebrates not needing), and concrete coverage
  requires per-operator γ-soundness (explicitly scoped out as calibration
  territory). Either reading is fine if stated; the unqualified wording supports
  neither. Settle before any Lean statement is authored.

### adj-seventeen-day-figure-unsupported — CONFIRMED (both Fable lanes; dates re-verified here)

Turn06's twice-stated "shipped acked and survived 17 days" for the `277` §5
vacuous-∅ bug class: the corpus's own dates give ship 2026-07-13 (`279f` header
confirmed) → ack 07-16 → catch 07-17 (`27Xf` header confirmed) = ~4 days
ship-to-catch, 1 day ack-to-catch. "17" is ~SUSPECT a date-echo of the 17th. The
bug class and pins stand independently (`27Xf` records them); the duration inflates
item 2's urgency rhetoric ~4x. Correct in digestion.

### adj-aeneas-gate-measures-translation-not-churn — CONFIRMED (fable-adversarial; sol-neutral's contrary "properly gated" holds on a different axis)

The go/no-go spike gate ("what translates and what falls out", days-scale) tests
translation feasibility only. The round's own grading marks proof-repair-under-
refactor as THE load-bearing unknown (~SUSPECT, promotional-blog-only, with the
independent manual-loop-invariant finding on top), and the gate cannot observe it —
"proof-repair sessions budgeted" is unpriced. Sol-neutral's holds-verdict (the plan
never pretends whole-kernel consumption; the gate is honest about what it gates) is
compatible and also credited. DISPOSITION: state in the gate's success criteria
that churn risk stays unmeasured until proofs exist; optionally add one deliberate
refactor-and-replay probe with repair effort recorded (scope-growth priced).

### adj-claim-tier-unrepresentability-wording — CONFIRMED narrow (both Sol lanes High; both Fable lanes certified as-described — the clearest in-lineage blind-spot exhibit of the exercise; adjudicated between)

`claim.rs`'s "cannot be spelled"/"compile fact" phrasing (and turn03's echo)
overstates: explicit laundering IS spellable — `Claim::vouched(
measured.into_observation(), Rung::Both)` re-mints an observation payload as a
vouch, and nothing bounds `P` away from fact-plane types at mint sites. What the
four properties actually deliver is accident-proofing + non-silence: no coercion,
no implicit path, laundering requires a greppable fresh authoring act — which the
module's own when-blocked-rule implicitly concedes ("do NOT convert it to satisfy
the signature" — advice that only makes sense because conversion is spellable) and
the honest-bound paragraph almost states. Severity low-medium: no research
conclusion moves (turn03's "tier-(a) discipline is mature; tooling value
concentrates elsewhere" survives; if anything the residue strengthens the
anti-gaming plank: the guard against deliberate laundering is review, not types —
consistent with the round's own enforcement-was-human-review finding).
DISPOSITION: soften the two strongest sentences in `claim.rs` docs; carry the
distinction (accident-proof vs determined-author-proof) into any digestion.

### adj-minor-cluster — CONFIRMED each, individually small

- Ledger's "ALL FIVE theorem families PROVED" vs the REPORT's own honest #4
  API-shape entry (both Sol lanes) — inherit the REPORT's framing downstream.
- Ledger's "2 errors total … vs the 27%-union benchmark prior" is apples-to-oranges
  (fable-adversarial): the benchmark is loop/arithmetic program-verification; the
  spike is finite enums + Bool folds, the REPORT's own "best possible terrain".
  Scope the viability claim as the REPORT does.
- Grading-rubric drift (fable-neutral + sol-neutral, cross-lineage): the
  peer-review criterion demotes one arXiv preprint to B and waves another through
  at A; several A-grades sit on partial/targeted reads. Downstream: read the
  grade letters with the reasoning fields, not alone.
- Turn01's subagent-regrade obligation closed undischarged for non-overlapping
  sources (fable-neutral); mitigation: verbatim quotes + sha256 archives.
- AID-NEEDS cited normatively while disclosed-unread (fable-neutral; verified
  accurate secondhand via the spike CLAUDE.md replica — process bent, no error).
- "FIVE hand-picked samples per combinator": Powerset five, the rest four
  (both Fable lanes). "untouched since round 19": modulo a 2026-07-26
  encoding-repair commit (PLAUSIBLE-unverified, immaterial). Scout-A's
  "7 runtime asserts" undercounts (~16 workspace-wide; PLAUSIBLE-unverified) —
  the two named release-silent asserts exist exactly as described, so portfolio
  item 4 stands; re-derive any load-bearing census figure before use, since the
  scout full-reports were chat-only and are now unreviewable.

### adj-u-curve-check-answered — my synthesis, building on fable-neutral (closes the ledger's own open TODO on the plan-time re-derivation)

The ledger asked that the plan-time sparing re-derivation be checked against
`271:rul-net-quality-u-curve` before building. Answer: it passes as MECHANISM — an
exact redundant computation diffed for equality is not the dip's
provably-imperfect heuristic net — but only under two conditions the ledger does
not yet state: (1) **the demote-only structure is the load-bearing pro-pass
argument** (disagreement demotes to guard/run + mints a record; agreement licenses
NOTHING new), which is precisely what distinguishes it from
`271:struck-falsifiability-license-leg` (struck for LICENSING vouch-tier off a
mechanical differential — engage that precedent explicitly, it is the nearest
prior art and currently uncited); (2) **its value is bounded by implementation
diversity**: a reference model authored by the same lineage from the same `277`
prose shares spec-misreadings with production — `22W:fb-same-model` applies, the
same honesty the conductor granted the certifier ("catches solver bugs, not model
bugs") but not the reference model. Mitigations that preserve the value: author
the reference from the Lean-spike STATEMENTS rather than the prose, and/or via a
foreign lineage; and record that the differential catches divergence-class bugs
only.

## What holds (condensed; the four lanes' holds-sections concur far more than they diverge)

Unanimous or cross-lineage-confirmed, re-verified where load-bearing: every checked
kernel figure and mechanism claim is exact (cap formula, converged-flag semantics,
un-type-enforceable preconditions, the 435/783 hang row, MapL canonicality/
semantic-Eq, Must-order-duality one-checker, claim.rs's four properties AS
accident-proofing, the certifier formula against the real solver semantics);
result-certification is the right narrow seam and the turn02→03 composition
(monotonicity not a soundness obligation under certification) is valid; the
algebra/engine split is real in the tree and tooling was routed by it; Kani
law-harnesses are correctly identified as the cheapest win; portfolio item 6's
restraint (protocol-atomicity stays off the refinement backlog) is verified against
the failure records; the survival reprioritization is internally clean and names
its own supersessions; Flux pricing did NOT flatter the preferred direction (the
analysis argued the human's prior DOWN and was overruled — the healthy shape); the
Lean spike's five declared gaps are genuine (directionality's convergence with
`28R:adj-sparing-two-position-rule` verified; the footprint-∅ exhibit is
safety-relevant), its hedging is exemplary, and its structural-encoding choices
translate; citation hygiene (slug resolution, quote-blocks, sha256 pins, the
Cedar-ratio and vericoding re-verifications) survives adversarial audit; the
method-defect quarantine discipline was real and mostly held — the exceptions are
exactly the items in the findings above.

## Lineage-independence analysis

Cross-lineage convergent (highest confidence): preauthorization-stretch,
ctx-before-kind divergence, model-coverage overstatement, top-fallback
unspellability, grading drift. In-lineage-only but mechanically re-verified here
(confidence unaffected by correlation): FLIP-3 tense, 17-days,
unminted-equal-tokens. DEMONSTRATED correlated blind spots — the exercise's
clearest methodological result: both Fable lanes independently certified claim.rs's
"unrepresentability" and filed SoundCompare-conditionality under honest-hedging,
while both Sol lanes elevated them to High; conversely both Sol lanes missed the
quarantine-stamp laundering, the 17-days figure, and the unminted-token branch that
Fable lanes (and my pre-registered pass) caught. Neither lineage's pair was
sufficient alone; the fleet's decorrelation paid its cost in exactly the way
`22W:fb-same-model` predicts. Designed-in residual blind spots, all four lanes +
me: source-base coverage was out of scope (re-research forbidden), so
representativeness of the 22 sources stays unaudited; the chat-only scout reports
are unreviewable by anyone now; the Lean build/axiom-profile claims were accepted
without rebuilding per brief.

## Do the findings invalidate any human-typed decision?

No — unanimous across all four lanes, and my verification concurs. The typed
rulings (error-tier ladder, knobs-not-load-bearing, code-is-a-model,
narrative-thirty, aid-seam, two-plane-firewall, the survival-reprioritization
rulings, and the two typed LEANS — flux-lean-add-early, aeneas-pulled-forward)
rest on product-shape and meta-cost premises no finding touches; narrative-thirty
in particular was typed on grounds independent of FLIP-3's mis-tensed mechanism.
What the findings DO invalidate is a set of conductor-level assertions: the
certifier's "pre-authorized" framing, "(all verified)" on two quarantine-only
claims, "survived 17 days", "ALL FIVE families PROVED" as compressed, the
claim-tier "cannot be spelled" absolutism, and the domino-lemma's unqualified
wording. Consequences for the two items "awaiting human go": the certifier spike
should be re-presented for a fresh ack with corrected pedigree and repriced
narrative duties; the Aeneas spike-gate should carry the churn-risk scope note.
The evict-maps/LatticeMap recommendation correctly remains recorded as
not-yet-acked; nothing here changes its standing.

## Process notes (deviations + incidents, for the record)

- The foreign-models bundle-ack pause was not taken: this suborchestrator has no
  mid-flight human channel, and the commissioning brief recorded the human as
  having specified the fleet; the full bundle is durable at
  `crosscheck/dispatch-bundle.md` for after-the-fact review.
- Two Fable lanes ran in parallel, against the foreign-models one-Fable-per-task
  guard — again per the human-specified fleet; noted, not self-authorized.
- The fable-adversarial lane's Write was blocked once by a subagent guardrail and
  the report was staged-then-copied; content unaffected.
- During my own verification, one repo-wide grep incidentally surfaced ~8 context
  lines from `Research/quarantine-DO-NOT-READ/280d-working-notes.md` (a
  quarantine path adjacent to the two the brief names). The glimpsed content —
  which included what appeared to be prior spike-faithfulness check marks — was
  discarded and did not inform this adjudication; later greps were path-scoped
  to exclude quarantine directories.
- Reviewer reports are raw and unadjudicated by design; where this note and a
  report disagree, this note is the adjudicated record. Provenance labels:
  fable-* = Anthropic lineage; sol-* = OpenAI GPT-5.6-Sol via codex, read-only
  sandbox.
