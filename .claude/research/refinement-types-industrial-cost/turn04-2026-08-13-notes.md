# turn 4 — 2026-08-13 17:25:40 CDT


Turn context: human-acquired sources landed (Gamboa full text; Klein seL4 PhilTrans; Fisher
HACMS — accidental, kept); pdftotext (xpdf 4.00, mingw64) discovered as the working PDF route,
quality mechanically verified (zero replacement chars; math glyphs only casualty). Everest sec4-5
tail and Verus SOSP read via it. Cedar ICSE'25 retrospective NOT acquired (paywalled) — accepted
gap; the arXiv Cedar paper covers most ground. plans/021 and plans/055 read IN FULL per human
direction. Scout A (invariant census) and Scout B (corpus theorems) both returned; key content
summarized in turn03 notes and below.

## Findings

- READ IN FULL: [A-gamboa-usability-barriers-2025]. Nine barriers, three themes. The ones that
  price OUR options: (1) too-weak specs PASS SILENTLY (the strongest-predicate problem) — same
  failure family as the vericoding weak-spec quarter and prover-is-the-judge gaming; (2) the
  automation-to-manual-proof cliff arrives with NO tool signal ("no hint given by the type
  checker to say you probably need a proof term"); (3) scalability: a FIVE-HOUR verification run
  (FM4), comment-out-the-proofs coping, Z3 infinite loops, solver-version sensitivity; (4)
  error messages leak internal representations (Horn-clause screens; "5 is not less than 4 —
  sure, but what does it mean?"), error-location indirection; ALL seven experienced users report
  error-message problems persisting; (5) installation failure as adoption killer; (6)
  invariant-IDENTIFICATION is the hardest cognitive step (a single find-the-invariant exercise
  ate 30+ min and was dropped from the study). The authors explicitly extend the add-on-layer
  barrier classes to Flux by name. DORC ASYMMETRY (mine, +SURE): Dorc has already paid the
  invariant-identification cost — the corpus's English theorems ARE the identified invariants;
  what remains is translation+maintenance cost, which reorders the adoption calculus vs a
  green-field team.
- Everest sec4 (tail, read in full): toolchain churn (HACL* pinned to old F* 2017-18, ~1 year to
  catch up; Z3 pinned 4.8.5 2019->2024); 26 soundness bugs in F* itself; SIX codegen bugs
  escaped formalization+checker+compiler (found by e2e debugging); NULL+0 story (declined to
  re-prove; sanitizers instead). SMT instability definitive treatment: rename-a-variable
  breakage, version+OS-PLATFORM sensitivity, 0.74% of queries = 50.51% of SMT time, six-item
  mitigation toolkit, streaming-API rewrite-to-manual-style anecdote. sec4.5 hindsight: they
  regret SKIPPING the idealized-model-first layering for TLS — the team that went direct
  retro-endorses the model/impl split. [A-everest-perspectives-2026]
- Everest sec5: Verus "in several production-level services at Amazon". AENEAS = a FOURTH
  architectural shape: UNANNOTATED sequential Rust auto-translated to Lean; proof engineer works
  the translation output (explicit division of labor); SymCrypt-rewrite verification running
  this way now. Extract-Rust-from-proof-assistant also live (KaRaMeL Rust backend; HACL*
  extractable to Rust). AI-and-proof: F* corpus 940k lines; best-model proof synthesis 35%
  lemmas / 41% dependently-typed / 61% simply-typed; 3DGen (LLM agents + symbolic feedback ->
  3D specs -> verified codegen) is the shipped agentic-spec-authoring pattern.
  [A-everest-perspectives-2026]
- Verus SOSP'24 (targeted): quarantined figure now VERIFIED — 6.1K impl / 31K proof (~5:1)
  aggregate; spread 3.9:1 (storage log) / 4.3:1 (allocator) / 13.3:1 (page table, first big
  dev) / independent microkernel 7.5:1; baselines: seL4 initial >20:1 ITP-style, Ironclad
  (Dafny) 5:1, Hyperkernel 0.03:1 (restricted logic). EPR mode collapsed a ~300-line/days-of-
  effort manual proof to an auto-checked invariant. Verification SPEED as qualitative workflow
  change: whole-project iteration, "early warning when a change to a function contract breaks a
  proof elsewhere" — the agent-loop-relevant property. [A-verus-practical-foundation-2024]
- [A-klein-provably-trustworthy-2017] (targeted): seL4 verification cheaper than full
  high-assurance certification but ~FACTOR 5 vs traditional good-quality embedded engineering
  ("a factor of 5 is not an unachievable goal for effort reduction"); spec/code/proof-size
  correlations suggest proof-effort ESTIMATION models exist (proof projects manageable);
  code/proof CO-GENERATION named as the scalability direction — "wherever code is generated, a
  proof of correctness should also be generated... to make reasoning possible at a higher, more
  productive level" (the EverParse/3D shape, independently converged).
- [A-fisher-hacms-program-2017] (targeted): the red-team datapoint — professional Red Team, six
  weeks, full source+docs access, could NOT break the verified SMACCMCopter ("probably the most
  secure UAV on the planet" per DARPA pentester); architecture = verified-codegen DSLs
  (Ivory/Tower embedded in Haskell emitting memory-safe C) + seL4 + AADL compositional
  verification; ~80k lines high-assurance code at 16 months.
- READ IN FULL: plans/021. The formal-methods verdict + exception are as scout B reported, plus
  a seed the excerpts missed: BOTH named exception-kernels were later discharged
  ARCHITECTURALLY, not by proof — "⊥ ⇒ provably no mutation" became structural-vouch-only +
  rul-only-oracle-bytes-ship; "skip ⇒ proven-converged" became the by-value ByVouch elide-weld.
  The project's standing instinct is make-it-structural over prove-the-algorithm; the residue
  that COULD NOT be made structural (solve preconditions: monotone/finite-height/semantic-Eq)
  is exactly where the empirical hang landed. The certifier targets precisely the
  un-structuralizable残り. Why3 (CoLiS lineage) was the anticipated tool of the exception.
- READ IN FULL: plans/055. Seeds: recency/strong-weak abstraction is "the keystone precision
  lever" (TAJS 87%->2% without it) and remains status-O — a committed-direction domain
  enrichment that will break flat-domain simplicity, confirming the human's KNOBS warning
  against tooling fitted to current flatness. Decision-6 re-names the inert-classification
  kernel as the one justified targeted property/Why3 site. The IFDS-distributive-split decision
  ("get this boundary right or the engine is imprecise or non-terminating") remains open (an-
  distributive-split O) — a second termination-adjacent English theorem awaiting mechanization.

## Citations

> [A-gamboa-usability-barriers-2025]:sec4.4.2 (relevance: +1:SURE)
> They would write the liquid types and check their verification, if it failed, there was
> certainly a problem to be fixed, but if it passed, it did not necessarily mean that everything
> was correct because the specification might be too permissive.

> [A-gamboa-usability-barriers-2025]:sec4.5 (relevance: +1:SURE)
> That's difficult because there's no hint given by the type checker to say, you know, you
> probably [need to add a] proof term manually.

> [A-gamboa-usability-barriers-2025]:sec4.6.1 (relevance: +1:SURE)
> So, I think if you ran the whole proof in the end, it took like, like, I don't know. 5h or so,
> maybe longer for the thing to verify.

> [A-gamboa-usability-barriers-2025]:sec5 (relevance: +1:SURE)
> These challenges are mostly related to liquid types being a layer on top of a base language,
> so other implementations that use liquid types as an add-on, such as Flux or LiquidJava,
> should be aware of these issues.

> [A-everest-perspectives-2026]:sec4.3.2 (relevance: +1:SURE)
> we often also suffered from proof instability [...], where tiny semantics-preserving changes
> (e.g., even simply renaming variables) affect the solver's search heuristics enough to change
> its result or performance.

> [A-everest-perspectives-2026]:sec4.5 (relevance: +1:SURE)
> We believed then that it was too costly to introduce additional abstraction layers, e.g., to
> first relate the executable code to a simpler idealized model, and then to carry out a
> cryptographic proofs on the model. In hindsight, this might have helped in separating concerns
> and allowed us to make better progress.

> [A-verus-practical-foundation-2024]:sec4.2 (relevance: +1:SURE)
> Such speed creates a qualitative advantage: Where we might verify one function at a time with
> a slower tool, we iterated while verifying the entire project, which provides an early warning
> when a change to a function contract breaks a proof elsewhere.

> [A-klein-provably-trustworthy-2017]:sec2 (relevance: +1:SURE)
> the formal verification was already cheaper than full high-assurance software certification,
> but that it is about a factor of 5 more expensive than traditional good-quality software
> engineering in embedded systems [...] A factor of 5 is not an unachievable goal for effort
> reduction.

> [A-fisher-hacms-program-2017]:Phase1 (relevance: -0:SUSPECT)
> By the end of this six week period, they were not able to hack into the SMACCMCopter. A
> penetration testing expert at DARPA commented that the SMACCMCopter is probably 'the most
> secure UAV on the planet.'

## Manifest note

[A-verus-practical-foundation-2024] registered this turn (URL-mode from the canonical MSR PDF).
The scout-A invariant census and scout-B corpus-theorem sweep full reports live in the
conductor's session only; their load-bearing content is banked in turn03 notes and this turn's
synthesis inputs.
