# turn 3 — 2026-08-13 17:05:51 CDT


Turn context: phase-2 scout — mapping Dorc's kernel + corpus against the phase-1 evidence base.
Human directives governing this turn: code是 a MODEL (throwaway); extract product-juncture
signals, not implementation detail; churn acceptable, future-irrelevance the danger; keep
result-certification AND Flux AND cheapest-wins all in scope; lean hybrid. Two sonnet scouts
dispatched (non-kernel-crate invariant census; corpus English-theorem sweep); kernel read
personally.

## Findings — kernel reads (core + analysis crates, personal)

- THE STRONGEST SIGNAL: `analysis/CLAUDE.md` solve-termination-unenforceable — the worklist's
  three preconditions (monotone transfer, finite-height domain, semantic Eq) are caller-upheld,
  explicitly "un-type-enforceable"; violation HANGS; empirically observed (ANALYZER-NEEDS
  an-monotonicity: 435/783 CPU-seconds). Mitigation today: iteration cap (n*1024+4096) +
  `converged == false` + a PER-CONSUMER obligation to check (`trust_reach` "never ambient").
  This is a discipline-shaped hole of exactly the class the phase-1 result-certification
  architecture closes: a post-fixpoint check per edge (`transfer(v, states[v]) ⊑ states[w]`)
  is ~10 lines, O(E), subsumes the flag, and validates capped solutions too.
- The Must<L> order-dual construction (lattice.rs) means ONE certifier covers both
  orientations automatically (Must's ⊑ IS the dual order). The union-where-you-meant-
  intersection bug is already a compile error (must-lattice-by-type; BoundedLattice gating).
  The kernel algebra is ~780 lines total (lattice 480 + solve 301) — tiny, stable-shaped,
  spec-frozen: the "crypto primitive" of the product per the Everest sizing lesson.
- Lattice laws (assoc/comm/idem/absorption/identity, both operators, + the two ⊑
  characterizations) are ∀-quantified equations currently checked against FIVE hand-picked
  samples per combinator (deliberate no-proptest: dependency-clean kernel). This is the
  exact Kani sweet spot: bounded exhaustive checking of equational laws over small element
  types, zero annotations, stable toolchain, no new deps in the kernel (harnesses live in a
  separate verification crate/dir). Also MapL's canonical-form invariant (no ⊥-valued key;
  structural Eq == semantic equality, which convergence detection RELIES on) is maintained
  by field privacy + two methods — a one-line Kani/property harness away from pinned.
- claim.rs is already AT the lightweight-tier ceiling: sealed tier traits, phantom-indexed
  Claim<Tier,P>, four unrepresentability properties (one-way demotion; mints demand tiers in
  signatures; no vouch→fact-plane path; rung structurally vouch-only) enforced by
  absence-of-code + compile_fail doctests as tripwires ("the one that repeals quietly — it
  starts compiling the day someone adds From<ByObservation> for ByVouch"). Neither Flux nor
  Kani strengthens whole-tree ABSENCE properties; the compile_fail-doctest pattern is already
  best practice (rustc-grade). Signal: tier-(a) discipline here is mature; tooling value
  concentrates elsewhere.
- unord.rs = rustc's UnordMap pattern (iteration API removed; sole exit is into_sorted_vec).
  Structural determinism enforcement, mature.
- erasure-licence-is-a-fence-not-a-guarantee (analysis/CLAUDE.md): ErasureLicense::for_site
  is PUBLIC and forgeable because analysis cannot dep plan; the real seal is one crate up; the
  in-crate fence is LEXICAL (a test asserting exactly one caller workspace-wide). Signal: the
  crate DAG inverts against the trust topology in places; type-system reach ends at crate
  boundaries and the project falls back to lexical gates. A named permanent juncture:
  license-minting seats want tool-checkable single-caller/information-flow properties.
- visibility-is-full-positional carries the sharpest admission in the corpus: a wrong-elision
  class where "no golden can see it" — site-keyed agreement (measuring one cell, keying the
  record to another) is structurally invisible to output-comparison testing. Calibration
  harness blindness is DOCUMENTED, named, pope-sin-tier. Mechanized checking (however
  delivered) has unique value exactly here.
- ANALYZER-NEEDS section O records TWO already-MEASURED wrong-elisions in the survival lane
  (28P: mid-emission body death → wrongly-narrow at-most footprint accepted and spared;
  missing dynamic reach-expansion → narrowed footprint → wrong elide). Both protocol/
  atomicity-shaped (transport framing, completion signals), NOT type-shaped: caught by
  measurement, fixed by whole-body-atomic refusal. Signal: the naked-trust cell's failure
  modes live in the authored-surface/transport protocol, where the tooling that helps is
  framing/closure discipline + DST, not refinement types.
- an-backing-set-meet names "an accidental existential in either consumer is an
  under-execution path" — the ∀-vs-∃ quantifier bug over backing-sets. Small, closed,
  logic-shaped: a good target for an exhaustive harness over small coordinate universes
  (Kani-able) and for the compare-chokepoint's centralization (already single-seat by law).
- an-lcg-quality: the DST harness ITSELF had a soundness bug (PRNG low-bit periodicity
  silently vanished a whole sweep topology cell). The verification net has its own
  correctness surface; a checker layer under the engine does not inherit DST's blind spots.
- kFAIL phase-keying (an-verdict-phase-keyed): "one lattice can't carry the two opposite
  kFAIL ⊤s" — phase-keyed verdict types built, with a flagged residual hole (151-SF3).
  Typestate-shaped, already mostly type-enforced.

## Synthesis-in-progress (mine; certainty markers inline)

- +SURE: the kernel decomposes exactly as phase-1 predicted. The algebra half (lattice,
  solve, claim, coord-compare chokepoints) is small, spec-frozen, permanent-shaped — every
  future implementation of this product needs these same laws. The engine half (effect.rs
  3.7k lines, value.rs 3.1k, cfg.rs 2.2k, funcenv.rs 2.2k) is churn-heavy, detail-entangled,
  and its correctness needs are mostly ALREADY addressed by structure (chokepoints, seals,
  lexical gates) + DST.
- ~SUSPECT: the highest-value single mechanism remains the post-fixpoint certifier at the
  solve seam — it directly closes the one empirically-bitten hole (hang/non-convergence
  trust), costs ~nothing at runtime (O(E) with cheap transfers), fails toward ⊤ = the
  product's own floor, and permanently de-risks the churny engine rewriting the solver
  heuristics. It is ALSO the natural home for later Flux/Lean attention (a tiny checker is
  affordable to verify at ANY tier).
- ~SUSPECT: Flux's marginal value on THIS kernel is lower than the human's prior suggests,
  not because quantifier-free is too weak (flat domains fit), but because the kernel's
  numeric-bound surface is thin (indices are newtyped and arena-checked; the heavy
  bounds/arity surface is the host-bytes intake in plan/cli — scout A confirms/denies) and
  the hard invariants are absence/flow-shaped (claim laundering) or protocol-shaped
  (survival-lane atomicity), which Flux does not address. The Flux-shaped wins that DO exist:
  interner/arena index validity, cap arithmetic, MapL canonicality, backing-set meet logic.
  Balance against: nightly-pin + ICE maturity cost is real today (phase-1
  [B-flux-verify-rust-std-tool-assessment-2026]).
- HUMAN DIRECTIVE (mid-turn, load-bearing): do NOT over-fit tooling choice to currently-welded
  KNOBS — several welds are spike-scoped (kSTATE "no state", intra-host order-sacred, etc.) and
  the tooling decision must SURVIVE knob changes; underpowered tooling chosen because a
  temporary weld keeps the domain simple is the named failure. Consequences: (a) the
  "flat-domains-fit-Flux's-fragment" argument DOWNGRADES from load-bearing to incidental —
  if richer/relational domains or the an-ide-value-layer arrive, quantifier-free stops
  sufficing, so Flux must be justified on its stable wins (index/overflow/cap arithmetic,
  struct invariants), not on domain flatness; (b) the certifier argument STRENGTHENS — a
  post-fixpoint check is domain-generic (any lattice, any future domain, same ~10 lines) and
  survives every currently-imaginable knob change including kSTATE unparking (a cached
  verdict re-validated by the checker is exactly the Blazy shape); (c) a kSTATE unpark drags
  hostile-host security work in with it, growing the INTAKE-trust surface — the tier that is
  newtype/bounds territory, where the existing discipline already concentrates.
- -GUESS: the cheapest-win ranking forming up: (1) Kani harnesses for lattice laws +
  MapL canonicality + backing-set universal-meet (stable, no deps, exhaustive at small
  types); (2) the solve certifier in plain hardened Rust (a DST-hammered ~50-line module);
  (3) compile_fail-doctest expansion to the remaining absence properties (contested
  write-once, funcenv lexical fences); (4) Flux trial scoped to core+analysis only, gated on
  its toolchain maturing; (5) a Lean/Dafny model of the coordinate-compare + sparing algebra
  ONLY if the field-trial makes the survival lane load-bearing (it is the one place a
  paper-sized model buys assurance the harness structurally cannot).
