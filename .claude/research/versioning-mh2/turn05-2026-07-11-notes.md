# turn 5 — 2026-07-11 11:51:31 CDT


Turn-05 (round close): the minimum-viable "now" set, derived design-agnostically; the
handoff to the live-work conductor REVISED (mostly retracted). Human context this turn:
nothing has yet been sent to the block-settle conductor; supply-chain/security remains
hard-deferred (typed; not elaborated, per standing practice).

## vers5-fd-relay-retraction (the headline: block-settle owes versioning ~nothing)

The turn01 §4 relay paragraph is RETRACTED in substance, on the round's own findings:

- Ask (i) — a version/grounding qualifier segment on coordinates — is WRONG by our own
  never-do: never-version-inside-identity (CVE-QWG external precedent) puts the window on
  the CLAIM, never the coordinate; and the corrected mechanism (a per-oracle gate whose rc
  demotes the sharp tier BEFORE any intersection runs) means version participates in NO
  algebra at all. Gate-not-algebra. Exclusion-checked across the four-by-two: backings,
  apply-phase guards, admin-side books, and lying-oracle attribution all key on
  coordinates/provenance, none on version — no cell wants version-in-key.
- Ask (ii) — hash-shaped canonical forms — is already unforeclosed by construction
  (resolve() output is an opaque string to the engine), EXCEPT for one soft edge: the
  entity/selector per-position CHARSETS being fixed in block-settle could accidentally
  forbid opaque/hash-shaped strings in canonical-form position.

Replacement handoff, total: one FYI line, zero design obligation —
> "Versioning asks nothing of the entity algebra (window-gates scope claims, not
> coordinates). Only: when fixing per-position charsets, don't make opaque/hash-shaped
> strings illegal in canonical-form position."

Consequence: turn01's mh2-lockin-1 (coordinate seam, TIME-SENSITIVE) is DISSOLVED — the
"shapes the fact-domain → retrofit-hostile" clause that kept MH2 scary is substantially
dead under gate-not-algebra. This is the round's biggest single de-risking.

## vers5-fd-minimum-viable-now (design-agnostic prerequisite acts, with true deadlines)

Filtered hard for "required by ANY improved version of the design" AND "irreversible if
missed." Three survive; only one is genuinely NOW, and it needs no conductor at all:

1. vers5-act-pin (HUMAN, one typed sentence, deadline = before any oracle corpus accretes
   — free today, rungs-locked once block-stdlib authors ~40 oracles and the trial invites
   outsiders): pin the reading of a version-silent oracle, e.g. "no version-promise ⇒
   sharp-tier withheld once tethering exists; everything below unchanged." Any improved
   design needs SOME pinned silence-reading; the rungs precedent (unmarked functions are
   never re-read) makes the flip unbuyable later. Dormant and harmless if versioning never
   ships.
2. vers5-act-stdlib-riders (banked here; deadline = block-stdlib DISPATCH, not now): (a)
   reserve the final version-role names in the munge-reservation lint at authoring time —
   permanent unversionable recognition surface, but the names themselves await the sibling
   work's promise-name conventions, so reserving now would reserve placeholders; (b)
   oracles born with machine-observed grounding stamps (observed build/version at
   authoring — capture-at-authoring: the data unrecoverable later), scoped per
   vers4-fd-division-of-labor to the machine-stamp half only.
3. vers5-act-central-pointer (CONDUCTOR, done this turn): TODO-ADDTL's MH2 entry refreshed
   to carry the round's outputs + these deadlines — the one agent-maintained central
   channel a future conductor reliably re-reads; everything else lives in
   .claude/research/versioning-mh2/ turns 01–05 and the sources manifest.

Explicitly NOT in the set (gentle-nack to implement-now, sustained): the engine gate, the
rc-glue, drift hints, `dorc pin bump` (both halves), the kind-owned compare floor — all
land naturally with block-stdlib or after block-context, where their substrate lives;
nothing about them is foreclosed by waiting; 270 §4's fence stands unamended.

## vers5-ru-hostile-window-residual (HUMAN-TYPED, closing the turn04 pushback)

Overclaim on the hacked-machine example conceded; a residual window is suspected of
"binary drifts that aren't the author's-versioning-speech-act's fault" (supply-chain-
flavored) — HARD-DEFERRED with the rest of security (memetic-hazard flag, not elaborated,
not to be picked up without a typed human ack).
