# turn 3 — 2026-07-11 11:07:07 CDT

Turn-03: the strawman round's rulings banked + the ship-evaluation ledger (Q2). The strawman
sh itself stays in-chat per house rule (conversation-flow only, no accidental spelling
lock-in); this note records the human's TYPED positions on it, the research-completeness
verdict (Q1), and the what-does-shipping-win analysis (Q2). Authority layers marked:
**HUMAN-TYPED** binding as positions · *conductor-derived* nack freely.

## Ack-ledger (HUMAN-TYPED this turn)

- vers3-ru-role-renames: the at-most family renames — `touches()` → **`cmd__disturbs()`**,
  reach → **`cmd__disturbance_reaches_only()`** (as typed this turn; né
  `kind__disturbance_only_reaches` in the prior turn's phrasing — word-order and the
  cmd-vs-kind keying of the reach role flagged for one canonicalization pass in the sibling
  work, where those names are being settled).
- vers3-ru-decline-rc2: a version-outside-window decline inside an emission role spells
  **`return 2`** — the family's can't-say lane ("no meaningful claim, cannot evaluate —
  because of version rather than because of arm") — not a bare `return 0`. Engine treats
  both as no-claim=wall; the rc lane carries the why for diagnostics/why-lens.
  *Conductor landmine noted for the eventual spec:* rc-0-with-empty-emission must NEVER read
  as "touches at most nothing" (an empty at-most set licenses everything); no-emission = no
  claim regardless of rc.
- vers3-ru-window-gate-approved (**approve HARD**, human-typed): the sm-window-gate shape —
  version-gating a traveling claim as ordinary control-flow around emission, one oracle
  entry-point (`__version_described`-shaped pin-probe) CALLED from another (`__disturbs`) —
  is approved as a direction. Grounds, near-verbatim: "very spelled-as-sh"; first real case
  of deriving meaning from one API entry-point calling another; and —
- vers3-principle-offramp-consumption (HUMAN-minted, candidate general principle): *show the
  off-ramp has value by consuming that value the way an off-ramped user would* — the user
  should type, daily, Dorc-free thingies that feel valuable the moment they type them;
  continuous advertising of the tool-choice. (Candidate for the human's root-doc queue —
  his voice, his call; registered here so it isn't lost.)
- vers3-ru-cargocult-gate: anti-cargocult razor applied to the explicit-gate-call question —
  if >80% would copy it and the <20% get no sharp immediate feedback, it's engine-handled
  ("Jarvis, activate the engine-handling"). IF sh-spelled version-gating ships, the rules to
  evolve are of the form "this is emitted into probes/applies at point X; rc Y has result Z"
  — **user-authors, we-glue** (house religion restated for this feature).
- vers3-ru-silent-enforce-lean: on the off-ramp-value question for the gate, the human's
  lean is **silent-enforce** — engine-inserted gating, not required-boilerplate — with a
  MAYBE exception when the call is statically present in the body (which doubles as the
  configurability/tuning exit-hatch). Two typed grounds: (1) in-oracle literal checks are
  not *prevented*, merely enforced — and the best-practices-degradation worry applies to
  books more than oracles; (2) the gate discharges a Dorc-specific amplified risk: oracles
  TRAVEL and PERSIST (slurped-up old code, unknown provenance/recency) in a way that
  post-off-ramp self-maintained scripts don't — increased risk borne of Dorc's own
  distribution story ⇒ increased responsibility Dorc must discharge.
- vers3-ru-window-grammar-tabled: the window-grammar fork (positional introduced/fixed args,
  epochs, OSV-events shape) is TABLED — not followed, not ruled; re-explain from scratch
  whenever it unparks.
- vers3-watch-unpark-temptation: the human is TEMPTED to un-park versioning into the current
  work-cycle on the strength of sm-window-gate ("dangerous finding"); evaluation = the Q2
  ledger below. NOT yet a decision; 270 §4's MH2 fence stands until he types otherwise.

## Q1 — remaining research? (conductor verdict)

+SURE the prior-art base is sufficient for the design-space the strawmen landed in
(window-gates + pin-as-probe + kind-owned compare + plan-surfaced binding). Nothing
literature-shaped blocks a spelling round. Optional confirmatory fetches, none blocking:
the TOFU-workflow pair (pip-compile-as-TOFU; ssh known-hosts UX) IF `dorc pin bump`
harvests live-host versions into the authored plane (the hostile-host/TOFU fence will need
its own design pass regardless); Drepper's DSO-howto ONLY if symbol-versioning mechanics
ever revive (the design did not go that way — plan-time binding replaced link-time marks);
GLEP 74 no longer needed (inline lean settled hard). The remaining unknowns are
design-empirical, not literature: the adequacy bite-rate (252 A1), real-world churn-rate vs
window-tightness (the never-tight-windows bound), and the no-orderable-version kind cell
(a genuine prior-art void — ours to invent).

## Q2 — the ship-evaluation ledger: what does this design win/cost if shipped now?

*(Conductor-derived throughout; the breach is priced first, per standing practice.)*

### The breach, priced first

- q2-cost-charter: `270` §4 fences MH2 out of round 27 BY the human's own ruling;
  block-settle (entity-algebra) is mid-flight under another conductor. Injecting a new role
  + engine gate mid-settle risks exactly the churn the round exists to prevent (the
  authored-once discipline: the corpus-respell deliberately waits so the fixture sweep
  happens once). Full unpark = charter amendment + coordination cost with the sibling work,
  paid immediately.
- q2-cost-engine: silent-enforce needs probe-emission glue (insert the pin-probe at point X,
  consume rc Y as result Z) in the probe compiler — which `270:block-context` is already
  churning (wrapper closure). Rider-sized, but riders are how rounds die; the
  static-presence exception-hatch additionally needs reached-path recognition (the only
  analysis-side cost in the design; skippable at v1).
- q2-cost-compare-floor: `never-universal-compare` forbids the cheap shortcut, so day-one
  needs real kind-owner compare code — portable pure-sh version comparison is a known small
  horror (the nvm specimen), and `dpkg --compare-versions` delegation is Debian-only. A
  bounded but genuine stdlib work-item.
- q2-cost-surface: +1 optional role for authors to learn. Mitigated: silence keeps every
  pre-existing behavior below the sharp tier (monotonic); the role is nine lines.

### What it wins (mapped to named standing corners)

- q2-win-tether: discharges the design's oldest acknowledged hole — the missing tether under
  rul24-divergence-is-the-game (the 24A §1b design-seed, delivered in comparison-key form
  rather than hash form: cheaper, churn-tolerant, and the hash arm can still grow quietly
  later exactly as scoped). Every evolution-skew scope-ruling that said "nearest answer:
  MH2 tethering" gets its answer.
- q2-win-honesty: the USER_STORY bought-unsoundness ledger gains one more MECHANICAL
  precondition before the corner can bite (live version within the claim's described
  window) — shrinking the undetectable class. Honestly bounded: claims wrong WITHIN their
  own window remain the frame problem, untouched.
- q2-win-stdlib-born-tethered (the sharpest timing argument): block-stdlib authors ~40
  oracles soon, then the field trial invites outside authorship. Shipping the CONTRACT
  (role name + the reading of its absence) before that moment means the corpus is born
  tethered; after it, the unmarked-reading pin is rungs-default-locked (unmarked functions
  can never be re-read) and the flip is permanently foreclosed. Today the flip is free —
  spike-only, zero outside users. This clock is the real argument for urgency, and it
  prices the CONTRACT, not the machinery.
- q2-win-context-synergy: the enabling substrate is already scheduled — the
  `$(tool --version)` capture is the sibling of `270:block-context`'s `$(hostname)`
  read-value slice, and `#version` facts are the first concrete consumer of the
  ide-value-layer coupling. Marginal build cost is the lowest it will ever be while that
  lane is open; it rises when the lane closes.
- q2-win-product-surface: the drift-hint lane ("foobar's oracle predates your foobar") is
  pure kWARN-rich material — warnings-are-the-product, keepalive-cheap; plus the
  offramp-consumption principle made concrete (the stripped pin-probe is daily-useful bare).
- q2-win-homes-old-debt: the founding `.version-compare` verb (homeless ~20 months) lands in
  the kind-owner family; an-version-applicability discharges in its cheapest form.

### The reconciliation that dissolves the sharpest cost

- q2-fd-open-windows-preserve-A1: shipping the tightening before the field trial measures
  the bite-rate it was meant to justify would muddy `252` A1's pre-registration
  (falsification-first says measure, then tighten). BUT: windows default OPEN
  (never-tight-windows), and an open window's gate always passes ⇒ semantics identical to
  the untethered system ⇒ A1's baseline measurement is preserved even with the surface
  shipped. The trial's adequacy data then TUNES window-tightness instead of justifying the
  mechanism's existence. Surface now, teeth dormant, evidence decides the tension. ~SUSPECT
  this fully resolves the methodological objection; it should be checked against 252's
  exact A1 wording on revival.

### The containment menu (the non-breaching cousin, and my lean)

Four reservations ship the contract today at ~zero block-risk; the machinery stays parked:

1. q2-res-names: reserve the role-name(s) in the munge-reservation lint (one line; the
   recognition surface is permanent and unversionable — cheapest irreversible act available).
2. q2-res-pin-ruling: the typed one-sentence human ruling on the unmarked reading ("an
   oracle without a version-promise: sharp-tier withheld once tethering exists") — free
   today, rungs-locked after the stdlib/trial. THE clock item.
3. q2-res-coordinate: the block-settle non-foreclosure (already relayed; CVE-QWG
   corroboration line available).
4. q2-res-observed-pins: stdlib-authoring brief rider — oracles born with observed-version
   pins from day one, even while the engine ignores them (capture-at-authoring: the data
   that can no longer be recovered later; lockin-3 discharged for the stdlib cohort).

Machinery (engine glue, drift hints, `dorc pin bump`) then lands as a named stage riding
block-stdlib or a micro-round after block-context — where its substrate lives anyway.
*Conductor lean:* reservations now, machinery on schedule, windows open through the trial.
Full-unpark's honest case rests on q2-win-context-synergy (build while the lane is open);
its honest price is q2-cost-charter. The human's call, and it interacts with the sibling
block-settle conductor either way.
