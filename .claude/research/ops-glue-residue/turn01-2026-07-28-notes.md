# turn 1 — 2026-07-28 07:46:00 CDT

Scope: pre-SSH lifecycle channels, and the feasibility/constraints of an offline compiled-guard
artifact (`dorc compile book.sh > payload.sh`). Eight questions; breadth over depth.

## Findings

- The hypothesis's channel claim survives; its **novelty** claim needs restating. A plain-sh
  every-boot user-data channel already exists in the dominant incumbent: `#cloud-boothook`
  [A-cloudinit-boothook-format-2026]. What does *not* exist is convergence machinery for it —
  upstream's own once-per-instance example is a hand-written guard.
- Upstream cloud-init **disclaims** re-running its own payload: "may be destructive and must never
  be done on a production system", framed purely as debugging [A-cloudinit-rerun-howto-2026]. The
  cell is not merely unoccupied — the incumbent has actively vacated it.
- "EC2 ~16KB" is **true and primary**: 16 KB raw, pre-base64 [A-aws-ec2-user-data-2026]. But it is
  the *tightest* mainstream cap by an order of magnitude — GCP allows 256 KB
  [A-gcp-startup-scripts-linux-2026], Azure 64 KB [A-azure-custom-data-2026], DigitalOcean 64 KiB.
  Sizing a compiled artifact against 16 KB is sizing against the worst cell, not the median.
- GCP is the counter-example cloud: `startup-script` runs on **every boot**, not once, and is
  manually re-runnable (`google_metadata_script_runner startup`) [A-gcp-startup-scripts-linux-2026].
  A GCP startup-script is already, semantically, a day-N convergence artifact with no convergence.
- Ignition/Talos are **not the same boundary**. Talos is genuinely conceded: no shell, no SSH, no
  busybox, at all [A-talos-philosophy-2026]. Ignition is *awkward*, not closed: maintainers'
  sanctioned pattern is a written file plus a one-shot `ConditionFirstBoot=true` systemd unit that
  "can run whatever commands you want" [A-ignition-issue-909-shell-2020].
- Ignition's rationale states the exact negation of artifact-unification as a design axiom:
  "Ignition runs only on the first boot… machine modification requires that users discard the old
  node and re-provision" [A-ignition-rationale-2026].
- Installer hooks are a *better* fit than cloud user-data, not worse: subiquity's own documented
  example is download-a-script-and-`/bin/sh`-it [A-subiquity-autoinstall-reference-2026]; preseed
  ships `preseed/run` which downloads a program and runs it [A-debian-preseed-custom-commands-2025].
  Both execute in a **busybox** installer environment [A-debian-installer-internals-2024], so the
  dialect floor question (Q6) is load-bearing here, not academic.
- Packer's shell provisioner is literally upload-one-file-and-exec:
  `chmod +x {{.Path}}; {{.Vars}} {{.Path}}`, default shebang `/bin/sh -e`
  [A-packer-shell-provisioner-2026]. Zero adaptation needed for a compiled artifact.
- The Q8 pain is real, long-lived, and **guard-shaped**: apt-daily racing cloud-init's own apt
  invocation, unfixed at the apt layer for years [A-cloudinit-aptdaily-lock-race-2017], with the
  community's answer being hand-rolled `while fuser /var/lib/dpkg/lock-frontend; do sleep 1; done`
  loops — 271 askubuntu hits for one query [B-sinjakli-apt-lock-timeout-2021].
- Secrets hazard, unprompted but design-bending: on EC2 the payload is an *instance attribute*
  readable back through IMDS, and Azure draws the line explicitly — custom-data is NOT in IMDS,
  user-data IS [A-azure-custom-data-2026], with the accompanying "We advise *not* to store sensitive
  data in custom data." A compiled Dorc artifact delivered this way is world-readable to every
  process on the box unless IMDS is locked down. Guards that embed probe *results* are fine; guards
  that embed credentials are a leak by construction.
- Re-run-on-clone is a genuine trap: cloud-init decides "first boot" by comparing instance-id
  against a cache, so an image captured from a `manual_cache_clean: true` instance silently never
  applies per-instance config again [A-cloudinit-first-boot-2026].

## Citations

### Q1 — cloud firstboot channels

> [A-aws-ec2-user-data-2026] (relevance: +1:SURE)
> User data is limited to 16 KB, in raw form, before it is base64-encoded. The size
> of a string of length *n* after base64-encoding is ceil(*n*/3)*4.
> User data is an instance attribute. If you create an AMI from an instance, the
> instance user data is not included in the AMI.

> [A-aws-ec2-user-data-2026] (relevance: +1:SURE)
> By default, you can include only one content type in user data at a time. However, you can use the
> `text/cloud-config` and `text/x-shellscript` content-types in a mime-multi part file to include
> both a shell script and cloud-init directives in your user data.

> [A-aws-ec2-user-data-2026] (relevance: -0:SUSPECT)
> EC2Launch v2 supports zipped user data as a method to submit user data that's larger than
> the 16 KB limit imposed by IMDS.
>
> └ note: this escape hatch is **Windows-only** (EC2Launch v2). No equivalent documented for Linux;
>   cloud-init's own gzip handling is the Linux analogue but is not an AWS-side cap increase.

> [A-gcp-startup-scripts-linux-2026] (relevance: +1:SURE)
> | `startup-script` | Passing a bash or non-bash startup script that is stored locally or added
> directly and that is up to 256 KB in size |
> | `startup-script-url` | Passing a bash or non-bash startup script that is stored in
> Cloud Storage and that is greater than 256 KB in size.

> [A-gcp-startup-scripts-linux-2026] (relevance: +1:SURE)
> | Metadata key | Order of execution |
> | `startup-script` | First during each boot after the initial boot |
> …
> ## Rerunning a Linux startup script
> `sudo google_metadata_script_runner startup`

> [A-azure-custom-data-2026] (relevance: +1:SURE)
> To use custom data, you must Base64-encode the contents before passing the data to the API…
> The size can't exceed 64 KB.

> [A-azure-custom-data-2026] (relevance: +1:SURE)
> When you enable custom data and run a script, the virtual machine will not report a successful VM
> provision until the script has finished executing. If the script exceeds the total VM provisioning
> time limit of 40 minutes, VM creation fails.

> [A-azure-custom-data-2026] (relevance: -0:SUSPECT)
> For single VMs, you can't update custom data in the VM model. But for Virtual Machine Scale Sets,
> you can update custom data.

> [A-azure-custom-data-2026] (relevance: +1:SURE)
> Azure also offers a separate user data feature. Unlike custom data, user data is retrievable from
> the Azure Instance Metadata Service (IMDS), is persistent for the lifetime of the VM, and can be
> updated without stopping or reimaging the VM.
> …
> ### Can I place sensitive values in custom data?
> We advise *not* to store sensitive data in custom data.

> [A-digitalocean-user-data-2026] (relevance: -0:SUSPECT)
> You cannot modify user data after a Droplet is created.

> [B-cloudinit-gzip-format-2026] (relevance: +1:SURE)
> Content found to be gzip compressed will be uncompressed… This may be useful when user-data size
> may be limited based on cloud platform.
> Some platforms are known to corrupt binary content, which prevents using this format.
>
> └ compression is the obvious answer to 16 KB, and it is explicitly NOT universally safe. Any
>   artifact-sizing argument that leans on gzip has to name the platforms.

> [A-cloudinit-boot-stages-2026] (relevance: +1:SURE)
> [Final] runs | as final part of boot (traditional "rc.local") … Things that run here include:
> package installations, configuration management plugins (Ansible, Puppet, Chef, salt-minion), and
> user-defined scripts (i.e., shell scripts passed as user-data).

> [B-cloudinit-boot-cmds-2026] (relevance: +1:SURE)
> `runcmd` only runs on first boot, **after** the instance has been started and all other
> configuration has been applied…
> `bootcmd` runs on every boot, and is typically used to run commands very early in the boot process

> [A-cloudinit-first-boot-2026] (relevance: +1:SURE)
> By default, `cloud-init` attempts to determine which case it is running in by checking the
> instance ID in the cache against the instance ID it determines at runtime. If they do not match,
> then this is an instance's first boot; otherwise, it's a subsequent boot.

> [A-cloudinit-first-boot-2026] (relevance: +1:SURE)
> If you want to capture an instance that is currently in `trust` mode as an image for launching
> other instances, you **must** manually clean the cache. If you do not do so, then instances
> launched from the captured image will all detect their first boot as a subsequent boot of the
> captured instance, and will not apply any per-instance configuration.

### Q2 — non-sh firstboot regimes (the hard boundary)

> [A-talos-philosophy-2026] (relevance: +1:SURE)
> We have no shell.
> We have no SSH.
> We have none of the GNU utilities, not even a rollup tool such as busybox.

> [A-talos-philosophy-2026] (relevance: +1:SURE)
> Everything which can be configured in Talos is done through a single YAML manifest.
> There is no scripting and no procedural steps.

> [A-ignition-rationale-2026] (relevance: +1:SURE)
> Ignition is designed to be used as a provisioning tool, not as a configuration management tool.
> Ignition encourages immutable infrastructure, in which machine modification requires that users
> discard the old node and re-provision the machine.

> [A-ignition-rationale-2026] (relevance: +1:SURE)
> Ignition configs do not allow users to provide arbitrary logic (including scripts for Ignition to
> run). Users describe which filesystems must exist, which files must be created, which users must
> exist, and etc. Any further customization must use systemd services, created by Ignition.

> [A-ignition-issue-909-shell-2020] (relevance: +1:SURE)
> Ignition configs are declarative and shell scripts are not. However, you can use Ignition to write
> a one-shot systemd unit that has `ConditionFirstBoot=true`, and that unit can run whatever
> commands you want.
> — bgilbert (CONTRIBUTOR)

> [A-ignition-issue-909-shell-2020] (relevance: -0:SUSPECT)
> E.g. a huge issue with cloud-init was ordering when scripts actually ran and often `bootcmd` and
> `runcmd` weren't good enough… With a systemd service, you can specify exactly when and how you
> want it to run in the native systemd DSL.
> — jlebon (MEMBER)

### Q3 — installer-time hooks

> [A-subiquity-autoinstall-reference-2026] (relevance: +1:SURE)
> [late-commands] Shell commands to run after the installation has completed successfully and any
> updates and packages installed, just before the system reboots. The commands are run in the
> installer environment with the installed system mounted at `/target`. You can run
> `curtin in-target -- $shell_command` to run in the target system

> [A-subiquity-autoinstall-reference-2026] (relevance: +1:SURE)
>   late-commands:
>     - curtin in-target -- apt-get update
>     - curtin in-target -- apt-get install -y curl vim
>     - curtin in-target -- curl -o /tmp/my-script.sh $script_url
>     - curtin in-target -- /bin/sh /tmp/my-script.sh

> [A-debian-preseed-custom-commands-2025] (relevance: +1:SURE)
> # This command is run just before the install finishes, but when there is
> # still a usable /target directory. You can chroot to /target and use it
> # directly, or use the apt-install and in-target commands…
> #d-i preseed/late_command string apt-install zsh; in-target chsh -s /bin/zsh

> [A-debian-preseed-custom-commands-2025] (relevance: +1:SURE)
> # Most flexibly of all, this downloads a program and runs it. The program
> # can use commands such as debconf-set to manipulate the debconf database.
> #d-i preseed/run string foo.sh

> [A-debian-installer-internals-2024] (relevance: +1:SURE)
> 2) **busybox init** : `/etc/inittab` is parsed; this contains: … init for VT2 and VT3 (busybox
> shell), and for VT4

> [A-debian-installer-internals-2024] (relevance: +1:SURE)
> **in-target** : Allows to run a command in a chroot on `/target` with the chroot being set up for
> more demanding operations. For example, `proc` and `sysfs` are mounted and a `policy-rc.d` is
> created. Can obviously only be used after the base system has been set up.

### Q4 — image-bake contexts

> [A-packer-shell-provisioner-2026] (relevance: +1:SURE)
> `execute_command` (string) - The command to use to execute the script. By default this is
> `chmod +x {{ .Path }}; {{ .Vars }} {{ .Path }}`

> [A-packer-shell-provisioner-2026] (relevance: +1:SURE)
> `inline_shebang` (string) - … By default, this is `/bin/sh -e`.

> [A-packer-shell-provisioner-2026] (relevance: -0:SUSPECT)
> *My shell script doesn't work correctly on Ubuntu*
> * On Ubuntu, the `/bin/sh` shell is dash. If your script has bash-specific commands in it, then
>   put `#!/bin/bash -e` at the top of your script.

### Q7 — prior art for artifact-unification

> [A-cloudinit-rerun-howto-2026] (relevance: +1:SURE)
> Most cloud-init configuration is only applied to the system once. This means that simply rebooting
> the system will only re-run a subset of cloud-init. Cloud-init provides two different options for
> re-running cloud-init **for debugging purposes**.
> Warning
> Making cloud-init run again may be destructive and must never be done on a production system.
> Artefacts such as ssh keys or passwords may be overwritten.

> [A-ignition-examples-2026] (relevance: -0:SUSPECT)
> In some cloud environments, there is a limit on the size of the config which may be provided to a
> machine. To work around this, Ignition allows configs to be replaced with the contents of an
> alternate, remote config. The following demonstrates this, using a SHA512 sum to verify the
> contents of the config.
>
> └ the size-cap escape hatch with precedent: a *small stub that fetches and hash-verifies the real
>   payload*, not compression. Note this reintroduces a network dependency the guard-artifact was
>   supposed to eliminate — the tradeoff is explicit, not free.

> [A-cloudinit-boothook-format-2026] (relevance: +1:SURE)
> A cloud boothook is similar to a user-data script in that it is a script run on boot… The boothook
> is different in that:
> * It is run very early in boot, during the network stage, before any cloud-init modules are run.
> * It runs every boot.

> [A-cloudinit-boothook-format-2026] (relevance: +1:SURE)
> ## Example of once-per-instance script
> #cloud-boothook
> #!/bin/sh
> # Early exit 0 when script has already run for this instance-id,
> # continue if new instance boot.
> cloud-init-per instance do-hosts /bin/false && exit 0
> echo 192.168.1.130 us.archive.ubuntu.com >> /etc/hosts
>
> └ this IS the hand-written idempotence guard Dorc's stage-1 lifts, shipped as upstream's own
>   documented idiom, in a plain-sh every-boot artifact. Direct evidence the shape is native.

> [A-cloudinit-cc-scripts-per-boot-2026] (relevance: +1:SURE)
> frequency = PER_ALWAYS
> …
> SCRIPT_SUBDIR = "per-boot"
> …
>     runparts_path = os.path.join(cloud.get_cpath(), "scripts", SCRIPT_SUBDIR)
>     subp.runparts(runparts_path)

> [A-k3s-manual-upgrades-2026] (relevance: +1:SURE)
> To upgrade K3s from an older version you can re-run the installation script using the same
> configuration options you originally used when running the install script.
> …
> curl -sfL https://get.k3s.io | <EXISTING_K3S_ENV> sh -s - <EXISTING_K3S_ARGS>
>
> └ the counter-thesis in its strongest form: a plain-sh, curl-piped, self-contained artifact whose
>   day-zero and day-N invocation are byte-identical, blessed by the vendor. Scope is the
>   distinguisher — one product, author-hand-rolled internal guards, and the doc immediately
>   enumerates what the script does NOT manage ("The contents of the configuration file are not
>   managed by the install script"; "does not drain or cordon the node").

### Q7 — read but ARCHIVE-BLOCKED (not citable as a slug; flagged for human retrieval)

`https://repost.aws/knowledge-center/execute-user-data-ec2` — read in full via mcp-fetch, but
`new-source.sh`'s `curl` download is refused by AWS re:Post (bot-blocked), so it could not be
registered. Substance, recorded here as ~SUSPECT-tier and NOT relied upon alone: AWS's own
documented recipe for running a user-data *shell script* on every restart is a MIME multipart
pairing a `text/cloud-config` part carrying `cloud_final_modules: - [scripts-user, always]` with a
`text/x-shellscript` part. Independently corroborated in mechanism by cloud-init's module-frequency
model [A-cloudinit-rerun-howto-2026] and by the existence of the every-boot plain-sh format
[A-cloudinit-boothook-format-2026]. A human should retrieve the page to confirm verbatim.

### Q8 — firstboot failure modes idempotence-guards would fix

> [A-cloudinit-aptdaily-lock-race-2017] (relevance: +1:SURE)
> apt-daily is now a systemd service rather than being invoked by cron.daily. If one builds a custom
> AMI it is possible that the apt-daily.timer will fire during boot. This can fire at the same time
> cloud-init is running and if cloud-init loses the race the invocation of apt (e.g. use of
> "packages:" in the config) will fail.
> …
> While it would be better to solve this in apt itself, I suggest that cloud-init be defensive when
> calling apt and implement some retry mechanism.

> [B-sinjakli-apt-lock-timeout-2021] (relevance: +1:SURE)
> This all started when I was looking into why instances in an auto scaling group were sometimes
> failing to bootstrap correctly. The machines were using a user data script…
> Looking in `/var/log/cloud-init-output.log`, it was clear that we were hitting a race condition
> part way through the script.

> [B-sinjakli-apt-lock-timeout-2021] (relevance: +1:SURE)
> while fuser /var/lib/dpkg/lock-frontend >/dev/null 2>&1 ; do
> echo "Waiting for other apt-get instances to exit"
> sleep 1
> done
> …
> Neither of these is especially satisfying. The first is susceptible to time-of-check to
> time-of-use (or TOCTTOU) bugs, while the second relies on the text output of `apt-get` never
> changing and eats your log output.

> [B-sinjakli-apt-lock-timeout-2021] (relevance: -0:SUSPECT)
> Working around the race condition was made harder in this case by the fact that this was all
> happening on a minimal Ubuntu image, where neither `fuser` nor `lsof` are installed by default.
>
> └ the guard-writer's tool-availability floor is *lower* than the runbook's. Bears on what a
>   compiled Dorc guard may legally emit.

> [A-cloudinit-runcmd-silent-drop-2019] (relevance: -0:SUSPECT)
> If user-data script contains a malformed runcmd script, cloud-init fails to parse it but do not
> mark cloud-init as failed. … But cloud-init status is "done". I expect the status to be "error"
