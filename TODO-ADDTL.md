(REWRITTEN AUTOMATICALLY, UNSTABLE. DO NOT REFERENCE IN OTHER DOCUMENTS.)

Dig through the design-docs in this repo; collate for me a list of 'undone design-work' that was either 1. mentioned by a human in-passing, but doesn't clearly map to any of the design-passes present; or 2. heavily pushed by a `plans/*.md` document, but seems like it may have gotten lost in the weeds. Sort higher: items with high design-consequences ("especially hard to unbake" or "can't be refactored"); and sort lower: items that seem known/deferred (in `TODO.md`, discussed by the human in recent design-passes, or clearly not-upfront-work.)

Update-by-overwriting this section; keep this descriptive header/prompt, just replace items. Keep it short; collapse similar/related items into one entry; this shouldn't grow over ~10 items. It's to catch *major work*, not nits.

> Notes by me, the human, in >-blockquote.

## TODO:

*(Last full refresh: 2026-07-10, at the round-24 close-out — re-graded against `notes/24U` (the reshuffle map) and `plans/270` (the round-27 charter). Everything owned by a dated, planned round was REMOVED — notably the corpus-respell / typeless-floor / entity-algebra-rebuild / wrapper-payload / stdlib arc (`270`), the tabled-but-specced field trial (`252` + `270` §5), and the tabled-but-specced multi-host round (`260`/`261`/`262` + `270` §5, which subsumed the old 22H-reactivity entry: r26 IS the 22H build). What remains below is cited by no owned round.)*

* [ ] **The Dorc language proper: the strict superset, the `unsafe` hatch, and the compat policy.** Still the canonical can't-unbake item, still homeless. The dialect marker landed (`# dorc-lang/v0.1`) but nothing defines what the superset adds/drops vs POSIX-sh; the welded must-handle set stands (const-prop, interprocedural, variables, heredocs — with the old "only punt: eval" weld now formally UNWELDED pending `270:adj-stopping-point`); the **kWHICHSH scope carve** is sharpened by evidence (`24R` §0b: famous dotfiles artifacts are bash/zsh; the human's lean — dorc-lang only in marked files, looser shells maybe tolerated in unmarked runbooks — is TODO.md-resident, undesigned); the human-required **Rust-`unsafe`-equivalent escape-hatch** (loses CFG-totality, taints its subgraph unskippable) appears in no current doc; and the fuller **verdict-churn / epoch / channel-stability policy** (`24O`'s homeless `dq-verdict-churn-policy` — same-book plan drift across releases is built-in and unnamed) has no home. Also parked here: the **verdict-family name-extensions** (the earned `.diff` verb; an is_noop-style fully-state-precise sibling — the family extends by name, never by re-reading a member).

* [ ] **The read-value/capture lane, past the first slice.** `270:block-context` builds only the single-LINE floor (un-⊤s `$(hostname)`-class captures). Everything deeper is orphaned and architecturally heavy (`notes/219` q-3/q-4): the **post-probe value re-bind** (a second value-flow pass — the value plane currently runs strictly BEFORE the probe; this back-edge is the deepest cost in the design and easy to bake wrong), multi-line/binary value-carriage (`219:fork-capture-wire`: base64-dependency vs pure-sh vs refuse), case-scrutinee arm-resolution precision (dead-arm folding on captured values), and `219:fork-capture-probe-body` (inner-command-verbatim vs declared probe body). High lock-in: pipeline-order and provenance decisions.

* [ ] **Cross-host shared-state soundness + the `kSTATE` persist-vs-recompute call.** Unchanged and unowned: the verdict-shape `(verdict, content-key, freshness)` is retrofit-hostile — decide the *shape* early even if built late; the human leans stateless (recompute from host truth). Standing riders: any unpark re-surfaces **an-host-as-adversary** (a reused verdict widens one host's lie to the fleet); the **wall-clock-keyed verdict classes** (pkgindex freshness, cert expiry) are the first hermeticity test-cases any content-key design must survive; rec-5 (probe-TAPE is write-only, never a reuse-cache) stands welded. NEW rider parked verbatim from `261:dec-timing-cache`: *does rec-5's no-re-ingest intent cover an advisory-only, license-nothing timing profile, or was it scoped to verdict/fact reuse?* — human-owned fence question, asked only when the cross-run-reorder punt lifts.

* [ ] **Precision + identity residue.** The selector half graduated (the entity-algebra rebuild, `270:block-rebuild`); what remains unowned: **no strong update exists** (the uniqueness bit is absent; `Kill` accumulates; the 231 fence — "probably unique" may only DEMOTE — stands); **partial-member convergence** (the across-MEMBERS half; needs per-member `self_reach` re-derivation); **an-ide-value-layer** (version-carrying facts; couples to MH2 below); the **may-alias-default RULING** (`24O` item-25: the data arrives from planned work — the yardstick-measurement sensitivity number + the trial's real-resolver fire-rate — but the flip-or-confirm decision has no owner and must never happen silently); and the **kind-unify / co-reference mechanism** (`24M:rul-kind-unify-owed` + `24S:A5` + `24C:strain-coreference-crosskind` — three pointers at ONE future mechanism: engine-mediated cross-kind/cross-name identification; design it once or two mechanisms will exist for one act).

* [ ] **MH2 — the version layer.** Components unchanged: version-aware verbs over a cross-package-manager version lattice (no join across managers); the binary-content-hash grounding gate (record-and-compare `$PATH` hash at probe/apply; fail-safe on mismatch); the purl-shaped version coordinate (hash = identity, purl = comparable intent). Still the named candidate-tightening if the field trial's adequacy data bites (the license-site ≠ elision-site tether nobody has built); still absent from DESIGN; still shapes the fact-domain → retrofit-hostile.

      > versioning (the simpler version of the concept, focused down to 'package-as-a-type-needing-special-attention, multi-providers, etc' ... and 'mapping oracle-written-for vs being-executed-on') needs close care, but it feels very deferrable compared to some of the critical core analyzer-design things that are affected by the "sh spelling" issues. continuing to defer.

* [ ] **Wrapper/payload residuals `270` does NOT own.** The context/payload spike builds user+ρ and R0/R1-as-ratified; orphaned beyond it: **fs-view, the Hard cell** (`24S` §3b: mount-table truth; naive prefix-rebase officially wrong; the enumerated-bridge ⊑ in-context-map ⊑ may-alias ladder and the DETECT-and-DEGRADE stance — seams reserved, nothing built, nobody owns the build); **guard-insertion under ELEVATED wrappers** (the 23J lane-privilege carve — Dorc-authored checks in a privileged lane; wrapped heredoc-books get elision-edits only until this is designed); the **become/doas privilege prior-art collation** (research round-2, HARD-DEFERRED behind a typed human ack — wanted before wrapper-oracle authoring widens past sudo/su/env); and 24S's W3–W4 stages if their gate adjudications (`270:adj-survival-flag-outcome`, `270:adj-trichotomy-spelling`) stall past the round.

* [ ] **Error-reporting/provenance: the N-tier locator-DAG firming.** The live half is built (structured diag API — the design-for-keeps exception; ProvId arena; why-lens + why-lanes); the never-had-a-consumer half — the per-host-forking locator DAG + transport-minted session correlation (`111` dac-A, an-loc-session) — gets its first real consumer with the first real executor, i.e. the multi-host resumption. Nobody owns scheduling that pass; the r25 runner's flat `{transcript, rc}` was a disclosed spike-cut, not precedent.

* [ ] **Oracle-author DX (linter / LSP / authoring harness).** Still DESIGN §5's "where the bulk of the work lies" and the #1 existential-risk lever (oracle quality); still machine-enforcement-not-author-discipline (the paranoid-author shipped-bug evidence stands). The backlog GREW this round: the wrapper-oracle quality bar (`24S:A6` — peel cross-check, argparse lints, self-vouch/self-footprint requirements, expressibility clause), the carrier bar (`24T:P-A4` — which-arg-is-code arity gates, parse-and-resolve cross-check, reconstruction differential, dorcism-in-payload lint), and the **adjudicability build-list** (`24S:A4` — machine-read kind-topology clauses, binding-smell lints, differential discharge in stdlib CI; MUST land before kinds go community-shared, and no round owns it). Low lock-in → sorts down; highest-leverage omission.

* [ ] **The human's root-doc queue (his voice; punted 2026-07-10 to post-spelling-settlement).** The README "lazy code motion for shell" positioning line (citations ready in `23N`); DESIGN's `(UNFINISHED)` typed-sh / Inference-limitations continuation (16Q §4 — settled material now vastly exceeds the old recommendation; the founding one-liner example also needs the post-respell spelling); the **post-respell wrongness set** (the day the corpus-respell lands: USER_STORY stage-3's dotted `foobar.is_converged()` becomes a parse-error form, stage-5's stringly `touches()` is dead, `fb.Certs` violates two-dots, share-a-file retold marker-gated, the stage-3 nine-liner's missing arity gate); the formal **dq-kOOB stamp line** (one human sentence; substantially discharged by the `24M` rulings); accumulated line-fixes (IMPLEMENTATION's "three possible outcomes" enumerating four).

* [ ] **(Deferred-surface seams grab-bag — reserve, don't build; each with a live re-entry pointer.)** The **barrier / placement-spectrum performance round** (`23O` §4: hoisted past-wall re-check waves — performance-only by the consent-wall; must COMPOSE with the r26 re-fold machinery when both exist); real-time **streaming/TUI** (dual-render, degrade to plain-text; why-elided/why-probed as primitives); `retries:`/`until:` convergence-by-retry as first-class syntax; `serial:`/rolling-batch **non-preclusion**; the parked **escape-hatches** (`235`: hatch-isolate, hatch-bump-exclude; unpark = admin-recourse pressure) and the **veto polarity** (owner-spelled judgment-tier veto list); the 077 remnants — the unprivileged **seccomp `socket(AF_INET)` observe backstop in core** and the **`--faithful` one-leaf-one-exec mode** (both carried to the real-executor era by `24O`, unowned); and the **`24R` cheap-adds pile** (local-exec blessing — triple-load-bearing, zero tests today; `dorc plan --exit-code`; book-reads-stdin + sudo-keepalive lints; previewer/pre-commit packaging; executable-inventory consumption; changed-report adapters) — small, unowned, natural round-27 riders if capacity appears.
