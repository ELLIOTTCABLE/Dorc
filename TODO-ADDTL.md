(REWRITTEN AUTOMATICALLY, UNSTABLE. DO NOT REFERENCE IN OTHER DOCUMENTS.)

Dig through the design-docs in this repo; collate for me a list of 'undone design-work' that was either 1. mentioned by a human in-passing, but doesn't clearly map to any of the design-passes present; or 2. heavily pushed by a `plans/*.md` document, but seems like it may have gotten lost in the weeds. Sort higher: items with high design-consequences ("especially hard to unbake" or "can't be refactored"); and sort lower: items that seem known/deferred (in `TODO.md`, discussed by the human in recent design-passes, or clearly not-upfront-work.)

Update-by-overwriting this section; keep this descriptive header/prompt, just replace items. Keep it short; collapse similar/related items into one entry; this shouldn't grow over ~10 items. It's to catch *major work*, not nits.

> Notes by me, the human, in >-blockquote.

## TODO:

*(Last full refresh: 2026-07-05, post round-24 cross-cutting audit — statuses re-graded against the ternary-verdict / claim-tier / footprint world of rounds 23–24 and the round-25 field-trial redefinition.)*

* [ ] **Reactivity / live-plan (22H): concurrent-probe streaming → incremental per-host re-analysis — DECISION DUE at r24 close (human).** The founding anti-Ansible real-time motivator: probes stream concurrently from N hosts, the per-host plan re-folds live. The unit is *replacement*, not run/not-run, and is NOT provably stable; the arrival-ordering seam / per-host fact-accumulators / classify-once-refold-per-arrival session loop must be *built* (`hostsim` has none). Highest lock-in in the corpus. Status shift (2026-07-05): the old "round-25-ish" slot no longer exists — r25 was redefined as the field trial — so 22H is currently UNSCHEDULED; the human's own reassess-at-r24-close trigger has fired, and the r24 conductor owes the decision inputs (cruft-verdict on spike reuse) at Stage-6 close-out (ack'd). Riders that live or die with it: the banked coordination-DST hooks (24B §5: three-valued-verdict-under-truncation, retry/no-double-apply under Unknown — they live at the transport-stream seam WITH this orchestrator, and their "round-25" labels now dangle); the 151-M3 physical knot it forces (`an-marker-protocol` / `an-backpressure-decouple` / `an-async-vs-statemachine` — mutually-exclusive DST cost-dodges, pick one day-1; the r25 trial legitimately dodged all of it via the welded whole-script floor, 22H cannot); and the parked placement-spectrum/barrier round (23O §4, the *performance* product) which overlaps the re-fold machinery — must compose, not collide. SEED: `Research/plans/22H`.

      > human note (2026-07-03): this is round-25-ish, not round-24. Genuinely important, *completely* unexplored territory — I want to get a move-on precisely so I can apply Fable-class attention to it ASAP (before Fable gets expensive). It MAY slot onto the same round-24 spike if that spike hasn't grown too crufty by then; reassess at round-24 close.

* [ ] **The Dorc language proper (the strict superset) + the `unsafe` escape-hatch.** Unchanged and still homeless: `DESIGN` ranks the language "*most* critical when calibrated against lock-in," yet nothing defines what the superset adds/drops vs POSIX-sh; the **welded must-handle set** (const-prop, interprocedural, variables, heredocs — *"the one and only construct I am punting on is `eval`"*); the **kWHICHSH** sub-axis (r23 h3 ruling leans dash-ish "barely-more-than-classic-POSIX"/POSIX2024, `local` expected); and the human-required **Rust-`unsafe`-equivalent escape-hatch** that loses CFG-totality and *taints its control-flow-subgraph so it can never be performantly skipped* — which appears in no current doc. Canonical can't-unbake item once users write scripts. (Replicated in `23O` §5; this entry is the outside-round home.)

* [ ] **Cross-host shared-state soundness + the `kSTATE` persist-vs-recompute call.** The verdict-shape `(verdict, content-key, freshness)` is retrofit-hostile — decide the *shape* now even if built later; the human leans stateless (recompute from host truth, rust-analyzer-style). Standing fences: **rec-5** (r22) — the probe-TAPE is a write-only postmortem durable, NEVER the kSTATE reuse-cache; do not conflate. New riders (2026-07-05 audit): (a) re-surface **`an-host-as-adversary`** *together with* any memoization unpark — a forged convergence verdict reused across an equivalence class widens the blast-radius from one host's plan to the fleet's; (b) the **wall-clock-keyed verdict classes** (pkgindex "index fresh", cert-expiry) are the first hermeticity test-cases for any cache — they are inherently volatile-keyed and will break naive content-keying first. (The probe→apply TOCTOU half stays RESOLVED-WONTFIX with the identified-cause clarifier: guards catch named in-book causes; open-world drift-machinery stays banned.)

* [ ] **Oracle-contract residual: the last unsettled spellings + the license surface.** The bulk is now settled and built (r23/r24): the role-sibling family, rc-partition, strip-fidelity, **rul24-vouch-is-verdict-authoring** (authoring the verdict-function IS the vouch; the tilde mark is dead), typed emission for `reaches()` (kind = trailing annotation; vocabulary closes at lift). Still genuinely open: the **kind-annotation's final concrete syllable** (dq-kOOB, human-reserved — `dest : fb.Certs = "$1"` is stamped-in-practice with the strip pass paying the off-ramp; the formal weld pending); **kind collision + evolution semantics**; **bootstrap-kind curation**; the **provides-license rung-ladder** (ORACLE_PROVIDES: rung-0 display / rung-1 guard / rung-2 elide — the wary-engineer hatch, in tension with the one-act weld of convergence-authorship to rungs 1+2; "the least sh-native thing in the entire design," the one place that may *earn* a loud first-class construct under native-or-break-loudly); and **verdict-family extensions by name** (the deferred `.diff` earned verb; the is_noop-style fully-state-precise stronger sibling — proviso-family-open: the family extends by name, never by re-reading an existing member).

      > human note: we're "spiking" on this, trying out code approaches, *because* it's a hard problem. last spike ran hard into limitations though.

* [ ] **The precision residue: strong-update / uniqueness / selectors / value-carrying facts.** The 16Q §1 keystone claim ("without the precision/recency layer nothing elides on a realistic book") is *partially discharged* by a different architecture than it imagined — the vouch + guard-tier + footprint machinery now elides a realistic book's ambient top — but its components remain open and re-key under entity-identity work: **no strong update exists** (the uniqueness bit is absent, not boolean; `Kill` accumulates; the 231 fence stands — a "probably unique" verdict may only DEMOTE toward weak/⊤, never promote); **per-entity selector precision**; **`an-ide-value-layer`** (version-carrying facts — couples to MH2 below); **partial-member convergence** (the across-MEMBERS half; needs a per-member `self_reach` re-derivation — the across-ENTITIES half is DEAD by the atomic-command axiom). Wrong entity-shape re-keys every transfer function and the substrate (`dq-entity-algebra`; lean = recursive JSON-adjacent struct, 17N §4). Stage-5A closed within-kind *aliasing* for resolver-bearing kinds; the rest of the identity cell stands. Refs: `16Q` §1/§3 · `24A` §2 (1b/1e disposals) · `24C` §Stage-5A.

* [ ] **MH2 — the version layer.** Components unchanged: version-aware oracle verbs over a **cross-PM version lattice** (partial order, no join across package-managers); the **binary-content-hash grounding gate** (oracle binds grounding to a content-hash; runner hash-checks `$PATH` at probe/apply; fail-safe on mismatch — record-and-compare, no registry); the **purl-shaped version coordinate** (hash = identity, purl = comparable intent — two jobs, never conflated). Status shift (r24): re-justified as **the missing tether** under rul24-divergence-is-the-game — the elision-site mechanically validating a traveled claim's contextual assumptions ("the binary you're eliding around is the binary I described") is the one defense against license-site ≠ elision-site divergence nobody has built; and it is now wired as A1's named candidate tightening in the r25 protocol, so the trial's adequacy-bite data feeds the unpark decision. Still absent from DESIGN; shapes the oracle-contract + fact-domain → retrofit-hostile.

      > versioning (the simpler version of the concept, focused down to 'package-as-a-type-needing-special-attention, multi-providers, etc' ... and 'mapping oracle-written-for vs being-executed-on') needs close care, but it feels very deferrable compared to some of the critical core analyzer-design things that are affected by the "sh spelling" issues. continuing to defer.

* [ ] **Error-reporting + provenance: the N-tier firming.** The live half is BUILT (r22: the structured diag API — battlefield-bound, the one design-for-keeps exception to spike disposability; the ProvId arena + the ru-11 one-way weld; the why-lens) and r24 extended it (why-lanes: run-cause / survives-attribution / guard; the 24H caret-art multi-span render). What remains unfirmed is exactly the part that has never had a consumer: the **N-tier, per-host-forking locator DAG** + the transport-minted per-host session correlation (`plans/111` dac-A, `an-loc-session`) — its first real consumer arrives with real hosts. The r25 P3 runner's flat `{transcript, rc}` shape is a *disclosed* ru-26-style spike cut, not precedent; when a real executor exists, this pass is due.

      > human note (2026-07-03): critical-TIER, but orthogonal enough to survive a redirect/defer, and safely REORDERABLE. Likely belongs on the same round-24 spike (rich provenance is what makes the elision-yardstick legible — "why did/didn't this line elide"), but not a round-24 blocker; sequence it in whenever convenient.

* [ ] **Oracle-author DX (linter / LSP / authoring harness).** `DESIGN` #5 = "where the bulk of the work lies"; the #1-existential-risk lever (oracle quality). The machine-enforcement lesson is now empirically double-grounded (151 X4: *paranoid* authors still ship silent wrong-skips — the ufw `.`-as-regex under-execute, the apt-get `-o` probe-mutation; "the contract needs machine-enforcement, not author-discipline"). Interim manual enforcement exists as the r25 P5 quality-bar checklist (17O regression classes R2-SHADOW/R2-IDCACHE/R2-ORTRUE/F-GETENT-HOSTS · R2-MULTIOP arity gates · `an-probe-shape` rc-conflation — no bare `cmd | grep -q` probes · the verdict-function negative contract); the standing lint backlog (don't-collapse verdict-function; branch-aware coverage lints) grew through r23/24. Low lock-in (buildable later) → sorts down; highest-leverage omission.

* [ ] **The human's root-doc queue (his voice; carried from r23 #16).** The README "**lazy code motion for shell**" positioning line (load-bearing framing, not metaphor; citations ready in `23N`: Knoop–Rüthing–Steffen LCM PLDI'92, Morel–Renvoise PRE CACM'79, optionally frame-rule + dynamic frames); adopt the `23N`/`23O` §6 vocabulary in DESIGN prose; DESIGN's `(UNFINISHED)` typed-sh section + the "Inference limitations" continuation (the settled shape per `16Q` §4: fact-centric named-kind anchor, 3-place, lifted-never-run); accumulated line-fixes (e.g. IMPLEMENTATION's "three possible outcomes" enumerating four, noted 2026-07-05).

* [ ] **(Deferred surface — reserve the seams, don't build.)** Each human-mentioned and *deferred-not-excluded*; each needs its seam reserved so it isn't unbakeable later: real-time **streaming** of remote command output + the dual-render TUI (must degrade into do-one-thing-well plain-text controls; why-elided/why-probed as the primitive queries); `serial:`/rolling-batch **non-preclusion** of the parallelism model; `retries:`/`until:` convergence-by-retry as first-class syntax; `sudo`/`become` execution-context first-classing + **lane-privilege** (`23J`: a vouch promotes oracle code into the book's elevated context once become exists) + wrapper-context kinds generally (17N §7: host/user/container/lock, designed once across analyzer + transport); the parked **escape-hatches** (`235`: hatch-isolate, hatch-bump-exclude; unpark signal = admin-recourse pressure) and the **veto polarity** (ORACLE_PROVIDES provides-license: owner-spelled judgment-tier veto-only list, safe-twin failure direction; unpark-bar = evidence that expansion-bridges don't suffice); and — **ownerless since round 5, flagged 2026-07-05** — the rest of the `plans/077` live-constraint set beyond the honored leaf-seam: the unprivileged **seccomp `socket(AF_INET)` observe backstop that ships in core for all users** (`an-network-cost`/`an-undeclared-net`: zero-annotation cost-class + undeclared-network detection, fail-closed by construction), **batch-attribution recoverability in optimized mode**, and the **`--faithful` one-leaf-one-exec mode** (kFIDELITY's reserved seam — the tracer's and the debugger's mode). r24 Stage-6 extraction is asked to carry the 077 set into the human docs or surface it for explicit retirement; this entry is the backstop.
