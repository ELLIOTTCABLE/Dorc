(REWRITTEN AUTOMATICALLY, UNSTABLE. DO NOT REFERENCE IN OTHER DOCUMENTS.)

Dig through the design-docs in this repo; collate for me a list of 'undone design-work' that was either 1. mentioned by a human in-passing, but doesn't clearly map to any of the design-passes present; or 2. heavily pushed by a `plans/*.md` document, but seems like it may have gotten lost in the weeds. Sort higher: items with high design-consequences ("especially hard to unbake" or "can't be refactored"); and sort lower: items that seem known/deferred (in `TODO.md`, discussed by the human in recent design-passes, or clearly not-upfront-work.)

Update-by-overwriting this section; keep this descriptive header/prompt, just replace items. Keep it short; collapse similar/related items into one entry; this shouldn't grow over ~10 items. It's to catch *major work*, not nits.

> Notes by me, the human, in >-blockquote.

## TODO:

*(Last full refresh: 2026-07-10, at the round-24 close-out — re-graded against `notes/24U` (the reshuffle map) and `plans/270` (the round-27 charter). Everything owned by a dated, planned round was REMOVED — notably the corpus-respell / typeless-floor / entity-algebra-rebuild / wrapper-payload / stdlib arc (`270`), the tabled-but-specced field trial (`252` + `270` §5), and the tabled-but-specced multi-host round (`260`/`261`/`262` + `270` §5, which subsumed the old 22H-reactivity entry: r26 IS the 22H build). What remains below is cited by no owned round.)*

*(Cleanup pass 2026-07-16, post-block-settle: re-graded against the block-settle closure (`plans/271` · `notes/276`–`278`), the transport resolution (`plans/27C`), and the crosscheck adjudication (`notes/279f`). Discharged clauses removed — most of the 2026-07-12 annotation-pass asks landed (the `276` language sitting happened; the capture-seam and uniqueness-seam riders were hand-carried and banked). No fresh corpus dig; no new items hunted.)*

* [ ] **The Dorc language proper — the residue after the `276` sitting.** The 2026-07-12 human dive HAPPENED (`notes/276` + the `278` v0.1 reference, DRAFT — the superset's adds/drops are now assembled there): kWHICHSH is WELDED to minimum-lcd as the posh∩dash two-binary executable floor and KNOBS-registered; the `unsafe` hatch is ruled bare-sh (`276:rul-unsafe-is-bare-sh` — descend-don't-license); the epoch/verdict-churn policy is discharged at design tier (`276:rul-verdicts-never-stable` — syntax marker-gated · names permanent · verdicts never stable); `trap` is dispositioned (recognized-but-unmodeled, walls loudly); the old "only punt: eval" question landed as `271:rul-stopping-point-unpinned` (closed, deliberately UNPINNED). Still homeless after all that: the **book-acceptance carve** (`276:rul-kwhichsh-oracle-scoped` deliberately left it open — the value-ladder for unmarked/bash-ish runbooks (parse-for-sites / probe-verdicts / guard-lift / guard-insertion) is undesigned and unowned; the `24R` §0b evidence — famous dotfiles artifacts are bash/zsh — still bears on it); and the **verdict-family name-extensions** (the earned `.diff` verb; an is_noop-style fully-state-precise sibling — extend-by-name-never-re-read is ruled, the members themselves stay parked).

* [ ] **The read-value/capture lane, past the first slice.** The 2026-07-12 seam-rider landed (`271:rider-value-recipe-reshape-capture-seams` — the re-bind seams are named in the value-recipe-reshape brief) and the claim shape settled (`notes/275`; its §6 transport chain stays NOT-RATIFIED/refused-as-posed — cross-context consumption routes through `plans/27C` now). Still orphaned and architecturally heavy (`notes/219` q-3/q-4): the **post-probe value re-bind** (a second value-flow pass — the value plane currently runs strictly BEFORE the probe; this back-edge is the deepest unowned cost in the design and easy to bake wrong), multi-line/binary value-carriage (`219:fork-capture-wire`: base64-dependency vs pure-sh vs refuse), case-scrutinee arm-resolution precision (dead-arm folding on captured values), and `219:fork-capture-probe-body` (inner-command-verbatim vs declared probe body). High lock-in: pipeline-order and provenance decisions.

* [ ] **Cross-host shared-state soundness + the `kSTATE` persist-vs-recompute call.** Unchanged and unowned: the verdict-shape `(verdict, content-key, freshness)` is retrofit-hostile — decide the *shape* early even if built late; the human leans stateless (recompute from host truth). Standing riders: any unpark re-surfaces **an-host-as-adversary** (a reused verdict widens one host's lie to the fleet); the **wall-clock-keyed verdict classes** (pkgindex freshness, cert expiry) are the first hermeticity test-cases any content-key design must survive; rec-5 (probe-TAPE is write-only, never a reuse-cache) stands welded. NEW rider parked verbatim from `261:dec-timing-cache`: *does rec-5's no-re-ingest intent cover an advisory-only, license-nothing timing profile, or was it scoped to verdict/fact reuse?* — human-owned fence question, asked only when the cross-run-reorder punt lifts.

* [ ] **Precision + identity residue.** Improved but not closed by `notes/277`: the **uniqueness-bit seam is now RESERVED** (`271:rider-entity-algebra-uniqueness-seam`; `277` §5 — room in the coordinate/comparison representation), but no strong update exists at v1 (`Kill` accumulates; the `231` "probably unique may only DEMOTE" fence stands; *building* the bit is unowned, and uniqueness is SF-1-undecidable — someday judgment-tier); the **kind-unify / co-reference mechanism stays parked but is consolidated as ONE mechanism** with a door-swing seam (`277` §6 — the kind-fence stays pre-canonicalization at v1; the old three-pointer scatter — `24M:rul24M-kind-unify-owed` · `24S:A5` · `24C:strain-coreference-crosskind` — now folds there). Still fully unowned: **partial-member convergence** (the across-MEMBERS half; needs per-member `self_reach` re-derivation); **an-ide-value-layer** (version-carrying facts; couples to MH2 below); and the **may-alias-default RULING** (`24O` item-25: the data arrives from planned work — the yardstick-measurement sensitivity number + the trial's real-resolver fire-rate — but the flip-or-confirm decision has no owner and must never happen silently).

* [ ] **MH2 — the version layer.** Substance unchanged from the 2026-07-11 side design-round (`.claude/research/versioning-mh2/` turns 01–05: the version-WINDOW gate as HUMAN speech-act — gate-not-algebra, so the old retrofit-hostility clause stays dissolved; MACHINE-stamped grounding; kind-owned comparison, never-universal-compare). Still deferred (`270` §4 stands); still the named tightening if trial A1 bites. Prerequisite-acts, updated for block-settle's closure: (1) HUMAN one-sentence pin of the version-silent-oracle reading — still free, still un-typed; the clock remains block-stdlib/outside-authors; (2) at block-stdlib dispatch: reserve final version-role names in the munge-lint + oracles born with machine-observed stamps (capture-at-authoring); (3) the charset FYI (don't forbid opaque/hash-shaped canonical forms) was never explicitly acked before block-settle closed — `277` §4b's POSIX-in-spirit charsets *look* compatible but this is unverified; confirm explicitly when block-rebuild pins the parser charsets.

      > versioning (the simpler version of the concept, focused down to 'package-as-a-type-needing-special-attention, multi-providers, etc' ... and 'mapping oracle-written-for vs being-executed-on') needs close care, but it feels very deferrable compared to some of the critical core analyzer-design things that are affected by the "sh spelling" issues. continuing to defer.

* [ ] **Wrapper/payload residuals no block owns.** Re-framed under `plans/27C` (context-entry probing now owns the wrapped-site probe question; `notes/273` owns the authored surface; the old W3–W4 stall-contingency is moot — both gate adjudications closed at block-settle, and the stages ride `270:block-context`). Still orphaned: **fs-view, the Hard cell** (`24S` §3b as re-read through `273`/`27C`: mount-table truth; naive prefix-rebase officially wrong; the enumerated-bridge ⊑ in-context-map ⊑ may-alias ladder and the DETECT-and-DEGRADE stance — seams reserved, nothing built, nobody owns the build; now explicitly sequenced BEHIND netns per `271:rul-networking-unpunt`, with `27A`'s wall inventory standing law for the fallback lane); **guard-insertion under ELEVATED wrappers** (the 23J lane-privilege carve — Dorc-authored checks in a privileged lane; `27C` is probe-side and does not answer this apply-lane question; wrapped heredoc-books stay elision-edits-only until it is designed); and the **become/doas privilege prior-art collation** (research round-2, HARD-DEFERRED behind a typed human ack — wanted before wrapper-oracle authoring widens past sudo/su/env).

* [ ] **Error-reporting/provenance: the N-tier locator-DAG firming.** The live half is built (structured diag API — the design-for-keeps exception; ProvId arena; why-lens + why-lanes); the never-had-a-consumer half — the per-host-forking locator DAG + transport-minted session correlation (`111` dac-A, an-loc-session) — gets its first real consumer with the first real executor, i.e. the multi-host resumption. Nobody owns scheduling that pass; the r25 runner's flat `{transcript, rc}` was a disclosed spike-cut, not precedent. (Human grading 2026-07-12: narrow-but-very-high-priority; rides the Opus/Fable capability line — workable at Opus-tier alongside the executor era.)

* [ ] **Oracle-author DX (linter / LSP / authoring harness).** Still DESIGN §5's "where the bulk of the work lies" and the #1 existential-risk lever (oracle quality); still machine-enforcement-not-author-discipline (the paranoid-author shipped-bug evidence stands). One slice found a ride — the stdlib quality-bar adds joined the block-rebuild dispatch package (`279f` §5 riders; `271` task-15 parked the rest to the stdlib quality-bar). Still unowned: the wrapper-oracle quality bar (`24S:A6` — peel cross-check, argparse lints, self-vouch/self-footprint requirements, expressibility clause), the carrier bar (`24T:P-A4` — which-arg-is-code arity gates, parse-and-resolve cross-check, reconstruction differential, dorcism-in-payload lint), and the **adjudicability build-list** (`24S:A4` — machine-read kind-topology clauses, binding-smell lints, differential discharge in stdlib CI; MUST land before kinds go community-shared, and no round owns it). Low lock-in → sorts down; highest-leverage omission.

      (2026-07-12, human-ruled — recorded here so it outlives the chat: the adjacent kTYANNOT tooling-viability question (TODO.md:8–11 — the LSP/shfmt/shellcheck "Microsoft-scale" jump-scare; the engine-internal EOL-comment-conversion-roundtrip idea, unevaluated) is an EXPLICIT PUNT: the spike itself IS the kTYANNOT experiment — "is not-using-EOL-comments livable?" is the spike's single biggest intended output, inline lived-in in anger, eyes-open, precisely to earn-or-refute the human's anti-EOL-comment lean — so this evaluation is post-spike adjudication input, never upfront work. Stakes sharpened, not dissolved: if inline survives the spike, it must prove ergonomic enough to justify forgoing the comment-adjacent tooling ecosystem.)

* [ ] **The human's root-doc queue (his voice; punted 2026-07-10 to post-spelling-settlement).** The punt condition is now ~met at design tier (the spelling settled in `276`–`278`; the corpus-respell itself lands at `270:block-rebuild`), and the queue gained a detailed per-strain spec: `_tmp-root-docs-staleness-report.md` (root checkout, untracked scratch, 2026-07-16) — its P1s are DESIGN presenting the welded kWHICHSH as "unsettled" and the dead "two flavours of simple information" oracle-contract paragraph; the `(UNFINISHED)` typed-sh continuation is graded the highest-value refresh there. USER_STORY dropped OUT of the queue (rewritten 2026-07-16, stages respelled). Still queued: the README positioning line ("lazy code motion for shell", citations ready in `23N`; plus the HM-inference reframe), DESIGN's Inference-limitations continuation + founding-example respell, IMPLEMENTATION's founding one-liner respell + "three possible outcomes" enumerating four, the formal **dq-kOOB stamp line** (one human sentence; substantially discharged by the `24M` rulings), and registering the fair-attribution law ("errors unfairly attributed are worse than unattributed") in the root docs.

* [ ] **(Deferred-surface seams grab-bag — reserve, don't build; each with a live re-entry pointer.)** The **barrier / placement-spectrum performance round** (`23O` §4: hoisted past-wall re-check waves — performance-only by the consent-wall; must COMPOSE with the r26 re-fold machinery when both exist); real-time **streaming/TUI** (dual-render, degrade to plain-text; why-elided/why-probed as primitives); `retries:`/`until:` convergence-by-retry as first-class syntax; `serial:`/rolling-batch **non-preclusion**; the parked **escape-hatches** (`235`: hatch-isolate, hatch-bump-exclude; unpark = admin-recourse pressure) and the **veto polarity** (owner-spelled judgment-tier veto list); the 077 remnants — the unprivileged **seccomp `socket(AF_INET)` observe backstop in core** and the **`--faithful` one-leaf-one-exec mode** (both carried to the real-executor era by `24O`, unowned); the **secrets-hygiene seams** (the round itself is TODO.md-resident, human-designated for a later non-Fable round — this entry is timing only: `270:block-context`'s read-value fold-back + the wire import put host-read bytes into plan/record/why-log surfaces for the first time, so the scrub-at-capture seam (`22A`) and the plan-display-vs-secret-hygiene tension (`24R:repurp-finding12`) want their seams *named* before those land); and the **`24R` cheap-adds pile** (local-exec blessing — triple-load-bearing, zero tests today; `dorc plan --exit-code`; book-reads-stdin + sudo-keepalive lints; previewer/pre-commit packaging; executable-inventory consumption; changed-report adapters) — small, unowned, natural round-27 riders if capacity appears.
