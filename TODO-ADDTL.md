(REWRITTEN AUTOMATICALLY, UNSTABLE. DO NOT REFERENCE IN OTHER DOCUMENTS.)

Dig through the design-docs in this repo; collate for me a list of 'undone design-work' that was either 1. mentioned by a human in-passing, but doesn't clearly map to any of the design-passes present; or 2. heavily pushed by a `plans/*.md` document, but seems like it may have gotten lost in the weeds. Sort higher: items with high design-consequences ("especially hard to unbake" or "can't be refactored"); and sort lower: items that seem known/deferred (in `TODO.md`, discussed by the human in recent design-passes, or clearly not-upfront-work.)

Update-by-overwriting this section; keep this descriptive header/prompt, just replace items. Keep it short; collapse similar/related items into one entry; this shouldn't grow over ~10 items. It's to catch *major work*, not nits.

> Notes by me, the human, in >-blockquote.

## TODO:

* [ ] **Reactivity / live-plan: concurrent-probe streaming → incremental per-host re-analysis (the analyzer's "time axis"; foundational, near-top per the human).** Generalizes the founding anti-Ansible real-time motivator (`archive` L73; also the deferred-surface bullet below): probes arrive concurrently from N hosts and the per-host plan re-folds **live** as they report — threading *reactivity through the whole kernel*, distinct from the plan-updating "real-time" DESIGN already names. Surfaced + scoped in r22 (the "ui-B" misnomer): corrected monotonicity (the unit is **replacement**, not run/not-run, and it is NOT provably stable); the concurrency / seeded-logical-clock / per-host-accumulator seam must be **built** (`hostsim` has none today). Deferred from r23 (deeper than first-framed) and decoupled from arch-4's durable tape — both key on the probe-result **stream**, so freezing either against the single-shot model first = rework. r23 addendum: the guard tier pins apply to book-order, and the commissioned re-verification placement round overlaps the re-fold machinery — these must compose, not collide (Research/README round-22 note). Highest lock-in. SEED: `Research/plans/22H`.

* [ ] **Oracle-contract sh-spelling — residual only (updated 2026-07-02; the bulk RESOLVED in r23).** The dialect the corpus long called open is now spelled, stamped, and spike-implemented: inline identity-annotations, trailing marks, strip-only law, the role-sibling family `predict()` / `is_converged()` / `is_diverged()` + the rc-partition (`plans/233`, `notes/23L`, spike/CLAUDE.md rulings blocks). Still genuinely open: the **converged-vouch's concrete sh spelling** (dq-kOOB cluster, human-reserved; new interplay — whether authoring a verdict-function partially IS the vouching act); **kind collision + evolution semantics**; **bootstrap-kind curation**. (kTYANNOT's inline-vs-eol question was overtaken in practice by the stamped inline dialect; the off-ramp cost is paid by the strip pass.)

      > human note: we're "spiking" on this, trying out code approaches, *because* it's a hard problem. last spike ran hard into limitations though.

* [ ] **MH2 — the version layer, reframed to the piece that survived.** Named in the founding thesis as 1-of-2 co-equal must-haves (`archive/ansible-conversation-text.md` ~L252: *"deep handling of versioning … an invisible annotation-layer … `npm install foobar@3.5.5` → derive the relevant `foobar.check`"*). The human himself later demoted the *registry / version-management* ambition to out-of-scope (`25486c1d`, 2026-06-03), but kept concrete pieces that are still undesigned: version-aware oracle verbs (`.version` / `mycmd.<provider>-version()`) over a **cross-PM version *lattice*** — partial order, no join across package-managers (`a88bd0a8`, 2026-06-04); a **binary-content-hash grounding gate** (oracle binds its grounding to a content-hash; runner hash-checks `$PATH` at probe/apply, fail-safe on mismatch — record-and-compare, no registry; `102` version-drift, parked); and a **version coordinate** for comparable intent (prior art: package-url/purl-spec, `pkg:apk/alpine/musl@1.2.5-r9?distro=3.21` — distinguishes "same version string, different bytes"; distinct job from the hash: purl = coordinate, hash = identity). Absent from DESIGN entirely; shapes the oracle-contract + fact-domain → retrofit-hostile.

      > versioning (the simpler version of the concept, focused down to 'package-as-a-type-needing-special-attention, multi-providers, etc' ... and 'mapping oracle-written-for vs being-executed-on') needs close care, but it feels very deferrable compared to some of the critical core analyzer-design things that are affected by the "sh spelling" issues. continuing to defer.

* [ ] **The `.diff` verb — the one survivor of the verb-menu item (updated 2026-07-02).** The old "verb-set + fail-fast contract decided but never written" entry is largely superseded: the role-family, the refuse idiom (loud `UNK` + exit 254 → the confused bucket), full-verbatim-args, and the narrowest-correct-primitive stance now live in the r23/23L rulings. Still homeless: `.diff` as the strictly-stronger, opt-in, authors-must-*earn*-it verb (`archive` L498/1204/1208/1265); byte-mechanics stay deferred to jq/diff/patch, only the convergence-predicate is Dorc's altitude.

* [ ] **The Dorc language proper (the strict superset) + the `unsafe` escape-hatch.** `DESIGN` ranks the language "*most* critical when calibrated against lock-in," yet nothing defines what the superset adds/drops vs POSIX-sh; the **welded must-handle set** the human has since fixed (const-prop, interprocedural, variables, heredocs — *"the one and only construct I am punting on is `eval`"*, `b820ef5e` 2026-06-08); the **kWHICHSH** sub-axis (strict-dash vs maximum-subset incl. `pipefail`; the ship-our-own-dash middle-ground, `a88bd0a8` — r23 h3 ruling leans dash-ish "barely-more-than-classic-POSIX"/POSIX2024, `local` expected); and the human-required **Rust-`unsafe`-equivalent escape-hatch** that explicitly loses CFG-totality and *taints its control-flow-subgraph so it can never be performantly skipped on a host* (`archive` ~L228) — which appears in no current doc. Canonical can't-unbake item once users write scripts.

* [ ] **Cross-host shared-state soundness + the `kSTATE` persist-vs-recompute call.** Cross-host hazards (write-skew, memo-key soundness, unreachable≠converged) scattered + unowned (`076 §3/§4b`, `090` D5). The verdict-shape `(verdict, content-key, freshness)` is retrofit-hostile. The human added central-state to the "genuinely-open" set and *leans stateless — recompute from on-disk/host truth, rust-analyzer-style* (`6b89f9ee`, 2026-06-01). Decide the *shape* now even if built later. (The old probe→apply/TOCTOU half of this entry is RESOLVED: deferred-to-actively-WONTFIX with the r23 identified-cause clarifier — guards catch in-book causes; open-world drift-machinery stays banned.)

* [ ] **Oracle-author DX (linter / LSP / authoring harness).** `DESIGN` #5 = "where the bulk of the work lies"; `088 §8` = "the lever on the #1 existential risk (oracle-quality)… a first-class workstream." Only the eBPF/trace slice (`077`/`078`, which the human pins as an *optional DX/meta-test*, not core — `4efae104` 2026-06-04) and the harness sketch (`021 §5`) exist. r23 grew its backlog: the don't-collapse verdict-function lint, the branch-aware coverage lints that replaced the per-selector filing floor. Lower lock-in (buildable later) → sorts down, but highest-leverage omission.

* [ ] **Error-reporting + provenance pass.** Provenance exists only as scattered engine capability; the human-seeded direction is one *N-tier, per-host-forking* derivation DAG (`notes/110` ‼DIRECTION + `plans/111`) but he never firmed its mechanics. (The run-delta convergence class that used to share this entry is largely absorbed: r23's rul-guard-license makes run-delta verbs never-guard — an oracle declines by not vouching — and the change-flag data-flow is consumed-observable machinery; the residual is only a DESIGN-prose boundary statement, riding the human's rewrite queue.)

* [ ] **(Deferred surface — reserve the seams, don't build.)** Each is human-mentioned and *deferred-not-excluded*, and each needs a reserved seam now so it isn't unbakeable later: real-time **streaming** of remote command output (the founding anti-Ansible motivator, `archive` L73 — distinct from the plan-updating "real-time" DESIGN already has) + the dual-render TUI (minimal-ship AST ↔ reconstructable-source greying as probes return) — 06-09: human-blessed that the TUI must degrade into do-one-thing-well plain-text controls, with why-elided/why-probed as the primitive queries (STALENESS-AUDIT dir-soundiness-ux); `serial:`/rolling-batch **non-preclusion** of the parallelism model (`archive` L1167); `retries:`/`until:` convergence-by-retry as first-class syntax (not derivable from a bare script); `sudo`/`become` execution-context first-classing (`notes/17O` R2-CONTEXT; r23 addendum — lane-*privilege* is the unnamed cell in the guard posture-shift, `notes/23J`: a vouch promotes oracle code into the book's elevated context when become exists).
