(REWRITTEN AUTOMATICALLY, UNSTABLE. DO NOT REFERENCE IN OTHER DOCUMENTS.)

Dig through the design-docs in this repo; collate for me a list of 'undone design-work' that was either 1. mentioned by a human in-passing, but doesn't clearly map to any of the design-passes present; or 2. heavily pushed by a `plans/*.md` document, but seems like it may have gotten lost in the weeds. Sort higher: items with high design-consequences ("especially hard to unbake" or "can't be refactored"); and sort lower: items that seem known/deferred (in `TODO.md`, discussed by the human in recent design-passes, or clearly not-upfront-work.)

Update-by-overwriting this section; keep this descriptive header/prompt, just replace items. Keep it short; collapse similar/related items into one entry; this shouldn't grow over ~10 items. It's to catch *major work*, not nits.

> Notes by me, the human, in >-blockquote.

## TODO:

* [ ] **The oracle-contract sh-spelling is STILL OPEN — and recent human voice contradicts the corpus's "settled" answer.** `plans/17N`/`175` treat the named-kind, *effect-keyed* `(kind, provider, verb)` contract as the resolution of the symbol-grounding hinge (`151`). But in round-19 the human pushed back hard: *"there's been a lot of markdown spilled claiming that everything 'has' to be 'effect-keyed', not command-keyed … I don't see any way it could possibly be not command-keyed"* (chat `c9fce2bc`, 2026-06-09), and earlier *"convince me not to layer a type-system on top of shell … we're building half of one, piecemeal, and poorly"* (`cf83c438`, 2026-06-06). The human-blessed part is only the *relational / referent-agnostic / grounding-is-declared-not-inferred* frame (`notes/095`, human-adjudicated). The actual sh idiom an author writes — command-keyed vs effect-keyed, the kind handle, the kTYANNOT inline-vs-eol spelling — is unspelled and deferred ≥4 rounds (`16Q` stubbed it). Highest lock-in (re-keys the fact-domain + every oracle); genuinely undecided, not merely unwritten → TOP. UPDATE (06-09, main-session): the keying half is now human-adjudicated — 19A §5's three-axis dissolution (command-keyed invocation / named-kind identity / fact-converged license; provider+verb *is* the command). Remaining genuinely-open: the annotation spelling (`fork-annotation-spelling`/kTYANNOT), kind collision+evolution semantics, bootstrap-kind curation — see STALENESS-AUDIT drift-grounding.

      > human note: we're "spiking" on this, trying out code approaches, *because* it's a hard problem. last spike ran hard into limitations though.

* [ ] **MH2 — the version layer, reframed to the piece that survived.** Named in the founding thesis as 1-of-2 co-equal must-haves (`archive/ansible-conversation-text.md` ~L252: *"deep handling of versioning … an invisible annotation-layer … `npm install foobar@3.5.5` → derive the relevant `foobar.check`"*). The human himself later demoted the *registry / version-management* ambition to out-of-scope (`25486c1d`, 2026-06-03), but kept two concrete pieces that are still undesigned: version-aware oracle verbs (`.version` / `mycmd.<provider>-version()`) over a **cross-PM version *lattice*** — partial order, no join across package-managers (`a88bd0a8`, 2026-06-04) — and a binary-content-hash grounding gate (`102` version-drift, parked). Absent from DESIGN entirely; shapes the oracle-contract + fact-domain → retrofit-hostile.

      > versioning (the simpler version of the concept, focused down to 'package-as-a-type-needing-special-attention, multi-providers, etc' ... and 'mapping oracle-written-for vs being-executed-on') needs close care, but it feels very deferrable compared to some of the critical core analyzer-design things that are affected by the "sh spelling" issues. continuing to defer.

* [ ] **The oracle verb-set + fail-fast contract — decided by the human, never written as a contract.** `.check` = the *narrowest correct primitive* (default; don't collapse "is-installed" into list-all-then-grep); `.diff` = strictly-stronger, opt-in, authors must *earn* it; fail-fast on any unparsable flag/arg; `check()` receives the *full verbatim* arg-string (no arg-derivation magic); byte-mechanics deferred to jq/diff/patch, only the convergence-predicate is Dorc's altitude (`archive` L498/1204/1208/1265; `c9fce2bc` 2026-06-09). DESIGN shows `.check` only as a one-line inference-limitation example; the contract verb-menu and the fail-fast rule have no home.

* [ ] **The Dorc language proper (the strict superset) + the `unsafe` escape-hatch.** `DESIGN` ranks the language "*most* critical when calibrated against lock-in," yet nothing defines what the superset adds/drops vs POSIX-sh; the **welded must-handle set** the human has since fixed (const-prop, interprocedural, variables, heredocs — *"the one and only construct I am punting on is `eval`"*, `b820ef5e` 2026-06-08); the **kWHICHSH** sub-axis (strict-dash vs maximum-subset incl. `pipefail`; the ship-our-own-dash middle-ground, `a88bd0a8`); and the human-required **Rust-`unsafe`-equivalent escape-hatch** that explicitly loses CFG-totality and *taints its control-flow-subgraph so it can never be performantly skipped on a host* (`archive` ~L228) — which appears in no current doc. Canonical can't-unbake item once users write scripts.

* [ ] **Probe→apply gap, cross-host shared-state soundness, and the `kSTATE` persist-vs-recompute call.** `090` raised the OCC-style probe/apply re-validation (TOCTOU window) as a real architectural choice; `099` parked it. Cross-host hazards (write-skew, memo-key soundness, unreachable≠converged) scattered + unowned (`076 §3/§4b`, `090` D5). The verdict-shape `(verdict, content-key, freshness)` is retrofit-hostile. The human added central-state to the "genuinely-open" set and *leans stateless — recompute from on-disk/host truth, rust-analyzer-style* (`6b89f9ee`, 2026-06-01). Decide the *shape* now even if built later.

* [ ] **Oracle-author DX (linter / LSP / authoring harness).** `DESIGN` #5 = "where the bulk of the work lies"; `088 §8` = "the lever on the #1 existential risk (oracle-quality)… a first-class workstream." Only the eBPF/trace slice (`077`/`078`, which the human pins as an *optional DX/meta-test*, not core — `4efae104` 2026-06-04) and the harness sketch (`021 §5`) exist. Lower lock-in (buildable later) → sorts down, but highest-leverage omission.

* [ ] **Error-reporting + provenance pass + the run-delta convergence class.** Already `TODO.md` item 1 + the run-delta entry (→ known; sorts low). Provenance exists only as scattered engine capability; the human-seeded direction is one *N-tier, per-host-forking* derivation DAG (`notes/110` ‼DIRECTION) but he never firmed its mechanics. The delta-gated effect class (restart/`reload`-iff-changed, the notify/handler pattern) is owed-to-DESIGN and un-probeable by host-state (`notes/17O` R2-CHANGEDELTA).

* [ ] **(Small doc-housekeeping, parked here 06-09 so the pruned STALENESS-AUDIT stays rewrite-only; human-auth edits.)** Root AGENTS.md trust-list: add IMPLEMENTATION.md to the human-written list (stamped hum-authored, commit 2d417c8); optionally reflect ANALYZER-NEEDS' demotion ("a convenient but fallible index", human ruling 196 §6) in its wording. When merging `ai/round19-keystone`: flatten, don't merge, the worktree AGENTS.md's "KNOBS.md [is] human-written" drift; and `git stash drop` the redundant `stash@{0}` (all hunks verified landed via 56e6933 + da6a4b5).

* [ ] **(Deferred surface — reserve the seams, don't build.)** Each is human-mentioned and *deferred-not-excluded*, and each needs a reserved seam now so it isn't unbakeable later: real-time **streaming** of remote command output (the founding anti-Ansible motivator, `archive` L73 — distinct from the plan-updating "real-time" DESIGN already has) + the dual-render TUI (minimal-ship AST ↔ reconstructable-source greying as probes return) — 06-09: human-blessed that the TUI must degrade into do-one-thing-well plain-text controls, with why-elided/why-probed as the primitive queries (STALENESS-AUDIT dir-soundiness-ux); `serial:`/rolling-batch **non-preclusion** of the parallelism model (`archive` L1167); `retries:`/`until:` convergence-by-retry as first-class syntax (not derivable from a bare script); `sudo`/`become` execution-context first-classing (`notes/17O` R2-CONTEXT).
