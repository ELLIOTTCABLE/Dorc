(REWRITTEN AUTOMATICALLY, UNSTABLE. DO NOT REFERENCE IN OTHER DOCUMENTS.)

Dig through the design-docs in this repo; collate for me a list of 'undone design-work' that was either 1. mentioned by a human in-passing, but doesn't clearly map to any of the design-passes present; or 2. heavily pushed by a `plans/*.md` document, but seems like it may have gotten lost in the weeds. Sort higher: items with high design-consequences ("especially hard to unbake" or "can't be refactored"); and sort lower: items that seem known/deferred (in `TODO.md`, discussed by the human in recent design-passes, or clearly not-upfront-work.)

Update-by-overwriting this section; keep this descriptive header/prompt, just replace items. Keep it short; collapse similar/related items into one entry; this shouldn't grow over ~10 items. It's to catch *major work*, not nits.

> Notes by me, the human, in >-blockquote.

## TODO:

*(Last full refresh: 2026-07-10, at the round-24 close-out — re-graded against `notes/24U` and `plans/270`; everything owned by a dated, planned round was REMOVED. Focused passes 2026-07-16/17 (block-settle, capture-lane re-rule, `26C` mint) are baked into the item texts below.)*

*(Curation pass 2026-07-26, at the why-surface implementation close (`notes/28F`): the 2026-07-19 rider dump DISSOLVED — done riders removed (flag-rename EXECUTED · skip-token respelled · `@`-permanence acked `28A` §2j · ksalience pointer already repointed · pathext spawn fixed), the door1 hint nit dropped back to its `27U` §3 bank (below this list's bar), survivors compressed into the pending-rulings entry; two entries added from the arc's residue. No fresh corpus dig.)*

* [ ] **The Dorc language proper — the residue after the `276` sitting.** The 2026-07-12 human dive HAPPENED (`notes/276` + the `278` v0.1 reference, DRAFT — the superset's adds/drops are now assembled there): kWHICHSH is WELDED to minimum-lcd as the posh∩dash two-binary executable floor and KNOBS-registered; the `unsafe` hatch is ruled bare-sh (`276:rul-unsafe-is-bare-sh` — descend-don't-license); the epoch/verdict-churn policy is discharged at design tier (`276:rul-verdicts-never-stable` — syntax marker-gated · names permanent · verdicts never stable); `trap` is dispositioned (recognized-but-unmodeled, walls loudly); the old "only punt: eval" question landed as `271:rul-stopping-point-unpinned` (closed, deliberately UNPINNED). Still homeless after all that: the **book-acceptance carve** (`276:rul-kwhichsh-oracle-scoped` deliberately left it open — the value-ladder for unmarked/bash-ish runbooks (parse-for-sites / probe-verdicts / guard-lift / guard-insertion) is undesigned and unowned; design inputs already banked in `276`: the full-support-or-display-only binary lean, the license-free drift-report tier, byte-honesty-as-the-priced-feature, the zsh grading (parser cheap and differential-testable; the semantic model is the gate), and the errand-render-kit-floor sizing spike (natural slot: after block-rebuild's render churn settles); the `24R` §0b evidence — famous dotfiles artifacts are bash/zsh — still bears on it); and the **verdict-family name-extensions** (the earned `.diff` verb; an is_noop-style fully-state-precise sibling — extend-by-name-never-re-read is ruled, the members themselves stay parked).

* [ ] **The read-value/capture lane — DEFERRED-BY-DIRECTION to the r26 revival; design bank = `notes/26B`.** Human-ruled 2026-07-17 (rubber-duck sitting): plan-construction is *eventually reactive* (`26B:rul-plan-construction-is-reactive` — incoming facts re-analyze, mint new probing, and mutate ongoing analysis; executors add AND discharge work), so the single-wave post-probe re-bind and the fold-time substitution channel are DEAD as designs — and building the fold single-wave in r27 would bake exactly the pipeline-order/provenance decisions this item was flagged high-lock-in for. `lane-read-value-slice` is STRUCK from block-context (`26B:dec-capture-fold-deferred`); block-stdlib + the field trial proceed unaffected, and the trial now *sizes* the lane (`26B:ask-trial-counts-capture-walls` — dynamic-value walls counted as a named category). The representation stays landed and reserve-shaped (`27H`/`27I`; recipes, ValueGrade, backing-SETS, the wire stdout field). Everything else moved into `26B`: the human-typed rulings (consent-cut absolute · one-attention-moment · iteration-waste puntable · reclassification-guards-are-floor), the chaotic-fixpoint reframe (confluence = byte-identical-plan-under-arrival-shuffle, the DST target), the obligations bank (cancellation-finality gate · quiescence-witness-at-mint · provenance-through-rounds · per-round harness), the re-homed `27J`/`279f` riders, and THE open gate-question, `26B:gate-binding-site-coherence` (freeze-in-artifact vs all-or-nothing folding — deliberately un-ruled; menu since sharpened by `26C` §6 with the (b′) structure-preserving-folds intermediate). The same-day deep pass `notes/26C` adds: the conflict carve on confluence, the cancellation package (demotion-only · guard-by-vouch landing · wall-standing finality), wire iteration-keys, the captured-bytes-as-data law ask, and the R0–R4 ladder re-cutting `262` S0/S1 + `260`/`261` for the revival.

      > human: not sure how this got lost in the weeds; was surprised to find single-wave getting designed around. this needs a deep and focused design-pass, re-pick-up and refresh r26 with new conductor and some thorough read and analysis.

* [ ] **Cross-host shared-state soundness + the `kSTATE` persist-vs-recompute call.** Unchanged and unowned: the verdict-shape `(verdict, content-key, freshness)` is retrofit-hostile — decide the *shape* early even if built late; the human leans stateless (recompute from host truth). Standing riders: any unpark re-surfaces **an-host-as-adversary** (a reused verdict widens one host's lie to the fleet); the **wall-clock-keyed verdict classes** (pkgindex freshness, cert expiry) are the first hermeticity test-cases any content-key design must survive; rec-5 (probe-TAPE is write-only, never a reuse-cache) stands welded. NEW rider parked verbatim from `261:dec-timing-cache`: *does rec-5's no-re-ingest intent cover an advisory-only, license-nothing timing profile, or was it scoped to verdict/fact reuse?* — human-owned fence question, asked only when the cross-run-reorder punt lifts.

* [ ] **Precision + identity residue.** Improved but not closed by `notes/277`: the **uniqueness-bit seam is now RESERVED** (`271:rider-entity-algebra-uniqueness-seam`; `277` §5 — room in the coordinate/comparison representation), but no strong update exists at v1 (`Kill` accumulates; the `231` "probably unique may only DEMOTE" fence stands; *building* the bit is unowned, and uniqueness is SF-1-undecidable — someday judgment-tier); the **kind-unify / co-reference mechanism stays parked but is consolidated as ONE mechanism** with a door-swing seam (`277` §6 — the kind-fence stays pre-canonicalization at v1; the old three-pointer scatter — `24M:rul24M-kind-unify-owed` · `24S:A5` · `24C:strain-coreference-crosskind` — now folds there). Still fully unowned: **partial-member convergence** (the across-MEMBERS half; needs per-member `self_reach` re-derivation); **an-ide-value-layer** (version-carrying facts; couples to MH2 below); and the **may-alias-default RULING** (`24O` item-25: the data arrives from planned work — the yardstick-measurement sensitivity number + the trial's real-resolver fire-rate — but the flip-or-confirm decision has no owner and must never happen silently).

* [ ] **MH2 — the version layer.** Substance unchanged from the 2026-07-11 side design-round (`.claude/research/versioning-mh2/` turns 01–05: the version-WINDOW gate as HUMAN speech-act — gate-not-algebra, so the old retrofit-hostility clause stays dissolved; MACHINE-stamped grounding; kind-owned comparison, never-universal-compare). Still deferred (`270` §4 stands); still the named tightening if trial A1 bites. Prerequisite-acts, updated for block-settle's closure: (1) HUMAN one-sentence pin of the version-silent-oracle reading — still free, still un-typed; the clock remains block-stdlib/outside-authors; (2) at block-stdlib dispatch: reserve final version-role names in the munge-lint + oracles born with machine-observed stamps (capture-at-authoring); (3) the charset FYI (don't forbid opaque/hash-shaped canonical forms) was never explicitly acked before block-settle closed — `277` §4b's POSIX-in-spirit charsets *look* compatible (hash-shaped entities reachable via the quoted form) but this is unverified, and the confirm is now DUE: stage-3 landed the §4b charsets + loud-reject 2026-07-17 (`notes/27G`).

      > human: versioning (the simpler version of the concept, focused down to 'package-as-a-type-needing-special-attention, multi-providers, etc' ... and 'mapping oracle-written-for vs being-executed-on') needs close care, but it feels very deferrable compared to some of the critical core analyzer-design things that are affected by the "sh spelling" issues. continuing to defer.

* [ ] **Wrapper/payload residuals no block owns.** Re-framed under `plans/27C` (context-entry probing now owns the wrapped-site probe question; `notes/273` owns the authored surface; the old W3–W4 stall-contingency is moot — both gate adjudications closed at block-settle, and the stages ride `270:block-context`). Still orphaned: **fs-view, the Hard cell** (`24S` §3b as re-read through `273`/`27C`: mount-table truth; naive prefix-rebase officially wrong; the enumerated-bridge ⊑ in-context-map ⊑ may-alias ladder and the DETECT-and-DEGRADE stance — seams reserved, nothing built, nobody owns the build; now explicitly sequenced BEHIND netns per `271:rul-networking-unpunt`, with `27A`'s wall inventory standing law for the fallback lane); **guard-insertion under ELEVATED wrappers** (the 23J lane-privilege carve — Dorc-authored checks in a privileged lane; `27C` is probe-side and does not answer this apply-lane question; wrapped heredoc-books stay elision-edits-only until it is designed); and the **become/doas privilege prior-art collation** (research round-2, HARD-DEFERRED behind a typed human ack — wanted before wrapper-oracle authoring widens past sudo/su/env).

* [ ] **Error-reporting/provenance: the N-tier locator-DAG firming.** The live half is built (structured diag API — the design-for-keeps exception; ProvId arena; why-lens + why-lanes; and as of the 28F arc, the whylog durable + receipt-first `dorc why`); the never-had-a-consumer half — the per-host-forking locator DAG + transport-minted session correlation (`111` dac-A, an-loc-session) — gets its first real consumer with the first real executor, i.e. the multi-host resumption. Nobody owns scheduling that pass; the r25 runner's flat `{transcript, rc}` was a disclosed spike-cut, not precedent. (Human grading 2026-07-12: narrow-but-very-high-priority; rides the Opus/Fable capability line — workable at Opus-tier alongside the executor era.)

* [ ] **Oracle-author DX (linter / LSP / authoring harness).** *(2026-07-18: the `dorc lint` entry-point LANDED (`notes/27R`/`27S`), and the whole aid category — incl. the unowned quality bars below — is now tracked in root `AID-NEEDS.md`.)* Still DESIGN §5's "where the bulk of the work lies" and the #1 existential-risk lever (oracle quality); still machine-enforcement-not-author-discipline (the paranoid-author shipped-bug evidence stands). Still unowned: the wrapper-oracle quality bar (`24S:A6` — peel cross-check, argparse lints, self-vouch/self-footprint requirements, expressibility clause), the carrier bar (`24T:P-A4` — which-arg-is-code arity gates, parse-and-resolve cross-check, reconstruction differential, dorcism-in-payload lint), and the **adjudicability build-list** (`24S:A4` — machine-read kind-topology clauses, binding-smell lints, differential discharge in stdlib CI; MUST land before kinds go community-shared, and no round owns it). Low lock-in → sorts down; highest-leverage omission.

      (2026-07-12, human-ruled — recorded here so it outlives the chat: the adjacent kTYANNOT tooling-viability question (TODO.md:8–11 — the LSP/shfmt/shellcheck "Microsoft-scale" jump-scare; the engine-internal EOL-comment-conversion-roundtrip idea, unevaluated) is an EXPLICIT PUNT: the spike itself IS the kTYANNOT experiment — "is not-using-EOL-comments livable?" is the spike's single biggest intended output, inline lived-in in anger, eyes-open, precisely to earn-or-refute the human's anti-EOL-comment lean — so this evaluation is post-spike adjudication input, never upfront work. Stakes sharpened, not dissolved: if inline survives the spike, it must prove ergonomic enough to justify forgoing the comment-adjacent tooling ecosystem.)

* [ ] **The human's root-doc queue (his voice) — MOSTLY DISCHARGED 2026-07-16; residue re-cut from the fix-review.** The human's fix commits (`c64d805`..`4b73521`, 2026-07-16 evening) landed nearly the whole old queue. The 2026-07-17 fix-review (in-chat, Fable) seats a NEW short queue, sharpest first: **fix-gsub-strip-claim** (DESIGN's new off-ramp item 4 calls strip "a simple gsub-style clean" — contradicts the correctness-critical strip pass: 17O F-OFFRAMP non-inertness; the trailing-`:` rc-clobber class; an agent quoting it regexes the strip); **fix-flag-gloss-composition-not-contradiction** (IMPLEMENTATION's UNFINISHED stub glosses the flag's territory as claims "that may in fact contradict" — contradiction is the dictate-tier refuse-both fail-fast category, never flag-gated; the residue is locally-faultless claims that COMPOSE, per `271:rul-flag-is-razor-residue`); **fix-kwhichsh-hedge-and-scope** ("at least for now… basically" hedges a human-typed WELD, and the section never scopes the floor to oracle/marked text — book-acceptance is deliberately open); **fix-marker-gate-absent** (DESIGN's Type-system never mentions the per-file dialect-marker gate — the containment that makes its off-ramp items 1–4 true); smalls: skip-vs-elide render divergence README↔USER_STORY + README's render lacks USER_STORY's ILLUSTRATIVE hedge (plan-as-API exposure) + the `[ "$2" = "" ]` arity gate vs the taught `${2-}` idiom + typos (preform · ownes · commnds · "at each state" · two unbalanced emphases). Still standing from the old queue: "three possible outcomes" enumerating four, and the formal **dq-kOOB stamp line** (one human sentence).

* [ ] **(Deferred-surface seams grab-bag — reserve, don't build; each with a live re-entry pointer.)** The **barrier / placement-spectrum performance round** (`23O` §4: hoisted past-wall re-check waves — performance-only by the consent-wall; must COMPOSE with the r26 re-fold machinery when both exist; second customer `plans/27C` §5's conditional-tail mechanics + the `236b`-alt2 generation-probe revival); real-time **streaming/TUI** (dual-render, degrade to plain-text; why-elided/why-probed as primitives; the weft skeleton now exists — the TUI rides its reflow, still future); `retries:`/`until:` convergence-by-retry as first-class syntax; `serial:`/rolling-batch **non-preclusion**; the parked **escape-hatches** (`235`: hatch-isolate, hatch-bump-exclude; unpark = admin-recourse pressure) and the **veto polarity** (owner-spelled judgment-tier veto list); the 077 remnants — the unprivileged **seccomp `socket(AF_INET)` observe backstop in core** and the **`--faithful` one-leaf-one-exec mode** (both carried to the real-executor era by `24O`, unowned); the **secrets-hygiene seams** (timing only; the deadline is the r26-revival's fold work — `26B:need-scrub-before-freeze` names the sequencing dependency); and the **`24R` cheap-adds pile** (local-exec blessing — triple-load-bearing, zero tests today; book-reads-stdin + sudo-keepalive lints; previewer/pre-commit packaging; executable-inventory consumption; changed-report adapters) — small, unowned, natural riders if capacity appears.

* [ ] **The catalog-case ratchet burn-down — upgraded from hygiene to CORRECTNESS (2026-07-26).** `28F:finding-caseless-example-drift`: a code still on `DEFINING_CASE_RATCHET` (35 remain) renders nowhere, so its frozen catalog `example` has NO mechanical tie to its emitter — any emitter reword silently falsifies the sample, forever (four found 2026-07-26, and only because that sweep's glyphs happened to trip the ASCII guard; a non-glyph reword drifts undetected). The corpus gate covers case-owned codes; the u-curve law rules out a source-scan net; the ratchet burn-down is the ONLY structural closure, and it has no owner (the phase-7 "lazy by design" grading predates this finding). Subsumes the old rider-defining-case-ratchet-ack (the redefinition was ridden through multiple typed rulings; the residual formal ack folds into whichever sitting picks this up). Also here: `28F:ask-munge-byte-render` (the sole corpus-gate allowlist row — byte-honesty or a `describe()` defect, decide when touched).

* [ ] **Pending human-ruling queue (small sittings; each read-in pointer suffices — survivors of the dissolved 2026-07-19 rider dump + the 28F why-surface arc).** **floors-ratification** (every diag code's severity-floor awaits the long-owed human pass; per-code proposals prepared — `27U` §7 item 2) · **decline-class starter-set + advise-verb deferral** (still soft-acked only — `27W` §0) · **C8 operand-value display** (fact-merge disagreement narratives carry sites, not values; veto-or-build seam — `27U` §7 item 6) · **prose-register schema** (terse/deep/first-encounter never schema'd; the why-lens remediation hint still lives outside the catalog; wants a sitting WITH transcripts to stare at — `282` §10; the W5⇄W4 interlock is RESOLVED — W4 executed 2026-07-26 (`28H`): drift/receipt why-rows have transcript faces, chain-row faces wait on the full-driver extraction, the const-vs-mirror render lag makes why-row loom-edits two-step meanwhile — both banked as r30 charters in `28H`) · **lint tc-queues** (the `dorc lint` conservative leans await re-rule — `27S` §5, `27T`) · **28F small rulings** (`Consented`-Knowability at first render · `--no-whylog` spelling · the coverage tool's swept separators · the two conductor prose acts to review) · **syncthing worktree exclusion** (PHNHRER live-syncs `.claude/worktrees/`; conflict-file incursions recurred through the 28F arc; `.stignore` repair is human-owned — `27U` §2).
