parity opted-out (D3b, item-1, anti-masking): probes are package__check (dpkg-query — now shimmable) AND pkgindex__check (`test -n "$(find … -newermt '-1 hour')"`). The pkgindex site is a genuine FILESYSTEM-TIMING observation; a `find` PATH shim forcing a non-empty line would FABRICATE freshness to match the fixture (the all-exit-0 masking inv-probe-sourced-values forbids). Since gate-1 parity is all-or-nothing per case and the pkgindex site can't be honestly reproduced, the case stays opted-out. gate-1(a) site-completeness still enforced. (tc-find-shim: flagged in 20G.)
