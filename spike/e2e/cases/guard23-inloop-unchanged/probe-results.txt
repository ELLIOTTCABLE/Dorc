# probe-results: site-keyed records (task-D1 WIRE; task-L2 item-4 member sub-keys). Grammar:
#   site <leafid>[.<member-idx>] effect=<holds|absent|cant-tell>
# The in-loop Members install (leaf 0) ships ONE check per member: site 0.0 = nginx,
# site 0.1 = curl. Both report `holds` (converged) ⇒ the all-or-nothing license elides the
# body. (Establish-class records, so rc is firewalled from the fold — omitted here, the
# convention; gate-1 parity reproduces these from the exit-0 dpkg-query shim.)
site 0.0 effect=holds
site 0.1 effect=holds
