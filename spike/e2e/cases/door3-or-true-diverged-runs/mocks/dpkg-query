#!/bin/sh
# INERT probe shim (gate-1 parity): `package` kind probes `dpkg-query -W <pkg>` ($2=pkg).
# nginx is NOT installed here (diverged) ⇒ rc 1 (absent), matching probe-results.txt.
. "${0%/*}/.log"; _dorc_logged "$@"
exit 1
