# probe-results: site-keyed records (task-D1 WIRE). Grammar:
#   site <leafid> effect=<holds|absent|cant-tell>
# All three installs are CONVERGED (maximal guard bait) — and every one of them still runs:
# the admin reads their rc (if / || / $?), and a mutator's own rc is never predictable
# (the establish-rc firewall), so neither elision nor guarding may touch these sites.
site 0 effect=holds
site 2 effect=holds
site 4 effect=holds
