pipe-guard owed value: a converged, described otelcol should lift the version-check-then-install line (elide), but the `||` reads the un-oracled last pipe stage `grep -q` and rule-query-validity withholds it (opaque otelcol upstream) — so it runs. Pin-the-gap, round-25 (design-input: round-24 Stage 6). XPASS ⇒ the pipe-guard lift landed; diff the plan by eye before promoting.
