#!/bin/sh
# INERT mock: logs argv, exits 0. NEVER runs here (the inner install elides via door-3 —
# curl converged + `|| true`); shipped so the bare-book gate-5 run (where it DOES run) is shimmed.
. "${0%/*}/.log"; _dorc_logged "$@"
exit 0
