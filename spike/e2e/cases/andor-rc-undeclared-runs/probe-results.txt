# The host already has the user (Converged) — but the probe declares NO rc (the real
# CLI/hostsim default after `19D`: an un-injected rc is ⊤, never a fabricated rc 0).
# `useradd`'s status is branch-consumed (the `|| mkdir` fallback turns on it) and its
# rc is undeclared ⇒ the engine refuses to replace it ⇒ it RUNS. The line stays
# verbatim; real `useradd` exits 9 ⇒ `|| mkdir` fires ⇒ `mkdir` runs. Both run — the
# safe floor. (Round-20 `fork-mutator-rc`: no declared-rc path exists for a mutator —
# the old rc=9-injecting contrast case was cut as masking; notes/203 §3.)
# task-W: the useradd check binds NO verb (ε-verb) and the entity is the FIRST OPERAND
# `deploy` (the baked-verb `useradd deploy ⇒ Singleton` wart of find-3 is dead, 19I §2),
# so the cell is now `user:deploy#present` (Operand), not the bare-kind Singleton.
user:deploy#present converged
