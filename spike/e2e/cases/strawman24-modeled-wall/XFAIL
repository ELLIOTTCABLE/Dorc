silence=wall unbuilt (fd10 "dangerous middle", 23O §1.2): a DIVERGED modeled mutator (apt-get install oldpkg — cell package:oldpkg#installed RUNS) must WALL every downstream converged site on OTHER cells, so apt-get install nginx must RUN post-wall. At HEAD the modeled-but-partial oldpkg poisons only its own cell, so nginx wrongly ELIDES (elide=1) — the under-execution fd10 hole. The sibling fd10/silence=wall fix (parallel worktree) lands this; expected.out/expected.ran are authored to the DESIGNED post-fix semantics (probe unchanged: past-wall sites still ship probes per the guard-tier design intent; apply runs nginx verbatim). head-expected.ran two-sided-pins the HEAD run-set so a disaster-shaped elision of oldpkg would go RED, not hide as xfail.
