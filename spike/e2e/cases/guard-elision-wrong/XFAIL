F1 priority-1 wrong-elision: a command whose exit STATUS is consumed by a guard (if/while/&&/||/!) is classified EstablishAmbient and :-stubbed when converged, destroying the branch decision. Here the `command -v nginx` guard elides -> dangling `then` (ap-2 catches the syntax); the deeper bug is under-execution. Root cause: 16G/16P-T10 "no status gate" (status vouched by establishes-contract) is unsound for a guard/probe-status. Safe fix: status-consumed-by-condition must block the license. notes/195 F1.
