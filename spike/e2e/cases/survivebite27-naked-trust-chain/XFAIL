the full-chain `dorc why 9` render (27V §4 flagship acceptance) does not exist yet — Lanes A (evidence plane) and B (whylog) are unbuilt, so the numbered-link / tier-word / file:line / naked-trust-epilogue chain the expected-why needles pin cannot be emitted. Legacy-kill (Lane C) landed; this case is the phase's acceptance test and stays a declared xfail until A+B land and the render surface is wired.
