parity IMPOSSIBLE (D3b, item-1): the load-bearing Query guard probes via `command -v` — a SHELL BUILTIN, so it cannot be shimmed via PATH=mocks-only (PATH only overrides EXTERNAL commands). Under mocks-only PATH the builtin resolves the operand as absent-from-PATH (rc 1), never the authored real-host `holds rc=0`. (D3b DID ship a dpkg-query shim for the install's establish probe, but the guard's builtin is the load-bearing site here.) Permanent opt-out, not a D3b-deferral. gate-1(a) site-completeness still enforced.
