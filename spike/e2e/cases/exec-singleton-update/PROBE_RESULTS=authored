parity opted-out (D3b, item-1, anti-masking): the only probe is pkgindex's `test -n "$(find /var/lib/apt/lists/ -newermt '-1 hour')"`. `test` is a shell builtin; `find -newermt` is a genuine FILESYSTEM-TIMING observation. A PATH shim for `find` COULD force a non-empty line ⇒ holds — but that would FABRICATE "there is a fresh apt list" to match the fixture, which is exactly the all-exit-0 masking inv-probe-sourced-values forbids (a shim must HONESTLY reproduce an observation, not manufacture the fixture's answer). The authored `holds` reflects a real host whose lists ARE fresh; no inert shim can honestly reproduce a time-relative observation. Permanent opt-out under anti-masking. gate-1(a) site-completeness still enforced. (tc-find-shim: flagged in 20G.)
