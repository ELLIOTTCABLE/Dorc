#!/bin/sh
# INERT mock (ap-2 exec harness): logs that it ran, mutates NOTHING, exits 0. Under the
# harness's PATH=mocks-only this shim is the only external thing that can run.
. "${0%/*}/.log"; _dorc_logged "$@"
exit 0
