# probe-results: site-keyed records (task-D2). The guard `command -v nginx` is site 1
# (a Query — its OWN rc=0 is fold-usable, rule-query-validity passes); the install is
# site 2 (an establish — its rc is the probe-command's, NEVER fold-usable, firewall).
# (`set -e` is site 0 — Pure/MustRun, unresolvable, no record.)
site 1 effect=holds rc=0
site 2 effect=absent rc=1
