# probe-results: site-keyed records (task-D1 WIRE). Grammar:
#   site <leafid> effect=<holds|absent|cant-tell>
# NO records: the cmdsub-RHS assignment is unresolvable (the install inside it is not a
# leaf), so nothing probes and gate-1 parity holds on the empty set.
