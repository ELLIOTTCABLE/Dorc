guard tier unbuilt: no guard decision exists, so no why-lens disclosure names the guard, its converged-vouch license, or the vouching oracle (gate-7 conjoined pattern: guard && vouch && package must co-occur in one why-line)
