#!/bin/sh
# dorc probe (read-only): checks per-SITE convergence, mutates nothing.
# When run, emits one results-record per site on stdout (the return channel):
#   site <leafid> effect=<holds|absent|cant-tell> rc=<n>
# effect is derived from the probe command's rc (0=holds, 1=absent, else cant-tell);
# rc is the raw PROBE-command status (opaque to Dorc — the record is the out-of-band lane).

# site:0 skip-unresolvable
# site 1: package:nginx#installed
package_installed__check() { dpkg-query -W "$1" >/dev/null 2>&1; }
package_installed__check 'nginx'; _rc=$?; if [ "$_rc" -eq 0 ]; then _e=holds; elif [ "$_rc" -eq 1 ]; then _e=absent; else _e=cant-tell; fi; printf 'site 1 effect=%s rc=%s\n' "$_e" "$_rc"
#!/bin/sh
# dorc apply: the book, with already-converged/dead lines elided (value-preserving stand-in).
# dorc guard preamble: the vouching oracle's own check body, shipped strip-only
# (annotations removed, `name.check()` -> `name_check()`, nothing else changed --
# rul-ternary-verdict: the same authored bytes the probe lane self-vouches).
apt_get__check() {
   while [ "${1#-}" != "$1" ]; do shift; done
   verb=$1; shift
   while [ "${1#-}" != "$1" ]; do shift; done
   pkg="$1"
   if [ "$2" = "" ]; then dpkg-query -W "$pkg" >/dev/null 2>&1; fi
}

# guard23-why-attribution (the disclosure floor: every guard decision must be ATTRIBUTED,
# user-visibly, to the mark that licensed it; XFAIL until the guard tier lands). Analysis-
# only case: one vouched converged site past an opaque wall — the minimal guard mint — and
# the pin is the why-lens (gate-7): the stderr disclosure must name (i) that the site is
# GUARDED, (ii) the vouch that licensed it, (iii) the vouching oracle. Attribution is the
# guard-license's entire enforcement story ("we can't prevent, so we attribute" — plans/233
# §"The guard-license"; blame attributable to the mark), and rul-attention-honesty makes
# the disclosure surface load-bearing: a guard the user cannot trace to its licensor is a
# hidden risk. The three expected-why patterns are deliberately loose substrings (guard /
# vouch / package) — the exact wording is the builder's; the presence of the disclosure,
# naming the mechanism, the license, and the oracle, is the pin.
hork wombat
apt_get__check install -y nginx || apt-get install -y nginx   # dorc: guard [package converged-vouch; probe: holds]
