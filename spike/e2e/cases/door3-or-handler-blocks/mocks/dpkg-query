#!/bin/sh
# INERT probe shim (gate-1 parity): `package` probes `dpkg-query -W <pkg>` ($2=pkg).
# nginx IS installed here (converged) ⇒ rc 0 (holds), matching probe-results.txt.
printf 'ran: %s %s\n' "${0##*/}" "$*" >>"$DORC_LOG"
exit 0
