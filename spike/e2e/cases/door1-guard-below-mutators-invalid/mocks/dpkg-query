#!/bin/sh
# INERT probe shim for the `package` establish site (`dpkg-query -W <pkg>`; $2=pkg). curl IS
# installed here (converged) ⇒ rc 0 (holds), matching probe-results.txt site 1. Read-only.
. "${0%/*}/.log"; _dorc_logged "$@"
exit 0
