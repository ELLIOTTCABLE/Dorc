#!/bin/sh
# INERT mock: logs argv, mutates nothing, exits 0. RUNS here (the block is reachable — the
# invalid guard never folds, and at runtime it fails so the `||` fires).
. "${0%/*}/.log"; _dorc_logged "$@"
exit 0
