# probe-results: site-keyed verdict records + the DERIVED-footprint coord lane (24E §5). site 0 =
# package:oldpkg#installed (absent ⇒ diverged ⇒ the oldpkg install RUNS = the footprinted wall);
# site 1 = package:nginx#installed (holds ⇒ converged). The `deriv <leafid> coord=…` lines are the
# host-run touches() output the derivation-probe reads back (the mock apt-manifest emits, per pkg,
# `package:<pkg>` + `file:/etc/<pkg>.conf`). Under --trust-footprints the nginx install SURVIVES the
# running oldpkg wall (package:nginx disjoint from the DERIVED footprint {package:oldpkg,
# file:/etc/oldpkg.conf}) ⇒ elides. dpkg-query is mock-reproducible (rc 1 oldpkg / 0 nginx).
site 0 effect=absent
site 1 effect=holds
deriv 0 coord=package:oldpkg
deriv 0 coord=file:/etc/oldpkg.conf
deriv 1 coord=package:nginx
deriv 1 coord=file:/etc/nginx.conf
