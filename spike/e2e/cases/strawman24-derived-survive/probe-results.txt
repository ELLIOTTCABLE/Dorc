# probe-results: site-keyed verdict records + the DERIVED-footprint coord lane (24E §5). site 0 =
# package:oldpkg#installed (absent ⇒ diverged ⇒ the oldpkg install RUNS = the footprinted wall);
# site 1 = package:nginx#installed (holds ⇒ converged). The `deriv <leafid> coord=…` lines are the
# host-run touches() output the derivation-probe reads back: `dpkg -L "$1" | sed` ALONE now, so each
# site emits ONLY its `file:/etc/<pkg>.conf` (the boilerplate `printf 'package:%s'` was stripped — the
# engine UNIONS the own coord package:<pkg> into the footprint, 24G §8). Under --trust-footprints the
# nginx install SURVIVES the running oldpkg wall (package:nginx disjoint from the hit-surface
# {file:/etc/oldpkg.conf} ∪ own package:oldpkg) ⇒ elides. dpkg-query is mock-reproducible (rc 1 oldpkg
# / 0 nginx).
site 0 effect=absent
site 1 effect=holds
deriv 0 coord=file:/etc/oldpkg.conf
deriv 1 coord=file:/etc/nginx.conf
