# probe-results: site-keyed records (task-D1 WIRE). Grammar:
#   site <leafid> effect=<holds|absent|cant-tell>
# (the records the rendered probe emits; effect from the probe command rc).
# site 0 = apt-get update (pkgindex:.fresh) DIVERGED ⇒ it RUNS = the wall.
# site 1 = apt-get install -y nginx (package:nginx#installed) CONVERGED on its own cell;
#          it would elide by the static ambient gate, but the running update walls it ⇒ runs.
site 0 effect=absent
site 1 effect=holds
