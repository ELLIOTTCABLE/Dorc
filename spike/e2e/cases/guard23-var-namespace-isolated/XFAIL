guard tier unbuilt (23C-fd1 variable-capture): a converged-past-wall vouched site must GUARD without clobbering the book variable namespace — the final `apt-get install -y "$pkg"` must still install VIM, not a check-clobbered "curl"; HEAD runs curl bare (no guard) and ships no past-wall probe
