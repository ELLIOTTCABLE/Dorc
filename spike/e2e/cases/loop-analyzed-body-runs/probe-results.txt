# probe-results: site-keyed records (task-D1 WIRE). Grammar:
#   site <leafid> effect=<holds|absent|cant-tell>
# NO records: the constant in-loop install (leaf 0) is a self-establishing body that
# becomes EstablishWritten via the back-edge (iteration 2 sees iteration 1's establish),
# so it is un-probeable (one `skip-unresolvable` comment) — and the in-loop render floor
# would bar eliding one iteration anyway. With nothing resolvable the body simply runs
# every iteration (the SAFE degrade, kFAIL-perform). The run-set (nginx twice, once per
# loop iteration a/b) is the real assertion; the empty fixture makes gate-1 parity hold
# vacuously.
