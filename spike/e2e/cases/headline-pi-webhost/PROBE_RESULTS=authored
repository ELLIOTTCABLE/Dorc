parity opted-out (D3b, item-1, anti-masking): a 4-kind probe mix — package (dpkg-query, shimmable), service (systemctl is-active, shimmable), firewall (`ufw status | grep`), AND pkgindex (`test -n "$(find … -newermt '-1 hour')"`). The pkgindex site is a genuine FILESYSTEM-TIMING observation no inert shim can honestly reproduce (forcing it would fabricate freshness — the masking inv-probe-sourced-values forbids). gate-1 parity is all-or-nothing per case, so the un-reproducible pkgindex site keeps the whole case opted-out. The purpose-built enforced ALL-converged headline is exercised by `headline-guarded-realistic`'s converged subset (item-2). gate-1(a) site-completeness still enforced.
