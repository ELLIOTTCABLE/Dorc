=== ANALYSIS ===

9|systemctl being skipped stood on all of the following, together:

* service.oracle.sh:12   reported  "Package:nginx@{enabled,active}" (ran
                                   01:59:52, rc 0)
* service.oracle.sh:9    vouches   "re-running an already-@active unit adds
                                   nothing worth running"
! certsync.oracle.sh:31  claims    "`certsync push` touches AT MOST @cert state"

   This speaks for everything it did NOT measure. Fact derived from as-written
   contract only; no run can back it:

      [ certsync.oracle.sh, as-written:
   30 | # surveyed 2026-05: cert store only.
   31 | push) printf '%s\n' "$1"  : disturbs org.foob.Certs ;;

so:  nothing 8|certsync claims to touch overlaps what 9|systemctl's skip was
     reported on; the skip stood.

=== NEXT STEPS ===

if 9|systemctl SHOULD have run:

   suspect:  the 8|certsync claim -- the only link speaking for unmeasured
             state. if `certsync push` also touches @enabled/@active, it is what
             wrongly kept 9|systemctl out.

   fix:      certsync.oracle.sh:31 (widen the claim; every consumer heals)

OR
   fix:      `dorc plan web.sh web1` (run without the risk flag, to distrust
             oracle-authored claims)

   verify:   `dorc plan --why web.sh web1` (re-measures under plan's standing
             consent, and leaves you one step from applying.)
   review:   `dorc why web.sh:9 --all` (every link, unselected, exhaustive)
