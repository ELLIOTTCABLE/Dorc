receipt: apply 2026-07-25 02:00:37, host web1, trigger cron
   oracles:  certsync 1.2, service 2.0, + base library
   plan:     2 ran, 1 guarded, 5 skipped

=== IMPROVEMENTS (2) ===

   [ web.sh, described:
12 ~ hork tune --profile web ... and the rest of a very long invocation that
     must wrap

( ufw__is_converged allow "$PORT"/tcp ) \
   || ufw allow "$PORT"/tcp

 3 | PORT=443               # the admin's own default
23 | ufw allow "$PORT"/tcp  # guarded on every apply

a convergence check for hork skips lines 12 and 13 whenever hork is converged
                                                              (dorc teach walls)

both
   hork is undescribed

and
   corp-agent is undescribed

so both walls stand between the report and this line's turn.

... 3 further links (--all)
(dorc why web.sh:13)
