# The verdict body returns 1 for both members, so verdict primacy must emit these records.
# At HEAD the aggregate probe runs `dpkg-query` instead, returns 0, and gate 1 catches the mismatch.
site 0.0 effect=absent rc=1
site 0.1 effect=absent rc=1
