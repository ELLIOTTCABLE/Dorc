XFAIL: the direct in-loop mutator already takes its own per-member lane, but the CALL's region has
no licence — the in-loop floor runs it whatever its population says. Greens with the loop
member-region licence (horizon end-of-r30).
