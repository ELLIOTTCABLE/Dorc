#!/bin/sh
# INERT mock for `apt-get` that FAILS (exit 1): the mutator itself errors when run. Logs
# argv, mutates nothing. The pin: the guard's fall-through hands over to the real mutator
# and the ENGINE adds no second-guess layer around its failure (rul-divergence-proceed) —
# in this un-errexit'd book the failure does not stop the book (faithful to bare sh).
. "${0%/*}/.log"; _dorc_logged "$@"
exit 1
