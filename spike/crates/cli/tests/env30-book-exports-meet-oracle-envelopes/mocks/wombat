#!/bin/sh
. "${0%/*}/.log"; _dorc_logged "$@"
case "$1" in
   status)
      shift
      [ "$1" = "--" ] && shift
      case "$1" in
         alpha) exit 0 ;;
         beta) exit 0 ;;
         *) exit 1 ;;
      esac
      ;;
   *) exit 0 ;;
esac
