#!/bin/sh
# wrapper-unmodeled-peel-walls (273 §1 floor): `sudo` has NO oracle ⇒ it is an opaque unmodeled
# command ⇒ the whole wrapped site walls (runs verbatim). The baseline: an unmodeled wrapper is
# just an unmodeled command (silence-licenses-nothing). Pins that peel machinery is invisible
# without a wrapper oracle (empty-world-byte-identical).
sudo hork setup
