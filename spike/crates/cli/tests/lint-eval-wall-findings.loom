---
run: lint
fixpoint: executed
exit: 1
---
-- book.sh --
eval "apt-get install -y nginx"
apt-get install -y curl

-- replay --
$ dorc lint --source=analysis-diagnostics --source=unmodeled-inventory book.sh
dorc lint findings (advisory; the machine format `--format=jsonl` is the stable surface):

book.sh:
  error[cfg-top-node]: unsupported construct (unknown): un-probeable and
  un-skippable
        [ book.sh:1:1
     1 | eval "apt-get install -y nginx"
       | \__/
  error[syntax-unsupported]: `eval` executes constructed code (un-analyzable)
        [ book.sh:1:1
     1 | eval "apt-get install -y nginx"
       | \__/
  1:1 info [unmodeled-inventory:unmodeled-wall-inventory] this book carries 1 unmodeled wall -- constructs Dorc cannot analyze, past which no probe result can be trusted; the first is here, and the ~1 command site(s) after it fall back to runtime guards instead of full elision.

dorc lint: 2 errors, 0 warnings, 1 info across 1 file.
