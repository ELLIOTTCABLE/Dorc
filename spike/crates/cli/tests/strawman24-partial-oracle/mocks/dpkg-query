#!/bin/sh
# INERT probe shim — `dpkg-query -W <pkg>`. HOST: nginx installed (rc 0 ⇒ holds). Only the
# PRE-wall nginx install is probed; curl is past the un-oracled `systemctl` wall ⇒
# unresolvable ⇒ never probed. Read-only; logs for symmetry.
. "${0%/*}/.log"; _dorc_logged "$@"
exit 0
