---
run: round-trip
fixpoint: executed
---
-- book.sh --
#!/bin/sh
# dorc-lang/v0.2
# pin30-wrapped-case-bodied-in-book-verdict — the canonical case-bodied verdict, sited in a BOOK above its wrapped site, answers nothing there. MEASURED, not expected: this rode stage-i as an expected-to-flip diagnostic and did NOT flip, so `28Q` §1's asserted cause (oracle-only vectors at the wrapped seat) is REFUTED — `build_wrapped_analysis` already took the source-wide vectors before that lane began, and the wrapper-lane conversion did not move it either.
# THE LEAD, from the corpus rather than from a hypothesis: pin28-wrapped-vouch-answers-at-a-live-site is the SAME world with the SAME sudo oracle and an in-book verdict at the same wrapped site, and it ships its check and elides. The only difference is the verdict body's SHAPE — straight-line there, case-with-decline-arm here — so the discriminator is body shape, not vector membership. The chase is banked; the behaviour is conservative (the site runs), so this is value-loss, never a license bug.
# `28Q` §8 STAGE-0 also did not flip it, and the re-cut refutes two more candidates: the wrapped ship seat now prefers a vouching verdict over any predict and nothing moved, and this consent mark is top-level, so `tolerated_on_path` lifts it identically to pin28's for every verb. Two candidate seats remain: `evaluate_verdict`'s tracer over a case body at the PEELED argv, and `peel_book_chain`.
hork__is_converged() {
   : safe-across user
   case "$1" in
   install) hork query -- "$2" ;;
   *) return 2 ;;
   esac
}
sudo hork install wombat

-- sudo.oracle.sh --
# dorc-lang/v0.2
# The coherent sudo wrapper of pin28-wrapped-vouch-answers-at-a-live-site, byte-for-byte: only the verdict body differs between the two cases.
sudo__predict() {
   while [ "${1#-}" != "$1" ]; do case "$1" in -u) shift 2 ;; *) shift ;; esac; done
   env -i HOME=/root "$@"
}
sudo__lend_map() {
   target=root
   while [ "${1#-}" != "$1" ]; do case "$1" in -u) target="$2"; shift 2 ;; *) shift ;; esac; done
   printf '%s\n' "$target" : lends user
   : lends fs-view
   : lends netns
   "$@"
}
sudo__enter() {
   sudo -n "$@"
}

-- probe-results.txt --
# EMPTY, and that emptiness is still the alarm: the day this site ships a check, gate-1 goes red
# because the probe produced a record this file does not carry. That is what a diagnosis landing
# looks like from here, so the cell stays exactly as it is until one does.

-- mocks/.log --
# newline-safe argv logger for the inert mock shims (slice-4). Sourced by each shim via
# `. "${0%/*}/.log"` (the shim's own dir); a DOTFILE so gate-5/gate-6's `ls`-derived shimset
# never lists it. Shims run under PATH=mocks-only ⇒ BUILTINS ONLY (no sed/awk).
#
# GRAMMAR (what expected.ran goldens carry): one `ran: <name> <arg>…` line per invocation; each
# arg is encoded — a literal backslash as `\\`, an embedded newline as the two chars `\n`. The
# two passes run in THIS order (backslash first) so a newline's encoded `\n` backslash is not
# re-doubled.
_dorc_logged() {
  _dl_out="ran: ${0##*/}"
  for _dl_a in "$@"; do
    _dl_o=""                                          # pass 1: \ -> \\
    while case $_dl_a in *\\*) true ;; *) false ;; esac; do
      _dl_o="$_dl_o${_dl_a%%\\*}\\\\"; _dl_a=${_dl_a#*\\}
    done
    _dl_a="$_dl_o$_dl_a"
    _dl_o=""                                          # pass 2: newline -> \n
    while case $_dl_a in *"$_DL_NL"*) true ;; *) false ;; esac; do
      _dl_o="$_dl_o${_dl_a%%"$_DL_NL"*}\\n"; _dl_a=${_dl_a#*"$_DL_NL"}
    done
    _dl_out="$_dl_out $_dl_o$_dl_a"
  done
  printf '%s\n' "$_dl_out" >>"$DORC_LOG"
}
_DL_NL='
'

-- mocks/hork --
#!/bin/sh
# INERT strawman mock (never a real mutator): logs its argv for the run-set, mutates nothing.
. "${0%/*}/.log"; _dorc_logged "$@"
exit 0

-- mocks/sudo --
#!/bin/sh
# INERT mock: logs its argv and exits 0; the guest of a `sudo -n` entry here is a shell function, so nothing is exec'd.
. "${0%/*}/.log"; _dorc_logged "$@"
exit 0

-- expected.ran --
ran: sudo hork install wombat

-- replay --
$ dorc --book=book.sh --pre-source sudo.oracle.sh --results - < probe-results.txt
reserved: warning[reserved-namespace-squat]: Book function `hork__is_converged`
sits in the `__is_converged` namespace reserved for oracle functions; Dorc
treats it as ordinary shell and runs it verbatim, but a shipped oracle preamble
defining the same name would collide with it. Rename it to stay clear of
`*__is_converged`.
      [ book.sh:6:1
   6 | hork__is_converged() {
     | \________________/
loading: note[role-defined-below-its-sites]: `hork__is_converged` is defined
below 1 of its family's sites, and a definition licenses nothing above itself,
so those sites run as if it were absent -- moving it above them is one
cut-and-paste.
      [ book.sh:6:1
   6 | hork__is_converged() {
     | \________________/
escalation: note[escalation-policy]: escalation policy: probe re-uses connection
authority (root) for `tolerates:`-vouched functions only (default); entry forms:
sudo -n. Forbid with --no-probe-escalation; widen with --escalate-any-probe.
#!/bin/sh
# dorc probe (read-only): checks per-SITE convergence, mutates nothing.
# When run, emits one results-record per site on stdout (the return channel):
#   site <leafid> effect=<holds|absent|cant-tell> rc=<n>
# effect is derived from the probe command's rc (0=holds, 1=absent, else cant-tell);
# rc is the raw PROBE-command status (opaque to Dorc -- the record is the out-of-band lane).

printf 'dorc-records/1 nonce=dorc attempt=1 host=localhost book=ba7d69414c68d9eaaa6def96dfe28ded23db86ee4f52053e6215c87cc65fc463 sites=0 @@dorc@@\n'
# site:0 unresolvable-no-probe
printf 'dorc-records-end/1 nonce=dorc @@dorc@@\n'
classify: note[cmdsub-operand-top]: operand 3 is an unresolvable positional
parameter (`$@`/`$*`/`$N`), so Dorc cannot know its value until the command runs
on the host -- there is nothing to resolve and no read-only probe to check. Dorc
elides a command only when it can prove the command's effect is already in
place, so this one is left to run on every apply.
      [ book.sh:9:27
   9 |    install) hork query -- "$2" ;;
     |                           \__/

   = repair:  Give the operand a value Dorc can resolve statically -- a literal,
              or a variable assigned from one -- so Dorc can probe it and elide
              the command once it has converged. If the value must stay dynamic,
              load an oracle that vouches for this command's convergence, and
              Dorc will guard the command instead of running it every time.
probe: note[site-unresolvable]: sm 1 site run unprobed (no read-only check could
be shipped): `sudo hork install wombat` -- run `dorc why` for the per-site
detail (the apply runs each anyway, to stay safe)
      [ book.sh:13:1
   13 | sudo hork install wombat
      | \______________________/

   = help:  sm site runs `sudo hork install wombat`
why: ran because operand 3 is a command-substitution `$(...)` or runtime-dynamic value -- its value couldn't be resolved (first seen at 9:13 `hork query -- "$2"`); so dorc runs it, to stay safe (when unsure, run). to skip it, make the operand a literal Dorc can resolve+probe [resolve-dynamism]
hint: 'sudo' (line 13) is unmodeled: it is the first wall -- an oracle vouching its convergence would elide it when converged
dorc: run `dorc why` for the per-site cause-chains, or `dorc why book.sh:N` to query a source line
#!/bin/sh
# dorc apply: the book, with already-converged/dead lines elided (value-preserving stand-in).

#!/bin/sh
# dorc-lang/v0.2
# pin30-wrapped-case-bodied-in-book-verdict — the canonical case-bodied verdict, sited in a BOOK above its wrapped site, answers nothing there. MEASURED, not expected: this rode stage-i as an expected-to-flip diagnostic and did NOT flip, so `28Q` §1's asserted cause (oracle-only vectors at the wrapped seat) is REFUTED — `build_wrapped_analysis` already took the source-wide vectors before that lane began, and the wrapper-lane conversion did not move it either.
# THE LEAD, from the corpus rather than from a hypothesis: pin28-wrapped-vouch-answers-at-a-live-site is the SAME world with the SAME sudo oracle and an in-book verdict at the same wrapped site, and it ships its check and elides. The only difference is the verdict body's SHAPE — straight-line there, case-with-decline-arm here — so the discriminator is body shape, not vector membership. The chase is banked; the behaviour is conservative (the site runs), so this is value-loss, never a license bug.
# `28Q` §8 STAGE-0 also did not flip it, and the re-cut refutes two more candidates: the wrapped ship seat now prefers a vouching verdict over any predict and nothing moved, and this consent mark is top-level, so `tolerated_on_path` lifts it identically to pin28's for every verb. Two candidate seats remain: `evaluate_verdict`'s tracer over a case body at the PEELED argv, and `peel_book_chain`.
hork__is_converged() {
   : safe-across user
   case "$1" in
   install) hork query -- "$2" ;;
   *) return 2 ;;
   esac
}
sudo hork install wombat
dorc: plan-summary sites=1 elide=0 omit=0 guard=0 run=1 may-alias=0
dorc: decision-digest b4d83eb2d036cd0d79c0cad3a842ec85cba30d21e056f401fa89b4a88e5f02f8
