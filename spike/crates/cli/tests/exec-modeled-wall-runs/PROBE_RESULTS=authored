parity opted-out (same rationale as exec-poison-wall-dead): the pkgindex site's probe is a genuine FILESYSTEM-TIMING observation (`test -n "$(find … -newermt '-1 hour')"`); a `find` PATH shim forcing a result would FABRICATE freshness (inv-probe-sourced-values forbids the all-exit-0 masking). Here the authored records deliberately diverge the pkgindex site (absent) and converge the package site (holds) to exercise the wall, so mock-execution parity cannot reproduce them. Also excludes the case from gate-6 (bare-vs-apply delta diverges by construction when probe-results are authored). gate-1(a) site-completeness + the apply run-set diff (gate-4) still enforced — the run-set is exactly what pins the wall.
