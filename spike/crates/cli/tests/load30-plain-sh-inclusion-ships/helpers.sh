# Ordinary sh: no dorc-lang marker, no roles, no marks — the file an admin already had, and the
# most common multi-file book shape. Nothing here is analyzed; the book's `.` of it still walls.
plain_helper_step() {
   wombat note done
}
