---
---
-- book.sh --
#!/bin/sh
# wrapper-modeled-peel-coheres-walls (273; 27J §2.1 zero-new-trust): a COHERENT sudo wrapper oracle
# is loaded (predict env-peels, lend_map answers user/fs-view; both peel to the same tail). It
# lifts clean and coheres (no fail-fast). But the wrapped book site STILL walls — this lane MODELS
# the peel, it does not yet consume it in the pipeline (the entry/dial/vouch machinery is the next
# lane). Byte-identical to wrapper-unmodeled-peel-walls: loading a valid wrapper oracle mints no
# elision (zero new trust surface).
sudo hork setup

-- sudo.oracle.sh --
# dorc-lang/v0.2
# A COHERENT sudo wrapper (273): predict env-scrubs then execs the guest; lend_map answers the
# user (mapped) and fs-view (full) dimensions and peels to the same tail. netns is unanswered ⇒ ⊤
# (the enumerate-every-dimension law). Both members flag-strip identically, so their "$@" agree.
sudo__predict() {
   while [ "${1#-}" != "$1" ]; do shift; done
   env -i TERM="${TERM-}" HOME=/root "$@"
}
sudo__lend_map() {
   while [ "${1#-}" != "$1" ]; do shift; done
   printf '%s\n' root : lends user
   : lends fs-view
   "$@"
}

-- probe-results.txt --
# `sudo hork setup` walls (sudo modeled-but-not-consumed) ⇒ no probe site.

-- mocks/.log --
# newline-safe argv logger for the inert mock shims (slice-4). Sourced by each shim via
# `. "${0%/*}/.log"` (the shim's own dir); a DOTFILE so gate-5/gate-6's `ls`-derived shimset
# never lists it. Replaces the old inline `printf 'ran: %s %s\n' "${0##*/}" "$*"` — an arg with
# an embedded NEWLINE would split the `ran:` line and silently break every line-based compare
# (exec_check, gate-5, gate-6). Shims run under PATH=mocks-only ⇒ BUILTINS ONLY (no sed/awk).
#
# GRAMMAR (what expected.ran goldens carry): one `ran: <name> <arg>…` line per invocation; each
# arg is encoded — a literal backslash as `\\`, an embedded newline as the two chars `\n`. The
# two passes run in THIS order (backslash first) so a newline's encoded `\n` backslash is not
# re-doubled. A newline-free, backslash-free argv encodes to itself ⇒ byte-identical to the old
# format (per-arg space-join == the old `"$*"` join) ⇒ zero golden churn.
_dorc_logged() {
  _dl_out="ran: ${0##*/}"
  for _dl_a in "$@"; do
    _dl_o=""                                          # pass 1: \ -> \\
    while case $_dl_a in *\\*) true ;; *) false ;; esac; do
      _dl_o="$_dl_o${_dl_a%%\\*}\\\\"; _dl_a=${_dl_a#*\\}
    done
    _dl_a="$_dl_o$_dl_a"
    _dl_o=""                                          # pass 2: newline -> \n
    while case $_dl_a in *"$_DL_NL"*) true ;; *) false ;; esac; do
      _dl_o="$_dl_o${_dl_a%%"$_DL_NL"*}\\n"; _dl_a=${_dl_a#*"$_DL_NL"}
    done
    _dl_out="$_dl_out $_dl_o$_dl_a"
  done
  printf '%s\n' "$_dl_out" >>"$DORC_LOG"
}
_DL_NL='
'

-- mocks/hork --
#!/bin/sh
# INERT strawman mock (never a real mutator): logs its argv for the run-set, mutates nothing.
. "${0%/*}/.log"; _dorc_logged "$@"
exit 0

-- mocks/sudo --
#!/bin/sh
# INERT strawman mock (never a real mutator): logs its argv for the run-set, mutates nothing.
. "${0%/*}/.log"; _dorc_logged "$@"
exit 0

-- expected.ran --
ran: sudo hork setup

-- replay --
$ dorc --book=book.sh --pre-source sudo.oracle.sh --results - < probe-results.txt
#!/bin/sh
# dorc probe (read-only): checks per-SITE convergence, mutates nothing.
# When run, emits one results-record per site on stdout (the return channel):
#   site <leafid> effect=<holds|absent|cant-tell> rc=<n>
# effect is derived from the probe command's rc (0=holds, 1=absent, else cant-tell);
# rc is the raw PROBE-command status (opaque to Dorc -- the record is the out-of-band lane).

printf 'dorc-records/1 nonce=dorc attempt=1 host=localhost book=b877194e521d84294fc5e9e2318ee235dd3061e1957f1b3cadcd0fc06fb3ad3f sites=0 @@dorc@@\n'
# site:0 unresolvable-no-probe
printf 'dorc-records-end/1 nonce=dorc @@dorc@@\n'
probe: note[site-unresolvable]: sm 1 site run unprobed (no read-only check could
be shipped): `sudo hork setup` -- run `dorc why` for the per-site detail (the
apply runs each anyway, to stay safe)
      [ book.sh:8:1
   8 | sudo hork setup
     | \_____________/

   = help:  sm site runs `sudo hork setup`
hint: 'sudo' (line 8) is unmodeled: it is the first wall -- an oracle vouching its convergence would elide it when converged
dorc: run `dorc why` for the per-site cause-chains, or `dorc why book.sh:N` to query a source line
#!/bin/sh
# dorc apply: the book, with already-converged/dead lines elided (value-preserving stand-in).

#!/bin/sh
# wrapper-modeled-peel-coheres-walls (273; 27J §2.1 zero-new-trust): a COHERENT sudo wrapper oracle
# is loaded (predict env-peels, lend_map answers user/fs-view; both peel to the same tail). It
# lifts clean and coheres (no fail-fast). But the wrapped book site STILL walls — this lane MODELS
# the peel, it does not yet consume it in the pipeline (the entry/dial/vouch machinery is the next
# lane). Byte-identical to wrapper-unmodeled-peel-walls: loading a valid wrapper oracle mints no
# elision (zero new trust surface).
sudo hork setup
dorc: plan-summary sites=1 elide=0 omit=0 guard=0 run=1 may-alias=0
dorc: decision-digest e4b61bb234f5e8e00aaef828ab8b4e26fec370cfd92498f2b92001378ea582cc
