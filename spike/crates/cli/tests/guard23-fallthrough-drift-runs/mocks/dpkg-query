#!/bin/sh
# INERT probe shim for `dpkg-query -W <pkg>` — exits 1 AT APPLY TIME: the world has
# drifted since the plan-time probe (probe-results.txt says holds). Read-only; logs argv.
. "${0%/*}/.log"; _dorc_logged "$@"
exit 1
