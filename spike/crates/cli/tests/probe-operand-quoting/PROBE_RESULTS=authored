parity opted-out (D3b, item-1): this is a QUOTING test (F-QUOTE), not a convergence test — its probe-results.txt DELIBERATELY authors ZERO site records, so both installs run regardless of convergence (the point is that a spaced/metachar operand renders as exactly one inert single-quoted arg). The rendered probe emits 2 site records, so any shim would PRODUCE records that diverge from the intentionally-empty fixture ⇒ parity cannot enforce without corrupting the test's purpose. A third opt-out reason beyond builtin/find (flagged 20G). gate-1(a) site-completeness still enforced (both emitted sites self-report).
