#!/bin/sh
# dorc probe (read-only): checks per-SITE convergence, mutates nothing.
# When run, emits one results-record per site on stdout (the return channel):
#   site <leafid> effect=<holds|absent|cant-tell> rc=<n>
# effect is derived from the probe command's rc (0=holds, 1=absent, else cant-tell);
# rc is the raw PROBE-command status (opaque to Dorc -- the record is the out-of-band lane).

printf 'dorc-records/1 nonce=dorc attempt=1 host=localhost book=1b00731cf3f3462ae444fc2c9a197d044f6bbf716589f66e8f1c09ce6b95901c sites=2 @@dorc@@\n'
# site 0.0: sm.dorc.Package:nginx@installed
apt_get__is_converged() {
   while [ "${1#-}" != "$1" ]; do shift; done
   verb=$1; shift
   while [ "${1#-}" != "$1" ]; do shift; done
   case $verb in
      install) aptcheck -q -- "$1" ;;
      *) return 2 ;;
   esac
}
apt_get__is_converged 'install' '-y' 'nginx'; _rc=$?; if [ "$_rc" -eq 0 ]; then _e=holds; elif [ "$_rc" -eq 1 ]; then _e=absent; else _e=cant-tell; fi; printf 'dorc site 0.0 effect=%s rc=%s @@dorc@@\n' "$_e" "$_rc"
# site 1.0: sm.dorc.Package:curl@installed
apt_get__is_converged 'install' '-y' 'curl'; _rc=$?; if [ "$_rc" -eq 0 ]; then _e=holds; elif [ "$_rc" -eq 1 ]; then _e=absent; else _e=cant-tell; fi; printf 'dorc site 1.0 effect=%s rc=%s @@dorc@@\n' "$_e" "$_rc"
printf 'dorc-records-end/1 nonce=dorc @@dorc@@\n'
#!/bin/sh
# dorc apply: the book, with already-converged/dead lines elided (value-preserving stand-in).

#!/bin/sh
# ONE authored region, TWO invocations, and the shared decision is universal over both
# (`30L:rul-shared-region-needs-universal-must`). Both operands are already installed, so every
# route agrees and the DERIVED whole-helper answer follows: each CALL independently replaces once
# its own position is fresh, and the inert definition keeps its AUTHORED TEXT
# (`30L:pin-whole-helper-derived-only`, §8) rather than acquiring stand-ins nothing reaches.
#
# The second call is fresh only in a LATER settlement round, once the first is proven not to
# execute — the grow-only ledger doing its job, not a special case.
install_pkg() {
   apt-get install -y "$1"
}

# install_pkg nginx   # dorc: elided (already converged / dead branch)
# install_pkg curl   # dorc: elided (already converged / dead branch)
