---
probe-results: authored
---
-- book.sh --
#!/bin/sh
# 27C §5 in-context GUARD (27O rider): the unmodeled horkwall wall poisons the wrapped establish to
# EstablishWritten, so a reached wrapped vouch + converged probe mint the entry-composed guard shape
# `( sudo__enter hork__is_converged … ) || sudo hork install frob` (27N scaffold, exercised).
horkwall poke
sudo hork install frob

-- hork.oracle.sh --
# dorc-lang/v0.2
# hork: an inert package-manager stub; `: safe-across user` vouches read-only under a user shift
# (27C §2), so a sudo-wrapped `hork install` may probe in the root context.
hork__is_converged() {
   : safe-across user
   case "$1" in
   install) hork query "$2" ;;
   *) return 2 ;;
   esac
}

-- sudo.oracle.sh --
# dorc-lang/v0.2
# A coherent sudo wrapper (273 + 27C §3): predict env-scrubs + execs the guest; lend_map maps user
# and full-lends fs-view + netns (enumerate-every-dimension); the entry form re-runs sudo `-n`
# (non-interactive by construction). Authoring `sudo__enter` IS the traversal vouch.
sudo__predict() {
   while [ "${1#-}" != "$1" ]; do case "$1" in -u) shift 2 ;; *) shift ;; esac; done
   env -i HOME=/root "$@"
}
sudo__lend_map() {
   target=root
   while [ "${1#-}" != "$1" ]; do case "$1" in -u) target="$2"; shift 2 ;; *) shift ;; esac; done
   printf '%s\n' "$target" : lends user
   : lends fs-view
   : lends netns
   "$@"
}
sudo__enter() {
   sudo -n "$@"
}

-- probe-results.txt --
site 1 effect=holds rc=0

-- mocks/.log --
# newline-safe argv logger for the inert mock shims (slice-4). Sourced by each shim via
# `. "${0%/*}/.log"` (the shim's own dir); a DOTFILE so gate-5/gate-6's `ls`-derived shimset
# never lists it. Replaces the old inline `printf 'ran: %s %s\n' "${0##*/}" "$*"` — an arg with
# an embedded NEWLINE would split the `ran:` line and silently break every line-based compare
# (exec_check, gate-5, gate-6). Shims run under PATH=mocks-only ⇒ BUILTINS ONLY (no sed/awk).
#
# GRAMMAR (what expected.ran goldens carry): one `ran: <name> <arg>…` line per invocation; each
# arg is encoded — a literal backslash as `\\`, an embedded newline as the two chars `\n`. The
# two passes run in THIS order (backslash first) so a newline's encoded `\n` backslash is not
# re-doubled. A newline-free, backslash-free argv encodes to itself ⇒ byte-identical to the old
# format (per-arg space-join == the old `"$*"` join) ⇒ zero golden churn.
_dorc_logged() {
  _dl_out="ran: ${0##*/}"
  for _dl_a in "$@"; do
    _dl_o=""                                          # pass 1: \ -> \\
    while case $_dl_a in *\\*) true ;; *) false ;; esac; do
      _dl_o="$_dl_o${_dl_a%%\\*}\\\\"; _dl_a=${_dl_a#*\\}
    done
    _dl_a="$_dl_o$_dl_a"
    _dl_o=""                                          # pass 2: newline -> \n
    while case $_dl_a in *"$_DL_NL"*) true ;; *) false ;; esac; do
      _dl_o="$_dl_o${_dl_a%%"$_DL_NL"*}\\n"; _dl_a=${_dl_a#*"$_DL_NL"}
    done
    _dl_out="$_dl_out $_dl_o$_dl_a"
  done
  printf '%s\n' "$_dl_out" >>"$DORC_LOG"
}
_DL_NL='
'

-- mocks/hork --
#!/bin/sh
. "${0%/*}/.log"; _dorc_logged "$@"
exit 0

-- mocks/horkwall --
#!/bin/sh
. "${0%/*}/.log"; _dorc_logged "$@"
exit 0

-- mocks/sudo --
#!/bin/sh
. "${0%/*}/.log"; _dorc_logged "$@"
exit 0

-- expected.ran --
ran: horkwall poke
ran: sudo -n hork__is_converged install frob

-- replay --
$ dorc --book=book.sh --pre-source hork.oracle.sh --pre-source sudo.oracle.sh --results - < probe-results.txt
escalation: note[escalation-policy]: escalation policy: probe re-uses connection
authority (root) for `tolerates:`-vouched functions only (default); entry forms:
sudo -n. Forbid with --no-probe-escalation; widen with --escalate-any-probe.
#!/bin/sh
# dorc probe (read-only): checks per-SITE convergence, mutates nothing.
# When run, emits one results-record per site on stdout (the return channel):
#   site <leafid> effect=<holds|absent|cant-tell> rc=<n>
# effect is derived from the probe command's rc (0=holds, 1=absent, else cant-tell);
# rc is the raw PROBE-command status (opaque to Dorc -- the record is the out-of-band lane).

printf 'dorc-records/1 nonce=dorc attempt=1 host=localhost book=4e16c777bdccecb05e272b22feebeda37b22f0ada6ff5fcf3d95e5c78fe12314 sites=1 @@dorc@@\n'
# site 1: dorc-auto:hork@converged
sudo__enter() {
   sudo -n "$@"
}
hork__is_converged() {
   :
   case "$1" in
   install) hork query "$2" ;;
   *) return 2 ;;
   esac
}
sudo__enter hork__is_converged 'install' 'frob'; _rc=$?; if [ "$_rc" -eq 0 ]; then _e=holds; elif [ "$_rc" -eq 1 ]; then _e=absent; else _e=cant-tell; fi; printf 'dorc site 1 effect=%s rc=%s @@dorc@@\n' "$_e" "$_rc"
# site:0 unresolvable-no-probe
printf 'dorc-records-end/1 nonce=dorc @@dorc@@\n'
probe: note[site-unresolvable]: sm 1 site run unprobed (no read-only check could
be shipped): `horkwall poke` -- run `dorc why` for the per-site detail (the
apply runs each anyway, to stay safe)
      [ book.sh:5:1
   5 | horkwall poke
     | \___________/

   = help:  sm site runs `horkwall poke`
why: site 1 guard [dorc-auto:hork] -- licensed by the dorc-auto:hork oracle's vouch that it is already satisfied; the original bytes survive and the check re-runs live at apply (to stay safe)
hint: 'horkwall' (line 5) is unmodeled: it is the first wall -- an oracle vouching its convergence would elide it when converged, and un-wall 1 downstream site
dorc: run `dorc why` for the per-site cause-chains, or `dorc why book.sh:N` to query a source line
#!/bin/sh
# dorc apply: the book, with already-converged/dead lines elided (value-preserving stand-in).

# dorc guard preamble: the vouching oracle's own verdict body, shipped strip-only
# (dialect annotations removed, nothing else changed -- rul-ternary-verdict: the
# authored bytes verbatim, no engine-synthesized sh).
sudo__enter() {
   sudo -n "$@"
}
hork__is_converged() {
   :
   case "$1" in
   install) hork query "$2" ;;
   *) return 2 ;;
   esac
}

# dorc: end of lifted material; the book follows, in its authored order.

#!/bin/sh
# 27C §5 in-context GUARD (27O rider): the unmodeled horkwall wall poisons the wrapped establish to
# EstablishWritten, so a reached wrapped vouch + converged probe mint the entry-composed guard shape
# `( sudo__enter hork__is_converged … ) || sudo hork install frob` (27N scaffold, exercised).
horkwall poke
( sudo__enter hork__is_converged install frob ) || sudo hork install frob   # dorc: guard [dorc-auto:hork converged-vouch; probe: holds]
dorc: plan-summary sites=2 elide=0 omit=0 guard=1 run=1 may-alias=0
dorc: decision-digest 4a4377fcb21d2b0d11a7ae7855f6d64dbf1edc071438360a054a052acb724f1c
