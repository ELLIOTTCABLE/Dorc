---
xfail: p-x-load31-punted-load-shapes
---
-- book.sh --
#!/bin/sh
# TARGET (the three r31 load shapes in one book): a command-substitution script-relative load
# (`principle-load-operands-evaluate-over-controller-known-inputs`, held by the open ruling
# `ask-dollar-zero-command-substitution-path`), a set-valued glob load of two dorc-lang plugins
# (same principle, member-population half), and an unconditional inclusion of ordinary sh
# (`principle-book-code-source-is-inclusion`).
#
# The script-relative load is spelled through SCRIPT_DIR rather than inline, because an inline
# `$(...)` in a `.` operand is refused at PARSE tier today and takes the whole invocation's exit
# code with it, which would blind the other two shapes. The inline spelling is pinned at unit tier
# (`p-x-load-operand-dirname-of-dollar-zero`).
#
# TARGET RUN SET: `ran: wombat note done` and nothing else. The three described sites (`hork tune`,
# `wombat sync`, `zork prime`) all have vouching verdicts and probe fixtures saying their cells
# hold, so they elide; `plain_helper_step` is a function this book can only have because the plain
# sh file was included, and the `wombat note` inside it names no described verb, so it runs.
SCRIPT_DIR=$(cd "$(dirname "$0")" && pwd)
. "$SCRIPT_DIR/hork.dorc.sh"

for plugin in ./*.plugin.dorc.sh; do
   . "$plugin"
done

. ./helpers.sh

hork tune web
wombat sync cache
zork prime cache
plain_helper_step

-- alpha.plugin.dorc.sh --
# dorc-lang/v0.2
# Glob member one. Both members are ordinary complete packages for their own tool, so each one's
# elision is independent evidence that the set-valued operand acquired it.
wombat__predict() {
   verb=$1; shift
   store : sm.dorc.Widget = "$1"
   case $verb in
      sync) wombat status --synced -- "$store"   : sm.dorc.Widget:"$store"@synced ;;
   esac
}

wombat__is_converged() {
   verb=$1; shift
   case $verb in
      sync) wombat status --synced -- "$1" ;;
      *) return 2 ;;
   esac
}

-- beta.plugin.dorc.sh --
# dorc-lang/v0.2
# Glob member two, describing a different tool: an ordered population has to acquire BOTH members,
# and a case that only ever needed the first could not tell an ordered set from a lucky singleton.
zork__predict() {
   verb=$1; shift
   cache : sm.dorc.Widget = "$1"
   case $verb in
      prime) zork status --primed -- "$cache"   : sm.dorc.Widget:"$cache"@primed ;;
   esac
}

zork__is_converged() {
   verb=$1; shift
   case $verb in
      prime) zork status --primed -- "$1" ;;
      *) return 2 ;;
   esac
}

-- helpers.sh --
# Ordinary sh: no dorc-lang marker, no roles, no marks — the file an admin already had. Its
# unconditional `.` in the book is the plain inclusion `principle-book-code-source-is-inclusion`
# owes, and `plain_helper_step` exists in the book only because that inclusion happened.
plain_helper_step() {
   wombat note done
}

-- hork.dorc.sh --
# dorc-lang/v0.2
# The package the book locates through `$(dirname "$0")` — the command-substitution spelling of
# the same script-location question `${0%/*}` answers without running anything.
hork__predict() {
   verb=$1; shift
   widget : sm.dorc.Widget = "$1"
   case $verb in
      tune) hork status --tuned -- "$widget"   : sm.dorc.Widget:"$widget"@tuned ;;
   esac
}

hork__is_converged() {
   verb=$1; shift
   case $verb in
      tune) hork status --tuned -- "$1" ;;
      *) return 2 ;;
   esac
}

-- probe-results.txt --
# The three described cells, reported converged: the target is that all three sites ELIDE.
# Site numbering today (leaves in book order): 0 the SCRIPT_DIR assignment, 1 the script-relative
# load, 2 the in-loop load, 3 the plain-sh load, 4/5/6 the three tool sites, 7 plain_helper_step.
# A numbering that moves under the acquisition work is re-blessed with the transcript at promotion.
site 4 effect=holds
site 5 effect=holds
site 6 effect=holds

-- mocks/.log --
_dorc_logged() {
   _dl_out="ran: ${0##*/}"
   for _dl_arg do _dl_out="$_dl_out $_dl_arg"; done
   printf '%s\n' "$_dl_out" >>"$DORC_LOG"
}

-- mocks/hork --
#!/bin/sh
# INERT mock: logs that it ran, mutates NOTHING, exits 0. `hork` is a non-functional stub name —
# under the harness's mocks-only PATH this file is the only thing an executed artifact can reach.
. "${0%/*}/.log"; _dorc_logged "$@"
exit 0

-- mocks/wombat --
#!/bin/sh
# INERT mock: logs that it ran, mutates NOTHING, exits 0. `wombat` is a non-functional stub name —
# under the harness's mocks-only PATH this file is the only thing an executed artifact can reach.
. "${0%/*}/.log"; _dorc_logged "$@"
exit 0

-- mocks/zork --
#!/bin/sh
# INERT mock: logs that it ran, mutates NOTHING, exits 0. `zork` is a non-functional stub name —
# under the harness's mocks-only PATH this file is the only thing an executed artifact can reach.
. "${0%/*}/.log"; _dorc_logged "$@"
exit 0

-- expected.ran --
ran: wombat note done

-- replay --
$ dorc --book=book.sh --artifact-dir=$ARTIFACT_DIR --results - < probe-results.txt
cfg: warning[cfg-errexit-unknown]: Dorc could not tell whether `set -e` is in
effect at one or more commands (a non-literal `set` argument, or paths that
disagree), so it plans as though each might abort the book on failure -- the
cautious reading; it can cost elisions, never correctness.
loading: warning[load-carriage-withheld-under-unknown-cwd]: [unwritten:
load-carriage-withheld-under-unknown-cwd]
      [ book.sh:24:1
   24 | . ./helpers.sh
      | \____________/
#!/bin/sh
# dorc probe (read-only): checks per-SITE convergence, mutates nothing.
# When run, emits one results-record per site on stdout (the return channel):
#   site <leafid> effect=<holds|absent|cant-tell> rc=<n>
# effect is derived from the probe command's rc (0=holds, 1=absent, else cant-tell);
# rc is the raw PROBE-command status (opaque to Dorc -- the record is the out-of-band lane).

printf 'dorc-records/1 nonce=dorc attempt=1 host=localhost book=361074577bda93e0cdf7dfd3d403208aef6cc4984c6f706a54d42e5e46dafa37 sites=0 @@dorc@@\n'
# site:0 unresolvable-no-probe
# site:1 unresolvable-no-probe
# site:2 unresolvable-no-probe
# site:3 unresolvable-no-probe
# site:4 unresolvable-no-probe
# site:5 unresolvable-no-probe
# site:6 unresolvable-no-probe
# site:7 unresolvable-no-probe
printf 'dorc-records-end/1 nonce=dorc @@dorc@@\n'
classify: note[cmdsub-operand-top]: operand 1 is an unresolvable positional
parameter (`$@`/`$*`/`$N`), so Dorc cannot know its value until the command runs
on the host -- there is nothing to resolve and no read-only probe to check. Dorc
elides a command only when it can prove the command's effect is already in
place, so this one is left to run on every apply.
      [ book.sh:1:9
   1 | #!/bin/sh
     |         ^
   2 | # TARGET (the three r31 load shapes in one book): a command-substitution script-relative load
     | \/

   = repair:  Give the operand a value Dorc can resolve statically -- a literal,
              or a variable assigned from one -- so Dorc can probe it and elide
              the command once it has converged. If the value must stay dynamic,
              load an oracle that vouches for this command's convergence, and
              Dorc will guard the command instead of running it every time.
classify: note[cmdsub-operand-top]: operand 1 is a runtime-dynamic variable
value (unset or branch-conflicted), so Dorc cannot know its value until the
command runs on the host -- there is nothing to resolve and no read-only probe
to check. Dorc elides a command only when it can prove the command's effect is
already in place, so this one is left to run on every apply.
      [ book.sh:18:3
   18 | . "$SCRIPT_DIR/hork.dorc.sh"
      |   \________________________/

   = repair:  Give the operand a value Dorc can resolve statically -- a literal,
              or a variable assigned from one -- so Dorc can probe it and elide
              the command once it has converged. If the value must stay dynamic,
              load an oracle that vouches for this command's convergence, and
              Dorc will guard the command instead of running it every time.
classify: note[cmdsub-operand-top]: operand 1 is a runtime-dynamic variable
value (unset or branch-conflicted), so Dorc cannot know its value until the
command runs on the host -- there is nothing to resolve and no read-only probe
to check. Dorc elides a command only when it can prove the command's effect is
already in place, so this one is left to run on every apply.
      [ book.sh:21:6
   21 |    . "$plugin"
      |      \_______/

   = repair:  Give the operand a value Dorc can resolve statically -- a literal,
              or a variable assigned from one -- so Dorc can probe it and elide
              the command once it has converged. If the value must stay dynamic,
              load an oracle that vouches for this command's convergence, and
              Dorc will guard the command instead of running it every time.
classify: note[cmdsub-inner-nonleaf]: Dorc cannot elide `dirname
\xe2\x9f\xa8\xe2\x8a\xa4\xe2\x9f\xa9` on its own: it runs inside a `$(...)`
substitution as part of evaluating its enclosing line, so it runs whenever that
line does.
      [ book.sh:1:1
   1 | #!/bin/sh
     | \_______/
   2 | # TARGET (the three r31 load shapes in one book): a command-substitution script-relative load
     | \/
classify: note[cmdsub-inner-nonleaf]: Dorc cannot elide `pwd` on its own: it
runs inside a `$(...)` substitution as part of evaluating its enclosing line, so
it runs whenever that line does.
      [ book.sh:2:15
   2 | # TARGET (the three r31 load shapes in one book): a command-substitution script-relative load
     |               \_/
probe: note[site-unresolvable]: sm 7 sites run unprobed (no read-only check
could be shipped): `. "$SCRIPT_DIR/hork.dorc.sh"`, `. "$plugin"`, `.
./helpers.sh`, `hork tune web`, `wombat sync cache`, `zork prime cache`,
`plain_helper_step` -- run `dorc why` for the per-site detail (the apply runs
each anyway, to stay safe)
      [ book.sh:18:1
   18 | . "$SCRIPT_DIR/hork.dorc.sh"
      | \__________________________/

   = help:  sm site runs `. "$SCRIPT_DIR/hork.dorc.sh"`
why: ran because operand 1 is a command-substitution `$(...)` or runtime-dynamic value -- its value couldn't be resolved (first seen at 1:1 `#!/bin/sh\x0a# `); so dorc runs it, to stay safe (when unsure, run). to skip it, make the operand a literal Dorc can resolve+probe [resolve-dynamism]
why: ran because operand 1 is a command-substitution `$(...)` or runtime-dynamic value -- its value couldn't be resolved (first seen at 18:1 `. "$SCRIPT_DIR/hork.dorc.sh"`); so dorc runs it, to stay safe (when unsure, run). to skip it, make the operand a literal Dorc can resolve+probe [resolve-dynamism]
why: ran because operand 1 is a command-substitution `$(...)` or runtime-dynamic value -- its value couldn't be resolved (first seen at 21:4 `. "$plugin"`); so dorc runs it, to stay safe (when unsure, run). to skip it, make the operand a literal Dorc can resolve+probe [resolve-dynamism]
hint: '.' (line 18) is unmodeled: it is the first wall -- an oracle vouching its convergence would elide it when converged; 6 more unmodeled walls -- dorc why
dorc: run `dorc why` for the per-site cause-chains, or `dorc why book.sh:N` to query a source line
#!/bin/sh
# dorc apply: the book, with already-converged/dead lines elided (value-preserving stand-in).

#!/bin/sh
# TARGET (the three r31 load shapes in one book): a command-substitution script-relative load
# (`principle-load-operands-evaluate-over-controller-known-inputs`, held by the open ruling
# `ask-dollar-zero-command-substitution-path`), a set-valued glob load of two dorc-lang plugins
# (same principle, member-population half), and an unconditional inclusion of ordinary sh
# (`principle-book-code-source-is-inclusion`).
#
# The script-relative load is spelled through SCRIPT_DIR rather than inline, because an inline
# `$(...)` in a `.` operand is refused at PARSE tier today and takes the whole invocation's exit
# code with it, which would blind the other two shapes. The inline spelling is pinned at unit tier
# (`p-x-load-operand-dirname-of-dollar-zero`).
#
# TARGET RUN SET: `ran: wombat note done` and nothing else. The three described sites (`hork tune`,
# `wombat sync`, `zork prime`) all have vouching verdicts and probe fixtures saying their cells
# hold, so they elide; `plain_helper_step` is a function this book can only have because the plain
# sh file was included, and the `wombat note` inside it names no described verb, so it runs.
SCRIPT_DIR=$(cd "$(dirname "$0")" && pwd)
. "$SCRIPT_DIR/hork.dorc.sh"

for plugin in ./*.plugin.dorc.sh; do
   . "$plugin"
done

. ./helpers.sh

hork tune web
wombat sync cache
zork prime cache
plain_helper_step
dorc: plan-summary sites=8 elide=0 omit=0 guard=0 run=8 may-alias=0
dorc: decision-digest ec765c955a427b713952af0e4f62abe80c5a1d48ce8a8dc980a697a7f1ed9d1e
