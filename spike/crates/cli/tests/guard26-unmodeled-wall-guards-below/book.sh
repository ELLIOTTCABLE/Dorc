#!/bin/sh
# guard26-unmodeled-wall-guards-below — the CONTROL half of a two-case pair; read it beside
# `guard26-classed-decline-demotes-guard`, whose book and oracle are byte-identical to this one.
# The pair's ONLY difference is that the sibling also loads a `hork` oracle that classes a decline.
#
# Here `hork` is unmodeled, so it is Opaque and WALLS. Both drops below it are therefore
# `EstablishWritten`, each with a reached vouch and a `holds` record on its own authored cell, so
# each mints a guard: `( check ) || original`. Two sites, two cells, two guards.
hork provision
wombat a.conf /etc/a.conf
wombat b.conf /etc/b.conf
