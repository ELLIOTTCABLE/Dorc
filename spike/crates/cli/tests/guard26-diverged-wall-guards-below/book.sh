#!/bin/sh
# guard26-diverged-wall-guards-below — FUTURE-pin (XFAIL): the DIVERGED flavor of the same guard
# void the `guard26-classed-decline-*` twins pin. Here `hork` bears a verdict function that ANSWERS
# `provision`, and the probe measures it diverged — so the line really runs: a modeled, attributed,
# RUNNING wall. This is USER_STORY stage-3/stage-5's drifted-day shape ("verify: converged, but
# past X"): the drops below carry a reached vouch and a `holds` record on their own authored cells,
# so the repaired engine should GUARD them. Today they classify `EstablishAmbient` (hork's establish
# touches only its own auto-cell), the wall walk demotes their elision Replace→Run only, and both
# mutation-capable drops ship verbatim.
#
# Same root as the classed-decline twin (`fnd-classed-decline-unwalls-guard-tier`), pinned
# separately so the BREADTH has an executable witness: the void fires below ANY modeled running
# wall — declined or diverged — not just below classed declines; as built, a guard can only ever
# exist below an UNMODELED wall. When the repair lands, this case goes XPASS (or head-expected.ran
# screams) — promote it. Evidence: `trial/r26/predictions.md` §7 · `notes/26F`.
hork provision
wombat a.conf /etc/a.conf
wombat b.conf /etc/b.conf
