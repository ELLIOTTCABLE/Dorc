// @generated by dorc-loom; DO NOT EDIT.
// This whole file is overwritten by arrangement promotion.

use super::{ArrangementEntry, Words};

#[rustfmt::skip]
pub const ARRANGEMENTS: &[ArrangementEntry] = &[
    ArrangementEntry {
        slug: "cli-help-page",
        occurrence: None,
        when_used: "`dorc --help` / `dorc -h` prints this whole page to stdout and exits 0.",
        why: "288 rul-help-text-is-loomable: help is product prose, so it gets an editable face like every other user-facing string.",
        words: Words::Migrated(&["dorc — spec-mining static-analysis orchestrator (implementation spike)\n\nusage: dorc [<mode>] --book=<book.sh> [-o <oracle.sh>]... [options]\n\nmodes (an optional leading token; default is the probe-then-apply round-trip):\n  probe        emit only the read-only probe artifact (phase 1) to stdout; reads no stdin\n  plan         preview the eliding apply on stdout, with the why-lens + diagnostics on stderr\n  apply        emit the byte-floored, receipt-free shippable apply artifact to stdout\n  why [<addr>] report (to stdout) WHY the run decided as it did — bare: the run's problems;\n               `book.sh:N`: the site on that source line; free text: matching commands\n  strip <file> print <file> with every dorc dialect construct erased — runnable stock POSIX sh\n               (the off-ramp cleaner; an unmarked file passes through unchanged)\n  lint <files> the oracle-author doctor/lint grab-bag over the files (no hosts, no probes):\n               parse/cfg diagnostics, unmodeled-wall inventory, verdict-body checks, and\n               shellcheck/checkbashisms when present. Flags: --format=human|jsonl,\n               --fail-on=error|warn|never, --no-tools, --require-tools, --expect-files N,\n               --source NAME (repeatable), --list-sources. Recommended CI line:\n               `dorc lint --format=jsonl --fail-on=warn --require-tools --expect-files N <files>`\n  (none)       the round-trip: probe then apply on stdout, full disclosure on stderr\n\noptions:\n  <book.sh>...          the book(s) to analyze — a positional path (`dorc plan book.sh`) or\n                        --book=PATH / --book PATH; repeatable ⇒ concatenated as one unit\n  -o, --oracle <o.sh>   an oracle file to load (repeatable; -o PATH, -oPATH, --oracle PATH)\n  --oracle-dir <dir>    load every *.oracle.sh in <dir> (repeatable; glob-sorted)\n  --results <file>      read the probe results from <file> (default: stdin)\n  --trust-footprints    opt into the survival tier (default off)\n  --debug-argv          echo the engine's per-site resolved argv to stderr\n  -h, --help            print this help to stdout and exit 0\n  --version             print the version to stdout and exit 0\n\nstdin:  probe results, one per line — `site <leafid> effect=<holds|absent|cant-tell> rc=<n>`\n        (unless --results <file>); stdout: the selected mode's artifact(s); stderr:\n        diagnostics / why-lens / decision-digest.\n\nexit codes:\n  0    success — the analysis completed and the artifact was emitted\n  2    usage error — a bad/unknown argument, a missing --book, or an unreadable file\n  10   parse error — the book carries a construct dorc cannot model (a syntax-level\n       ⊤-reject / CFG ⊤-node); the artifact still ships byte-identically, but the exit\n       signals partial understanding so a `dorc … && deploy` chain stops. First of the\n       reserved 10..19 dorc-semantic fast-fail range (vacuous/obvious, dorc-specific).\n\n  lint exit codes (distinct family — a ⊤-reject book is a FINDING here, never an exit-10):\n  0    clean — no findings at or above --fail-on\n  1    findings at or above --fail-on were reported\n  3    operational — the lint itself is compromised (no lintable files, an --expect-files\n       mismatch, or a --require-tools absence): distinct from both clean and findings\n"]),
    },
    ArrangementEntry {
        slug: "cli-decision-digest-line",
        occurrence: None,
        when_used: "Printed to stderr after every plan/apply/round-trip. The value is the digest itself.",
        why: "plans/240 Stage-1 yardstick: the run states the identity of the decisions it just made.",
        words: Words::Migrated(&["dorc: decision-digest ", ""]),
    },
    ArrangementEntry {
        slug: "cli-plan-summary-line",
        occurrence: None,
        when_used: "The stderr yardstick line after a plan/apply/round-trip. Values, in order: site, elide, omit, guard, run, and may-alias counts.",
        why: "plans/240 Stage-1 yardstick, with 24F section 3a's may-alias fire-rate surfaced so a swamped count reads as a finding.",
        words: Words::Migrated(&["dorc: plan-summary sites=", " elide=", " omit=", " guard=", " run=", " may-alias=", ""]),
    },
    ArrangementEntry {
        slug: "cli-why-pointer-line",
        occurrence: None,
        when_used: "The aggregate pointer a `dorc plan` preview closes with. The value is the book's filename.",
        why: "ack-2: the preview points at the focused query surface rather than inlining every chain.",
        words: Words::Migrated(&["dorc: run `dorc why` for the per-site cause-chains, or `dorc why ", ":N` to query a source line"]),
    },
    ArrangementEntry {
        slug: "cli-usage-synopsis",
        occurrence: None,
        when_used: "Appended to stderr by the invocation-error print seat, beneath the rendered diagnostic.",
        why: "288 section 6: seat-appended usage belongs to no one code, so it has no catalog home.",
        words: Words::Migrated(&["usage: dorc [probe|plan|apply] --book=<book.sh> [-o <oracle.sh>]... [--debug-argv]"]),
    },
    ArrangementEntry {
        slug: "lint-advisory-preamble",
        occurrence: None,
        when_used: "The one-time header above a `dorc lint` human render that has findings.",
        why: "27R section 8 delta-advisory-preamble-taste: the human surface disowns itself and points at the stable machine format.",
        words: Words::Migrated(&["dorc lint findings (advisory; the machine format `--format=jsonl` is the stable surface):"]),
    },
    ArrangementEntry {
        slug: "lint-clean-sentence",
        occurrence: None,
        when_used: "The whole quiet-on-clean sentence a findings-free `dorc lint` prints instead of a report. Values, in order: file count, its plural suffix, source count, its plural suffix.",
        why: "27R section 8 delta-positive-clean-sentence: clean is stated positively, never as silence.",
        words: Words::Migrated(&["dorc lint: clean — nothing found across ", " file", ", ", " source", "."]),
    },
    ArrangementEntry {
        slug: "lint-fidelity-approximate",
        occurrence: None,
        when_used: "Tags a compact finding whose location survived a remap only approximately. Repeats per finding, sharing this one entry.",
        why: "27R section 4 remap-fidelity: a re-derived location says so, rather than passing as exact.",
        words: Words::Migrated(&[" (approximate location)"]),
    },
    ArrangementEntry {
        slug: "lint-fidelity-raw",
        occurrence: None,
        when_used: "Tags a compact finding whose location never remapped at all. Repeats per finding, sharing this one entry.",
        why: "27R section 4 remap-fidelity: as lint-fidelity-approximate, one rung less trustworthy.",
        words: Words::Migrated(&[" (raw)"]),
    },
    ArrangementEntry {
        slug: "lint-source-analysis-diagnostics",
        occurrence: None,
        when_used: "The `dorc lint --list-sources` line for the analysis-diagnostics source.",
        why: "27R section 8 delta-named-sources-selectable: each source describes itself to the admin choosing a subset.",
        words: Words::Migrated(&["engine parse/cfg diagnostics over each file (no world)"]),
    },
    ArrangementEntry {
        slug: "lint-source-checkbashisms",
        occurrence: None,
        when_used: "The `dorc lint --list-sources` line for the checkbashisms relay source.",
        why: "27R section 8 delta-named-sources-selectable: as lint-source-analysis-diagnostics.",
        words: Words::Migrated(&["checkbashisms --lint over each stripped file"]),
    },
    ArrangementEntry {
        slug: "lint-source-oracle-declined-inventory",
        occurrence: None,
        when_used: "The `dorc lint --list-sources` line for the authored-decline inventory source.",
        why: "27R section 8 delta-named-sources-selectable: as lint-source-analysis-diagnostics.",
        words: Words::Migrated(&["authored decline classes per verdict arm (tier-1, oracle-solo)"]),
    },
    ArrangementEntry {
        slug: "lint-source-oracle-validate",
        occurrence: None,
        when_used: "The `dorc lint --list-sources` line for the book-free oracle validator.",
        why: "27R section 8 delta-named-sources-selectable: as lint-source-analysis-diagnostics.",
        words: Words::Migrated(&["book-free oracle-side validation (lift/check/coherence/reserved/marker)"]),
    },
    ArrangementEntry {
        slug: "lint-source-shellcheck",
        occurrence: None,
        when_used: "The `dorc lint --list-sources` line for the shellcheck relay source.",
        why: "27R section 8 delta-named-sources-selectable: as lint-source-analysis-diagnostics.",
        words: Words::Migrated(&["shellcheck -f json1 -s sh over each stripped file"]),
    },
    ArrangementEntry {
        slug: "lint-source-unmodeled-inventory",
        occurrence: None,
        when_used: "The `dorc lint --list-sources` line for the unmodeled-wall inventory source.",
        why: "27R section 8 delta-named-sources-selectable: as lint-source-analysis-diagnostics.",
        words: Words::Migrated(&["per-book ⊤-wall inventory (first wall + downstream degradation)"]),
    },
    ArrangementEntry {
        slug: "lint-source-verdict-body",
        occurrence: None,
        when_used: "The `dorc lint --list-sources` line for the verdict-body checker.",
        why: "27R section 8 delta-named-sources-selectable: as lint-source-analysis-diagnostics.",
        words: Words::Migrated(&["status-flattening in __is_converged bodies (terminal pipeline)"]),
    },
    ArrangementEntry {
        slug: "lint-summary-sentence",
        occurrence: None,
        when_used: "The whole closing summary line of a `dorc lint` human render that has findings. Values, in order: error/warning/info tallies each followed by its plural suffix, then the file count and its suffix.",
        why: "27R section 5: the report closes by naming itself and its tallies.",
        words: Words::Migrated(&["dorc lint: ", " error", ", ", " warning", ", ", " info", " across ", " file", "."]),
    },
    ArrangementEntry {
        slug: "why-remediation-declare-identity",
        occurrence: None,
        when_used: "Closes every why-lens reason whose remediation class is DeclareIdentity — the `why:` stderr line and the same reason inside a `dorc why` cause-chain.",
        why: "ru-6/ru-27 remediation classes: the class says WHAT KIND of fix clears the forced run, and its one-line hint is user-facing prose, so it gets an editable home like every other user-facing string (288 section 1).",
        words: Words::Migrated(&["to elide it, add the missing kind/selector/Query declaration [declare-identity]"]),
    },
    ArrangementEntry {
        slug: "why-remediation-provide-model",
        occurrence: None,
        when_used: "Closes every why-lens reason whose remediation class is ProvideModel.",
        why: "As why-remediation-declare-identity, for the class cleared by authoring or extending an oracle.",
        words: Words::Migrated(&["to elide it, an oracle must declare a read-only probe for this kind [provide-model]"]),
    },
    ArrangementEntry {
        slug: "why-remediation-resolve-dynamism",
        occurrence: None,
        when_used: "Closes every why-lens reason whose remediation class is ResolveDynamism.",
        why: "As why-remediation-declare-identity, for the class cleared by making a dynamic operand resolvable.",
        words: Words::Migrated(&["to elide it, make the operand a literal Dorc can resolve+probe [resolve-dynamism]"]),
    },
    ArrangementEntry {
        slug: "why-remediation-structural",
        occurrence: None,
        when_used: "Closes every why-lens reason whose remediation class is Structural.",
        why: "As why-remediation-declare-identity — and the one that must never imply a user fix exists (271 rul-sin-ordering: mis-attribution is the worst aid failure).",
        words: Words::Migrated(&["no user fix — Dorc cannot model this construct [structural]"]),
    },
];
