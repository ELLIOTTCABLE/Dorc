// @generated by dorc-loom; DO NOT EDIT.
// This whole file is overwritten by catalog promotion.

use super::{CatalogEntry, HelpRegister};

#[rustfmt::skip]
pub const CATALOG: &[CatalogEntry] = &[
    CatalogEntry {
        slug: "cmdsub-operand-top",
        when_fires: "a `$(…)`/runtime-dynamic operand (or the command word) forced a command to ⊤, so it runs (never elided). effect.rs finalize_cmdsub_tops.",
        why: "no-silent-phantoms disclosure (find-3); the template fills `{position}` from `OperandPosition::describe()` and `{cause}` from `TopCause::describe()`. Case-owned (the cmdsub flagship, `crates/dorc-loom/cases/cmdsub-operand-top.loom`): prose authored at the transcript surface, awaiting the human rewrite pass.",
        params: &["position", "cause"],
        example: "operand 3 is a command-substitution `$(...)` / arithmetic / operator-form expansion, so Dorc cannot know its value until the command runs on the host -- there is nothing to resolve and no read-only probe to check. Dorc elides a command only when it can prove the command's effect is already in place, so this one is left to run on every apply.",
        message: Some("{{position}} is {{cause}}, so Dorc cannot know its value until the command runs on the host -- there is nothing to resolve and no read-only probe to check. Dorc elides a command only when it can prove the command's effect is already in place, so this one is left to run on every apply."),
        help: HelpRegister::Written("Give the operand a value Dorc can resolve statically -- a literal, or a variable assigned from one -- so Dorc can probe it and elide the command once it has converged. If the value must stay dynamic, load an oracle that vouches for this command's convergence, and Dorc will guard the command instead of running it every time."),
    },
    CatalogEntry {
        slug: "site-unresolvable",
        when_fires: "a probe could not ship a read-only check for one or more command-sites, so the apply runs each. cli unresolvable_diagnostics (né dq-site-unresolvable).",
        why: "kFAIL-perform: unsure ⇒ run. PASSTHROUGH — `detail` reproduces BOTH the aggregate label (N sites …) AND the folded `\\n  = note: site runs `{excerpt}`` line the old render_body emitted, so the migrated output is byte-identical bar the `sm ` prefix.",
        params: &["detail"],
        example: "sm 2 sites run unprobed (no read-only check could be shipped): `make install`, `ldconfig`",
        message: Some("sm {{detail}}"),
        help: HelpRegister::Absent,
    },
    CatalogEntry {
        slug: "render-heredoc-refused",
        when_fires: "the leaf-exact render would elide/guard a licensed leaf whose span covers a `<<` heredoc opener (not its body), so the leaf runs verbatim instead.",
        why: "kFAIL-perform, arch-1 d-6: substituting the opener span would strand the heredoc body — an Error-class give-up (a broken artifact otherwise). `{verb}` = elide/guard.",
        params: &["verb", "command"],
        example: "sm leaf-exact render refuses to elide a heredoc-bearing command (`cat <<EOF`): its span covers the `<<` operator, not the body lines, so substituting it would strand the heredoc body -- it runs verbatim",
        message: Some("sm leaf-exact render refuses to {{verb}} a heredoc-bearing command (`{{command}}`): its span covers the `<<` operator, not the body lines, so substituting it would strand the heredoc body -- it runs verbatim"),
        help: HelpRegister::Written("sm split the heredoc body to its own leaf, or mark the kind un-elidable"),
    },
    CatalogEntry {
        slug: "cmdsub-inner-nonleaf",
        when_fires: "an effect-bearing command runs inside a `$(…)` substitution body, so it has no independent leaf. effect.rs classify (né dq-cmdsub-inner-nonleaf).",
        why: "q-1.f silent-1/silent-4 disclosure; the inner command runs whenever its enclosing line runs. `{inner}` = the resolved inner argv.",
        params: &["inner"],
        example: "sm command `id -u` runs inside a `$(...)` substitution => effect-bearing but not independently elidable (it runs whenever its enclosing line runs)",
        message: Some("sm command `{{inner}}` runs inside a `$(...)` substitution => effect-bearing but not independently elidable (it runs whenever its enclosing line runs)"),
        help: HelpRegister::Absent,
    },
    CatalogEntry {
        slug: "redir-target-top",
        when_fires: "a write-redirect (`>`/`>>`) to a dynamic/unresolved target joins ⊤. effect.rs classify Redir arm (né dq-redir-target-top).",
        why: "y-1 / 21F imp-1: the target is unresolvable so no per-path `file` cell can be keyed. NOTE plain-language pass owed: ⊤/top wording (law-plain-language-surfaces, 24H ack-4).",
        params: &[],
        example: "sm write-redirect to a dynamic/unresolved target => no per-path `file` cell can be keyed, so the write joins ⊤ and the command runs (never elided)",
        message: Some("sm write-redirect to a dynamic/unresolved target => no per-path `file` cell can be keyed, so the write joins ⊤ and the command runs (never elided)"),
        help: HelpRegister::Absent,
    },
    CatalogEntry {
        slug: "depth-2-positional-unthreaded",
        when_fires: "a depth-2 inlined call's argument references a positional that does not thread two inline levels. cfg.rs (né dq-depth-2-positional-unthreaded).",
        why: "216 §1.2 correction: the inner body's positional resolves ⊤, so the call runs verbatim. `{name}` = the refused call's function name. NOTE plain-language pass owed: ⊤/top wording (law-plain-language-surfaces, 24H ack-4).",
        params: &["name"],
        example: "sm call `deploy` not inlined: its argument references a positional (`$1`..`$9`/`$#`) that does not thread through two inline levels => the inner body's positional is ⊤ -- it runs as an ordinary unmodeled command (depth-2 positional threading is out of the modeled subset)",
        message: Some("sm call `{{name}}` not inlined: its argument references a positional (`$1`..`$9`/`$#`) that does not thread through two inline levels => the inner body's positional is ⊤ -- it runs as an ordinary unmodeled command (depth-2 positional threading is out of the modeled subset)"),
        help: HelpRegister::Absent,
    },
    CatalogEntry {
        slug: "syntax-unsupported",
        when_fires: "the parser hit an unmodeled/out-of-scope sh construct; it becomes an `Unsupported` ⊤-node and parsing continues.",
        why: "inv-top-reject: under-modeling is a loud correctness boundary. PASSTHROUGH — the parser's own description of the construct rides `detail`.",
        params: &["detail"],
        example: "sm process substitution `<(...)` is not modeled",
        message: Some("sm {{detail}}"),
        help: HelpRegister::Absent,
    },
    CatalogEntry {
        slug: "syntax-malformed",
        when_fires: "the parser hit a structurally malformed sh construct (a parse error); parsing continues fail-soft.",
        why: "inv-no-throw: errors are data. PASSTHROUGH — the parser's description rides `detail`.",
        params: &["detail"],
        example: "sm unterminated double-quote",
        message: Some("sm {{detail}}"),
        help: HelpRegister::Absent,
    },
    CatalogEntry {
        slug: "cfg-top-node",
        when_fires: "an `Unsupported` AST ⊤-node became a CFG `Top` node (an unsupported construct, or the CFG nesting bound). cfg.rs lower_top + fresh(Top).",
        why: "the conservative ⊤-absorbing semantics; any command after it may mutate anything. PASSTHROUGH — the CFG builder's reason rides `detail`. NOTE plain-language pass owed: slug carries top/⊤ wording (law-plain-language-surfaces, 24H ack-4).",
        params: &["detail"],
        example: "sm unsupported construct (⊤): un-probeable and un-skippable",
        message: Some("sm {{detail}}"),
        help: HelpRegister::Absent,
    },
    CatalogEntry {
        slug: "cfg-errexit-unknown",
        when_fires: "the errexit-region pass hit an unknown/unmodeled command; the `set -e` failure-edge is conservatively assumed. cfg.rs (SPANLESS — spans a region).",
        why: "over-approximate, sound. PASSTHROUGH — the pass's description rides `detail`.",
        params: &["detail"],
        example: "sm errexit state is ⊤ at one or more commands; failure-edges added conservatively (over-approximate, sound)",
        message: Some("sm {{detail}}"),
        help: HelpRegister::Absent,
    },
    CatalogEntry {
        slug: "cfg-inline-refused",
        when_fires: "a function call could not be inlined. cfg.rs — SEVEN distinct emit paths under one slug: redefinition, recursion, inline-depth budget, unmodeled positional in body, unmodeled write-redirect in body (tc-M2), per-call node budget, per-book node budget. Each names its own reason.",
        why: "the call runs as an ordinary unmodeled command (MustRun, safe). PASSTHROUGH — the per-path reason rides `detail`.",
        params: &["detail"],
        example: "sm call to `helper` exceeds the inline-depth budget (8); not inlined -- it runs as an ordinary unmodeled command",
        message: Some("sm {{detail}}"),
        help: HelpRegister::Absent,
    },
    CatalogEntry {
        slug: "cfg-builtin-shadowed",
        when_fires: "a book funcdef shadows a shell builtin the engine relies on (dash resolves a function before a regular builtin). cfg.rs warn_shadowed_relied_builtins.",
        why: "find-I: builtin-dependent conclusions may be unsound for this book. PASSTHROUGH — the disclosure text rides `detail`; the primary span is the funcdef name_span.",
        params: &["detail"],
        example: "sm function `test` shadows a shell builtin the engine relies on (dash resolves a function before a regular builtin): analysis treats the bare word `test` as the builtin when classifying effects and minting stand-ins, so builtin-dependent conclusions may be unsound for this book",
        message: Some("sm {{detail}}"),
        help: HelpRegister::Absent,
    },
    CatalogEntry {
        slug: "effect-kind-disagreement",
        when_fires: "a check's annotation kind disagrees with the effect-map kind for the same verb; the annotation wins. effect.rs (SPANLESS — mid-resolution, no leaf).",
        why: "204 §6 open seam: declared identity wins. PASSTHROUGH — the disagreement rides `detail`.",
        params: &["detail"],
        example: "sm check annotation kind `sm.dorc.Package` disagrees with the effect-map kind `sm.dorc.File` for this verb -- the annotation (declared identity) wins",
        message: Some("sm {{detail}}"),
        help: HelpRegister::Absent,
    },
    CatalogEntry {
        slug: "predict-out-of-dialect",
        when_fires: "a check function body uses a construct outside the check dialect (a strict subset of sh). oracle/predict.rs lift_failure.",
        why: "out-of-dialect input is a lift failure. PASSTHROUGH — the check parser's description rides `detail`.",
        params: &["detail"],
        example: "sm check body uses `[[ ... ]]`, outside the check dialect",
        message: Some("sm {{detail}}"),
        help: HelpRegister::Absent,
    },
    CatalogEntry {
        slug: "predict-unterminated",
        when_fires: "a check function body is structurally unterminated (missing `;;`/`esac`). oracle/predict.rs lift_failure.",
        why: "the check cannot be lifted. PASSTHROUGH — the check parser's description rides `detail`.",
        params: &["detail"],
        example: "sm check body ends mid-`case` (missing `esac`)",
        message: Some("sm {{detail}}"),
        help: HelpRegister::Absent,
    },
    CatalogEntry {
        slug: "munge-name-invalid",
        when_fires: "an emitted `<munged>__<role>` funcname is not a legal sh NAME (leading digit, dot, non-ASCII). oracle/reserved.rs lint_oracle_reserved_names.",
        why: "ca-munge-charclass (24M §4b): a broken function name cannot ship — REFUSED. `{problem}` is `ShNameProblem::describe()`.",
        params: &["source", "funcname", "problem"],
        example: "sm `ä¸\u{ad}pkg` munges to the sh function name `ä¸\u{ad}pkg__predict`, which is not a legal NAME: non-ASCII character `ä` (a NAME admits only ASCII letters/digits/`_`) (ca-munge-charclass, 24M section 4b) -- REFUSED (a broken function name cannot ship; the munger must transliterate or the name must be renamed)",
        message: Some("sm `{{source}}` munges to the sh function name `{{funcname}}`, which is not a legal NAME: {{problem}} (ca-munge-charclass, 24M section 4b) -- REFUSED (a broken function name cannot ship; the munger must transliterate or the name must be renamed)"),
        help: HelpRegister::Absent,
    },
    CatalogEntry {
        slug: "munge-name-collision",
        when_fires: "two DISTINCT source names munge to one sh funcname. oracle/reserved.rs. `{count}` interpolates twice (the count and the funcdef count).",
        why: "non-injective munge: refuse-and-run (never silently last-writer-wins).",
        params: &["source", "funcname", "count", "names"],
        example: "sm `a.b` munges to the sh function name `a_b`, shared by 2 distinct source names (a.b, a-b) -- REFUSED, never silently merged (the shipped artifact would carry 2 same-named funcdefs, last-writer-wins; align with the reingest-collision floor: refuse-and-run)",
        message: Some("sm `{{source}}` munges to the sh function name `{{funcname}}`, shared by {{count}} distinct source names ({{names}}) -- REFUSED, never silently merged (the shipped artifact would carry {{count}} same-named funcdefs, last-writer-wins; align with the reingest-collision floor: refuse-and-run)"),
        help: HelpRegister::Absent,
    },
    CatalogEntry {
        slug: "reserved-namespace-squat",
        when_fires: "a book funcdef coincidentally named `*__<role>` squats the reserved oracle namespace. oracle/reserved.rs lint_book_reserved_names. `{role}` twice.",
        why: "rul24M-bare-dorcism-names: accepted-not-prevented; the disclosure is loud (warnings tune high this era).",
        params: &["name", "role"],
        example: "sm book function `nginx__predict` squats the reserved `__predict` oracle namespace (rul24M-bare-dorcism-names): if unintended, it coincidentally matches an emitted oracle function name -- it is treated as an ordinary opaque command here (run-verbatim), but a shipped oracle preamble of the same name would collide (last-writer-wins). Rename it to stay clear of `*__predict`.",
        message: Some("sm book function `{{name}}` squats the reserved `{{role}}` oracle namespace (rul24M-bare-dorcism-names): if unintended, it coincidentally matches an emitted oracle function name -- it is treated as an ordinary opaque command here (run-verbatim), but a shipped oracle preamble of the same name would collide (last-writer-wins). Rename it to stay clear of `*{{role}}`."),
        help: HelpRegister::Absent,
    },
    CatalogEntry {
        slug: "missing-dialect-marker",
        when_fires: "a dorc-lang dialect construct (a bind or trailing mark) appears in a file lacking the `# dorc-lang/v0.2` version marker. oracle/marker.rs.",
        why: "marker-gates-syntax-only: a loud file-level refusal. Static — the marker text is inline (MARKER / MARKER_WINDOW compile-time constants).",
        params: &[],
        example: "sm this file uses a dorc-lang dialect construct (a bind `name : kind = ...` or a trailing `:`/`:!`/`:?` mark) but lacks the `# dorc-lang/v0.2` version marker (marker-gates-syntax-only): add `# dorc-lang/v0.2` as a standalone comment in the first 10 lines, or drop the dialect (the bare `__role` floor works markerless)",
        message: Some("sm this file uses a dorc-lang dialect construct (a bind `name : kind = ...` or a trailing `:`/`:!`/`:?` mark) but lacks the `# dorc-lang/v0.2` version marker (marker-gates-syntax-only): add `# dorc-lang/v0.2` as a standalone comment in the first 10 lines, or drop the dialect (the bare `__role` floor works markerless)"),
        help: HelpRegister::Absent,
    },
    CatalogEntry {
        slug: "marker-version-unrecognized",
        when_fires: "a dorc-lang dialect construct appears in a file whose `# dorc-lang/vX.Y` version marker names a version this binary does not recognize (only v0.2 today), distinct from a wholly-missing marker. oracle/marker.rs.",
        why: "marker-gates-syntax-only + versioned-additive: an unrecognized version is a loud file-level refusal SEPARATE from missing-marker, so a vNEXT/typo'd-version file is not mis-blamed as markerless (`28A` §2l). `{found}` = the version tag read from the marker.",
        params: &[],
        example: "[unwritten: marker-version-unrecognized]",
        message: None,
        help: HelpRegister::Absent,
    },
    CatalogEntry {
        slug: "tolerates-unknown-dimension",
        when_fires: "an unknown context-dimension token appears on a `tolerates:` vouch. oracle/entry.rs collect_tolerance. `{expected}` = the known-dimension list.",
        why: "27C §2: the mark vouches nothing and the site stays walled on that dimension (engine-owned closed vocabulary).",
        params: &["token", "expected"],
        example: "sm `netns2` is not a known context dimension on a `tolerates:` vouch (expected one of user, fs-view, netns); the mark vouches nothing and the site stays walled on that dimension (`27C` section 2).",
        message: Some("sm `{{token}}` is not a known context dimension on a `tolerates:` vouch (expected one of {{expected}}); the mark vouches nothing and the site stays walled on that dimension (`27C` section 2)."),
        help: HelpRegister::Absent,
    },
    CatalogEntry {
        slug: "tolerates-over-identity-dependence",
        when_fires: "a `tolerates:user` vouch sits over a body that visibly reads identity (`id`/`$USER`/`$HOME`). oracle/entry.rs corroborate_tolerance_over_identity.",
        why: "27C §6 corroboration (recognize-never-license): the ask never blocks. Static.",
        params: &[],
        example: "sm this `is_converged` carries `tolerates:user` but VISIBLY reads the caller's identity (`id`/`$USER`/`$HOME`): are you sure the body is read-only under a user shift, not just answer-varying? A shifted user must not make it MUTATE (`27C` section 2 corroboration).",
        message: Some("sm this `is_converged` carries `tolerates:user` but VISIBLY reads the caller's identity (`id`/`$USER`/`$HOME`): are you sure the body is read-only under a user shift, not just answer-varying? A shifted user must not make it MUTATE (`27C` section 2 corroboration)."),
        help: HelpRegister::Absent,
    },
    CatalogEntry {
        slug: "heavy-context-no-tolerance",
        when_fires: "a body reads identity but carries no tolerance vouch. oracle/entry.rs hint_heavy_context_no_vouch (reverse-direction corroboration hint).",
        why: "27C §6 (recognize-never-license): a one-line vouch would make it context-shiftable. Static.",
        params: &[],
        example: "sm this `is_converged` reads the caller's identity but carries no tolerance vouch -- a wrapped site (`sudo ...`) will run/guard instead of eliding. One line makes it context-shiftable: `: safe-across user` (`27C` section 2).",
        message: Some("sm this `is_converged` reads the caller's identity but carries no tolerance vouch -- a wrapped site (`sudo ...`) will run/guard instead of eliding. One line makes it context-shiftable: `: safe-across user` (`27C` section 2)."),
        help: HelpRegister::Absent,
    },
    CatalogEntry {
        slug: "lend-map-unknown-dimension",
        when_fires: "an unknown lend_map dimension token appears on a `__lend_map` line. oracle/wrapper.rs walk_lend_body. `{expected}` = the known-dimension list.",
        why: "273 §8: the line mints no lend and the dimension it meant to answer walls. NOTE plain-language pass owed: ⊤/top wording (law-plain-language-surfaces, 24H ack-4).",
        params: &["token", "expected"],
        example: "sm `netns2` is not a known lend_map dimension (expected one of user, netns, fs-view); the line mints no lend and the dimension it meant to answer stays ⊤ (walls). Dimension marks are an engine-owned closed vocabulary (`273` section 8).",
        message: Some("sm `{{token}}` is not a known lend_map dimension (expected one of {{expected}}); the line mints no lend and the dimension it meant to answer stays ⊤ (walls). Dimension marks are an engine-owned closed vocabulary (`273` section 8)."),
        help: HelpRegister::Absent,
    },
    CatalogEntry {
        slug: "carry-netns-on-net-kernel-forbidden",
        when_fires: "a kind's per-netns `net-kernel` store claims `invariant:netns` (a contradiction). oracle/carry.rs lift. `{kind_munged}` = the munged kind.",
        why: "27C §4(a): network kernel state is namespaced; the false invariance line is dropped.",
        params: &["kind_munged"],
        example: "sm `invariant:netns` is forbidden on the per-netns `net-kernel` store of `sm_dorc_KernelParam` -- network kernel state is namespaced, never netns-invariant",
        message: Some("sm `invariant:netns` is forbidden on the per-netns `net-kernel` store of `{{kind_munged}}` -- network kernel state is namespaced, never netns-invariant"),
        help: HelpRegister::Absent,
    },
    CatalogEntry {
        slug: "mark-brace-verdict-single-cell",
        when_fires: "a brace-alternation `@{a,b}` appears on a single-cell verdict/observe mark. oracle/predict/derive.rs. Static (literal braces escaped in the template).",
        why: "277 §4c: a verdict/observe mark asserts exactly one cell; the brace mints no cell and the site runs (a role-aware rejection the parser cannot make).",
        params: &[],
        example: "sm verdict and observe marks are single-cell; brace alternation `@{a,b}` is claim-emission-only (`277` section 4c) -- this mark mints NO cell and the site will run. Split it into one marked probe line per cell.",
        message: Some("sm verdict and observe marks are single-cell; brace alternation `@{a,b}` is claim-emission-only (`277` section 4c) -- this mark mints NO cell and the site will run. Split it into one marked probe line per cell."),
        help: HelpRegister::Absent,
    },
    CatalogEntry {
        slug: "mark-unknown-verb",
        when_fires: "the new-grammar mark parser hit a period-free head/continuation token that is not a known verb (`281` §4 rule-3 miss). oracle/predict/mark_grammar.rs.",
        why: "281 §4 keystone (rul-verbs-dotless-kinds-dotted): a dotless mark token is a verb; an unknown one is malformed committed syntax ⇒ the block drops to ⊤ (`inv-top-reject`). `{token}` = the bad token, `{expected}` = the known-verb vocabulary.",
        params: &[],
        example: "[unwritten: mark-unknown-verb]",
        message: None,
        help: HelpRegister::Absent,
    },
    CatalogEntry {
        slug: "mark-rc-arity-exceeded",
        when_fires: "the new-grammar mark parser found two rc-consuming marks (`asserts`/`refutes`) in one block, incl. continuations (`281` §7). oracle/predict/mark_grammar.rs.",
        why: "281 §7 rc-arity: one exit code witnesses one cell, so two verdicts on one block is unmeasurable ⇒ the block drops to ⊤ (`inv-top-reject`).",
        params: &[],
        example: "[unwritten: mark-rc-arity-exceeded]",
        message: None,
        help: HelpRegister::Absent,
    },
    CatalogEntry {
        slug: "mark-standalone-rc-consumer",
        when_fires: "the new-grammar mark parser found a standalone mark-block (no command to bind) carrying an rc-consumer or `reads`. oracle/predict/mark_grammar.rs.",
        why: "28A:rul-continuation-attachment: a standalone block has no statement to measure/back, so a verdict/observe there is unbacked ⇒ the block drops to ⊤ (`inv-top-reject`).",
        params: &[],
        example: "[unwritten: mark-standalone-rc-consumer]",
        message: None,
        help: HelpRegister::Absent,
    },
    CatalogEntry {
        slug: "mark-hashcolon-malformed",
        when_fires: "the new-grammar mark parser found a `#:` comment that looks like a mark-block but did not parse (`281` §9). oracle/predict/mark_grammar.rs.",
        why: "281 §9 graceful degradation: the hash-colon carrier is left a plain comment (never mis-erased) but diagnosed (Warning) so a broken one is never silently ignored.",
        params: &[],
        example: "[unwritten: mark-hashcolon-malformed]",
        message: None,
        help: HelpRegister::Absent,
    },
    CatalogEntry {
        slug: "records-headerless-refused",
        when_fires: "a records stream carried no framing at all (headerless) on the strict production path. plan/records.rs deframe_headerless_refused. Static.",
        why: "27D E4: a real dorc probe always frames, so a headerless stream is corruption or an alien source — refuse (kFAIL-withhold, the fold is withheld, the host runs).",
        params: &[],
        example: "sm a records stream carried no `dorc-records/1` framing at all (headerless -- truncated before the header, or a non-dorc source) -- refused on the strict production path, the fold is withheld and the host runs (kFAIL-withhold)",
        message: Some("sm a records stream carried no `dorc-records/1` framing at all (headerless -- truncated before the header, or a non-dorc source) -- refused on the strict production path, the fold is withheld and the host runs (kFAIL-withhold)"),
        help: HelpRegister::Absent,
    },
    CatalogEntry {
        slug: "records-glued-line",
        when_fires: "a records line carried bytes after its terminal token (two atomic writes glued). plan/records.rs deframe_framed. Static.",
        why: "262 §2: reject the whole read unit, the safe direction (kFAIL-perform).",
        params: &[],
        example: "sm a records line carried bytes after its terminal token (two writes glued) -- the whole read unit is refused, the host runs (kFAIL-perform)",
        message: Some("sm a records line carried bytes after its terminal token (two writes glued) -- the whole read unit is refused, the host runs (kFAIL-perform)"),
        help: HelpRegister::Absent,
    },
    CatalogEntry {
        slug: "records-header-missing",
        when_fires: "a framed records stream carried no header (torn or absent). plan/records.rs finalize. Static.",
        why: "262 §1/§2: a missing header refuses the read unit (kFAIL-perform).",
        params: &[],
        example: "sm a framed records stream carried no `dorc-records/1` header (torn/absent) -- the read unit is refused, the host runs (kFAIL-perform)",
        message: Some("sm a framed records stream carried no `dorc-records/1` header (torn/absent) -- the read unit is refused, the host runs (kFAIL-perform)"),
        help: HelpRegister::Absent,
    },
    CatalogEntry {
        slug: "records-sentinel-nonce",
        when_fires: "the end-sentinel carried a nonce that is not this attempt's. plan/records.rs deframe_framed. Static.",
        why: "26A amend-retry-hygiene: ignored — the stream's own records are keyed independently.",
        params: &[],
        example: "sm the end-sentinel carried a nonce that is not this attempt's -- ignored (the stream's own records are keyed independently)",
        message: Some("sm the end-sentinel carried a nonce that is not this attempt's -- ignored (the stream's own records are keyed independently)"),
        help: HelpRegister::Absent,
    },
    CatalogEntry {
        slug: "records-fact-truncated",
        when_fires: "fewer site records arrived than the header declared. plan/records.rs finalize. `{received}`/`{declared}`/`{unseen}` are the site counts.",
        why: "plans/128 fc-2: a computable range, not a refusal — the unseen sites fold Unknown ⇒ run on their own. NOTE plain-language pass owed: Unknown/⊤-join wording (law-plain-language-surfaces, 24H ack-4).",
        params: &["received", "declared", "unseen"],
        example: "sm fact lane truncated: 3 of 5 declared site records received -- the 2 unseen site(s) fold Unknown (run)",
        message: Some("sm fact lane truncated: {{received}} of {{declared}} declared site records received -- the {{unseen}} unseen site(s) fold Unknown (run)"),
        help: HelpRegister::Absent,
    },
    CatalogEntry {
        slug: "records-integrity-refused",
        when_fires: "the records header failed an integrity key (nonce/attempt/host/book). plan/records.rs read_header. `{which}` names the mismatched key.",
        why: "262 §2: any known-key mismatch refuses the whole read unit (kFAIL-perform).",
        params: &["which"],
        example: "sm the records header failed integrity on host (a mis-plumbed peer host's stream) -- the whole read unit is refused, the host runs (kFAIL-perform)",
        message: Some("sm the records header failed integrity on {{which}} -- the whole read unit is refused, the host runs (kFAIL-perform)"),
        help: HelpRegister::Absent,
    },
    CatalogEntry {
        slug: "records-torn-line",
        when_fires: "torn record lines (fragments that lost their terminating write) were counted. plan/records.rs finalize. `{count}` = the aggregate count.",
        why: "262 §1 pin-late-and-alien-records: counted, never folded; one aggregated warning.",
        params: &["count"],
        example: "sm 2 torn (no terminal token) record line(s) discarded (counted, never folded)",
        message: Some("sm {{count}} torn (no terminal token) record line(s) discarded (counted, never folded)"),
        help: HelpRegister::Absent,
    },
    CatalogEntry {
        slug: "records-alien-line",
        when_fires: "alien (non-nonce) record lines were counted. plan/records.rs finalize. `{count}` = the aggregate count.",
        why: "262 §1 pin-late-and-alien-records: counted, never folded; one aggregated warning.",
        params: &["count"],
        example: "sm 1 alien (non-nonce) record line(s) discarded (counted, never folded)",
        message: Some("sm {{count}} alien (non-nonce) record line(s) discarded (counted, never folded)"),
        help: HelpRegister::Absent,
    },
    CatalogEntry {
        slug: "records-late-line",
        when_fires: "late (after the end-sentinel) record lines were counted. plan/records.rs finalize. `{count}` = the aggregate count.",
        why: "262 §1 pin-late-and-alien-records: counted, never folded; one aggregated warning.",
        params: &["count"],
        example: "sm 1 late (after the end-sentinel) record line(s) discarded (counted, never folded)",
        message: Some("sm {{count}} late (after the end-sentinel) record line(s) discarded (counted, never folded)"),
        help: HelpRegister::Absent,
    },
    CatalogEntry {
        slug: "host-evidence-admission-refused",
        when_fires: "bounded host evidence failed closed admission before entering the decision plane.",
        why: "host evidence remains controller-owned and decision-inert until admission succeeds.",
        params: &[],
        example: "[unwritten: host-evidence-admission-refused]",
        message: None,
        help: HelpRegister::Absent,
    },
    CatalogEntry {
        slug: "footprint-incoherent",
        when_fires: "a touches() footprint is incoherent. cli/main.rs — TWO emit paths: the SPANNED own-coordinate canary (footprint omits its own effect coordinate), and the SPANLESS malformed-derived-coordinate refusal (the SPANLESS_SITE_PAYLOADS one).",
        why: "24A §1b / 24E §7: an at-most claim cannot be partial — refuse ⇒ the site walls. PASSTHROUGH — the per-path text rides `detail`.",
        params: &["detail"],
        example: "sm touches() footprint omits this command's own effect coordinate (at-least ⊄ at-most) -- footprint refused, the site walls",
        message: Some("sm {{detail}}"),
        help: HelpRegister::Absent,
    },
    CatalogEntry {
        slug: "touches-escalated",
        when_fires: "a payload-bound touches() escalated to host-derivation. cli/main.rs merge_derived_footprints. `{site}` = node id, `{call}` = the escalated call.",
        why: "ru-26 SPIKE-ONLY: makes the static→dynamic boundary visible in the render; must not leak into greenfield as a permanent per-escalation requirement.",
        params: &["site", "call"],
        example: "sm site 4: touches() escalated to host-derivation (dpkg-query -W nginx)",
        message: Some("sm site {{site}}: touches() escalated to host-derivation ({{call}})"),
        help: HelpRegister::Absent,
    },
    CatalogEntry {
        slug: "deriv-family-incomplete",
        when_fires: "a derived footprint family did not close completely (missing deriv-end, or a count mismatch). cli/main.rs. `{site}` = node id, `{reason}` = the match.",
        why: "262 §2 / 26A stop-1: an at-most family cannot be partial — refuse ⇒ the site walls total.",
        params: &["site", "reason"],
        example: "sm site 4: derived footprint family incomplete (declared n=3, received 2) -- footprint refused, the site walls total (an at-most claim cannot be partial)",
        message: Some("sm site {{site}}: derived footprint family incomplete ({{reason}}) -- footprint refused, the site walls total (an at-most claim cannot be partial)"),
        help: HelpRegister::Absent,
    },
    CatalogEntry {
        slug: "escalation-policy",
        when_fires: "the authority-disclosure line for the probe-escalation policy. cli/main.rs (SPANLESS). Consent legibility.",
        why: "27C §2: the disclosure varies by dial (default vs --escalate-any-probe). PASSTHROUGH — the policy text rides `detail`.",
        params: &["detail"],
        example: "sm escalation policy: probe re-uses connection authority for `tolerates:`-vouched functions only (default)",
        message: Some("sm {{detail}}"),
        help: HelpRegister::Absent,
    },
    CatalogEntry {
        slug: "carried-across-substrate-axis",
        when_fires: "a cross-context elision carried a substrate-axis fact via pure-predicate carry. cli/main.rs. Spanned (the carried site's span).",
        why: "27C §9: every cross-context elision renders its attribution chain from day one. PASSTHROUGH — the chain text rides `detail`.",
        params: &["detail"],
        example: "sm elision carried across the fs-view axis: backing kind `sm_dorc_File` vouches `invariant:fs-view`; the verdict body is read-set-closed",
        message: Some("sm {{detail}}"),
        help: HelpRegister::Absent,
    },
    CatalogEntry {
        slug: "wrapped-site-adoption-hint",
        when_fires: "a wrapped BOOK site degraded on a missing `tolerates:` vouch. cli/main.rs (27N). Spanned (the wrapped site's span).",
        why: "27C §2 (recognize-never-license): the one-line adoption hint. PASSTHROUGH — the hint text rides `detail`.",
        params: &["detail"],
        example: "sm this `sudo`-wrapped site could elide with a `tolerates:user` vouch on its is_converged (adoption hint)",
        message: Some("sm {{detail}}"),
        help: HelpRegister::Absent,
    },
    CatalogEntry {
        slug: "resolver-conflict",
        when_fires: "two oracle files declare one kind's resolver. cli/main.rs. `{kind}` = the kind, `{count}` = the resolver count.",
        why: "24F §3 at-most-one-resolver-per-kind: BOTH refused (never first-wins-silently); the kind keeps token-equality.",
        params: &["kind", "count"],
        example: "sm kind 'sm.dorc.Package' has 2 resolvers across oracle files -- at-most-one-resolver-per-kind (24F section 3): BOTH refused, the kind keeps token-equality (never first-wins-silently)",
        message: Some("sm kind '{{kind}}' has {{count}} resolvers across oracle files -- at-most-one-resolver-per-kind (24F section 3): BOTH refused, the kind keeps token-equality (never first-wins-silently)"),
        help: HelpRegister::Absent,
    },
    CatalogEntry {
        slug: "resolver-provider-collision",
        when_fires: "a resolver is keyed to a name matching a known COMMAND provider. cli/main.rs. `{name}` = the colliding name.",
        why: "corr-kind-keying §10: resolvers are keyed by KIND, not command — a likely mis-key (kept; the warning surfaces the risk).",
        params: &["name"],
        example: "sm resolver 'nginx.resolve()' is keyed to a name matching a known COMMAND provider -- resolvers are keyed by KIND, not command (corr-kind-keying section 10); this mints identity for a kind no coordinate may use (a likely mis-key)",
        message: Some("sm resolver '{{name}}.resolve()' is keyed to a name matching a known COMMAND provider -- resolvers are keyed by KIND, not command (corr-kind-keying section 10); this mints identity for a kind no coordinate may use (a likely mis-key)"),
        help: HelpRegister::Absent,
    },
    CatalogEntry {
        slug: "dangling-reference",
        when_fires: "a coordinate resolved DANGLING (no such entity on an enumerable kind). cli/main.rs dangling_diagnostics. `{coord}` = the rendered coordinate.",
        why: "24F §4: turns a third-party-typo from silent value-loss into a pointed hint; the coord rides the may-alias degrade (the site runs). ADVISORY (fail toward run).",
        params: &["coord"],
        example: "sm coordinate sm.dorc.Package:nginx resolved DANGLING -- the kind's resolver reports no such entity (a likely typo / stale name); it degrades to may-alias (the site runs)",
        message: Some("sm coordinate {{coord}} resolved DANGLING -- the kind's resolver reports no such entity (a likely typo / stale name); it degrades to may-alias (the site runs)"),
        help: HelpRegister::Absent,
    },
    CatalogEntry {
        slug: "reaches-conflict",
        when_fires: "two oracle files declare one kind's reach-function. cli/main.rs. `{kind}` = the kind, `{count}` = the reach-function count.",
        why: "24G §4 at-most-one-reaches-per-kind: BOTH refused (never first-wins-silently); the kind's footprints do not expand.",
        params: &["kind", "count"],
        example: "sm kind 'sm.dorc.Package' has 2 reach-functions across oracle files -- at-most-one-reaches-per-kind (24G section 4): BOTH refused, the kind's footprints do not expand (never first-wins-silently)",
        message: Some("sm kind '{{kind}}' has {{count}} reach-functions across oracle files -- at-most-one-reaches-per-kind (24G section 4): BOTH refused, the kind's footprints do not expand (never first-wins-silently)"),
        help: HelpRegister::Absent,
    },
    CatalogEntry {
        slug: "reaches-provider-collision",
        when_fires: "a reach-function is keyed to a name matching a known COMMAND provider. cli/main.rs. `{name}` = the colliding name.",
        why: "24G §4: reaches is keyed by KIND, not command — a likely mis-key.",
        params: &["name"],
        example: "sm reach-function 'nginx.reaches()' is keyed to a name matching a known COMMAND provider -- reaches is keyed by KIND, not command (24G section 4); this expands a kind no coordinate may use (a likely mis-key)",
        message: Some("sm reach-function '{{name}}.reaches()' is keyed to a name matching a known COMMAND provider -- reaches is keyed by KIND, not command (24G section 4); this expands a kind no coordinate may use (a likely mis-key)"),
        help: HelpRegister::Absent,
    },
    CatalogEntry {
        slug: "wrapper-entry-incoherent",
        when_fires: "a wrapper's `__enter` and `__lend_map` disagree on argv flow. cli/main.rs. Spanned (the entry name_span).",
        why: "27C:rul-fold-entry-coherence-failfast (declarations-genuinely-contradict): a pre-network fail-fast. PASSTHROUGH — the refusal text rides `detail`.",
        params: &["detail"],
        example: "sm wrapper `sudo`: __enter and __lend_map disagree on argv flow (entry consumes 1 leading arg(s), the lend-fold consumes 0) -- static incoherence (27C:rul-fold-entry-coherence-failfast, declarations-genuinely-contradict). The entry form drops/transforms args the fold relied on; make the entry pass the fold's guest verbatim.",
        message: Some("sm {{detail}}"),
        help: HelpRegister::Absent,
    },
    CatalogEntry {
        slug: "wrapper-peel-incoherent",
        when_fires: "a wrapper's `__predict` and `__lend_map` disagree on the peel tail position. cli/main.rs. Spanned (the predict name_span).",
        why: "273 §5 (declarations-genuinely-contradict): a pre-network fail-fast. PASSTHROUGH — the refusal text rides `detail`.",
        params: &["detail"],
        example: "sm wrapper `sudo`: __predict and __lend_map disagree on the peel tail position (predict reaches \"$@\" after 1 argv token(s), lend_map after 0)",
        message: Some("sm {{detail}}"),
        help: HelpRegister::Absent,
    },
    CatalogEntry {
        slug: "whylog-version-refused",
        when_fires: "`dorc why --last` opened a durable whose `dorc-whylog/N` header tag names a format version this binary does not understand, so replay is refused (we never replay a format we cannot parse). plan/whylog.rs parse; cli --last reader.",
        why: "whylog-write-only-replay + versioned-additive format: a durable is version-tagged and NOT byte-stable across versions, so a newer/older format is refused politely rather than mis-parsed. Pull-surface (the user asked): Warning, Floor::None. `{found}` = the tag read from the header. Remediation register (help) wanted: re-run the live analysis (`dorc why` without --last) since the old durable cannot be replayed by this binary.",
        params: &["found"],
        example: "the saved why-log uses format `dorc-whylog/2`, which this version of dorc cannot read back",
        message: Some("the saved why-log uses format `{{found}}`, which this version of dorc cannot read back"),
        help: HelpRegister::Written("ask the question live instead: `dorc why` (without `--last`) re-analyzes the current book directly"),
    },
    CatalogEntry {
        slug: "whylog-book-desync",
        when_fires: "`dorc why --last` found a durable whose recorded book/oracle content digest (or its stored decision digest) diverges from the current on-disk inputs, so a deterministic replay would NOT reconstruct the recorded run. cli --last reader (the `22F` book-identity/desync guard, cer-2-shaped).",
        why: "determinism-is-the-replay-license: the durable stores digests, not book/oracle CONTENT, and re-reads them from disk; a changed input breaks the replay tie, so the recorded run is never silently re-derived against the wrong source. Pull-surface: Warning, Floor::None. `{which}` = the diverged input (`book`, an oracle path, or `decision-digest`). Since 28F rul-drift-replay-d1 the `book` case is the ENTRY to a DEGRADED receipt rather than a dead end — the durable-derived header, oracle inventory and disposition tally still render, with the drift stated in them and every source-keyed section suppressed; the other two still refuse outright. Remediation register (help) wanted: the book/oracle changed since that run — re-run the live analysis for a current answer.",
        params: &["which"],
        example: "the saved why-log no longer matches what is on disk: `book` has changed since it was written",
        message: Some("the saved why-log no longer matches what is on disk: `{{which}}` has changed since it was written"),
        help: HelpRegister::Written("replaying old decisions against changed files would mislead; re-run `dorc why` live for a current answer"),
    },
    CatalogEntry {
        slug: "whylog-absent",
        when_fires: "`dorc why --last` was asked to replay the last run but no durable exists in the whylog directory (no prior run wrote one, or the wrong directory). cli --last reader.",
        why: "the durable is written only when a plan/apply/round-trip run was asked to (spike: the `--whylog-dir` opt-in; product: quietly beside its work); a `--last` with nothing to replay is a benign no-answer, not a crash. Pull-surface: Warning, Floor::None. `{dir}` = the whylog directory searched. Remediation register (help) wanted: run a plan/apply first (or point --whylog-dir at the right directory) to produce a durable to replay.",
        params: &["dir"],
        example: "no saved why-log to read back in `.whylog`",
        message: Some("no saved why-log to read back in `{{dir}}`"),
        help: HelpRegister::Written("a why-log is saved when a plan or apply runs with `--whylog-dir`; run one first, or point `--whylog-dir` at the right directory"),
    },
    CatalogEntry {
        slug: "whylog-corrupt",
        when_fires: "`dorc why --last` found a durable but it is truncated or otherwise unparseable (a partial write, a clobbered file). plan/whylog.rs parse (`inv-no-throw`: malformed bytes are DATA, never a panic).",
        why: "inv-no-throw: a corrupt durable is diagnostics, never a crash — the reader refuses politely and names the parse-failure reason. Pull-surface: Warning, Floor::None. `{detail}` = the parse-failure reason (e.g. missing header/end sentinel, an unrecognized section). Remediation register (help) wanted: the durable is damaged — re-run the live analysis to regenerate it.",
        params: &["detail"],
        example: "the saved why-log is damaged and cannot be read back (no dorc-whylog-end sentinel (a truncated write?))",
        message: Some("the saved why-log is damaged and cannot be read back ({{detail}})"),
        help: HelpRegister::Written("this usually means an interrupted write; re-run `dorc why` live, and the next plan or apply will save a fresh why-log"),
    },
    CatalogEntry {
        slug: "aid-unloaded-sibling-oracle",
        when_fires: "the cli-edge scan found `*.oracle.sh` files on disk beside the loaded oracles (or the book) that were NOT loaded this run. cli emit_unloaded_sibling_oracles.",
        why: "24H ack-6 (suggest, never auto-load): a likely-forgotten oracle is a common cause of a wall that a present-but-unloaded oracle would lift; the run is unchanged (advisory Note). PASSTHROUGH — the cli builds `{detail}` (the sorted unloaded-sibling list).",
        params: &["detail"],
        example: "sibling oracle files exist on disk but were not loaded: 1 sibling oracle exists on disk but was not loaded: `redis.oracle.sh`",
        message: Some("sibling oracle files exist on disk but were not loaded: {{detail}}"),
        help: HelpRegister::Written("load them with `--oracle <file>` (or point `--oracle-dir` at their directory); dorc never loads an oracle you did not name"),
    },
    CatalogEntry {
        slug: "authored-decline-class",
        when_fires: "a verdict arm carries a report-sink emission whose `<verb> <class>` header WAS readable statically. lint/source_oracle_solo.rs, tier-1 inventory. `{class}` is the read class token.",
        why: "27W decline-class-emission - classing a decline is enhancement, and the inventory is how an author sees what they classed. Advisory only, never gates; the license plane never reads a class.",
        params: &["class"],
        example: "sm this verdict arm authors a deliberate decline classed `unsound` (a `decline unsound` report emission) -- the site will run; the class routes the enhancement nags (advisory only).",
        message: Some("sm this verdict arm authors a deliberate decline classed `{{class}}` (a `decline {{class}}` report emission) -- the site will run; the class routes the enhancement nags (advisory only)."),
        help: HelpRegister::Absent,
    },
    CatalogEntry {
        slug: "authored-decline-class-unreadable",
        when_fires: "a verdict arm emits a report line whose `<verb> <class>` header is NOT readable statically - a dynamic format or an unrecognized class token. lint/source_oracle_solo.rs. No params.",
        why: "AID-NEEDS:law-codes-vary-by-world-not-grammar - the sibling of authored-decline-class, split by WORLD-STATE (the class resolves only at runtime) rather than by a template hole. 27W:rul-report-noise-tolerant: an unrecognized header degrades to a note, never an error.",
        params: &[],
        example: "sm this verdict arm authors a deliberate decline whose class is not statically readable (a dynamic format or an unrecognized class token) -- the site will run; the class resolves at runtime (advisory only).",
        message: Some("sm this verdict arm authors a deliberate decline whose class is not statically readable (a dynamic format or an unrecognized class token) -- the site will run; the class resolves at runtime (advisory only)."),
        help: HelpRegister::Absent,
    },
    CatalogEntry {
        slug: "lint-tool-absent",
        when_fires: "a configured external linter is not on PATH, so its checks did not run. lint/source_external.rs. `{tool}` is the tool name.",
        why: "27R §4 dir-absent-is-info - a missing tool is the admin's environment, disclosed rather than silently skipped; --require-tools raises it to a hard CI error. World-as-payload: an honest trigger would have to run a real foreign tool, which replay never does.",
        params: &["tool"],
        example: "sm `shellcheck` was not found on PATH -- its checks were skipped. Install it, pass --no-tools to silence this, or --require-tools to make it a hard CI error.",
        message: Some("sm `{{tool}}` was not found on PATH -- its checks were skipped. Install it, pass --no-tools to silence this, or --require-tools to make it a hard CI error."),
        help: HelpRegister::Absent,
    },
    CatalogEntry {
        slug: "lint-tool-failed-without-findings",
        when_fires: "an external linter exited nonzero but produced nothing parseable. lint/source_external.rs. `{tool}` is the tool, `{rc}` its exit status.",
        why: "27R §8 delta-exit-trichotomy-sharpened - the adapter never interprets a foreign tool-rc beyond zero/nonzero (checkbashisms' additive 1|2|4 codes are the named trap), so an unexplained failure is reported as itself. World-as-payload: replay never runs a foreign tool.",
        params: &["tool", "rc"],
        example: "sm `shellcheck` exited with status 2 but produced no parseable findings",
        message: Some("sm `{{tool}}` exited with status {{rc}} but produced no parseable findings"),
        help: HelpRegister::Absent,
    },
    CatalogEntry {
        slug: "lint-tool-output-unparsable",
        when_fires: "an external linter produced non-empty output that no tier of the adapter ladder recognized. lint/source_external.rs. `{tool}` is the tool, `{output}` its bytes flattened to one truncated line (PASSTHROUGH).",
        why: "27R §4 - upstream drift may cost precision, never a crash and never silence, so the raw bytes ship as one opaque finding rather than being dropped. World-as-payload: replay never runs a foreign tool.",
        params: &["tool", "output"],
        example: "sm unrecognized `checkbashisms` output (raw): possible bashism in - line 4 (should be '.'):",
        message: Some("sm unrecognized `{{tool}}` output (raw): {{output}}"),
        help: HelpRegister::Absent,
    },
    CatalogEntry {
        slug: "unmodeled-wall-inventory",
        when_fires: "a linted book carries at least one unmodeled ⊤-wall; the finding names the FIRST one. lint/source_unmodeled.rs. `{wall_count}`/`{wall_word}` are the census, `{downstream}` the approximate leaf count after the first wall.",
        why: "27R §2 item-2, the gradual-enhancement nag - silence licenses nothing, so a wall is disclosed with the cost it imposes. The downstream count is APPROXIMATE (all downstream commands, not only the modeled ones) - seam-unmodeled-degradation-count.",
        params: &["wall_count", "wall_word", "downstream"],
        example: "sm 2 unmodeled ⊤-walls in this book; the first is here. Downstream commands (~3 leaf site(s) after it) lose full-elision and fall back to runtime guards until each wall's tool has an oracle.",
        message: Some("sm {{wall_count}} unmodeled ⊤-{{wall_word}} in this book; the first is here. Downstream commands (~{{downstream}} leaf site(s) after it) lose full-elision and fall back to runtime guards until each wall's tool has an oracle."),
        help: HelpRegister::Absent,
    },
    CatalogEntry {
        slug: "verdict-terminal-pipeline",
        when_fires: "a `__is_converged` body's last status-bearing statement is a PIPELINE, so the answering status is the tail stage's. lint/source_verdict.rs. No params - the span says which body.",
        why: "rul-rc-partition - a verdict function's exit status reads one fixed table, so a tail stage's status flattens `1 = complement` and `>=2 = cannot-say` into whatever the tail produced. FALSIFICATION-FIRST (rul-unprovable-rides-the-vouch): the check may under-report, never gate.",
        params: &[],
        example: "sm this __is_converged body answers with a pipeline's tail status (oracle-contract section 3): the exit status is the LAST stage's, not necessarily the tool-under-description's -- prefer a shape where the modeled tool produces the status directly.",
        message: Some("sm this __is_converged body answers with a pipeline's tail status (oracle-contract section 3): the exit status is the LAST stage's, not necessarily the tool-under-description's -- prefer a shape where the modeled tool produces the status directly."),
        help: HelpRegister::Absent,
    },
    CatalogEntry {
        slug: "cli-file-not-found",
        when_fires: "an input file does not exist. cli/lib.rs humane_read_error, NotFound arm. {kind} names what we were reading, {path} is the users own spelling.",
        why: "AID-NEEDS:law-codes-vary-by-world-not-grammar: one of THREE sibling codes over io::ErrorKind - a missing file, an unreadable one, and an unclassed OS failure are different world-states with different repairs. Never the platforms raw phrasing.",
        params: &["kind", "path"],
        example: "sm cannot read book `webhost.sh`: no such file",
        message: Some("sm cannot read {{kind}} `{{path}}`: no such file"),
        help: HelpRegister::Absent,
    },
    CatalogEntry {
        slug: "cli-file-permission-denied",
        when_fires: "an input file exists but this process may not read it. cli/lib.rs humane_read_error, PermissionDenied arm. {kind} and {path}.",
        why: "the read-error trichotomys second arm: the file is there, so the repair is permissions, not the path - a distinction the raw OS string buries.",
        params: &["kind", "path"],
        example: "sm cannot read oracle `/etc/dorc/nginx.oracle.sh`: permission denied",
        message: Some("sm cannot read {{kind}} `{{path}}`: permission denied"),
        help: HelpRegister::Absent,
    },
    CatalogEntry {
        slug: "cli-file-unreadable",
        when_fires: "an input file failed to read for a reason outside the two named arms. cli/lib.rs humane_read_error, residual arm. {kind}, {path}, and PASSTHROUGH {detail}.",
        why: "the residual arm passes the platforms own words through, because there is nothing more useful to say about an unclassed OS error than to quote it - and inventing a category would mis-attribute.",
        params: &["kind", "path", "detail"],
        example: "sm cannot read results `probe-results.txt`: Is a directory (os error 21)",
        message: Some("sm cannot read {{kind}} `{{path}}`: {{detail}}"),
        help: HelpRegister::Absent,
    },
    CatalogEntry {
        slug: "cli-flag-needs-value",
        when_fires: "a flag that takes a value was given without one. cli/lib.rs, every value-taking flag. {flag} is the flag, {wants} what it wants.",
        why: "AID-NEEDS:law-codes-vary-by-world-not-grammar: ONE code for the whole family - the flags differ, the failure does not, so this is grammar-fit and takes a hole rather than a sibling per flag.",
        params: &["flag", "wants"],
        example: "sm --book needs a path",
        message: Some("sm {{flag}} needs {{wants}}"),
        help: HelpRegister::Absent,
    },
    CatalogEntry {
        slug: "cli-flag-requires-mode",
        when_fires: "a flag scoped to one invocation was given under another (or its required companion was omitted). cli/lib.rs. {flag} and the {mode} it belongs to.",
        why: "the flag surface is per-invocation, so accepting it elsewhere would imply a capability that mode does not have. tc-whylog-default-off: the spike opt-in siting is the disclosed cut here.",
        params: &["flag", "mode"],
        example: "sm --whylog is only valid with dorc why",
        message: Some("sm {{flag}} is only valid with {{mode}}"),
        help: HelpRegister::Absent,
    },
    CatalogEntry {
        slug: "cli-flag-value-not-a-number",
        when_fires: "a numeric flag was given a non-number. cli/lib.rs parse_expect_count (--expect-files). {flag} and the {got} token.",
        why: "27R section 8b: --expect-files is a CI scope assertion, so a malformed count is refused rather than coerced - a silently-zero expectation would pass vacuously.",
        params: &["flag", "got"],
        example: "sm --expect-files needs a non-negative integer, got `many`",
        message: Some("sm {{flag}} needs a non-negative integer, got `{{got}}`"),
        help: HelpRegister::Absent,
    },
    CatalogEntry {
        slug: "cli-flag-value-not-recognized",
        when_fires: "a flag value outside its closed vocabulary. cli/lib.rs (--probe-capability, --format, --fail-on). {flag}, {got}, and the accepted {expected} set.",
        why: "27C axis-1 and 27R section 5: these vocabularies are engine-owned and closed, so naming the whole accepted set is both possible and the fastest repair.",
        params: &["flag", "got", "expected"],
        example: "sm unknown --probe-capability `wat` (expected root|nopasswd|degraded)",
        message: Some("sm unknown {{flag}} `{{got}}` (expected {{expected}})"),
        help: HelpRegister::Absent,
    },
    CatalogEntry {
        slug: "cli-flags-mutually-exclusive",
        when_fires: "two flags that cannot both hold were both given. cli/lib.rs parse_args_from. {first} and {second} name them.",
        why: "declarations-genuinely-contradict: a pre-network fail-fast rather than a precedence rule, because either silent winner would be a guess at intent.",
        params: &["first", "second"],
        example: "sm --whylog and --whylog-dir are mutually exclusive",
        message: Some("sm {{first}} and {{second}} are mutually exclusive"),
        help: HelpRegister::Absent,
    },
    CatalogEntry {
        slug: "cli-no-book-given",
        when_fires: "an analysis invocation named no book, by positional or by flag. cli/lib.rs parse_args_from. No params.",
        why: "there is nothing to analyze and no safe default: guessing a book would be a plan the admin never asked for. dorc why --last is the one exemption, since it reconstructs its book from the durable.",
        params: &[],
        example: "sm no book given (a positional path or --book=PATH)",
        message: Some("sm no book given (a positional path or --book=PATH)"),
        help: HelpRegister::Absent,
    },
    CatalogEntry {
        slug: "cli-shim-dir-unwritable",
        when_fires: "the --shim-dir materialization edge could not create or write the per-run PATH shims. cli/main.rs. {path} and PASSTHROUGH {detail}.",
        why: "274 section 5: shim materialization is session-establishment I/O the wrapped probe needs, so a failure is loud rather than a silently shim-less run that would resolve the wrong guest.",
        params: &["path", "detail"],
        example: "sm cannot write shim `/run/dorc/shims`: Read-only file system (os error 30)",
        message: Some("sm cannot write shim `{{path}}`: {{detail}}"),
        help: HelpRegister::Absent,
    },
    CatalogEntry {
        slug: "cli-strip-got-a-flag",
        when_fires: "dorc strip got a positional that starts with a dash. cli/lib.rs parse_args_from. {got} is the token found.",
        why: "AID-NEEDS:law-codes-vary-by-world-not-grammar: the SIBLING of cli-strip-needs-path - an argument WAS given, so the user asked for a surface strip does not have, and the remediation differs.",
        params: &["got"],
        example: "sm strip needs a file path, got the flag `--book=webhost.sh`",
        message: Some("sm strip needs a file path, got the flag `{{got}}`"),
        help: HelpRegister::Absent,
    },
    CatalogEntry {
        slug: "cli-strip-needs-path",
        when_fires: "dorc strip was given no path. cli/lib.rs parse_args_from. No params - the invocation says everything.",
        why: "288 rul-dorc-strip-is-the-offramp: strip takes exactly one positional, so an empty one is unambiguous rather than a guess at intent.",
        params: &[],
        example: "sm strip needs a file path",
        message: Some("sm strip needs a file path"),
        help: HelpRegister::Absent,
    },
    CatalogEntry {
        slug: "cli-unknown-flag",
        when_fires: "an unrecognized flag with no near neighbour in the flag table. cli/lib.rs parse_args_from and parse_lint_args. {flag} is the token.",
        why: "inv-top-reject in the small: an unmodeled flag is refused loudly rather than ignored, because a silently-dropped flag is a plan the admin did not ask for.",
        params: &["flag"],
        example: "sm unknown flag `--boook=webhost.sh`",
        message: Some("sm unknown flag `{{flag}}`"),
        help: HelpRegister::Absent,
    },
    CatalogEntry {
        slug: "cli-unknown-flag-did-you-mean",
        when_fires: "an unrecognized flag that IS within edit-distance 2 of a real one. cli/lib.rs parse_args_from. {flag} is the token, {suggestion} the nearest real flag.",
        why: "AID-NEEDS:law-codes-vary-by-world-not-grammar: the sibling of cli-unknown-flag, split by a WORLD-STATE (the flag table has a near neighbour) with its own remediation.",
        params: &["flag", "suggestion"],
        example: "sm unknown flag `--wat` -- did you mean `--last`?",
        message: Some("sm unknown flag `{{flag}}` -- did you mean `{{suggestion}}`?"),
        help: HelpRegister::Absent,
    },
    CatalogEntry {
        slug: "cli-unknown-mode",
        when_fires: "the leading token is not a mode but is within edit-distance 2 of one. cli/lib.rs parse_args_from. {mode} is the token, {suggestion} the nearest real mode.",
        why: "27S did-you-mean: a near-miss is a typo, and naming the intended mode costs one line and saves a round-trip. A wholly-different word is a positional book instead, never a wrong guess.",
        params: &["mode", "suggestion"],
        example: "sm unknown mode `pln` -- did you mean `plan`?",
        message: Some("sm unknown mode `{{mode}}` -- did you mean `{{suggestion}}`?"),
        help: HelpRegister::Absent,
    },
    CatalogEntry {
        slug: "dorc-sh-exec-failed",
        when_fires: "dorc-sh could not exec stock sh. cli/bin/dorc-sh.rs. PASSTHROUGH {detail}.",
        why: "the strip succeeded and the environment failed: distinguishing that from a script error is the difference between fixing the host and hunting a bug that is not there.",
        params: &["detail"],
        example: "sm cannot exec sh: No such file or directory (os error 2)",
        message: Some("sm cannot exec sh: {{detail}}"),
        help: HelpRegister::Absent,
    },
    CatalogEntry {
        slug: "dorc-sh-script-unreadable",
        when_fires: "dorc-sh could not read the script it was handed. cli/bin/dorc-sh.rs. {path} and PASSTHROUGH {detail}.",
        why: "dorc-sh is the shebang runner, so an unreadable script is the whole invocation failing before any strip or exec - reported as itself rather than as a shell error.",
        params: &["path", "detail"],
        example: "sm cannot read webhost.sh: No such file or directory (os error 2)",
        message: Some("sm cannot read {{path}}: {{detail}}"),
        help: HelpRegister::Absent,
    },
    CatalogEntry {
        slug: "dorc-sh-usage",
        when_fires: "dorc-sh was invoked with no script. cli/bin/dorc-sh.rs. No params.",
        why: "288 rul-dorc-sh-not-carved-out: dorc-sh joins the registry like every other surface - slugs, canonical looms, auditable - with only its terse print framing as a surface selection.",
        params: &[],
        example: "sm usage: dorc-sh <script> [args...]",
        message: Some("sm usage: dorc-sh <script> [args...]"),
        help: HelpRegister::Absent,
    },
    CatalogEntry {
        slug: "lint-file-count-drift",
        when_fires: "the lintable-file count disagrees with --expect-files. cli/main.rs lint_command. {expected} is the declared count, {found} what the invocation saw.",
        why: "27R section 8b: the count is a scope assertion, so drift means the glob silently stopped covering what CI thought it covered - clean-because-empty is the failure it catches.",
        params: &["expected", "found"],
        example: "sm --expect-files: expected 12 lintable file(s), saw 9 (scope drift)",
        message: Some("sm --expect-files: expected {{expected}} lintable file(s), saw {{found}} (scope drift)"),
        help: HelpRegister::Absent,
    },
    CatalogEntry {
        slug: "lint-no-lintable-files",
        when_fires: "dorc lint was given nothing lintable. cli/main.rs lint_command. No params.",
        why: "27R section 8b: zero files is OPERATIONAL, never clean - a vacuous pass is the CI failure mode the exit trichotomy exists to prevent.",
        params: &[],
        example: "sm no lintable files given (operational, not clean)",
        message: Some("sm no lintable files given (operational, not clean)"),
        help: HelpRegister::Absent,
    },
    CatalogEntry {
        slug: "lint-required-tools-missing",
        when_fires: "--require-tools was given and a configured external linter is absent from PATH. cli/main.rs lint_command. {tools} lists them.",
        why: "27R section 4 dir-absent-is-info, inverted by explicit request: absence is advisory by default, and --require-tools is how CI says a partial lint must not read as a pass.",
        params: &["tools"],
        example: "sm --require-tools: required tool(s) missing from PATH: checkbashisms, shellcheck",
        message: Some("sm --require-tools: required tool(s) missing from PATH: {{tools}}"),
        help: HelpRegister::Absent,
    },
    CatalogEntry {
        slug: "whylog-unwritten",
        when_fires: "a plan/apply/round-trip run finished but its posthoc-why durable did not land - the whylog directory is unusable, the serializer refused a whole document, the bytes exceeded the retention cap, or the create/write failed. cli/main.rs write seat, over the cli-local hardened store. {dir} and the closed {reason} word.",
        why: "28D:must-default-durable-lands-with-its-hardening bills a durable for VISIBLE persistence failure; this write path previously answered all five of its failure modes with a bare return, which 28D names among the whylog's each-looked-local decisions. Error-floor rather than advisory (28F:rul-write-failure-is-error-floor) because the advisory plane is suppressed under `apply`, and an apply whose receipt silently vanished is exactly the run an admin later comes back asking about. The artifact on stdout is unaffected - a postmortem aid failing is loud, never fatal. Remediation register (help) wanted: name what to check for the reported reason, and note that the run itself completed and its artifact is unaffected.",
        params: &[],
        example: "[unwritten: whylog-unwritten]",
        message: None,
        help: HelpRegister::Absent,
    },
    CatalogEntry {
        slug: "oracle-role-fn-unlifted",
        when_fires: "a `# dorc-lang/v0.2`-marked oracle declares a `<name>__<role>` funcdef that the lift did not produce, so every bind and mark inside it is inert while the file still parses. oracle/validate.rs unlifted_role_fns, comparing the role-funcdefs the parse RECOGNIZED against the ones the lifted sets hold. `{funcname}` = the declaration, spanned at its own name.",
        why: "26G:haz-silence-is-the-common-cause - three of the four r26 analyzer findings were one defect class, a safety-correct degrade that nothing announced, and each was found by a human diffing declared against lifted by hand. Cause-AGNOSTIC on purpose: it reports the LOSS and never a reason, so it also catches roles whose own give-up diagnostics reach no surface (`__lend_map`, `__enter`, `__resolve` and the rest) and drop paths nobody has hit yet. Warn-tier, never a refusal: the constructs that void a body are legal POSIX and stay accepted (parse-permissively/trace-conservatively), and an unlifted funcdef vouches for nothing - it only stops helping. Fires alongside a cause-bearing error rather than deferring to one: `predict-out-of-dialect` names the offending LINE, this names the FUNCTION that line took down, and the r26 authors read the first without drawing the second. Remediation register (help) wanted: point at the funcdef, say its binds and marks are not in effect, and note that the file parsing clean is not evidence it lifted.",
        params: &[],
        example: "[unwritten: oracle-role-fn-unlifted]",
        message: None,
        help: HelpRegister::Absent,
    },
    CatalogEntry {
        slug: "shared-cell-measurements-disagree",
        when_fires: "two or more probe sites resolved to ONE cell and their records did not agree, so the cross-site meet degraded the cell to unknown and de-licensed every site on it - including sites that reported cleanly. cli/main.rs facts_from_sites, read back at the plan stderr advisory tier. `{cell}` = the shared coordinate, `{sites}` = how many sites measure it.",
        why: "26G:fnd-shared-auto-cell-collides - a verdict-only oracle's sites all key one synthesized per-provider cell, so a book that copies six files with one `cp` puts all six on one coordinate. One unreadable destination then de-licenses the other five, and a sibling that merely failed to report does it as readily as one that disagreed, because cant-tell is the default for any site the probe could not answer. The collapse is safety-correct (every site runs) and was entirely silent: the merge minted a decision-inert narrative that no surface consumed. Cell-keyed, ONE line however many sites disagree - the collapse is a property of the cell, and per-pair lines read as N unrelated problems. Spanless: a cell is a cross-site coordinate, and pointing the caret at any one line would blame it for a shared collapse. Remediation register (help) wanted: name the sibling sites, and point at authoring a verdict coordinate as what splits the cell.",
        params: &[],
        example: "[unwritten: shared-cell-measurements-disagree]",
        message: None,
        help: HelpRegister::Absent,
    },
    CatalogEntry {
        slug: "mark-on-and-or-list",
        when_fires: "a `# dorc-lang/v0.2`-marked oracle trails an effect mark on an item of an and-or list (`probe : sm.dorc.K:\"$e\"@sel || return 2`, `a && b :? …`), so the mark is refused and mints no cell. oracle/predict/parser.rs refuse_item_marks, at parse, spanned at the refused mark. The list itself still parses and still ships byte-exact; only the mark is dropped, and `dorc strip` still erases its bytes.",
        why: "a verdict mark claims THIS command's rc establishes the property, but an and-or list answers with the LIST's rc, so the claim has no owner. In `probe : k:e@sel || return 2` the marked cell's complement sense can never be spoken - rc 1 is unreachable, the `||` having routed every failure to 2 - and in `probe : k:e@sel || true` the rc is forged 0 on every host whatever the world says, which is the errexit-masked rc R2-ORTRUE forbids as a verdict and the always-skip guard shape (23H 9.4). `281` 7's one-verdict-per-line rc-arity reaches the same place from the grammar side. Refusing is the loud half of a fix, not a new restriction: before and-or lists were lexed at all, a `&&` list's mark was honored off a command whose right-hand statements no tracer could see. Warn-tier, never a refusal - the cell is simply unminted, so the site runs, which is the safe direction; but an author who wrote an annotation that does nothing must hear it (the silent-inert authored-construct class). Remediation register (help) wanted: the mark belongs on the command whose rc it reads, on its own line, with the gate above it.",
        params: &[],
        example: "[unwritten: mark-on-and-or-list]",
        message: None,
        help: HelpRegister::Absent,
    },
    CatalogEntry {
        slug: "transport-apply-failed",
        when_fires: "a remote apply ran to completion and its artifact exited non-zero. cli/main.rs ship_consented_apply. {host} is the destination, {status} the status the completion marker carried.",
        why: "law-lane-discipline. The third world state in the transport family and the only KNOWN one: it ran, it finished, it exited non-zero. The status is reproduced, never interpreted. A zero is equally not a health claim - plans/252 section 8 - which is what a verify re-probe is for.",
        params: &[],
        example: "[unwritten: transport-apply-failed]",
        message: None,
        help: HelpRegister::Absent,
    },
    CatalogEntry {
        slug: "transport-crlf-refused",
        when_fires: "bytes about to be shipped to a host carry a carriage return. cli/transport_edge.rs first_carriage_return, at plan time on the book and re-checked at apply time on the already-rendered plan. {which} names the artifact, {line} is the 1-based line of the first CR.",
        why: "260 dec-26-crlf. A CRLF shebang is an exec failure the remote kernel raises before any shell of ours exists, so nothing we ship can catch it there - it can only be refused on the controller. Refuses rather than repairs: silently rewriting bytes someone is about to run on a server trades a one-line fix for an invisible edit.",
        params: &[],
        example: "[unwritten: transport-crlf-refused]",
        message: None,
        help: HelpRegister::Absent,
    },
    CatalogEntry {
        slug: "transport-not-attempted",
        when_fires: "no session process could be created, so the host was never contacted. cli/transport_edge.rs. {host} is the destination that went uncontacted, {detail} the spawn failure.",
        why: "AID-NEEDS:law-codes-vary-by-world-not-grammar. The one transport outcome licensed to claim the host was untouched, because the failure is local and provable - the controller could not spawn a child at all. Every other no-output case cannot separate never-ran from ran-and-was-lost.",
        params: &[],
        example: "[unwritten: transport-not-attempted]",
        message: None,
        help: HelpRegister::Absent,
    },
    CatalogEntry {
        slug: "transport-session-lost",
        when_fires: "a session produced no completion marker, so whether the remote artifact ran is unknown. cli/transport_edge.rs, both phases. {host} is the destination, {attempts} how many were made, {diagnosis} a decision-inert reading of what severed it.",
        why: "26A stop-2 plus rul-integrity-failure-withholds-mutation. Absence of output cannot prove absence of execution, so this is neither clean nor failed. Sibling of transport-not-attempted by WORLD STATE, not grammar: there nothing ran and the repair is the invocation, here something may have run and the repair is to re-probe.",
        params: &[],
        example: "[unwritten: transport-session-lost]",
        message: None,
        help: HelpRegister::Absent,
    },
];
