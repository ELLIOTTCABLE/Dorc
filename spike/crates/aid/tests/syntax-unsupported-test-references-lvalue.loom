---
arrangement: syntax-unsupported-test-references-lvalue
---
-- book.sh --
#!/bin/sh
if [ -v HORK_HOME ]; then
   hork add nginx
fi

-- replay --
$ dorc lint book.sh --no-tools
dorc lint findings (advisory; the machine format `--format=jsonl` is the stable surface):

book.sh:
  error[cfg-top-node]: unsupported construct (unknown): un-probeable and
  un-skippable
        [ book.sh:2:4
     2 | if [ -v HORK_HOME ]; then
       |    ^
  error[syntax-unsupported]: `test -v` / `[ -v ]` references a variable lvalue
        [ book.sh:2:4
     2 | if [ -v HORK_HOME ]; then
       |    ^
  error[predict-out-of-dialect]: test operator must be `=` or `!=` (string
  comparison)
        [ book.sh:2:9
     2 | if [ -v HORK_HOME ]; then
       |         \_______/
  2:4 info [unmodeled-inventory:unmodeled-wall-inventory] this book carries 1 unmodeled wall -- constructs Dorc cannot analyze, past which no probe result can be trusted; the first is here, and the ~1 command site(s) after it fall back to runtime guards instead of full elision.

dorc lint: 3 errors, 0 warnings, 1 info across 1 file.

$ dorc-loom --this vars
case: syntax-unsupported-test-references-lvalue
{{wall_count}} = "1"
{{wall_word}} = "wall"
{{downstream}} = "1"
{{v0}} = "3"
{{v1}} = "s"
{{v2}} = "0"
{{v3}} = "s"
{{v4}} = "1"
{{v5}} = ""
{{v6}} = "1"
{{v7}} = ""
