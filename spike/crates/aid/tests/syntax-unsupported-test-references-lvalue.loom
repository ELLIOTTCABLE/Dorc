---
arrangement: syntax-unsupported-test-references-lvalue
edit-loop: edit a sentence in the transcript below, then run `mise run loom:compile -- syntax-unsupported-test-references-lvalue && mise run loom:promote -- syntax-unsupported-test-references-lvalue`; prove it stuck with `mise run test`, which is also what catches anything else the promotion moved. `mise run loom:vars -- --all syntax-unsupported-test-references-lvalue` lists this case's values; type {{name}} to insert or move one.
---
-- book.sh --
#!/bin/sh
if [ -v HORK_HOME ]; then
   hork add nginx
fi

-- replay --
$ dorc lint book.sh --no-tools
dorc lint findings (advisory; the machine format `--format=jsonl` is the stable surface):

book.sh:
  error[cfg-top-node]: unsupported construct (unknown): un-probeable and
  un-skippable
        [ book.sh:2:4
     2 | if [ -v HORK_HOME ]; then
       |    ^
  error[syntax-unsupported]: `test -v` / `[ -v ]` references a variable lvalue
        [ book.sh:2:4
     2 | if [ -v HORK_HOME ]; then
       |    ^
  error[predict-out-of-dialect]: test operator must be `=` or `!=` (string
  comparison)
        [ book.sh:2:9
     2 | if [ -v HORK_HOME ]; then
       |         \_______/
  2:4 info [unmodeled-inventory:unmodeled-wall-inventory] sm 1 unmodeled ⊤-wall in this book; the first is here. Downstream commands (~1 leaf site(s) after it) lose full-elision and fall back to runtime guards until each wall's tool has an oracle.

dorc lint: 3 errors, 0 warnings, 1 info across 1 file.
