---
arrangement: cfg-inline-refused-redirect-dynamic
---
-- book.sh --
#!/bin/sh
out=/var/log/hork.log
install_one() { hork add nginx > "$out"; }
install_one

-- replay --
$ dorc lint book.sh --no-tools
dorc lint findings (advisory; the machine format `--format=jsonl` is the stable surface):

book.sh:
  warning[cfg-inline-refused]: call to `install_one` not inlined: its body has
  an unmodeled write-redirect (`>` to a dynamic/unresolved target) -- it runs as
  an ordinary unmodeled command (tc-M2)
        [ book.sh:4:1
     4 | install_one
       | \_________/

dorc lint: 0 errors, 1 warning, 0 infos across 1 file.

$ dorc-loom --this vars
case: cfg-inline-refused-redirect-dynamic
{{v0}} = "install_one"
{{v1}} = "`>` to a dynamic/unresolved target"
{{v2}} = "1"
{{v3}} = ""
{{v4}} = "0"
{{v5}} = "s"
{{v6}} = "1"
{{v7}} = ""
