---
arrangement: cfg-inline-refused-redirect-dynamic
edit-loop: edit a sentence in the transcript below, then run `mise run loom:compile -- cfg-inline-refused-redirect-dynamic && mise run loom:promote -- cfg-inline-refused-redirect-dynamic`; prove it stuck with `mise run test`, which is also what catches anything else the promotion moved. `mise run loom:vars -- --all cfg-inline-refused-redirect-dynamic` lists this case's values; type {{name}} to insert or move one.
---
-- book.sh --
#!/bin/sh
out=/var/log/hork.log
install_one() { hork add nginx > "$out"; }
install_one

-- replay --
$ dorc lint book.sh --no-tools
dorc lint findings (advisory; the machine format `--format=jsonl` is the stable surface):

book.sh:
  error[predict-out-of-dialect]: unexpected token in command
        [ book.sh:3:12
     3 | install_one() { hork add nginx > "$out"; }
       |            ^
  warning[cfg-inline-refused]: call to `install_one` not inlined: its body has
  an unmodeled write-redirect (`>` to a dynamic/unresolved target) -- it runs as
  an ordinary unmodeled command (tc-M2)
        [ book.sh:4:1
     4 | install_one
       | \_________/

dorc lint: 1 error, 1 warning, 0 infos across 1 file.
