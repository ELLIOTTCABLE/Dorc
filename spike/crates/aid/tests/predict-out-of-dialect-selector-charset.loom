---
arrangement: predict-out-of-dialect-selector-charset
edit-loop: edit a sentence in the transcript below, then run `mise run loom:compile -- predict-out-of-dialect-selector-charset && mise run loom:promote -- predict-out-of-dialect-selector-charset`; prove it stuck with `mise run test`, which is also what catches anything else the promotion moved. `mise run loom:vars -- --all predict-out-of-dialect-selector-charset` lists this case's values; type {{name}} to insert or move one.
---
-- hork.oracle.sh --
# dorc-lang/v0.2
hork__predict() {
   hork --query "$1" : sm.dorc.Package:"$1"@2fast
}

-- replay --
$ dorc lint hork.oracle.sh --no-tools
dorc lint findings (advisory; the machine format `--format=jsonl` is the stable surface):

hork.oracle.sh:
  warning[oracle-role-fn-unlifted]: [unwritten: oracle-role-fn-unlifted]
        [ hork.oracle.sh:2:1
     2 | hork__predict() {
       | \___________/
  error[predict-out-of-dialect]: selector must be a POSIX name
  (letter/underscore, then letters/digits/underscores) or a brace-alternation
  `{a,b}`
        [ hork.oracle.sh:3:50
     3 |    hork --query "$1" : sm.dorc.Package:"$1"@2fast
       |                                                  ^
     4 | }
       | ^

dorc lint: 1 error, 1 warning, 0 infos across 1 file.
