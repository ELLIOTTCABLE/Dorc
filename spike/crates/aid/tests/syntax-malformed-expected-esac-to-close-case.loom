---
arrangement: syntax-malformed-expected-esac-to-close-case
---
-- book.sh --
#!/bin/sh
case $1 in
   add) hork add nginx ;;

-- replay --
$ dorc lint book.sh --no-tools
dorc lint findings (advisory; the machine format `--format=jsonl` is the stable surface):

book.sh:
  error[syntax-malformed]: expected `esac` to close `case`
        [ book.sh:4:1
     4 | 
       | ^
  error[predict-out-of-dialect]: unterminated `case` (expected `esac`)
        [ book.sh:4:1
     4 | 
       | ^

dorc lint: 2 errors, 0 warnings, 0 infos across 1 file.

$ dorc-loom --this vars
case: syntax-malformed-expected-esac-to-close-case
{{v0}} = "2"
{{v1}} = "s"
{{v2}} = "0"
{{v3}} = "s"
{{v4}} = "0"
{{v5}} = "s"
{{v6}} = "1"
{{v7}} = ""
