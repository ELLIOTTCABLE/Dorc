---
arrangement: syntax-unsupported-unset-dynamic-lvalue
edit-loop: edit a sentence in the transcript below, then run `mise run loom:compile -- syntax-unsupported-unset-dynamic-lvalue && mise run loom:promote -- syntax-unsupported-unset-dynamic-lvalue`; prove it stuck with `mise run test`, which is also what catches anything else the promotion moved. `mise run loom:vars -- --all syntax-unsupported-unset-dynamic-lvalue` lists this case's values; type {{name}} to insert or move one.
---
-- book.sh --
#!/bin/sh
name=HORK_HOME
unset "$name"

-- replay --
$ dorc lint book.sh --no-tools
dorc lint findings (advisory; the machine format `--format=jsonl` is the stable surface):

book.sh:
  error[cfg-top-node]: unsupported construct (unknown): un-probeable and
  un-skippable
        [ book.sh:3:1
     3 | unset "$name"
       | \___/
  error[syntax-unsupported]: `unset` of a dynamic lvalue
        [ book.sh:3:1
     3 | unset "$name"
       | \___/
  3:1 info [unmodeled-inventory:unmodeled-wall-inventory] this book carries 1 unmodeled wall -- constructs Dorc cannot analyze, past which no probe result can be trusted; the first is here, and the ~0 command site(s) after it fall back to runtime guards instead of full elision.

dorc lint: 2 errors, 0 warnings, 1 info across 1 file.
