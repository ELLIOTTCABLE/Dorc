---
arrangement: cfg-inline-refused-unmodeled-positional
---
-- book.sh --
#!/bin/sh
install_one() { shift; hork add nginx; }
install_one

-- replay --
$ dorc lint book.sh --no-tools
dorc lint findings (advisory; the machine format `--format=jsonl` is the stable surface):

book.sh:
  warning[cfg-inline-refused]: call to `install_one` not inlined: its body uses
  `shift` (out of the modeled subset) -- it runs as an ordinary unmodeled
  command
        [ book.sh:3:1
     3 | install_one
       | \_________/

dorc lint: 0 errors, 1 warning, 0 infos across 1 file.
