---
code: tolerates-over-identity-dependence
when-fires: a `tolerates:user` vouch sits over a body that visibly reads identity (`id`/`$USER`/`$HOME`). oracle/entry.rs corroborate_tolerance_over_identity.
why: 27C §6 corroboration (recognize-never-license): the ask never blocks. Static.
edit-loop: edit a sentence in the transcript below, then run `mise run loom:compile -- tolerates-over-identity-dependence && mise run loom:promote -- tolerates-over-identity-dependence`. `mise run loom:vars -- --all tolerates-over-identity-dependence` lists this case's values; type {{name}} to insert or move one.
---
-- oracle.sh --
# dorc-lang/v0.2
hork__is_converged() {
   : safe-across user
   hork --query --as "$USER" -- "$1"
}

-- replay --
$ dorc lint oracle.sh --no-tools
dorc lint findings (advisory; the machine format `--format=jsonl` is the stable surface):

oracle.sh:
  warning[tolerates-over-identity-dependence]: sm this `is_converged` carries
  `tolerates:user` but VISIBLY reads the caller's identity
  (`id`/`$USER`/`$HOME`): are you sure the body is read-only under a user shift,
  not just answer-varying? A shifted user must not make it MUTATE (`27C` section
  2 corroboration).
        [ oracle.sh:2:1
     2 | hork__is_converged() {
       | \________________/

dorc lint: 0 errors, 1 warning, 0 infos across 1 file.
