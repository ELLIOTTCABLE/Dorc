---
arrangement: syntax-unsupported-dynamic-command-name
edit-loop: edit a sentence in the transcript below, then run `mise run loom:compile -- syntax-unsupported-dynamic-command-name && mise run loom:promote -- syntax-unsupported-dynamic-command-name`; prove it stuck with `mise run test`, which is also what catches anything else the promotion moved. `mise run loom:vars -- --all syntax-unsupported-dynamic-command-name` lists this case's values; type {{name}} to insert or move one.
---
-- book.sh --
#!/bin/sh
tool=hork
"$tool" add nginx

-- replay --
$ dorc lint book.sh --no-tools
dorc lint findings (advisory; the machine format `--format=jsonl` is the stable surface):

book.sh:
  error[cfg-top-node]: unsupported construct (unknown): un-probeable and
  un-skippable
        [ book.sh:3:1
     3 | "$tool" add nginx
       | \_____/
  error[syntax-unsupported]: dynamic command name (first word is not a fixed
  literal)
        [ book.sh:3:1
     3 | "$tool" add nginx
       | \_____/
  3:1 info [unmodeled-inventory:unmodeled-wall-inventory] this book carries 1 unmodeled wall -- constructs Dorc cannot analyze, past which no probe result can be trusted; the first is here, and the ~0 command site(s) after it fall back to runtime guards instead of full elision.

dorc lint: 2 errors, 0 warnings, 1 info across 1 file.
