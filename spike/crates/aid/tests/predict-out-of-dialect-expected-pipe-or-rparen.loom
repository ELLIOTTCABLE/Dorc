---
arrangement: predict-out-of-dialect-expected-pipe-or-rparen
---
-- hork.oracle.sh --
# dorc-lang/v0.2
hork__predict() {
   case "$1" in
      add hork --query "$2" ;;
   esac
}

-- replay --
$ dorc lint hork.oracle.sh --no-tools
dorc lint findings (advisory; the machine format `--format=jsonl` is the stable surface):

hork.oracle.sh:
  warning[oracle-role-fn-unlifted]: Dorc recognized `hork__predict` as a role
  function but could not lift it: its binds and marks are not in effect, and it
  vouches for nothing. The file parsing clean is not evidence the function
  lifted; a companion diagnostic usually names the exact line that took it down.
        [ hork.oracle.sh:2:1
     2 | hork__predict() {
       | \___________/
  error[cfg-top-node]: unsupported construct (unknown): un-probeable and
  un-skippable
        [ hork.oracle.sh:4:29
     4 |       add hork --query "$2" ;;
       |                             \/
  error[syntax-malformed]: expected `)` after case pattern
        [ hork.oracle.sh:4:7
     4 |       add hork --query "$2" ;;
       |       \_/
  error[syntax-malformed]: expected `esac` to close `case`
        [ hork.oracle.sh:4:11
     4 |       add hork --query "$2" ;;
       |           \__/
  error[syntax-unsupported]: `;;` outside a case arm
        [ hork.oracle.sh:4:29
     4 |       add hork --query "$2" ;;
       |                             \/
  error[predict-out-of-dialect]: expected `|` or `)` in case-arm pattern
        [ hork.oracle.sh:4:11
     4 |       add hork --query "$2" ;;
       |           \__/
  4:29 info [unmodeled-inventory:unmodeled-wall-inventory] this book carries 2 unmodeled walls -- constructs Dorc cannot analyze, past which no probe result can be trusted; the first is here, and the ~0 command site(s) after it fall back to runtime guards instead of full elision.
  error[cfg-top-node]: unsupported construct (unknown): un-probeable and
  un-skippable
        [ hork.oracle.sh:5:4
     5 |    esac
       |    \__/
  error[syntax-unsupported]: reserved word in command position
        [ hork.oracle.sh:5:4
     5 |    esac
       |    \__/

dorc lint: 7 errors, 1 warning, 1 info across 1 file.
