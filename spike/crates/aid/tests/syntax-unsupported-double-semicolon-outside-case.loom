---
arrangement: syntax-unsupported-double-semicolon-outside-case
edit-loop: edit a sentence in the transcript below, then run `mise run loom:compile -- syntax-unsupported-double-semicolon-outside-case && mise run loom:promote -- syntax-unsupported-double-semicolon-outside-case`. `mise run loom:vars -- --all syntax-unsupported-double-semicolon-outside-case` lists this case's values; type {{name}} to insert or move one.
---
-- book.sh --
#!/bin/sh
hork add nginx
;;

-- replay --
$ dorc lint book.sh --no-tools
dorc lint findings (advisory; the machine format `--format=jsonl` is the stable surface):

book.sh:
  error[cfg-top-node]: unsupported construct (unknown): un-probeable and
  un-skippable
        [ book.sh:3:1
     3 | ;;
       | \/
  error[syntax-unsupported]: `;;` outside a case arm
        [ book.sh:3:1
     3 | ;;
       | \/
  error[predict-out-of-dialect]: statement does not start with a word
        [ book.sh:3:1
     3 | ;;
       | \/
  3:1 info [unmodeled-inventory:unmodeled-wall-inventory] sm 1 unmodeled ⊤-wall in this book; the first is here. Downstream commands (~0 leaf site(s) after it) lose full-elision and fall back to runtime guards until each wall's tool has an oracle.

dorc lint: 3 errors, 0 warnings, 1 info across 1 file.
