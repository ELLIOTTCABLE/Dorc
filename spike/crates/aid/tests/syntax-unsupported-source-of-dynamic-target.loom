---
arrangement: syntax-unsupported-source-of-dynamic-target
edit-loop: edit a sentence in the transcript below, then run `mise run loom:compile -- syntax-unsupported-source-of-dynamic-target && mise run loom:promote -- syntax-unsupported-source-of-dynamic-target`; prove it stuck with `mise run test`, which is also what catches anything else the promotion moved. `mise run loom:vars -- --all syntax-unsupported-source-of-dynamic-target` lists this case's values; type {{name}} to insert or move one.
---
-- book.sh --
#!/bin/sh
. "$(hork profile)"

-- replay --
$ dorc lint book.sh --no-tools
dorc lint findings (advisory; the machine format `--format=jsonl` is the stable surface):

book.sh:
  error[cfg-top-node]: unsupported construct (unknown): un-probeable and
  un-skippable
        [ book.sh:2:1
     2 | . "$(hork profile)"
       | ^
  error[syntax-unsupported]: `.`/`source` of a non-literal target
        [ book.sh:2:1
     2 | . "$(hork profile)"
       | ^
  2:1 info [unmodeled-inventory:unmodeled-wall-inventory] this book carries 1 unmodeled wall -- constructs Dorc cannot analyze, past which no probe result can be trusted; the first is here, and the ~0 command site(s) after it fall back to runtime guards instead of full elision.

dorc lint: 2 errors, 0 warnings, 1 info across 1 file.
