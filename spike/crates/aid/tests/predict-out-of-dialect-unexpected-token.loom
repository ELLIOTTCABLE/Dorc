---
arrangement: predict-out-of-dialect-unexpected-token
edit-loop: edit a sentence in the transcript below, then run `mise run loom:compile -- predict-out-of-dialect-unexpected-token && mise run loom:promote -- predict-out-of-dialect-unexpected-token`; prove it stuck with `mise run test`, which is also what catches anything else the promotion moved. `mise run loom:vars -- --all predict-out-of-dialect-unexpected-token` lists this case's values; type {{name}} to insert or move one.
---
-- hork.oracle.sh --
# dorc-lang/v0.2
hork__predict() {
   hork --query ( "$1" )
}

-- replay --
$ dorc lint hork.oracle.sh --no-tools
dorc lint findings (advisory; the machine format `--format=jsonl` is the stable surface):

hork.oracle.sh:
  warning[oracle-role-fn-unlifted]: [unwritten: oracle-role-fn-unlifted]
        [ hork.oracle.sh:2:1
     2 | hork__predict() {
       | \___________/
  error[cfg-top-node]: unsupported construct (unknown): un-probeable and
  un-skippable
        [ hork.oracle.sh:3:19
     3 |    hork --query ( "$1" )
       |                   \__/
  error[syntax-unsupported]: dynamic command name (first word is not a fixed
  literal)
        [ hork.oracle.sh:3:19
     3 |    hork --query ( "$1" )
       |                   \__/
  error[predict-out-of-dialect]: unexpected token in command
        [ hork.oracle.sh:3:17
     3 |    hork --query ( "$1" )
       |                 ^
  3:19 info [unmodeled-inventory:unmodeled-wall-inventory] this book carries 1 unmodeled wall -- constructs Dorc cannot analyze, past which no probe result can be trusted; the first is here, and the ~0 command site(s) after it fall back to runtime guards instead of full elision.

dorc lint: 3 errors, 1 warning, 1 info across 1 file.
