---
arrangement: cfg-inline-refused-per-book-budget
edit-loop: edit a sentence in the transcript below, then run `mise run loom:compile -- cfg-inline-refused-per-book-budget && mise run loom:promote -- cfg-inline-refused-per-book-budget`. `mise run loom:vars -- --all cfg-inline-refused-per-book-budget` lists this case's values; type {{name}} to insert or move one.
---
-- book.sh --
#!/bin/sh
install_all() {
   hork
   hork
   hork
   hork
   hork
   hork
   hork
   hork
   hork
   hork
   hork
   hork
   hork
   hork
   hork
   hork
   hork
   hork
   hork
   hork
   hork
   hork
   hork
   hork
   hork
   hork
   hork
   hork
   hork
   hork
}
install_all
install_all
install_all
install_all
install_all
install_all
install_all
install_all
install_all
install_all
install_all
install_all
install_all
install_all
install_all
install_all
install_all
install_all
install_all
install_all
install_all
install_all
install_all
install_all
install_all
install_all
install_all
install_all
install_all
install_all
install_all
install_all
install_all
install_all
install_all
install_all
install_all
install_all
install_all
install_all

-- replay --
$ dorc lint book.sh --no-tools
dorc lint findings (advisory; the machine format `--format=jsonl` is the stable surface):

book.sh:
  error[predict-out-of-dialect]: unexpected token in command
        [ book.sh:2:12
     2 | install_all() {
       |            ^
  warning[cfg-inline-refused]: call to `install_all` exceeds the per-book
  inline-node budget (990 spliced + 62 more > 1024); not inlined -- it runs as
  an ordinary unmodeled command
        [ book.sh:67:1
     67 | install_all
        | \_________/
  warning[cfg-inline-refused]: call to `install_all` exceeds the per-book
  inline-node budget (990 spliced + 62 more > 1024); not inlined -- it runs as
  an ordinary unmodeled command
        [ book.sh:68:1
     68 | install_all
        | \_________/
  warning[cfg-inline-refused]: call to `install_all` exceeds the per-book
  inline-node budget (990 spliced + 62 more > 1024); not inlined -- it runs as
  an ordinary unmodeled command
        [ book.sh:69:1
     69 | install_all
        | \_________/
  warning[cfg-inline-refused]: call to `install_all` exceeds the per-book
  inline-node budget (990 spliced + 62 more > 1024); not inlined -- it runs as
  an ordinary unmodeled command
        [ book.sh:70:1
     70 | install_all
        | \_________/
  warning[cfg-inline-refused]: call to `install_all` exceeds the per-book
  inline-node budget (990 spliced + 62 more > 1024); not inlined -- it runs as
  an ordinary unmodeled command
        [ book.sh:71:1
     71 | install_all
        | \_________/
  warning[cfg-inline-refused]: call to `install_all` exceeds the per-book
  inline-node budget (990 spliced + 62 more > 1024); not inlined -- it runs as
  an ordinary unmodeled command
        [ book.sh:72:1
     72 | install_all
        | \_________/
  warning[cfg-inline-refused]: call to `install_all` exceeds the per-book
  inline-node budget (990 spliced + 62 more > 1024); not inlined -- it runs as
  an ordinary unmodeled command
        [ book.sh:73:1
     73 | install_all
        | \_________/

dorc lint: 1 error, 7 warnings, 0 infos across 1 file.
