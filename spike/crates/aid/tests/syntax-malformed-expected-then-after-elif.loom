---
arrangement: syntax-malformed-expected-then-after-elif
edit-loop: edit a sentence in the transcript below, then run `mise run loom:compile -- syntax-malformed-expected-then-after-elif && mise run loom:promote -- syntax-malformed-expected-then-after-elif`; prove it stuck with `mise run test`, which is also what catches anything else the promotion moved. `mise run loom:vars -- --all syntax-malformed-expected-then-after-elif` lists this case's values; type {{name}} to insert or move one.
---
-- book.sh --
#!/bin/sh
if hork check; then
   hork add nginx
elif hork other
   hork add redis
fi

-- replay --
$ dorc lint book.sh --no-tools
dorc lint findings (advisory; the machine format `--format=jsonl` is the stable surface):

book.sh:
  error[predict-out-of-dialect]: expected `[` to open a test
        [ book.sh:2:4
     2 | if hork check; then
       |    \__/
  error[cfg-top-node]: unsupported construct (unknown): un-probeable and
  un-skippable
        [ book.sh:6:1
     6 | fi
       | \/
  error[syntax-unsupported]: reserved word in command position
        [ book.sh:6:1
     6 | fi
       | \/
  6:1 info [unmodeled-inventory:unmodeled-wall-inventory] this book carries 1 unmodeled wall -- constructs Dorc cannot analyze, past which no probe result can be trusted; the first is here, and the ~0 command site(s) after it fall back to runtime guards instead of full elision.
  error[syntax-malformed]: expected `then` after `elif` condition
        [ book.sh:7:1
     7 | 
       | ^
  error[syntax-malformed]: expected `fi` to close `if`
        [ book.sh:7:1
     7 | 
       | ^

dorc lint: 5 errors, 0 warnings, 1 info across 1 file.
