---
arrangement: syntax-malformed-expected-then-after-elif
edit-loop: edit a sentence in the transcript below, then run `mise run loom:compile -- syntax-malformed-expected-then-after-elif && mise run loom:promote -- syntax-malformed-expected-then-after-elif`. `mise run loom:vars -- --all syntax-malformed-expected-then-after-elif` lists this case's values; type {{name}} to insert or move one.
---
-- book.sh --
#!/bin/sh
if hork check; then
   hork add nginx
elif hork other
   hork add redis
fi

-- replay --
$ dorc lint book.sh --no-tools
dorc lint findings (advisory; the machine format `--format=jsonl` is the stable surface):

book.sh:
  error[predict-out-of-dialect]: expected `[` to open a test
        [ book.sh:2:4
     2 | if hork check; then
       |    \__/
  error[cfg-top-node]: unsupported construct (unknown): un-probeable and
  un-skippable
        [ book.sh:6:1
     6 | fi
       | \/
  error[syntax-unsupported]: reserved word in command position
        [ book.sh:6:1
     6 | fi
       | \/
  6:1 info [unmodeled-inventory:unmodeled-wall-inventory] sm 1 unmodeled ⊤-wall in this book; the first is here. Downstream commands (~0 leaf site(s) after it) lose full-elision and fall back to runtime guards until each wall's tool has an oracle.
  error[syntax-malformed]: expected `then` after `elif` condition
        [ book.sh:7:1
     7 | 
       | ^
  error[syntax-malformed]: expected `fi` to close `if`
        [ book.sh:7:1
     7 | 
       | ^

dorc lint: 5 errors, 0 warnings, 1 info across 1 file.
