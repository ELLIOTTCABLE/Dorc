---
arrangement: predict-out-of-dialect-expected-in-after-case
edit-loop: edit a sentence in the transcript below, then run `mise run loom:compile -- predict-out-of-dialect-expected-in-after-case && mise run loom:promote -- predict-out-of-dialect-expected-in-after-case`. `mise run loom:vars -- --all predict-out-of-dialect-expected-in-after-case` lists this case's values; type {{name}} to insert or move one.
---
-- hork.oracle.sh --
# dorc-lang/v0.2
hork__predict() {
   case "$1"
      add) hork --query "$2" ;;
   esac
}

-- replay --
$ dorc lint hork.oracle.sh --no-tools
dorc lint findings (advisory; the machine format `--format=jsonl` is the stable surface):

hork.oracle.sh:
  warning[oracle-role-fn-unlifted]: [unwritten: oracle-role-fn-unlifted]
        [ hork.oracle.sh:2:1
     2 | hork__predict() {
       | \___________/
  error[syntax-malformed]: expected `in` after `case` word
        [ hork.oracle.sh:3:13
     3 |    case "$1"
       |             ^
     4 |       add) hork --query "$2" ;;
       | ^
  error[predict-out-of-dialect]: expected `in` after `case` scrutinee
        [ hork.oracle.sh:4:7
     4 |       add) hork --query "$2" ;;
       |       \_/

dorc lint: 2 errors, 1 warning, 0 infos across 1 file.
