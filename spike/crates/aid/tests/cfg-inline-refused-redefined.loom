---
arrangement: cfg-inline-refused-redefined
---
-- book.sh --
#!/bin/sh
install_one() { hork add nginx; }
install_one() { hork add redis; }
install_one

-- replay --
$ dorc lint book.sh --no-tools
dorc lint findings (advisory; the machine format `--format=jsonl` is the stable surface):

book.sh:
  warning[cfg-inline-refused]: function `install_one` is defined more than once;
  the call is not inlined (redefinition tracking is out of the modeled subset)
  -- it runs as an ordinary unmodeled command
        [ book.sh:4:1
     4 | install_one
       | \_________/

dorc lint: 0 errors, 1 warning, 0 infos across 1 file.

$ dorc-loom --this vars
case: cfg-inline-refused-redefined
{{v0}} = "install_one"
{{v1}} = "s"
{{v2}} = "1"
{{v3}} = ""
{{v4}} = "0"
{{v5}} = "s"
{{v6}} = "1"
{{v7}} = ""
