---
arrangement: cfg-inline-refused-redefined
edit-loop: edit a sentence in the transcript below, then run `mise run loom:compile -- cfg-inline-refused-redefined && mise run loom:promote -- cfg-inline-refused-redefined`. `mise run loom:vars -- --all cfg-inline-refused-redefined` lists this case's values; type {{name}} to insert or move one.
---
-- book.sh --
#!/bin/sh
install_one() { hork add nginx; }
install_one() { hork add redis; }
install_one

-- replay --
$ dorc lint book.sh --no-tools
dorc lint findings (advisory; the machine format `--format=jsonl` is the stable surface):

book.sh:
  error[predict-out-of-dialect]: unexpected token in command
        [ book.sh:2:12
     2 | install_one() { hork add nginx; }
       |            ^
  warning[cfg-inline-refused]: function `install_one` is defined more than once;
  the call is not inlined (redefinition tracking is out of the modeled subset)
  -- it runs as an ordinary unmodeled command
        [ book.sh:4:1
     4 | install_one
       | \_________/

dorc lint: 1 error, 1 warning, 0 infos across 1 file.
