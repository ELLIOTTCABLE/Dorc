---
arrangement: syntax-unsupported-source-of-non-literal-target
edit-loop: edit a sentence in the transcript below, then run `mise run loom:compile -- syntax-unsupported-source-of-non-literal-target && mise run loom:promote -- syntax-unsupported-source-of-non-literal-target`; prove it stuck with `mise run test`, which is also what catches anything else the promotion moved. `mise run loom:vars -- --all syntax-unsupported-source-of-non-literal-target` lists this case's values; type {{name}} to insert or move one.
---
-- book.sh --
#!/bin/sh
cfg=/etc/hork/env
. "$cfg"

-- replay --
$ dorc lint book.sh --no-tools
dorc lint findings (advisory; the machine format `--format=jsonl` is the stable surface):

book.sh:
  error[cfg-top-node]: unsupported construct (unknown): un-probeable and
  un-skippable
        [ book.sh:3:1
     3 | . "$cfg"
       | ^
  error[syntax-unsupported]: `.`/`source` of a non-literal target
        [ book.sh:3:1
     3 | . "$cfg"
       | ^
  3:1 info [unmodeled-inventory:unmodeled-wall-inventory] sm 1 unmodeled ⊤-wall in this book; the first is here. Downstream commands (~0 leaf site(s) after it) lose full-elision and fall back to runtime guards until each wall's tool has an oracle.

dorc lint: 2 errors, 0 warnings, 1 info across 1 file.
