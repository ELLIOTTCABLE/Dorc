---
code: wrapper-peel-incoherent
when-fires: a wrapper's `__predict` and `__lend_map` disagree on the peel tail position. cli/main.rs. Spanned (the predict name_span).
why: 273 §5 (declarations-genuinely-contradict): a pre-network fail-fast. The holes are typed: `{wrapper}` plus the two disagreeing depths.
edit-loop: edit a sentence in the transcript below, then run `mise run loom:compile -- wrapper-peel-incoherent && mise run loom:promote -- wrapper-peel-incoherent`; prove it stuck with `mise run test`, which is also what catches anything else the promotion moved. `mise run loom:vars -- --all wrapper-peel-incoherent` lists this case's values; type {{name}} to insert or move one.
---
-- replay --
$ dorc plan --book=book.sh
error[wrapper-peel-incoherent]: sm wrapper `sudo`: __predict and __lend_map
disagree on the peel tail position (predict reaches "$@" after 1 argv token(s),
lend_map after 0) -- static incoherence (273 section 5,
declarations-genuinely-contradict). The guest would start at a different token
depending on which member dispatched; fix the argparse so both peel to the same
tail.
