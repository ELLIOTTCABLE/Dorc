---
arrangement: why-reason-run-not-elidable
when-used: The ANALYSIS row for a site that ran for none of the more specific reasons.
why: The residual arm: a mutator with no converged report, an inert builtin, or a running wall above it. Named as a set because the render cannot yet say which member applies.
---
-- book.sh --
#!/bin/sh
# `cd` is a no-target-state builtin: no read-only check could ever be authored for it, so the
# firehose deliberately says nothing about it (cheap-7) and the addressed answer is the quiet one.
cd /var/tmp
hork tune --profile web

-- replay --
$ dorc why book.sh:4 --book=book.sh --results - < /dev/null 2>/dev/null
      [ book.sh, participating lines:
   4 | cd /var/tmp

   [unwritten: why-participating-lines-closure]

   === OUTCOME ===

   4|cd ran RATHER THAN guarded:
      nothing licensed removing this line: it mutates with no converged report, or a wall
      above it runs

(Receipt analysis: the run's own records, nothing re-measured. Filtered for presumed
relevance; `--all` enumerates everything the engine holds.)

$ dorc-loom --this vars
case: why-reason-run-not-elidable
{{v0}} = "book.sh"
{{v1}} = "ran"
{{v2}} = "guarded"
