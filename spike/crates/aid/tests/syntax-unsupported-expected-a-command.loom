---
arrangement: syntax-unsupported-expected-a-command
edit-loop: edit a sentence in the transcript below, then run `mise run loom:compile -- syntax-unsupported-expected-a-command && mise run loom:promote -- syntax-unsupported-expected-a-command`; prove it stuck with `mise run test`, which is also what catches anything else the promotion moved. `mise run loom:vars -- --all syntax-unsupported-expected-a-command` lists this case's values; type {{name}} to insert or move one.
---
-- book.sh --
#!/bin/sh
hork add nginx
!

-- replay --
$ dorc lint book.sh --no-tools
dorc lint findings (advisory; the machine format `--format=jsonl` is the stable surface):

book.sh:
  error[cfg-top-node]: unsupported construct (unknown): un-probeable and
  un-skippable
        [ book.sh:3:2
     3 | !
       |  ^
     4 | 
       | ^
  error[syntax-unsupported]: expected a command
        [ book.sh:3:2
     3 | !
       |  ^
     4 | 
       | ^
  3:2 info [unmodeled-inventory:unmodeled-wall-inventory] this book carries 1 unmodeled wall -- constructs Dorc cannot analyze, past which no probe result can be trusted; the first is here, and the ~0 command site(s) after it fall back to runtime guards instead of full elision.

dorc lint: 2 errors, 0 warnings, 1 info across 1 file.
