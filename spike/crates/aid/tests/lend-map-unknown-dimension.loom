---
code: lend-map-unknown-dimension
when-fires: an unknown lend_map dimension token appears on a `__lend_map` line. oracle/wrapper.rs walk_lend_body. `{expected}` = the known-dimension list.
why: 273 §8: the line mints no lend and the dimension it meant to answer walls. NOTE plain-language pass owed: ⊤/top wording (law-plain-language-surfaces, 24H ack-4).
edit-loop: edit a sentence in the transcript below, then run `mise run loom:compile -- lend-map-unknown-dimension && mise run loom:promote -- lend-map-unknown-dimension`. `mise run loom:vars -- --all lend-map-unknown-dimension` lists this case's values; type {{name}} to insert or move one.
---
-- oracle.sh --
# dorc-lang/v0.2
runas__lend_map() {
   printf '%s\n' "$1" : lends frobnicate
}

-- replay --
$ dorc lint oracle.sh --no-tools
dorc lint findings (advisory; the machine format `--format=jsonl` is the stable surface):

oracle.sh:
  warning[lend-map-unknown-dimension]: sm `frobnicate` is not a known lend_map
  dimension (expected one of user, fs-view, netns); the line mints no lend and
  the dimension it meant to answer stays ⊤ (walls). Dimension marks are an
  engine-owned closed vocabulary (`273` section 8).
        [ oracle.sh:3:23
     3 |    printf '%s\n' "$1" : lends frobnicate
       |                       \________________/

dorc lint: 0 errors, 1 warning, 0 infos across 1 file.
