---
code: reaches-provider-collision
when-fires: a reach-function is keyed to a name matching a known COMMAND provider. cli/kinds.rs build_kind_reaches. `{name}` = the colliding name.
why: 24G §4: reaches is keyed by KIND, not command — a likely mis-key.
---
-- book.sh --
#!/bin/sh
hork install nginx

-- hork.oracle.sh --
#!/usr/bin/env dorc-sh
# dorc-lang/v0.2
hork__predict() {
   pkg : sm.dorc.Package = "$1"
   hork --query "$pkg" >/dev/null 2>&1 : sm.dorc.Package:"$pkg"@installed
}

# Keyed by the COMMAND this file describes rather than by the kind whose disturbance it surveys.
hork__disturbance_reaches_only() {
   hork --contents -- "$1"    : disturbs sm.dorc.File
}

-- replay --
$ dorc plan --book=book.sh --pre-source hork.oracle.sh > /dev/null
reaches: warning[reaches-provider-collision]: `hork__disturbance_reaches_only()`
is keyed by a command name, but a reach-function belongs to a kind -- as written
it widens a kind no coordinate can name (a likely mis-key).
      [ hork.oracle.sh:9:1
   9 | hork__disturbance_reaches_only() {
     | \____________________________/
dorc: run `dorc why` for the per-site cause-chains, or `dorc why book.sh:N` to query a source line
dorc: plan-summary sites=1 elide=0 omit=0 guard=0 run=1 may-alias=0
dorc: decision-digest e3582dc08ff53e7e

$ dorc-loom --this vars
case: reaches-provider-collision
{{name}} = "hork"
{{v0}} = "book.sh"
{{v1}} = "0"
{{v2}} = "0"
{{v3}} = "0"
{{v4}} = "1"
{{v5}} = "0"
