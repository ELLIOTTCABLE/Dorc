---
arrangement: syntax-malformed-expected-then-after-if
edit-loop: edit a sentence in the transcript below, then run `mise run loom:compile -- syntax-malformed-expected-then-after-if && mise run loom:promote -- syntax-malformed-expected-then-after-if`; prove it stuck with `mise run test`, which is also what catches anything else the promotion moved. `mise run loom:vars -- --all syntax-malformed-expected-then-after-if` lists this case's values; type {{name}} to insert or move one.
---
-- book.sh --
#!/bin/sh
if hork check
hork add nginx
fi

-- replay --
$ dorc lint book.sh --no-tools
dorc lint findings (advisory; the machine format `--format=jsonl` is the stable surface):

book.sh:
  error[predict-out-of-dialect]: expected `[` to open a test
        [ book.sh:2:4
     2 | if hork check
       |    \__/
  error[cfg-top-node]: unsupported construct (unknown): un-probeable and
  un-skippable
        [ book.sh:4:1
     4 | fi
       | \/
  error[syntax-unsupported]: reserved word in command position
        [ book.sh:4:1
     4 | fi
       | \/
  4:1 info [unmodeled-inventory:unmodeled-wall-inventory] this book carries 1 unmodeled wall -- constructs Dorc cannot analyze, past which no probe result can be trusted; the first is here, and the ~0 command site(s) after it fall back to runtime guards instead of full elision.
  error[syntax-malformed]: expected `then` after `if` condition
        [ book.sh:5:1
     5 | 
       | ^
  error[syntax-malformed]: expected `fi` to close `if`
        [ book.sh:5:1
     5 | 
       | ^

dorc lint: 5 errors, 0 warnings, 1 info across 1 file.
