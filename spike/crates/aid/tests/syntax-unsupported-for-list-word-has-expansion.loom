---
arrangement: syntax-unsupported-for-list-word-has-expansion
edit-loop: edit a sentence in the transcript below, then run `mise run loom:compile -- syntax-unsupported-for-list-word-has-expansion && mise run loom:promote -- syntax-unsupported-for-list-word-has-expansion`; prove it stuck with `mise run test`, which is also what catches anything else the promotion moved. `mise run loom:vars -- --all syntax-unsupported-for-list-word-has-expansion` lists this case's values; type {{name}} to insert or move one.
---
-- book.sh --
#!/bin/sh
for pkg in $(hork list)
do
   hork add "$pkg"
done

-- replay --
$ dorc lint book.sh --no-tools
dorc lint findings (advisory; the machine format `--format=jsonl` is the stable surface):

book.sh:
  error[cfg-top-node]: unsupported construct (unknown): un-probeable and
  un-skippable
        [ book.sh:2:1
     2 | for pkg in $(hork list)
       | \_____________________/
     3 | do
       | \/
     4 |    hork add "$pkg"
       | \________________/
     5 | done
       | \__/
  error[syntax-unsupported]: `for` list word contains a
  command-substitution/arithmetic (deferred per HOLE#1)
        [ book.sh:2:1
     2 | for pkg in $(hork list)
       | \_____________________/
     3 | do
       | \/
     4 |    hork add "$pkg"
       | \________________/
     5 | done
       | \__/
  error[predict-out-of-dialect]: empty command
        [ book.sh:2:9
     2 | for pkg in $(hork list)
       |         \/
  2:1 info [unmodeled-inventory:unmodeled-wall-inventory] this book carries 1 unmodeled wall -- constructs Dorc cannot analyze, past which no probe result can be trusted; the first is here, and the ~0 command site(s) after it fall back to runtime guards instead of full elision.

dorc lint: 3 errors, 0 warnings, 1 info across 1 file.
