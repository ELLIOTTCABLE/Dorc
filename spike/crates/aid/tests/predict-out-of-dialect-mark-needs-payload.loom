---
arrangement: predict-out-of-dialect-mark-needs-payload
edit-loop: edit a sentence in the transcript below, then run `mise run loom:compile -- predict-out-of-dialect-mark-needs-payload && mise run loom:promote -- predict-out-of-dialect-mark-needs-payload`. `mise run loom:vars -- --all predict-out-of-dialect-mark-needs-payload` lists this case's values; type {{name}} to insert or move one.
---
-- hork.oracle.sh --
# dorc-lang/v0.2
hork__predict() {
   hork --query "$1" : disturbs
}

-- replay --
$ dorc lint hork.oracle.sh --no-tools
dorc lint findings (advisory; the machine format `--format=jsonl` is the stable surface):

hork.oracle.sh:
  warning[oracle-role-fn-unlifted]: [unwritten: oracle-role-fn-unlifted]
        [ hork.oracle.sh:2:1
     2 | hork__predict() {
       | \___________/
  error[predict-out-of-dialect]: dialect mark requires a payload after the verb
        [ hork.oracle.sh:3:32
     3 |    hork --query "$1" : disturbs
       |                                ^
     4 | }
       | ^

dorc lint: 1 error, 1 warning, 0 infos across 1 file.
