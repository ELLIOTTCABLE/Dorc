---
arrangement: syntax-unsupported-for-without-variable-name
---
-- book.sh --
#!/bin/sh
v=pkg
for "$v" in nginx redis
do
   hork add nginx
done

-- replay --
$ dorc lint book.sh --no-tools
dorc lint findings (advisory; the machine format `--format=jsonl` is the stable surface):

book.sh:
  error[cfg-top-node]: unsupported construct (unknown): un-probeable and
  un-skippable
        [ book.sh:3:1
     3 | for "$v" in nginx redis
       | \_____________________/
     4 | do
       | \/
     5 |    hork add nginx
       | \_______________/
     6 | done
       | \__/
  error[syntax-unsupported]: `for` requires an iteration-variable name
        [ book.sh:3:1
     3 | for "$v" in nginx redis
       | \_____________________/
     4 | do
       | \/
     5 |    hork add nginx
       | \_______________/
     6 | done
       | \__/
  error[predict-out-of-dialect]: empty command
        [ book.sh:3:10
     3 | for "$v" in nginx redis
       |          \/
  3:1 info [unmodeled-inventory:unmodeled-wall-inventory] this book carries 1 unmodeled wall -- constructs Dorc cannot analyze, past which no probe result can be trusted; the first is here, and the ~0 command site(s) after it fall back to runtime guards instead of full elision.

dorc lint: 3 errors, 0 warnings, 1 info across 1 file.

$ dorc-loom --this vars
case: syntax-unsupported-for-without-variable-name
{{wall_count}} = "1"
{{wall_word}} = "wall"
{{downstream}} = "0"
{{v0}} = "3"
{{v1}} = "s"
{{v2}} = "0"
{{v3}} = "s"
{{v4}} = "1"
{{v5}} = ""
{{v6}} = "1"
{{v7}} = ""
