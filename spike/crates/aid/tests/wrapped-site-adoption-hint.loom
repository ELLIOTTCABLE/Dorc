---
code: wrapped-site-adoption-hint
when-fires: a wrapped BOOK site degraded on a missing `tolerates:` vouch. cli/survival.rs build_wrapped_analysis (27N). Spanned (the wrapped site's span).
why: 27C §2 (recognize-never-license): the one-line adoption hint. The holes are typed: `{provider}` and `{dimension}` name the missing vouch.
---
-- book.sh --
#!/bin/sh
runas hork add nginx

-- runas.oracle.sh --
#!/usr/bin/env dorc-sh
# dorc-lang/v0.2
# A coherent wrapper: the predict scrubs and execs the guest, the fold enumerates every dimension,
# and the entry form re-runs the tool non-interactively.
runas__predict() {
   while [ "${1#-}" != "$1" ]; do shift; done
   env -i HOME=/root "$@"
}

runas__lend_map() {
   while [ "${1#-}" != "$1" ]; do shift; done
   printf '%s\n' root : lends user
   : lends fs-view
   : lends netns
   "$@"
}

runas__enter() {
   runas -n "$@"
}

-- hork.oracle.sh --
#!/usr/bin/env dorc-sh
# dorc-lang/v0.2
# The inner tool's verdict carries no `safe-across user`, so the wrapper cannot enter its context
# and the site falls back to running.
hork__predict() {
   verb=$1; shift
   pkg : sm.dorc.Package = "$1"
   case $verb in
      add) hork --query "$pkg" >/dev/null 2>&1 : sm.dorc.Package:"$pkg"@installed ;;
   esac
}

hork__is_converged() {
   hork --query -- "$1"
}

-- replay --
$ dorc plan --book=book.sh --pre-source runas.oracle.sh --pre-source hork.oracle.sh > /dev/null
escalation: note[escalation-policy]: escalation policy: probe re-uses connection
authority (root) for `tolerates:`-vouched functions only (default); entry forms:
runas -n. Forbid with --no-probe-escalation; widen with --escalate-any-probe.
wrapped: note[wrapped-site-adoption-hint]: This site would elide if hork's
oracle vouched its verdict across the wrapped context -- one line in the verdict
function: `: safe-across user`.
      [ book.sh:2:1
   2 | runas hork add nginx
     | \__________________/
oracle: warning[oracle-matched-zero-sites]: [unwritten:
oracle-matched-zero-sites]
probe: note[site-unresolvable]: sm 1 site run unprobed (no read-only check could
be shipped): `runas hork add nginx` -- run `dorc why` for the per-site detail
(the apply runs each anyway, to stay safe)
      [ book.sh:2:1
   2 | runas hork add nginx
     | \__________________/

   = help:  sm site runs `runas hork add nginx`
hint: 'runas' (line 2) is unmodeled: it is the first wall -- an oracle vouching its convergence would elide it when converged
dorc: run `dorc why` for the per-site cause-chains, or `dorc why book.sh:N` to query a source line
dorc: plan-summary sites=1 elide=0 omit=0 guard=0 run=1 may-alias=0
dorc: decision-digest 98d702dd6c3918b01da282806e92096810a3be00f4fc03b77a38aa111926f0a4

$ dorc-loom --this vars
case: wrapped-site-adoption-hint
{{v0}} = "root"
{{v1}} = "runas -n"
{{provider}} = "hork"
{{dimension}} = "user"
{{count}} = "1"
{{site_word}} = "site"
{{v2}} = "0"
{{v3}} = "0"
{{v4}} = "1"
{{v5}} = "0"
