---
arrangement: syntax-unsupported-arithmetic-as-command
edit-loop: edit a sentence in the transcript below, then run `mise run loom:compile -- syntax-unsupported-arithmetic-as-command && mise run loom:promote -- syntax-unsupported-arithmetic-as-command`; prove it stuck with `mise run test`, which is also what catches anything else the promotion moved. `mise run loom:vars -- --all syntax-unsupported-arithmetic-as-command` lists this case's values; type {{name}} to insert or move one.
---
-- book.sh --
#!/bin/sh
$((1 + 1))
hork add nginx

-- replay --
$ dorc lint book.sh --no-tools
dorc lint findings (advisory; the machine format `--format=jsonl` is the stable surface):

book.sh:
  error[cfg-top-node]: unsupported construct (unknown): un-probeable and
  un-skippable
        [ book.sh:2:1
     2 | $((1 + 1))
       | \________/
  error[syntax-unsupported]: arithmetic expansion `$(( ... ))` used as a command
        [ book.sh:2:1
     2 | $((1 + 1))
       | \________/
  error[predict-out-of-dialect]: unexpected token in command
        [ book.sh:2:2
     2 | $((1 + 1))
       |  ^
  2:1 info [unmodeled-inventory:unmodeled-wall-inventory] sm 1 unmodeled ⊤-wall in this book; the first is here. Downstream commands (~1 leaf site(s) after it) lose full-elision and fall back to runtime guards until each wall's tool has an oracle.

dorc lint: 3 errors, 0 warnings, 1 info across 1 file.
