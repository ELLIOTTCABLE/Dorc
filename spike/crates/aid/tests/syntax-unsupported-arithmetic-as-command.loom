---
arrangement: syntax-unsupported-arithmetic-as-command
---
-- book.sh --
#!/bin/sh
$((1 + 1))
hork add nginx

-- replay --
$ dorc lint book.sh --no-tools
dorc lint findings (advisory; the machine format `--format=jsonl` is the stable surface):

book.sh:
  error[cfg-top-node]: unsupported construct (unknown): un-probeable and
  un-skippable
        [ book.sh:2:1
     2 | $((1 + 1))
       | \________/
  error[syntax-unsupported]: arithmetic expansion `$(( ... ))` used as a command
        [ book.sh:2:1
     2 | $((1 + 1))
       | \________/
  error[predict-out-of-dialect]: unexpected token in command
        [ book.sh:2:2
     2 | $((1 + 1))
       |  ^
  2:1 info [unmodeled-inventory:unmodeled-wall-inventory] this book carries 1 unmodeled wall -- constructs Dorc cannot analyze, past which no probe result can be trusted; the first is here, and the ~1 command site(s) after it fall back to runtime guards instead of full elision.

dorc lint: 3 errors, 0 warnings, 1 info across 1 file.
