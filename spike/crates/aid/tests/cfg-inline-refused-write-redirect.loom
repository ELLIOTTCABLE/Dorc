---
arrangement: cfg-inline-refused-write-redirect
owns:
  - cfg-inline-refused-redirect-to-path
edit-loop: edit a sentence in the transcript below, then run `mise run loom:compile -- cfg-inline-refused-write-redirect && mise run loom:promote -- cfg-inline-refused-write-redirect`; prove it stuck with `mise run test`, which is also what catches anything else the promotion moved. `mise run loom:vars -- --all cfg-inline-refused-write-redirect` lists this case's values; type {{name}} to insert or move one.
---
-- book.sh --
#!/bin/sh
install_one() { hork add nginx > /var/log/hork.log; }
install_one

-- replay --
$ dorc lint book.sh --no-tools
dorc lint findings (advisory; the machine format `--format=jsonl` is the stable surface):

book.sh:
  warning[cfg-inline-refused]: call to `install_one` not inlined: its body has
  an unmodeled write-redirect (`> /var/log/hork.log`) -- it runs as an ordinary
  unmodeled command (tc-M2)
        [ book.sh:3:1
     3 | install_one
       | \_________/

dorc lint: 0 errors, 1 warning, 0 infos across 1 file.
