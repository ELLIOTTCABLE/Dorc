---
arrangement: syntax-malformed-unterminated-brace-group
---
-- book.sh --
#!/bin/sh
install_one() {
   hork add nginx

-- replay --
$ dorc lint book.sh --no-tools
dorc lint findings (advisory; the machine format `--format=jsonl` is the stable surface):

book.sh:
  error[syntax-malformed]: unterminated brace group `{` (no `}`)
        [ book.sh:2:15
     2 | install_one() {
       |               ^

dorc lint: 1 error, 0 warnings, 0 infos across 1 file.

$ dorc-loom --this vars
case: syntax-malformed-unterminated-brace-group
{{v0}} = "1"
{{v1}} = ""
{{v2}} = "0"
{{v3}} = "s"
{{v4}} = "0"
{{v5}} = "s"
{{v6}} = "1"
{{v7}} = ""
