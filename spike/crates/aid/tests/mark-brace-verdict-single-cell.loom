---
code: mark-brace-verdict-single-cell
when-fires: a brace-alternation `@{a,b}` appears on a single-cell verdict/observe mark. oracle/predict/derive.rs. Static (literal braces escaped in the template).
why: 277 §4c: a verdict/observe mark asserts exactly one cell; the brace mints no cell and the site runs (a role-aware rejection the parser cannot make).
edit-loop: edit a sentence in the transcript below, then run `mise run loom:compile -- mark-brace-verdict-single-cell && mise run loom:promote -- mark-brace-verdict-single-cell`; prove it stuck with `mise run test`, which is also what catches anything else the promotion moved. `mise run loom:vars -- --all mark-brace-verdict-single-cell` lists this case's values; type {{name}} to insert or move one.
---
-- oracle.sh --
# dorc-lang/v0.2
# One rc cannot witness two cells, so the alternation has nothing to expand into here.
hork__predict() {
   hork --query "$1" >/dev/null 2>&1 : sm.dorc.Package:"$1"@{installed,pinned}
}

-- replay --
$ dorc lint oracle.sh --no-tools
dorc lint findings (advisory; the machine format `--format=jsonl` is the stable surface):

oracle.sh:
  warning[mark-brace-verdict-single-cell]: A verdict mark asserts exactly one
  cell -- one exit code cannot witness an alternation -- so this `@{a,b}` mints
  no cell and the site will run. Split it into one marked probe line per cell;
  braces stay legal on observe (`:?`) marks and claim emissions, which fan out.
        [ oracle.sh:4:38
     4 |    hork --query "$1" >/dev/null 2>&1 : sm.dorc.Package:"$1"@{installed,pinned}
       |                                      \_______________________________________/

dorc lint: 0 errors, 1 warning, 0 infos across 1 file.
