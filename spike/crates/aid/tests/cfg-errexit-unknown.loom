---
code: cfg-errexit-unknown
when-fires: the errexit-region pass hit an unknown/unmodeled command; the `set -e` failure-edge is conservatively assumed. cfg.rs (SPANLESS — spans a region).
why: over-approximate, sound. PASSTHROUGH — the pass's description rides `detail`.
edit-loop: edit a sentence in the transcript below, then run `mise run loom:compile -- cfg-errexit-unknown && mise run loom:promote -- cfg-errexit-unknown`. `mise run loom:vars -- --all cfg-errexit-unknown` lists this case's values; type {{name}} to insert or move one.
---
-- book.sh --
#!/bin/sh
# The option word is not a literal, so whether errexit is on from here is unknown.
set "$SHELL_FLAGS"
hork --settle

-- replay --
$ dorc plan --book=book.sh
warning[cfg-errexit-unknown]: sm errexit state is unknown at one or more
commands; failure-edges added conservatively (over-approximate, sound)
