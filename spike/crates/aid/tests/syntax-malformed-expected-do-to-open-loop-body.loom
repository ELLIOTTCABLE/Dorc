---
arrangement: syntax-malformed-expected-do-to-open-loop-body
---
-- book.sh --
#!/bin/sh
while hork check
   hork add nginx
done

-- replay --
$ dorc lint book.sh --no-tools
dorc lint findings (advisory; the machine format `--format=jsonl` is the stable surface):

book.sh:
  error[predict-out-of-dialect]: expected `[` to open a test
        [ book.sh:2:7
     2 | while hork check
       |       \__/
  error[cfg-top-node]: unsupported construct (unknown): un-probeable and
  un-skippable
        [ book.sh:4:1
     4 | done
       | \__/
  error[syntax-unsupported]: reserved word in command position
        [ book.sh:4:1
     4 | done
       | \__/
  4:1 info [unmodeled-inventory:unmodeled-wall-inventory] this book carries 1 unmodeled wall -- constructs Dorc cannot analyze, past which no probe result can be trusted; the first is here, and the ~0 command site(s) after it fall back to runtime guards instead of full elision.
  error[syntax-malformed]: expected `do` to open the loop body
        [ book.sh:5:1
     5 | 
       | ^
  error[syntax-malformed]: expected `done` to close the loop
        [ book.sh:5:1
     5 | 
       | ^

dorc lint: 5 errors, 0 warnings, 1 info across 1 file.

$ dorc-loom --this vars
case: syntax-malformed-expected-do-to-open-loop-body
{{wall_count}} = "1"
{{wall_word}} = "wall"
{{downstream}} = "0"
{{v0}} = "5"
{{v1}} = "s"
{{v2}} = "0"
{{v3}} = "s"
{{v4}} = "1"
{{v5}} = ""
{{v6}} = "1"
{{v7}} = ""
