---
code: heavy-context-no-tolerance
when-fires: a body reads identity but carries no tolerance vouch. oracle/entry.rs hint_heavy_context_no_vouch (reverse-direction corroboration hint).
why: 27C §6 (recognize-never-license): a one-line vouch would make it context-shiftable. Static.
edit-loop: edit a sentence in the transcript below, then run `mise run loom:compile -- heavy-context-no-tolerance && mise run loom:promote -- heavy-context-no-tolerance`; prove it stuck with `mise run test`, which is also what catches anything else the promotion moved. `mise run loom:vars -- --all heavy-context-no-tolerance` lists this case's values; type {{name}} to insert or move one.
---
-- oracle.sh --
# dorc-lang/v0.2
hork__is_converged() {
   hork --query --as "$USER" -- "$1"
}

-- replay --
$ dorc lint oracle.sh --no-tools
dorc lint findings (advisory; the machine format `--format=jsonl` is the stable surface):

oracle.sh:
  note[heavy-context-no-tolerance]: This `is_converged` reads the caller's
  identity but carries no `safe-across` vouch, so a wrapped site (`sudo ...`)
  runs or guards instead of eliding; if the body stays read-only under a user
  shift, one `: safe-across user` line makes it context-shiftable.
        [ oracle.sh:2:1
     2 | hork__is_converged() {
       | \________________/

dorc lint: 0 errors, 0 warnings, 1 info across 1 file.
