---
arrangement: predict-lex-unterminated-quote
---
-- hork.oracle.sh --
# dorc-lang/v0.2
hork__predict() {
   hork --query "$1
}

-- replay --
$ dorc lint hork.oracle.sh --no-tools
dorc lint findings (advisory; the machine format `--format=jsonl` is the stable surface):

hork.oracle.sh:
  error[syntax-malformed]: unterminated brace group `{` (no `}`)
        [ hork.oracle.sh:2:17
     2 | hork__predict() {
       |                 ^
  warning[oracle-role-fn-unlifted]: Dorc recognized `hork__predict` as a role
  function but could not lift it: its binds and marks are not in effect, and it
  vouches for nothing. The file parsing clean is not evidence the function
  lifted; a companion diagnostic usually names the exact line that took it down.
        [ hork.oracle.sh:2:1
     2 | hork__predict() {
       | \___________/
  error[predict-out-of-dialect]: out-of-dialect token in command: unterminated
  quote
        [ hork.oracle.sh:3:17
     3 |    hork --query "$1
       |                 \_/
     4 | }
       | ^
     5 | 
       | ^

dorc lint: 2 errors, 1 warning, 0 infos across 1 file.

$ dorc-loom --this vars
case: predict-lex-unterminated-quote
{{funcname}} = "hork__predict"
{{v0}} = "unterminated\n  quote"
{{v1}} = "s"
{{v2}} = "1"
{{v3}} = ""
{{v4}} = "0"
{{v5}} = "s"
{{v6}} = "1"
{{v7}} = ""
