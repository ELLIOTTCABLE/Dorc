---
code: records-fact-truncated
when-fires: fewer site records arrived than the header declared. plan/records.rs finalize. `{received}`/`{declared}`/`{unseen}` are the site counts.
why: plans/128 fc-2: a computable range, not a refusal — the unseen sites fold Unknown ⇒ run on their own. NOTE plain-language pass owed: Unknown/⊤-join wording (law-plain-language-surfaces, 24H ack-4).
edit-loop: edit a sentence in the transcript below, then run `mise run loom:compile -- records-fact-truncated && mise run loom:promote -- records-fact-truncated`. `mise run loom:vars -- --all records-fact-truncated` lists this case's values; type {{name}} to insert or move one.
---
-- replay --
$ dorc plan --book=book.sh < probe-results.txt
note[records-fact-truncated]: sm fact lane truncated: 3 of 5 declared site
records received -- the 2 unseen site(s) fold Unknown (run)
