A-bazel-test-encyclopedia-2024 {"url":"https://bazel.build/reference/test-encyclopedia","grading-certainty":"-0:SUSPECT","grading-reasoning":"A-not-B: the canonical, explicitly-normative first-party Bazel specification of the test execution environment (size, timeout, hermeticity, sandbox enforcement); read substantially; authoritative by its own statement that the spec takes precedence over implementation. SUSPECT only because read partial + extracts, not the full page.","relevance-certainty":"+1:SURE","relevance-description":"The enforce-by-sandbox prior art for the tier boundary: tests SHOULD be hermetic (access only declared deps); size determines timeout AND assumed resource usage; the sandbox default-denies network so a non-requires-network-tagged test that touches the net fails (--sandbox_default_allow_network=false detects them), and tags=[requires-network] is the hand-annotated opt-out — the network-reach tier is hand-declared but violations are auto-detected at the sandbox layer.","graded-by":"top-level-agent","published":"2024","via":"mcp__kagi-ken__kagi_search_fetch(queries: [Bazel test tags requires-network external local exclusive size hermetic sandbox])"}
B-saflate-network-assumption-inference-2022 {"url":"https://arxiv.org/pdf/2208.01106","grading-certainty":"-0:SUSPECT","grading-reasoning":"B-not-A: a serious academic preprint with a working implementation (saflate) evaluated on six real projects, but arXiv with peer-review status unconfirmed and read via a substantial extract; B-not-C because it is primary research, not secondary summary.","relevance-certainty":"+1:SURE","relevance-description":"The infer-frontier and the key warning for concl-quadrant: hand-annotation of network-dependency is unreliable (it is difficult or even impossible for engineers to write assumptions that reliably check network availability), so saflate DYNAMICALLY infers the dependency by instrumenting network-exception allocation sites + stack traces and skips-with-provenance (not black-box); the strongest evidence that detecting the network boundary is better done by observing the effect than by static annotation.","graded-by":"top-level-agent","published":"2022-08","via":"mcp__claude_ai_Exa__web_search_exa(query: automatically detect or statically infer whether a test needs real network access)"}
B-pytest-test-categories-network-isolation-2025 {"url":"https://pytest-test-categories.readthedocs.io/en/latest/user-guide/network-isolation.html","grading-certainty":"-0:SUSPECT","grading-reasoning":"B-not-A: a concrete implementation plus an architecture-decision-record for enforcing Google test-sizes by socket interception, but a single-author project with the network-isolation part partly still planned (v0.4.0); B-not-C because the design (ports/adapters, GuardedSocket, the Small/Medium/Large network table) is explicit and well-reasoned.","relevance-certainty":"+1:SURE","relevance-description":"The cleanest existing instance of mechanically enforcing the Small/Medium/Large network-reach boundary: patch sockets and, per the test's declared size, block (Small) / allow-localhost (Medium) / allow (Large), raising or warning on violation — the test size defines the constraint, not the other way around — the precise pattern a Dorc tier-enforcer would mirror.","graded-by":"top-level-agent","published":"2025","via":"mcp__claude_ai_Exa__web_search_exa(query: automatically detect or statically infer whether a test needs real network access)"}
B-kselftest-docs-2026 {"url":"https://docs.kernel.org/dev-tools/kselftest.html","grading-certainty":"-0:SUSPECT","grading-reasoning":"B-not-A: canonical first-party Linux kernel selftest documentation, but a usage/how-to doc rather than a normative spec; read via extract.","relevance-certainty":"+1:SURE","relevance-description":"The non-containerizable precedent: kernel selftests self-check for required kernel features/modules at runtime and emit a dedicated SKIP code ($KSFT_SKIP) when the real capability is absent — the canonical needs-a-real-kernel-feature gating, runtime self-detection of exactly the effects a container cannot fake (the hard end of the containerizability axis), and it is hand-written per test, not inferred.","graded-by":"top-level-agent","published":"2026","via":"mcp__kagi-ken__kagi_search_fetch(queries: [Linux kselftest skip test on missing kernel feature SKIP exit code])"}
B-k8s-e2e-best-practices-2023 {"url":"https://www.kubernetes.dev/blog/2023/04/12/e2e-testing-best-practices-reloaded/","grading-certainty":"-0:SUSPECT","grading-reasoning":"B-not-A: first-party core-maintainer (Patrick Ohly) account of the Kubernetes E2E framework conventions, but a blog/best-practices post rather than a normative spec; read the body in full, the specific label names corroborated via search.","relevance-certainty":"+1:SURE","relevance-description":"The hand-labeled environment-need tier plus the teardown discipline: E2E tests are validated against real clusters and hand-labeled via Ginkgo (the [Serial]/[Slow]/[Conformance] family, migrating to framework.WithSerial/WithSlow), and every test must restore the cluster to its prior state (DeferCleanup, LIFO) — the multi-host high-fidelity tier pairs spin-up with mandatory restore, and the needs-real-cluster boundary is hand-declared.","graded-by":"top-level-agent","published":"2023-04-12","via":"mcp__kagi-ken__kagi_search_fetch(queries: [Kubernetes e2e test labels Serial Slow Feature Conformance Disruptive ginkgo])"}
