T14 render-fidelity wrong-render (NON-guard; ap-2 dash -n catches it): a case-arm whose pattern, body, and `;;` share ONE source line is mangled by the line-granular render_apply. The body command (`apt-get install -y nginx`) is legitimately elidable (converged+ambient — classification is CORRECT here, unlike F1), but commenting its line ALSO comments the structural `nginx) ... ;;` scaffolding, leaving `case nginx in` followed by a bare `:` where a `pat)` is required ⇒ `dash -n` Syntax error (newline unexpected, expecting `)`). This is the LeafId↔AstId line-blur the plan/CLAUDE.md predicted ("comment the lone body of an if/while/case arm ... a multi-leaf line blurs the back-map"). It is render-layer, NOT classification-layer (so distinct from the F1 guard family gw-1 owns). The SAFE behavior: a leaf-exact render that comments only the body token and keeps `nginx)`/`;;` intact (the multi-LINE arm form already renders dash-clean — see that the same book with the body on its own line passes). Fix lands in render_apply (seam-prov leaf-exact fidelity), not classification. notes/199 cluster-C (render-fidelity).
