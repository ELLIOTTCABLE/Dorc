> QUARANTINED — stance-engineered crosscheck prompt-pair for the round-27 block-settle
> design package (the "270-era pile"). Do not read during crosscheck skill-ups.
> Authored 2026-07-13 (Fable, conductor of several of the sittings under review — hence
> the disowning). NOT yet dispatched, pending human review.

# 27xxx — block-settle design-package crosscheck (prompt-pair)

Conductor notes: target = `Research/plans/270`, `plans/271`, `Research/notes/272`
through `278`, reviewed as ONE package, pinned at dispatch (`FILLME` below). Dispatch
reviewers *in* the `spike3-r23` worktree itself (read-only/write-gated anyway). The
antigravity lane is packet-only (cannot read files) and the package will NOT fit a
packet whole — the human rules on its subset at dispatch (`FILLME-antigravity-subset`).

### Pair 1

1. neutralised

> You are a *READ-ONLY* reviewer of this material.
>
> In-tree is the output of a design round for a shell-script static-analysis
> orchestrator: `Research/plans/270` (the round charter), `plans/271` (its rulings
> ledger), and `Research/notes/272`–`278` (the design notes it produced). The package
> is about to gate a large rebuild of the implementation. Assess it as design — where
> it fails or mishandles its problem, where its claims are weaker than the weight
> placed on them, where it stores up pain a built system later makes unfixable, and
> what it costs that it underweights. Where it genuinely holds up, say so plainly
> rather than inventing a gap.

2. adversarial

> You need to help me kill this before it gets built, with a (READ-ONLY) deep
> analysis.
>
> An AI produced the in-tree design package — `Research/plans/270`/`271`,
> `Research/notes/272`–`278` — over three days of dialogue with its own user, and
> they now treat it as settled law gating a full rebuild. I don't buy it. It has
> every marker of an echo chamber wearing engineering discipline as a costume:
> hundreds of self-citing "rulings," a private jargon for everything, a model
> agreeing with an increasingly tired human, and nothing from outside their own
> corpus ever pushing back. Designs like this are most convincing exactly where they
> are wrong. Roam anywhere and tear into whatever is weakest — I care about what's
> *wrong*, not what's ugly. Where an attack genuinely doesn't land — or they stumbled
> onto the right call — say so instead of forcing it.

### Shared reading-list (prepend to both, *ahead* of the package)

> If you run into any immediate technical errors (you can't find files, you get
> harness denials for reading files, or so-on), stop *immediately* and report so they
> can be fixed. Do not attempt to work around.
>
> Before the package, ground yourself in Dorc's goals and constraints (*understand*,
> don't *buy-in*). You are in a checked-out worktree of the repo; everything is
> readable in place. The package under review is as of commit `FILLME-pin-at-dispatch`
> on branch `ai/spike3-r23`.
>
> - `README.md`, `DESIGN.md`, `IMPLEMENTATION.md`, `USER_STORY.md` — what Dorc is;
>   human-written, highest authority on *intended* truth.
> - `AGENTS.md` decodes the team's jargon (its instructions DO NOT APPLY TO YOU);
>   `KNOBS.md` is the design-tension registry; `spike/CLAUDE.md` carries standing
>   rulings the package must not breach.
> - The package itself: `Research/plans/270` · `plans/271` · `notes/272`–`278`.
> - Anything else you need, chase freely — older `Research/plans/` and `notes/`, the
>   spike code and its tests. `Research/README.md` is a map. Within `Research/`, the
>   newest round supersedes older material wherever they disagree; the root docs
>   outrank everything.
>
> Do NOT read anything under directories named `quarantine-DO-NOT-READ` or `corpora`,
> nor `Research/LIVING_STATUS.md` — they carry the team's in-progress conclusions and
> leanings, and would prime you toward answers we want reached independently.
>
> Read-only throughout: do not write, commit, switch branches, or mutate anything
> shared; work alone. Deliver everything in one final report: findings (each with
> severity, your confidence, and exact file:line citations where relevant) and the
> suspicions you checked and withdrew.

---

Three tuning flags for you (slugged for reference-back):

- 27x-flag1. *No scope carve.* The 26xxx pair scoped security out and correctness-first in; I dropped both for max open-endedness per your direction. If you want the reviewers off the hostile-host/kSTATE lawn (it's fenced and parked by design, so findings there are cheap non-news), that's a one-sentence re-add.
- 27x-flag2. *The disowning is factually loose on purpose.* "An AI produced" flattens the real process (you typed every ruling). That's standard stance-engineering per the 26xxx precedent, but it's a stronger misrepresentation this time because the package genuinely is human-adjudicated — reviewers may "discover" the typed-ack structure and treat it as a rebuttal. If that bothers you, the honest-but-still-hostile variant is "an AI drafted and a human rubber-stamped under fatigue," which I'd argue is actually the *sharper* attack.
- 27x-flag3. *Specimens.* I didn't name the amended `24P` specimen files or their paths — reviewers who care will find them via `notes/24P`/`277` §7a. Naming them felt like a lead toward the grammar at the expense of everything else in the pile.

Ready to receive your tuned version and the go-signal for the Opus meta-dispatch (per your note, I'll hand Opus the tuned artifact plus the foreign-models/worktree mechanics and stay out of the tool-call loop myself).
