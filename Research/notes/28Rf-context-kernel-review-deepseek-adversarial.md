I now have all the material needed. Here is the report.

---

### 1. `syn-zero-new-spellings` is unsustainable without §10's deferred design
**severity: high** · **where:** §0 `syn-zero-new-spellings` + §3 para 3 + §10 · **claim attacked:** "all three pillars consume EXISTING sh acts: sourcing, subshells, `unset -f`, creator/destroyer commands with ordinary oracles, ssh lines. No new authored surface anywhere (the §10 language dig may add *oracle-member vocabulary* for describing lifecycle events; it must clear the sent-language-is-becoming-crufty bar)."

**how it breaks:** P3's lifecycle-event machinery requires the engine to know which commands begin/end contexts. §3 claims "Begin/end-describing commands are ordinary oracle-described commands [ACKED]" and gives `useradd alice` as the strawman. But the existing oracle vocabulary (predict bodies, verdict bodies, marks, lends) describes *convergence and state*, not lifecycle events — there is no extant mechanism for an oracle author to declare "this command *begins* the `user=alice` context" or "its cell marks measure whether it already exists." §10 explicitly admits the authored surface is "deliberately empty until that sitting" and that it must clear the `sent-language-is-becoming-crufty` bar. The plan therefore asserts zero new spellings in §0 while simultaneously deferring the design of the spellings that P3's core mechanism (availability computation from begin/end descriptions) cannot function without. Without a demonstrated path from existing marks to lifecycle-event description, `syn-zero-new-spellings` is an aspiration dressed as a settled constraint. **confidence: sure**

### 2. P2's closure-custody rests on an acknowledged-unsettled foundation
**severity: high** · **where:** §2 ("Sourcing is the promise 'I treated this as if I wrote it' (`28M:dir-ownership-is-transitive-inclusion` [TYPED])") + `28M` §10 · **claim attacked:** The entry-closure is THE speaker, and sourcing IS the package boundary, grounded on `dir-ownership-is-transitive-inclusion` as a [TYPED] foundation.

**how it breaks:** `28M` §10 grades `dir-ownership-is-transitive-inclusion` as "[TYPED 2026-07-31; needs-more-attention flag is the human's own]." The human explicitly flagged it as unsettled: "the 'owner's file' is an insufficient concept everywhere this corner has used it" and "To re-examine at ruling: the fence's `SourceFileId` span proxy re-keys to entry-closure identity." The grade [TYPED] means the human wrote the words but also attached a "needs-more-attention" flag, which indicates the concept is not settled design law. `28Q` cites it as a bare [TYPED] without the flag, omitting the human's stated uncertainty, and builds the entire P2 pillar — DefinitionCustody re-key, the frame-relative committee fence, kind-owner single-occupancy — on this foundation. If the re-examination changes the concept (e.g., the fence re-keys differently than closure-identity), P2's mechanics unwind. The plan does not acknowledge this dependency risk. **confidence: sure**

### 3. Wait-loop wall-transparency is claimed as a derivation with no detection mechanism
**severity: medium** · **where:** §3 ("The inverse wait is the trajectory available→departed→available; wait-loops with pure-delay bodies mint no events ⇒ wall-transparency derives (the `26K` sit-wall-transparent-delay-loops ratification becomes a corollary to ack, not a standalone rule)") · **claim attacked:** Wall-transparency for wait-loops derives as a corollary from the availability model.

**how it breaks:** The availability model tracks begin/end events to determine when contexts are available. A wait-loop that contains no lifecycle events correctly produces no availability changes — but the *wall* (opaque/unmodeled command) that precedes the loop is unaffected by availability. Wall-transparency means the wall is treated as if it didn't run (because the loop body is pure delay), allowing downstream facts to survive. The availability model provides no mechanism for *classifying* a command as pure-delay vs mutating, nor for *detecting* that a loop is a wait-loop. This classification is separate from availability tracking — it requires either oracle-authored declarations of purity or engine-level analysis of loop bodies. The plan provides neither, yet claims the result "derives" from the availability model. If the claim is that the `26K` ratification already solved this, then the corollary claim is misattributed — it belongs to that ratification's mechanism, not to the availability model. **confidence: sure**

### 4. §0's symmetry table conflates plan-time licensing with apply-time runtime under "silence"
**severity: medium** · **where:** §0 `syn-one-context-two-planes` table, "silence" row · **claim attacked:** Both planes obey "ONE discipline" with parallel silence rows: load-plane "unresolvable load ⇒ ⊤, walls" mirrors world-plane "unmodeled command / unmarked kind ⇒ wall."

**how it breaks:** The table presents these as structurally parallel under one discipline, but they are not the same category of thing. Load-plane silence (unresolvable load ⇒ ⊤) operates at *analysis time* and produces a *licensing consequence*: the affected families' licenses are withheld. World-plane silence (unmodeled command ⇒ wall) operates at *both* plan time and apply time, and the "wall" is a *runtime behavior* (the command runs, poisoning downstream analysis). The table's "walls" term is overloaded — in the load plane it means "licenses withheld" (a plan-level decision), while in the world plane it means "opaque-poison-is-the-product" (a runtime-execution fact). The plan later relies on this symmetry to claim that the same crossing-claims discipline applies uniformly, but the underlying mechanisms — `inv-top-reject` withholding vs runtime opaque-poison — have different correctness properties and different failure modes. The unified table obscures this, and the uniform-discipline claim is weaker than represented. **confidence: suspect**

### 5. Incarnation is "softly defined" but defaults to hard partition
**severity: medium** · **where:** §3 ("The incarnation, defined softly and on purpose" paragraph) · **claim attacked:** "an incarnation distinguishes LIFETIMES of one context — same identity coordinates, new instance — for *keying* and *default distrust*, never as a hard partition."

**how it breaks:** The plan says the default is: "silence walls — a fact keyed to incarnation N licenses nothing in N+1." This IS a hard partition in practice — facts from one incarnation are entirely dead in the next unless explicitly marked as crossing. The "softness" is only that marks CAN carry facts across, which is the same as every other dimension's crossing mechanism in `27C` §4. The qualifying language ("never as a hard partition" + "Dorc deliberately does not, because much of this design's value lives in CORRELATING instances") implies a softer default than exists — but correlation across incarnations is explicitly deferred to `res-incarnation-correlation-door` (§9), which is "named and NOT designed here." The actual delivered behavior (silence walls between incarnations) is indistinguishable from hard partition for any book whose authors haven't marked crossing claims. The "softly" rhetoric misrepresents the delivered semantics; a reader could reasonably expect that the engine does something to correlate instances by default, which it does not. **confidence: suspect**

### 6. Stage-iii multi-scope typing may violate the byte-identical gate for books without lifecycle events
**severity: medium** · **where:** §8 stage-iii + §3 ("Facts carry their context including the incarnation marker; the controller mints incarnation identity... `WidthOneAttemptScope` goes multi, deliberately, at stage-iii") · **claim attacked:** Stage-iii's gate is "a book with no lifecycle events is byte-identical."

**how it breaks:** The plan says `WidthOneAttemptScope` goes from carrying one scope to carrying multiple scopes (the `rul-attribution-is-controller-minted` re-entry trigger). Adding incarnation markers to facts — even for a book with no lifecycle events, where there is exactly one incarnation — changes the fact representation. The scope-type change is not gated by the presence of lifecycle events: it's a structural change to how facts are stored and compared. If facts now carry `(kind, entity, selector, host, incarnation)` instead of `(kind, entity, selector, host)`, probe artifacts derived from those facts could differ in bytes (comparison keys, record formats) even when the incarnation slot is always the same value. The plan asserts byte-identical output but does not explain how the multi-scope fact representation is kept wire-compatible with the single-scope one for the degenerate case. If the comparison or serialization changes, the gate fails. **confidence: suspect**

### 7. `adj-never-live-exactness-accepted` is mischaracterized as a hazard the plan "dissolves"
**severity: low** · **where:** §1 ("the exact-subtraction hazard (`28P:adj-never-live-exactness-accepted`) dissolves into the general mechanism") · **claim attacked:** The never-live exact-subtraction is a "hazard" that the DefinitionId factoring dissolves.

**how it breaks:** `28P:adj-never-live-exactness-accepted` does not characterize the exactness as a "hazard" — it characterizes it as an explicitly *accepted* risk with a permanent constraint: "any future widening of decidable-set v0 now widens a WINNER-SHIFTING surface." The word `accepted` in the slug is load-bearing. The plan reframes this accepted, priced risk as a problem to be solved ("dissolved"), which shifts the framing from "we deliberately accepted this and here are the permanent constraints" to "we will fix this." The DefinitionId factoring may genuinely eliminate the EXACT-subtraction path (a never-live definition simply has no frame where it answers), but the plan asserts dissolution without demonstrating that the frame mechanism is safer — it replaces one conservative-except-one-exact-seat mechanism (whole-unit resolution with exact subtraction) with another (frame-keyed resolution with conservative branch handling), and the second mechanism's safety properties are not yet built or measured. The framing as "dissolves a hazard" implies the new mechanism is strictly safer, which is undemonstrated. **confidence: suspect**

### 8. `price-multi-file-package` "softens to nothing" conflates same-closure and cross-closure splits
**severity: low** · **where:** §2 ("`price-multi-file-package` softens to nothing — one entry sourcing its parts is one speaker; the bitem6-proven two-file helper-package shape becomes the canonical packaging story") · **claim attacked:** The multi-file-package price dissolves under entry-closure custody.

**how it breaks:** `28M` §6's `price-multi-file-package` describes "one author/team publishing one family across several files" — meaning one role function's arms split across multiple files. The bitem6-proven shape (`28P` `res-cross-file-helper-composition-works`) is a *different* scenario: a helpers file (non-role names) plus a thin entrypoints file (the role's collision surface). In that shape, each role lives in exactly one file — the entrypoints file. Under closure-custody, a family split across files *within the same closure* would not be fenced, which does soften the price for same-closure splits. But `28M` §6's scenario includes cross-closure splits (e.g., a community oracle's family split across files consumed by different entry files), and the plan's "softens to nothing" claim overstates the resolution — it only softens for the same-closure case. The cross-closure case remains priced. **confidence: suspect**

### 9. Interaction between `lend_map` and temporal availability is unspecified
**severity: low** · **where:** §3 ("`lend_map` never grows a time key — nothing lends time") + `27C` §3 composition algebra · **claim attacked:** The availability model composes cleanly with `27C`'s existing dimension-keyed context algebra.

**how it breaks:** `27C` §3's composition algebra maps wrapper chains to context keys (the folded per-dimension normal form). `lend_map` answers "what user/fs-view/netns does this wrapper chain denote" at a given site. Under `28Q`'s model, contexts have temporal extent — the user=alice context exists only after `useradd alice` (arrives-at(6)). The plan says `lend_map` never grows a time key, which is correct — `lend_map` answers the *what* question, not the *when*. But the interaction is: at plan time, for a wrapped site at line 40 with `sudo -u alice`, the engine must first compute the context key via `lend_map` (user=alice), then check availability (did user=alice arrive before line 40?). If the context hasn't arrived, probing can't enter. The plan doesn't specify whether `lend_map` is consulted at all for unavailable contexts, or whether the availability check short-circuits before `27C`'s composition machinery runs. This is a mechanical gap, not a contradiction, but it leaves unspecified whether an unavailable context's `lend_map` could produce a key that interacts with other machinery (e.g., fact keying, batching). **confidence: suspect**

### 10. "Identified-cause" claim ignores plan-edit TOCTOU
**severity: low** · **where:** §3 ("a planned lifecycle event is an IDENTIFIED-CAUSE, in-book event (the creator line is the cause) — analysis-plane, fails toward run") · **claim attacked:** Planned lifecycle events are safely identified as causes because the creator line is in the book.

**how it breaks:** The plan says the creator line is the "identified cause" of the context's arrival. At plan time, the creator is probed and its convergence status determines the context's availability for downstream probing. But between plan and apply, the user may *edit* the plan — adding lines before the creator, removing lines, or modifying the creator itself. The edited plan's creator may have a different convergence status than the original plan's. Since the user explicitly has the right to edit the plan (this is a core UX feature — the plan is presented for ack/*edits*), the "identified-cause" that was valid at plan-presentation time may be invalid at apply time. The integrity plane catches *unplanned* lifetime churn (host-side drift), but not *user-initiated* plan edits. The plan fails toward run conservatively (if the creator is edited to no longer converge, downstream contexts become unavailable and probing degrades), so this is a value-loss issue, not a correctness issue — but the claim "IDENTIFIED-CAUSE" implies a stronger guarantee than the plan/apply separation actually provides. **confidence: suspect**

### 11. P3's authoring mechanism for begin/end description is the same gap as finding #1, specific to oracle vocabulary
**severity: low** · **where:** §3 para 3 + §10 · **claim attacked:** "Begin/end-describing commands are ordinary oracle-described commands [ACKED, `rul-availability-is-universal`]."

**how it breaks:** This is the surface-level counterpart of finding #1. The existing oracle vocabulary has no mechanism for declaring lifecycle events. A command like `useradd` has a predict body (checking whether the user exists) and/or a verdict body (measuring whether the user is in the desired state), but neither body *declares* that the command *begins* the `user=alice` context. The plan assumes that a creator's convergence (determined by its existing oracle's predict/verdict) IS the signal that the context becomes available, which would couple the oracle's *convergence-testing* vocabulary (designed for elision) to an *availability-signaling* semantics (new purpose). The rule `rul-availability-is-universal` is marked [ACKED] — human-confirmed in dialogue — but the *mechanism* for how an oracle communicates begin/end semantics has no authored spelling. The plan's §10 explicitly defers this to "the next design dig, human-led." The ACKED ruling is therefore a direction-ack (the human agrees with the concept) without a settled mechanism, and the plan presents it as mechanically settled. **confidence: guess**

---
**did not hold:**

- **Verification of `rul-verdict-primacy-at-the-ship-seat` [TYPED 2026-08-01] provenance**: The ruling is dated today and appears in the LLM-authored plan. Suspicious, but the grading rules say [TYPED] items are human-ratified and we attack only their unpriced consequences. The ruling itself is internally consistent with `28M` §8's correction paragraph (which notes the as-built predict-wins ordering was falsified by `28P:fnd-a-split-family-elides-on-two-authors`). If the human did type this ruling in today's sitting, it stands. No attack surface found in the ruling's mechanics.
- **P1 chimera-free claim**: The DefinitionId factoring genuinely makes cross-author cell reads unrepresentable — if identity and cells both key to the same DefinitionId, the mismatched-read is structurally impossible. This claim is mechanically sound.
- **`27C` consent machinery preservation**: Read carefully, the plan says probing enters "if available, under `27C`'s unchanged consent machinery." The availability check is a *gate* before `27C`'s machinery, not a modification of it. The interaction is coherent (gate → if available → invoke `27C`'s dial×vouch×entry).
- **Host aliasing as "resolve-machinery territory"**: The plan defers host identity resolution without designing it. This is named as `res-host-identity-wants-resolve` and explicitly out-of-scope. Not a design defect — it's an honest deferral.
- **Stage-0 outcome-byte-stable gate plausibility**: The verdict-primacy re-cut could change outcomes if verdict and predict bodies disagree on rc, but the gate explicitly says "an outcome move is a finding, never churn" — meaning if it happens, it's treated as a bug to fix, not accepted churn. Reasonable gate design.
- **`price-patch-author` unchanged claim**: `28M` §6 lists `price-patch-author` as "the largest and most sympathetic screwed population." `28Q` §2 says it's "UNCHANGED (member-granular patching across closures is still committee; honesty preserved)." This is consistent — the plan doesn't claim to solve it, just preserves the existing honesty about it.
- **§3's "Guards in arriving contexts are sound BY CONSTRUCTION"**: Traced fully: if creator converged → context available at probe → probing enters → guard uses probe answer → at apply, creator elides → guard's answer is valid. If creator didn't converge → context not available → can't-say → guard runs runtime check → at apply, creator runs → guard measures actual state. Both branches degrade correctly. The BY CONSTRUCTION claim holds — it's the in-sequence ordering that makes it sound.