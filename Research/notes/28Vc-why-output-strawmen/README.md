# 28Vc (née 292) — why-output strawmen (imagination-tier, written UNPOISONED)

AI-authored (Fable conductor, 2026-07-25, at human direction) BEFORE reading the
whystrawmen lane's captured output — deliberately unpoisoned by the as-built renders.
These are NOT mechanism, NOT settled design, NOT render contracts: they are what a
battle-tested, feature-rich Dorc *should* show an end-user, invented from whole cloth,
as comparison material for the seat design-round (`289` §2w). Non-functional looms:
context sections are abbreviated; nothing here runs; ceremony omitted on purpose.

Invented capabilities exercised across the five (so the comparison discussion can
point at them by name):

- **inv-freshness-relativized** — every measurement carries its age ("probed 4m ago,
  06:11:52"); live surfaces relativize, committed corpora would pin (`286` freshness).
- **inv-backing-watch** — an elision names *what would bring the line back* (its
  fact's backing, inverted into user language).
- **inv-chain-as-sh** — causality rendered AS annotated sh where the story is
  CFG-shaped (the `27W` §4 banked direction), instead of "line 9, therefore line 16".
- **inv-show-the-code-gutter** — inlined oracle arms in a source gutter with locus,
  author comments riding as display (`27W:rul-report-surface-massaging`).
- **inv-claim-blast-radius** — a trusted claim discloses how many other books/hosts
  on this controller rest on it (fleet-aware attribution).
- **inv-anti-nag-statement** — an `unsound` decline renders as a settled judgment
  ("no oracle improvement can change this"), suppressing enhancement pressure.
- **inv-model-offer** — a decline explains what the model COULD answer (the
  two-cells sysctl teaching move) with a contrasting probeable spelling.
- **inv-replay-banner** — `--last` renders open with a one-line replayed-voice
  disclaimer (receipt, not world).
- **inv-attention-arithmetic** — the zero-arg report opens with the removal ledger
  split by license ("4 by proof, 1 by trusted claim") and closes with the cheapest
  improvement, quantified.
- **inv-explain-cross-pointers** — `see: dorc explain <concept>` footers (`286`
  staging).
- The ruled flag name `--risk-faultless-skips` is used throughout (aspiration
  ignores the code's `--trust-footprints` spelling).

Files: `a-elided.loom` (restraint; the two-link minimum) · `b-guarded.loom`
(wall-referencing; chain-as-sh) · `c-survived.loom` (maximal chain + epilogue +
replay) · `d-declined.loom` (the authored refusal as a first-class answer) ·
`e-problems-report.loom` (the zero-arg aggregate + trust-spent ledger).
