# 20I — Charter-adherence pair reconciliation (crosscheck #3) + repair dispositions

> Round-20 close-out crosscheck (the ap-3 rotation's final face). Both passes at HEAD
> ce14c14; both independently reran the suites and built scratch books through the real
> binary. Convergence = signal; adversarial-only items orchestrator-traced before adoption.

## §1 The convergent verdict

The keystone is REAL and the round did not repeat 16Q's ap-1/ap-2: entity resolution
genuinely runs book value-flow through the oracle's own argparse (no engine-side fallback —
both passes grepped/traced for one); the floors hold under independent scratch probing
(⊤⇒run everywhere traced; the no-fabricated-values grep is complete: the only production
`Predicted::Value` mints are the Query firewall and the inert reserved out-claims);
deferrals are recorded-and-greppable (the adversarial's planned findings kept turning out
to already be in the strain notes — its words: "the correct outcome"). Strongest shared
evidence of honest grading: the round accepted its headline demo regressing 6→0 rather
than protecting it. The residual pattern both name: the SUMMARY layer (29/38, "axes
re-grounded") does not inherit the strain-notes' caveats.

## §2 Adversarial find-1 — the live one (CONFIRMED; repair dispatched as task-P)

The probe ships ONE wrapper per KIND; a kind with multiple selectors gets every selector's
convergence record from the same (single-selector) probe body. Concretely:
`service:nginx#enabled`'s record is produced by `systemctl is-active` — on an
active-but-not-enabled host the apply WRONGLY ELIDES `systemctl enable nginx`. This is a
real under-execute reachable at HEAD, inside the enforced-parity set; worse, gate-1 parity
forces fixtures to agree with the blind probe, so the corpus cannot even express the
dangerous host. Recorded pre-existing as strain-D1-perselector/tc-perselector-probe/
F-BLESSED — but unannotated at the 19I-group-B/headline-number layer, and un-fenced.
**Repair (task-P): per-selector probe declarations** (`oracle_probe_<kind>_<selector>`,
falling back to the per-kind form ONLY when the effect-map shows exactly one selector for
that kind); a multi-selector kind without per-selector probes ⇒ those sites are
UN-PROBEABLE (skip-unresolvable ⇒ run) — the F-BLESSED "an honest service probe is two
commands" floor, enforced. Corpus: service oracles gain both probes; affected goldens
re-bless; a boundary case expresses the enabled≠active host.

## §3 Other adversarial finds, dispositioned

- **find-2 (Query value-story thin at e2e):** every fold-demonstrating case is
  parity-opted-out (builtin guards); the one enforced Query elision is the UNRULED
  tc-query-bare-elision behavior. Repairs in task-P: a shimmed-Query-fold corpus case
  (possible since the 20H gate-5 carve-out; currently demonstrated only in a discarded
  temp run). tc-query-bare-elision stays FLAGGED for the human (sound — no mutation,
  probe-sourced — but possibly over-eager; behavior unchanged pending his ruling).
- **find-3 (multi-operand oracle cliff):** engine-side refusal died with find-3-the-
  stand-in by C-1's own logic (an engine that parses nothing cannot count operands); the
  safety now lives in each oracle's `[ "$2" = "" ]` guard. Repairs in task-P: a
  pinned-hazard unit test demonstrating the naive-oracle drop (documented as KNOWN, the
  anti-masking way); the guard added to 19H §2.1's annotation (the example authors will
  copy); an oracle-quality-bar line in oracle/CLAUDE.md joining the 17O class.
- **find-4:** confirms 204 strain-1 as recorded; no action beyond find-3's annotation.
- **find-5 (ledger staleness) — dispositions, stated here as the round-close record:**
  - *Static vouch-closure check*: **discharged-as-moot at HEAD** — under st-2 the shipped
    probe wrapper contains ONLY the kind's self-vouched declared `oracle_probe_*` body
    plus generated scaffolding (printf/if — blessed-pure), so there is no un-vouched call
    for a static closure check to refuse; the executable half (gate-1(c) rc-127) covers
    mocks cases. REVIVES the moment any future emitter ships check()-body spans
    (rule-anno-render's residual rides the same trigger — 20C §7's hazard note is the
    pointer). spike/CLAUDE.md's "remains owed (task-D)" wording updated by task-P.
  - *rule-anno-render*: discharged-as-moot same trigger (no annotation line ships today —
    verified zero in all expected.out); the rule text stays in 205 §1 as the revival spec.
  - *cm-1 (differential product-gate)*: NOT silently dropped any longer — recorded here as
    **deferred-with-reason**: the human priced the isolation tier and deferred it
    (2026-06-10 conversation); the msys-tier version remains the top next-round
    harness item, with urgency INCREASED by the 20H gate-5 disposition carve-out (no
    differential gate observes elided sites now; wrong-elision detection rests on goldens
    + unit pins until cm-1 exists).
- **find-6:** (a) `facts_from_sites` same-cell last-write-wins — task-P makes conflicting
  same-cell records merge conservatively (conflict ⇒ Unknown/⊤); (b) cant-tell+rc≥2
  composition recorded under tc-probe-parity-projection (task-P tightens parity to carry
  rc for Query sites); (c)/(d) comment fixes ride task-P's re-bless.
- **Neutral-only nits:** spike/CLAUDE.md round-job text gains the st-2 annotation
  (task-P); fold-domain-widening gets its explicit one-line deferral in the round report
  (it lives in 209 brk-4's roadmap).

## §4 What both passes graded solid (carried to the report)

Entity axis real (scratch-verified twice over); rc axis Query-only by construction with
the firewall pinned in three directions; convergence 29/38 enforced-parity with exact
arithmetic and non-tautological shims (entity-branched host-state, corruption-proven) —
honest framing: parity-mediated, second-order grounding, the live wire is cm-1's job;
render-exec strengthened beyond any prior round (six gates, each perturbation-proven,
zero xfail). The strain-note corpus (201–20H) is the round's primary product and survived
two hostile audits with its honesty intact.
