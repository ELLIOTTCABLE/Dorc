# 21K — Direction batch 3 (PROVISIONAL — recorded at human direction, explicitly NOT
# the for-sure plan; he reserves the right to reverse any of it)

> Orchestrator ledger of the 2026-06-11 direction conversation. Everything here is
> direction-grade, reversible, and awaits per-round charters. Commit at next quiesce.

- d-1 r22 lean: errors + provenance. Includes the human's voluminous-durable idea: not
  only a targeted `why` query but a DURABLE-PRODUCING dump mode — full derivation log
  per run — serving (a) real-life postmortem and (b) a DST plugin-point: golden-TRACE
  fixtures ("do x ⇒ exactly this derivation ⇒ z verdict") for critical-tier logic,
  tiered deliberately because full-trace goldens churn (critical-tier only pins traces;
  the rest pins verdicts). The `why` command becomes a query over the dump — one
  producer, many lenses.
- d-2 OTel (human, his wheelhouse): the provenance durable WANTS light OpenTelemetry
  integration — tracing + trace-propagation built in early (controller→host
  propagation; the verdict lane as a carrier candidate). Round-11 caveat stands:
  import-the-ideas-not-the-machinery [B-cramer-otel-critique-2024]; OTel's fork/join
  links-not-parent finding already matched our DAG shape.
- d-3 First-contact reframed (two corrected assumptions): the human has NO coherent
  existing book-set (an agent will spider his personal code for extractables anyway),
  AND his pending ops work is gated BEHIND this project — so any books he writes now
  are shaped by current constraints (not a blind study), and serious oracle-authoring
  is gated on settling the oracle language, which is gated on seeing the finished
  product. Looming: the YOLO gate (strict-by-default vs YOLO-by-default regime;
  homelabber adoption is the game). His hybrid probe: ~3 of his real todo items +
  cheap AI agents + a cheap VPS + slopped oracles/books — a FEEL-test, not a corpus;
  expected failure mode = an ocean of UX whack-a-mole with no design signal;
  disposition = attempt cheaply, abandon freely. Orchestrator mitigation to propose:
  pre-register 3-5 design questions the probe must answer (oracle-authoring friction
  shape; precedence-default feel; disclosure legibility; the YOLO-gate taste evidence)
  plus a hard abandon criterion — converts whack-a-mole into a survey with an exit.
- d-4 Subagent commit policy (human feedback, judgment applied): no-commit briefs
  compress evidence. New default: builders commit GRANULARLY on their detached
  worktree bases; orchestrator harvests by cherry-pick (gates before each pick),
  preserving their commit series as evidence. Main-tree builders may commit granularly
  too; orchestrator verifies after. (His suggested rebase-serially is his workflow;
  cherry-pick is the hook-compatible equivalent here.)
- d-5 r-5 (scoped-mutation rung) stays deferred; rationale expanded in conversation
  2026-06-11: it sanctions DECLARED mutation-boundaries on the kFAIL-withhold side —
  the one phase whose failure is trust-catastrophic and education-proof — under the
  standing mutation-analysis-impossible ruling (the engine can never check the
  boundary), with note 222's evidence that declared counterfactuals are the
  claim-family ecosystems bled on (c-2/c-3) and that probe-side "non-mutating" intent
  already caused real fleet outages (sur-2). Zero measured population needing it.
  Revisit only against real-usage measurement.
- d-6 arch-5 entry-gate verification (code read, 2026-06-11): CONFIRMED —
  `self_reach_holds` (effect.rs ~620) requires `Reach::is_pristine` = EMPTY suppressed
  in-state: GLOBAL-pristine, the strict reading. Nuance vs the r1A framing: the
  committed doc-comment states the strict form in its own words ("no pre-loop writer"
  unqualified), so this is impl-matching-its-own-doc with the ambiguity upstream in
  20S §3.1's wording — i.e., a deliberate-looking strict reading, not an accident.
  The human-authorized cell-family re-scope proceeds under the EXPANDED obligations
  (deep-preamble pole, sibling-writer re-runs, two-leaf floor pin, value-plane pole,
  multi-effect future-hazard note, dedicated hostile pass), with the bar raised one
  notch by this nuance: the 20T crosscheck validated the strict form's rationale, so
  the relaxation's hostile pass is load-bearing, not ceremonial.
