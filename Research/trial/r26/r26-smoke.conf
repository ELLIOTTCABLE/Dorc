# r26 live-smoke marker vhost. Port 8088 so it cannot collide with Debian's default site.
server {
    listen 8088;
    server_name _;

    location = /r26 {
        default_type text/plain;
        return 200 "dorc r26 smoke ok\n";
    }
}
