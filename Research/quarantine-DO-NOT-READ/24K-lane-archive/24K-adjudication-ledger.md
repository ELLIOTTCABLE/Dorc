> QUARANTINED RAW LANE MATERIAL - 24K cross-model language-design crosscheck (2026-07-05).
> NOT adjudicated findings. The adjudicated verdict is Research/notes/24Kc-language-crosscheck-adjudication.md.
> Conductor-authored working ledger (Fable, adjudicating session): six-lane distillations, convergence tallies, friction log. Superseded by 24Kc on any conflict.
> Archived verbatim from the session scratchpad post-compaction, at the human's direction.
> Do not cite as authority; do not read during future crosscheck skill-ups (contaminates pre-registration).

# 24K adjudication ledger (conductor-private working notes; compressed per-report on arrival)

Credal stance: deflationary; convergence = mild signal (cross-model blind spots correlate);
codex over-flags severity per calibration note. Target outcome: ≤1 large coherent item.

## Lane status
- fable-neutral (24Ka): RUNNING
- fable-adversarial (24Kb): RUNNING
- codex-adversarial: DONE (exit 0, 14KB; 2 TPM-rate-limit reconnects; shim = sonnet, Bash+Read only — no Write tool, used verified heredoc appends)
- codex-neutral: RUNNING (v3)
- deepseek-neutral: RUNNING (v2)
- deepseek-adversarial: RUNNING (v2)

## codex-adversarial (GPT-5.5, foreign lineage) — distilled
Pre-registered lessons: 10, credible, CogDims/TS-PEP484-erasability/Go-Swift-compat/Oils-fish framing.
Protocol compliance: good (lessons-first honored, kill-attempts real, cleared-list present).

Findings (their slugs 23-fdN; my provisional adjudication in CAPS):
- fd1 shared-file oracle+book breaks the book's own off-ramp (USER_STORY 231-246 showcases
  append-to-book; dotted names + inline binds ⇒ file no longer dash-runnable; kTYANNOT's
  "books stay verbatim" containment falsified by the flagship first-oracle path).
  NEW-AND-CRISP. Candidate for THE coherent item. Cheap fix-shape exists (showcase sidecar
  file instead; or dialect-inert subset for shared files). sev high/conf high (theirs);
  my read: REAL, ~SUSPECT the strongest single item so far.
- fd2 is_converged named-as-answer acts-as-license. = kCONTRACT-RUNGS (registered TODAY;
  ORACLE_PROVIDES ladder). CONVERGENT-KNOWN; external urgency signal for resolving before P5.
- fd3 rc-partition spellable-backwards (inverted-sense; !/||true collapse). KNOWN
  (hz-refusepath, inverted-vouch floor pins, negative contract, declared-dual glue). NOT NEW.
- fd4 set -eu defeats elision on the flagship style. = the errexit-collision, already routed
  (predict-rc experiment w/ r24). CONVERGENT-KNOWN-IN-FLIGHT.
- fd5 strip-only helper-name reservation lacks hygiene story (reingest-collision pins
  refuse-and-run = safe floor). MINOR; language-round agenda (namespace policy).
- fd6 per-role trailing-mark semantics (predict establish vs reaches typing). KNOWN COST
  (24G names it + document-loudly ruling). Language-round agenda.
- fd7 no compat/version policy while compat-candidates accrete (strawman-label vs
  welded-rulings split). = lost-language-lockin/the v0-stamp decision — THE ROUND'S PREMISE.
  CONVERGENT with our own pivot; feeds the stamp text.

Cleared (theirs): survival-tier honesty; silence-degrades-safely; artifact/render split;
reaches() naming win; "corpus has noticed its own sharp edges."

## codex-neutral (GPT-5.5, foreign lineage) — distilled
DONE (exit 0 attempt-2 via stdin; report 14.6KB; verified target docs unchanged 259b27d→e3f67a5).
12 pre-registered lessons, sourced (Wadler CHI-dims, TS goals, PEP484, Typed Racket, fish design,
Oils FAQ, TC39 types-as-comments, Guido). Protocol compliance good; 10 web searches ran.

Findings (their fdN → my adjudication):
- fd1 is_converged over-licenses (answer≠permission; no rung-0/1 escape). = kCONTRACT-RUNGS. CONVERGENT.
- fd2 inline surface ⇒ the STRIP PASS is a correctness-critical compiler pass that is unspecified/
  unversioned — "part of the language runtime contract, not an implementation detail." Same cluster
  as adv-fd1 (shared-file path); NEUTRAL adds the spec/version-the-stripper demand. STRONG cluster.
- fd3 name-carried semantic OVERLOAD (role+keying+rung+polarity+ownership all ride bare function
  names, no signatures; external law needed to read them). NEW-ish angle on 24G name-as-contract.
- fd4 per-role trailing-: mini-language. = adv-fd6. CONVERGENT-KNOWN (24G cost).
- fd5 bespoke argparse repeated across role-siblings (3 near-identical parsers/oracle; copy-paste
  divergence = semantic bugs). KNOWN-ADJACENT (resid-argparse-drift + coherence net + Stage-4
  derivation) but the LANGUAGE/DX framing (boilerplate razor) is fair. Semi-new.
- fd6 rc-partition corruption-prone under !/||true/pipeline-tail. = adv-fd3. CONVERGENT-KNOWN.
- fd7 emitted/example name instability (systemctl_check vs foo_is_converged vs apt_get__predict
  across USER_STORY/fixtures). Real doc-level inconsistency; cheap fix. Semi-new, minor.
- fd8 --trust-footprints consent-fatigue = weak versioning story. KNOWN-PRICED (mode-gate ruling
  says marketing/theatre itself). CONVERGENT-KNOWN.
- fd9 kind ownership/collision/evolution under-specified pre-stdlib. = dq-kOOB residual
  (TODO-ADDTL). CONVERGENT-REGISTERED.

Cleared: guard form (right choice); rul-attention-honesty ("language-design gold"); silence-default
right; declared-identity right; resolve/reaches split "a real repair"; survival-tier honesty; the
strawman-shield WITHDRAWN as unreviewability-defense (load-bearing shapes already repeated).

Running convergence tally:
offramp-strip cluster (shared-file path + unspecified stripper): 2 (codex-adv, codex-neutral)
rungs/over-licensing: 2 (+ our kCONTRACT-RUNGS = registered)
rc-backwards ergonomics: 2 (known: pins + negative contract)
per-role-marks: 2 (known: 24G cost)
compat-policy/kind-evolution: 2 (registered: v0-stamp + dq-kOOB residual)
errexit-fights-flagship: 1 (known-in-flight)
name-semantic-overload: 1 (new-ish)
argparse-duplication DX: 1 (semi-new; known-adjacent)
emitted-name-stability: 1 (minor, cheap)
namespace-hygiene/strip-collision: 1 (known floor)

## fable-adversarial (Anthropic, in-lineage; 426k tok, 81 tools, notes 24Kb-01..04 committed in its worktree) — distilled
23 pre-registered lessons, gate PROVABLY pre-corpus (own commit ordering); [inj]-tagged the four
lessons plausibly selected under the auto-injected AGENTS.md (honest contamination disclosure).
Empirically EXECUTED the shell claims (dash/bash live).

Findings (F1-F8 → my adjudication):
- F1 authored-form contradiction cluster. = the offramp-strip cluster, NOW 3 LANES + new teeth:
  (a) raw-run authored oracle ANSWERS WRONG (marks become argv: dpkg-query extra args ⇒ always
  rc 1 — silent wrong answer, worse than crash); (b) strip transform inconsistently specified
  across SETTLED layers (name_predict vs apt_get__is_converged vs foobar_is_converged vs
  foobar_check renders) — verifiable doc/impl drift; (c) USER_STORY:233 share-a-file contradicts
  the KNOBS containment (= codex-adv fd1 exactly). THE cluster. sev HIGH, my read: REAL.
- F2 return-decline two-readers. NEW+SHARP: the #12 return-vouches fix made explicit-return
  style (`if probe; then return 0; else return 1; fi`) parse as ALL-DECLINES ⇒ silently-inert
  oracle, no diagnostic — while USER_STORY teaches rc-is-read. Doc/behavior mismatch; mainstream
  style lands in the corner. Cheap fix pre-first-author. Candidate top-tier item. VERIFY in code.
- F3 kTYANNOT resolved-by-construction (process). TRUE (my own KNOBS text records it); = the
  round's own premise; convergent codex fd7. Being addressed BY this round.
- F4 stage-3 on-ramp teaches arity-ungated oracle (multi-operand under-execute; R2-MULTIOP known
  but the TEACHING-ORDER angle is new-ish). ~SUSPECT pending binary check (they flagged).
- F5 colon sigil variance. = marks cluster, NOW 3 LANES.
- F6 namespace governance + NON-INJECTIVE MUNGE (apt-get/apt_get both → apt_get__*, grep-verified,
  recorded nowhere). = namespace cluster 3 lanes; the munge collision is new + cheap to doc/lint.
- F7 interpretation-versioning (trigger-set shrinkage ⇒ same-book verdict drift release-over-
  release, unnamed policy). NEW angle on compat cluster; cheap disclaimer+changelog fix.
- F8 channel-stability undeclared (# dorc: trailer grammar frozen-by-law, never designed;
  probe-record/UNK/tally grammars unversioned). New-ish declare-now item.
Cleared: rc-partition genuinely well-chosen (ran its own DETECT); emitted-code craft above-grade;
error-posture Dialyzer-shaped; name-as-contract praised; bought-unsoundness honesty; dash-break
"nobody noticed" WITHDRAWN (known+priced, survives only as F1's contradiction form).
Meta: "no outside shell-literate human has ever seen this surface" → cold-reads/friend-test
(converges with the banked 251 friend-test).

## deepseek-neutral: TRUNCATED at the gate (20 good lessons, zero findings — nested run ended
turn at phase-1). Continuation dispatched (lessons fed back verbatim). Contamination note:
AGENTS.md auto-loaded pre-gate (same as fable-adv's disclosure).

## fable-neutral (Anthropic, in-lineage; 374k tok, 106 tools; notes 24Ka-01..06 committed on
branch ai/24Ka-langreview in its worktree) — distilled
24 pre-registered lessons, gate git-proven; zero post-exposure additions; CENSUS-BACKED
(counted idioms across 187 oracle files + 154 books). Findings F1-F11:
- F1 compiled-dialect (HIGH): the offramp cluster's strongest form. NEW: census 0/187 oracle
  files dialect-free (no sh-only authoring path exists); books side HOLDS (153/154 pure POSIX);
  THE LEVER: argues the kOOB redline's own config-vs-metadata wording may make eol-comment
  annotations redline-CLEAN (erased annotation = metadata not configuration) ⇒ the inline
  pole's off-ramp cost was possibly an UNFORCED trade — aimed precisely at the open
  dq-kOOB/kTYANNOT weld. (Counter to hold in adjudication: the human's kOOB clarification
  EXPLICITLY lists no-comment-parsing in the verboten set — the reviewer is asking the human
  to re-derive whether that ban's purpose covers erased annotations. Legitimate question for
  the weld-owner, not a settled refutation.) Fallback framing if inline stays: drop the
  identity claim — "compiled superset, one-command off-ramp", ship `dorc strip` first-class,
  un-bless share-a-file for dash-targeted books.
- F2 mark-polysemy: marks cluster. Census: ACK/POISON have ZERO corpus occurrences (law without
  usage); `command -v` marked establish in one family vs observe in another (author-taste
  polarity drift — live coherence evidence).
- F3 license-in-a-name: rungs cluster (now ALL FOUR lanes) + the grammar angle (three liability
  tiers share no naming-grammar marker; 24G's own name-as-contract principle argues tier
  should ride the grammar).
- F4 kind-namespace: kind cluster + census (bare stdlib kinds beside fb.* beside plumbing-kind
  grepmatch; the 17N reverse-DNS lean appears NOWHERE in the wild corpus).
- F5 no-epoch: compat cluster (all four lanes) — no dialect-version marker greppable anywhere;
  goldens gate the engine against its corpus, nothing gates future engines against PUBLISHED
  artifacts (the freezing direction).
- F6 migration-debt: 185 X__predict vs 4 X.predict; 71/187 files mix schemes — the corpus
  largely teaches the STRIPPED form as if it were the authored form. Concrete + new.
- F7 flagstrip ceremony ×405 (wrong for `-o Opt val`); their own cargo-cult razor applies;
  capped by provides-decoding failing safe. = argparse-dup cluster (2 lanes).
- F8 two-declines synonymy (weakest lesson; hold lightly).
- F9 nounset idioms in flagship examples: `[ "$2" = "" ]` set-u-fatal (verified), 133 files,
  REGRESSED vs older strawmen's safe `$#` form; DORC_REPORT unset-fatal raw. Cheap fix. New.
- F10 strip-spec drift (single vs double underscore vs hyphen-munge; non-injective) = 3-lane
  cluster with fable-adv F1(b) + codex-n fd7.
- F11 emitted collapse `: | true || : | :` unreadable — watch-item for the render round.
Cleared (generous + specific): rc-partition "better than I expected to find anywhere"; guard
shape robust; book-erasability HOLDS; never-lie posture; the two-audience ladder; ORACLE_PROVIDES
as the demanded feature-ledger; 24G naming doctrine; the harness's dash-as-semantic-oracle.
Meta: "did the analysis right, then let accretion override it under momentum" (= fable-adv F3).

Updated convergence tally:
offramp-strip/authored-form cluster: 3 LANES (codex-adv, codex-neutral, fable-adv) + empirical teeth
per-role-marks/sigil-variance: 3 LANES · namespace/munge: 3 LANES · compat/versioning: 3 LANES
rungs/over-licensing: 2 · rc-backwards-ergonomics: 2 (+ fable CLEARED the partition itself as
well-chosen — tension is ergonomic-surface, not the table) · errexit: 1 (known-in-flight)
NEW singles worth verification: return-decline-inert (fable F2, top candidate) ·
interpretation-versioning (F7) · channel-stability (F8) · name-load (codex-n fd3) ·
argparse-dup (codex-n fd5) · emitted-name-instability (codex-n fd7, overlaps F1(b))

## deepseek-neutral continuation (DeepSeek V4-Pro, foreign lineage; full report 44KB) — distilled
15 findings + 10 cleared. ADDS NO NEW LOAD-BEARING FINDINGS — independently confirms every
cluster from the third lineage (the confirmation IS the value; weigh near-frontier):
F2 offramp "Critical" (cluster, now ALL LANES; same share-a-file containment-leak argument) ·
F1 two-surface dot/underscore mental-model (= migration/strip-drift cluster) · F3 colon
polysemy (cluster) · F4 kind-namespace (cluster) · F5 answer/license conflation (cluster —
every lane found kCONTRACT-RUNGS independently) · F14 no-versioning (cluster) · F7 argparse
viscosity (cluster) · F10 kTYANNOT deferral = "cost of lock-in without clarity of commitment"
(process cluster) · F6 theatre-flag (known-priced, quotes the ruling's own words) · F12
name-carries-legal-force-without-static-defense (2 lanes w/ codex-n fd3) · F13/F15 mostly
self-killed. POSITIVES converge too: rc-partition well-chosen (ALL lanes now), attention-
honesty praised, guard-not-a-second-language, phase separation, no-registry-principled.

## Tooling friction accumulated (for the human's wrapper work)
1. reviewer agent defs lack Write (Bash+Read only) → packets written via chunked heredoc appends.
2. mise codex shim REJECTS multi-line argv ("batch file arguments are invalid") → argv $(cat) form
   fails; stdin form `codex exec ... - < packet.md` WORKS. v2 protocol should use stdin.
3. single-shot large heredoc (~8.7KB) hit bash "unexpected EOF" (length truncation, msys layer) →
   chunked appends + Read-verify is the robust pattern.
4. org GPT-5.5 TPM cap (500k/min) causes mid-stream reconnects → slow lanes, self-healing.
5. no model: frontmatter in reviewer defs → shims inherit parent (Fable) unless explicit
   model:"sonnet" per dispatch. Monitor-UI header shows parent model (suspected UI bug).
6. subagent bash PATH lacks ~/.claude/bin → bare `ds-review` = exit 127; absolute path
   ("C:/Users/ec/.claude/bin/ds-review") works. Reviewer defs should absolute-path their
   CLIs or export PATH. Conductor pre-writing packet files (own Write tool) + cat-only
   shims = the robust dispatch pattern overall.
7. CLEAN-CONTEXT LEAK: both the worktree Fable reviewer and the nested ds-review Claude
   auto-loaded the repo's AGENTS.md (+ memory index) BEFORE reading their briefs — partial
   contamination of the pre-registration gate (both disclosed honestly; fable tagged the
   affected lessons [inj]). For future crosscheck machinery: run clean-context reviewers
   from OUTSIDE the repo cwd, or suppress instruction-loading, if the gate is load-bearing.
8. Permission classifier: ds-review flagged as exfiltration at every agent tier + main loop;
   resolved by user-added standing allow rules (global settings). Codex lanes were NOT
   flagged for the same packet→OpenAI flow — classifier inconsistency worth knowing about.
9. ds-review nested run can end its turn early (deepseek-neutral stopped at the gate,
   exit 0, is_error false — a silent half-report). Continuation-dispatch pattern works;
   for one-shot briefs, consider instructing "the report is the ONLY valid final message."
