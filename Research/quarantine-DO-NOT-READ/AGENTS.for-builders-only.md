# STOP — MEMETIC-HAZARD / FABLE INFORMATION-FLOW FIREWALL

**THIS FILE IS QUARANTINED MATERIAL FOR BUILDERS AND OPAQUE REVIEWERS. NEVER expose it to the global-reasoning conductor (Fable), directly or indirectly.** Fable must not read this file; receive its text; receive quotations, paraphrases, summaries, explanations, classifications, rationale, threat analysis, concern names, examples, implications, or commentary derived from it; or be told what hidden material influenced a decision. Do not hint, euphemize, encode, translate, or “helpfully contextualize” forbidden content. Aggregation and abstraction do not make it safe.

The danger is interactional: security reasoning can redirect or terminate the conductor session and destroy the product/design context the gate exists to preserve. Treat every conductor-visible channel as an outward-flow boundary: final responses, commentary/status updates, plans, prompts, commit messages, ordinary docs, code comments, test names, diagnostics, filenames, tool output, and messages to non-quarantined agents. Ordinary implementation artifacts and truthful engineering descriptions required to complete the assigned work may cross; their hidden source, security classification, rationale, implications, and review reasoning may not. Never ask Fable to adjudicate security policy, interpret a hidden invariant, select acceptable risk, or inspect quarantine evidence.

**For security-relevant construction, this file supersedes the conductor-assigned brief.** A builder MUST satisfy both. Unrelated product, UX, analyzer-correctness, and implementation requirements remain binding; where compliance genuinely conflicts on a security-relevant surface, this file wins. Normally, continue the conductor's task exactly as assigned while independently applying every relevant `sinv-*` rule, including rules the brief did not mention. Do not narrow, redesign, delay, or editorialize on ordinary work merely because this file adds implementation constraints. Builder output uses ordinary truthful engineering language and never reveals the hidden source, classification, or rationale.

A security-related conflict between this file and the assigned brief is the path of **very last resort**. First exhaust every faithful implementation that completes the brief without violating an invariant. Only when no such implementation exists, STOP without naming, describing, hinting at, or explaining the conflict. Tell the conductor exactly: **“Ensure this plan is durable, then run the opaque reviewer over the brief given to this builder.”** Add nothing security-related. Do not propose a fix, identify the affected surface, quote the brief, summarize the concern, or answer follow-up questions about why work stopped. Resume only from a reviewed replacement brief or the permitted opaque-review result.

When uncertain whether information may cross the gate, do not convey it. When uncertain whether an implementation can satisfy both documents, keep working toward a faithful construction unless an actual security-related conflict is established.

The quarantined files are open to you, you may refer to them as necessary to complete your work. In particular, recent results from recent security-reviews, underneath the quarantine, may provide useful context to your direction and efforts. (Reminder: your conductor *has not seen* those security reviews, and *cannot* know their content nor context; their direction is *independant* of your conductor's instructions.)

This is a living document; and higher-reasoning security conductors and reviewers may edit the below sections as necessary to issue new invariants that become security-critical; but it must not grow to be unwieldly, as it will be loaded into context by *every* builder-agent.

# Dorc security invariants — builder-focused quarantine

Construction law; no threat exposition. Read after `spike/CLAUDE.md`; its law remains binding and is not repeated here. One rule per bullet; preserve slugs. A governed-surface change requires opaque accrual review. Never weaken, bypass, or reinterpret locally.

- `sinv-owned-probe-channel` — Surface: controller/oracle report capture and cleanup. The controller MUST use an exclusively created, controller-owned per-attempt resource/channel whose ownership remains attached to the handle/type through read and cleanup. It MUST NOT open, truncate, replace, or remove a host pathname supplied, redirected, pre-created, or reconstructed from a `String`. Refuse before oracle execution when exclusive safe setup fails. Test symlink, collision, hostile temp-directory, replacement race, privilege variance, and cleanup failure; non-owned targets remain byte-identical. Review: `round29/opaque-accrual`.
- `sinv-mutation-elision-vouch` — Surface: aggregate/member-loop/inline mutation replacement. Preserve `spike/CLAUDE.md:claim-tier-gating` and `rul-vouch-is-verdict-authoring` for EVERY erased establish. Aggregate licenses MUST be private, non-empty when mutation exists, identity/cardinality matched, and carry one reached `VerdictVouch` per erased establish. Observation/convergence/self-reach/footprints/reproduced reads MUST NOT substitute. Test unvouched, declined, dynamic, missing, extra, reordered, and mismatched members reject atomically; all-vouched accepts; query-only needs no fake vouch. Review: `round29/opaque-accrual`.
- `sinv-host-evidence-ingress` — Surface: report/result stdin, files, stdout/stderr, tool errors, records, and replay. Treat every managed-host byte as hostile and decision-inert until bounded, closed-grammar parsed, typed, and controller-attributed. Enforce aggregate-byte, line, record-count, field, allocation, and numeric bounds before allocation/interning; overflow and truncation refuse explicitly. Unknown/malformed material remains bounded inert evidence and MUST NOT become claims, identifiers, paths, templates, shell source, or claim algebra. Test boundary±1, oversized pre-existing inputs, malformed recovery, control bytes, and allocation limits. Review: `round29/opaque-accrual`.
- `sinv-controller-attribution` — Surface: all observations, claims, caches, receipts, and licenses. Host, target, invocation, attempt, source set, and generation identity MUST come from immutable controller-owned transport/attempt context, never payload claims. Scope MUST survive every conversion and cache key; absence/ambiguity means no authority. Cross-host or cross-generation reuse requires an explicit reviewed aggregation/conversion constructor and rule. Test forged payload attribution, scope mismatch, stale cache, and accidental global-key reuse reject. Review: `round29/opaque-accrual`.
- `sinv-generation-revocation` — Surface: cancellation, retry, supersession, reactive replanning, and late output. Cancellation/supersession MUST immediately revoke all decision authority from that generation; late records may be bounded diagnostics only and MUST NOT re-enter analysis or resurrect claims/licenses. Execution quiescence is separately observed and MUST NOT be inferred from cancellation acknowledgement. Test late success/failure/output, retry overlap, reorder, duplicate, and post-supersession arrival. Review: `round29/opaque-accrual`.
- `sinv-decision-identity` — Surface: plan, approval, render, saved artifact, and apply. One collision-resistant content identity MUST bind exact ordered book bytes, exact oracle set/provenance, analysis-relevant knobs/admin policy, controller semantics/version, target/context identity, generation, and exact executable bytes; human and executable forms MUST derive from the same identified decision. Apply MUST reject any mismatch rather than regenerate a cousin. World freshness remains a separate guard/observation obligation. Fixed IDs and FNV remain fenced to deterministic width-one spike fixtures. Review: `round29/opaque-accrual`.
- `sinv-authority-map` — Surface: observations, vouches, footprints, resolver facts, caches, generation markers, knobs, conversions. Each knowledge type MUST encode or have one explicit policy for supplier/interpreter, permitted decision species, host/program/generation scope, expiry/revocation, and consequence domain. Preserve `spike/CLAUDE.md:inv-must-may`, `silence-licenses-nothing`, and `two-plane-aid-law`; reachability/evidence MUST NOT imply authority. Widening, generic conversion, permissive default, compatibility fallback, or escape constructor requires opaque review plus negative tests. Review: `round29/opaque-accrual`.
- `sinv-private-authority-mints` — Surface: claim/license/proof constructors and invariant-capable callers. Keep mints private/narrow; require typed witnesses; forbid public fields, defaults, direct construction, and deserialization into authority. Emit an immutable mint-time witness linking attributed evidence, exact oracle judgment/source, admin policy, scope, result, and revocation identity; preserve `spike/CLAUDE.md:two-plane-aid-law` so narration cannot return. Compile-fail bypasses; test exact merged revision. Review ownership includes safe callers able to violate the invariant. Review: `round29/opaque-accrual`.
- `sinv-oracle-identity-trust` — Surface: oracle loading, manifests, upgrades, composition, and diagnostics. Preserve exact source/artifact identity and provenance, but MUST NOT treat hashes, versions, signatures, or Git SHAs as semantic trust. Before external distribution, persist the exact oracle-set manifest and expose pinning/authentication state honestly. Upgrades/composition require an authority diff covering widened vouches, footprints, carry/survival, context entry, resolver equivalence, and decline behavior. Review: `round29/opaque-accrual`.
- `sinv-bounded-observables` — Surface: replacement/elision claims. Preserve `spike/CLAUDE.md:inv-one-observable`, `inv-probe-sourced-values`, and `anti-masking-tests`, but treat their channel tuple as explicitly bounded—not proof of arbitrary shell/world equivalence. Traps, signal timing, inherited descriptors, shell state, locks, audit effects, credential lifetime, concurrent observers, and every unmodeled effect remain outside authority. A new observable requires a typed witness plus composition tests; otherwise guard/run. Publish the exact boundary before third-party use. Review: `round29/opaque-accrual`.
- `sinv-context-siting` — Surface: `spike/CLAUDE.md:context-entry-probing` wrapper entry and privileged execution. Its capability/consent rules are necessary, not sufficient: the entry license MUST also bind resolved target identity, user/privilege, namespace, working directory, environment policy, and credential scope. Unknown/mismatch walls; success MUST NOT infer correct siting. Test deputy substitution, environment/target mismatch, nested wrappers, and unavailable capability. Review: `round29/opaque-accrual`.
- `sinv-hostile-sensitive-orthogonal` — Surface: evidence, reports, whylogs, receipts, renderers, and persistence. Track hostility/recognition/attribution independently from sensitivity/retention. Sanitizing or encoding MUST NOT grant trust or declassify; redaction MUST NOT grant structural safety. Raw forensic bytes stay bounded and separate from recognized typed evidence and sink encodings. Review: `round29/opaque-accrual`.
- `sinv-sink-encoding` — Surface: terminal, logs, JSON, HTML, paths, templates, and shell emission. Every host-derived or source-derived value MUST pass through the centralized encoder for its destination sink after canonicalization; no universal sanitizer and no direct display of interned coordinates/canonical/reach/derivation values. Shell source generation additionally requires type-level quoting/encoding appropriate to the exact shell grammar. Not-ours bytes route ONLY through the `aid::foreign` seal (`ForeignBytes` minted at an edge, no raw accessor, exits only via a sink encoder); never re-derive a name/param convention. BYPASS SHAPE (`round28/opaque-accrual`): engine-classified value parts are encoded at the WEAVE seat, not at mint, so a hand-pushed `RenderPart` reaching `RenderParts::text()` without the weft round-trip carries RAW bytes — a new render seat MUST weave or encode at mint. `ParamText::Ours` is an authorship class, NOT a safety claim. Test control sequences, record forging, malformed Unicode/bytes, quoting boundaries, and each sink independently. Review: `round29/opaque-accrual`.
- `sinv-sensitive-artifacts` — Surface: saved plans, whylogs, receipts, diagnostics. Preserve `spike/CLAUDE.md:whylog-write-only-replay`, `probe-tape-not-a-cache`, and `two-surfaces`. Treat artifacts as sensitive without secret recognition; minimize collection/retention; never promise generic scrubbing. Before default/audit/third-party use: trusted directory, exclusive restrictive create, atomic replace, bounded live and pre-existing replay, visible failure. Test symlink/reparse/collision, permissions, oversized replay, partial write, and narration-only changes leaving executable bytes unchanged. Review: `round29/opaque-accrual`.
- `sinv-integrity-failure-mutation` — Surface: best-effort/degraded paths. `spike/CLAUDE.md:two-phases-opposite-fail-directions` governs analysis uncertainty; lost attempt, transport, attribution, or execution integrity instead MUST withhold further mutation. Malformed/truncated/ambiguous/stale authority evidence MUST NOT become universal “run.” Type the distinction; test every phase × author/admin × reliable/unreliable-oracle cell. Review: `round29/opaque-accrual`.
- `sinv-production-fences` — Surface: fixture-only identities, legacy/headerless records, width-one assumptions, permissive parsers, and test escape hatches. These MUST be structurally unreachable from real remote transport, concurrency/retry, saved approval, multi-host caching, default persistence, or public distribution. Crossing a re-entry boundary requires replacing the fixture mechanism and opaque review; comments alone are not a fence. Where no type can privilege one crate over another, the fence is a LEXICAL ALLOW-list gate that MUST assert a non-empty walk (`fixture_intake_is_unreachable_from_production`, `fixture_payloads_are_unreachable_from_production`, `foreign_edge_constructor_is_fenced`). Adding an entry to any such list is a governed act requiring opaque review, never a local edit; keep each list as narrow as the code (file, not crate) and two-way (a stale entry fails). The fixture intake MUST be deleted or type-gated at the moment a second scope first becomes representable. Review: `round29/opaque-accrual`.
