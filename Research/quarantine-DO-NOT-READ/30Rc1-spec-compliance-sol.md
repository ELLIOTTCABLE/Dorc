# 30Rc1 — spec-compliance adjudication (OpenAI Codex / GPT-5.6-Sol, foreign lineage)

> RAW, UNADJUDICATED foreign output. Provenance: GPT-5.6-Sol, read-only, dispatched
> 2026-08-24 over the 37 deviations recorded in `30Rc`. Remit was spec-compliance ONLY.
> Verdicts: A compliant · B spec oversight, departure is the right reading · C genuine
> departure, human must rule · D contradicts the spec, revert. Conductor's own reading
> lives in `30Rc`, not here. Codex is known to over-flag severity.

1. B — +SURE. The one-crate wording conflicts with the higher-authority kernel dependency rule; read it as requiring one pure receipt kernel, with nondeterministic crypto implementations isolated behind capability traits.

2. A — +SURE. The specification explicitly assigns SHA-256 identities to the receipt crate and makes `plan` depend on it.

3. A — +SURE. Module names are TYPE LEAN, and `capability.rs` preserves the specified boundary.

4. A — +SURE. The concrete two-arm result directly implements the REQUIRED prohibition on caller-selected trust markers.

5. B — +SURE. The grammar table omitted an `attempt` slot despite requiring attempt attribution; it should include the field.

6. B — +SURE. The specification requires rich admitted-record bytes and defines `record-stream`, but omitted their record slot; `admission.stream` is the coherent reading.

7. B — ~SUSPECT. The exact table omitted the member coordinate needed to identify the rendered site; read the row as including `member`.

8. B — +SURE. `apply-assignment.context` is required and opaque-capable, while the tag table omitted its corresponding tag.

9. A — +SURE. The specification expressly forbids merging source variants with distinct repair, authority, or epistemic meaning.

10. A — +SURE. A closed exhaustive token vocabulary may include currently unreachable variants without creating empty durable rows.

11. A — ~SUSPECT. The specification asks for complete region-route populations; projection omission correctly accounts for an unavailable subdivision.

12. A — +SURE. Attempt identity is required, while an environment-derived fixture override is forbidden receipt input.

13. A — +SURE. The associated overlay shape is explicitly required; the associated `Region` type is a permissible spelling.

14. A — +SURE. Narrowing the plaintext mint conforms to the private, affine overlay boundary.

15. A — +SURE. The single refusal family is not required; the three-family spelling preserves distinct required refusal states.

16. A — +SURE. Removing the generic accessor directly follows the prohibition on raw `Reingested<T>` access.

17. A — +SURE. `PartialReceipt<PartialReason>` is TYPE LEAN; a closed non-generic representation preserving reasons and non-convertibility complies.

18. A — +SURE. The generic `OutcomeAvailability` spelling is TYPE LEAN, not a required effect.

19. A — +SURE. The graph section explicitly permits different storage when all correlation, trust, and report-only effects remain represented.

20. A — +SURE. Shared `NonEmpty<T>` is TYPE LEAN; private vectors with enforcing constructors preserve the required non-empty state.

21. C — +SURE. The names themselves are permissible, but changing the shared public API after the declared Stage 1 freeze is an unlicensed schedule departure.

22. C — +SURE. The REQUIRED fence corpus admits no temporary empty-subject exception; leaving authority mints unfenced needs a human staging ruling.

23. D — +SURE. The specification treats receipt identity as document identity, calls plain output a new document, and requires same-ID/different-byte inputs to remain distinguishable; cross-projection byte differences cannot be collapsed into one node.

24. A — +SURE. The specification explicitly requires plain SHA-256 of the literal skeleton span; domain-separated PAE is specified for identities and signatures, not this binding digest.

25. B — ~SUSPECT. The row grammar supplies only site coordinates, so a region license has no truthful row representation; projection omission is the intended fallback.

26. C — +SURE. The specification explicitly binds `PlanPlane::PresentedPlanIdentity` to `PresentedPlanId`; substituting a three-identity aggregate is not licensed.

27. A — ~SUSPECT. With no pre-specialization multiplicity represented, final specialization is vacuously established rather than bypassed.

28. A — +SURE. A typed quiescence witness strengthens the required mintability condition without changing its meaning.

29. C — +SURE. Truthful source ordering is required, but restructuring the live invocation model and collapsing its source categories exceeds the named accessor/census latitude; the temporary human acknowledgement correctly remains unwelded.

30. B — ~SUSPECT. The spec requires exhaustive preservation of classification distinctions but is soft about where upstream information loss is repaired; retaining the distinctions in `core` is the correct meaning.

31. A — +SURE. Adding `ast` is a truthful record change explicitly permitted and necessary for the REQUIRED row.

32. A — ~SUSPECT. The specification requires truthful classification projection but does not prescribe the adapter location; settlement recording complies.

33. A — +SURE. Typed quiescence directly represents the required distinction and replaces the weaker boolean spelling.

34. A — +SURE. The specification explicitly requires replacing `SpineDigest` with `SpinePresentedPlan` and the corresponding presented-plan identity path.

35. A — +SURE. Stage 6 schedules removal no later than close; earlier removal preserves every required effect and follows the higher-authority instruction against needless deferral.

36. A — ~SUSPECT. A completion gate that refuses to report success after running no checks enforces, rather than departs from, the REQUIRED green-gate condition.

37. A — +SURE. These controls support byte-exact fixtures, prevent unintended vector mutation, and constrain the accepted dependency graph without changing specified receipt behavior.

Human review priority: 23 (D), 29 (C), 26 (C), 22 (C), 21 (C).